include: package:flutter_lints/flutter.yaml

analyzer:
  # Phase 03 plan 03-04: `custom_lint` plugin activation temporarily removed.
  # `drift_dev` 2.32.1 requires analyzer ^10.0.0 (pubspec `dependency_overrides`);
  # `custom_lint` 0.8.1 targets analyzer ^8.0.0 and cannot load its plugin
  # under analyzer 10. Since there are no `@riverpod`-annotated providers
  # yet (first ones land in plan 03-06), silently degrading the lint plugin
  # is acceptable. Re-enable the block when `custom_lint` ships an
  # analyzer-^10 release (or when we drop the analyzer override).
  #   plugins:
  #     - custom_lint
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    avoid_print: error
    use_build_context_synchronously: error
    missing_required_param: error
    missing_return: error
    todo: ignore
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated/**"
    - "build/**"

linter:
  rules:
    avoid_print: true
    # depend_on_referenced_packages: direct imports MUST declare the package
    # in pubspec.yaml (not rely on transitive pulls). Inherited via
    # flutter_lints 6.0.0 → lints 6.1.0 → core.yaml; declared explicitly here
    # so the Phase 01 decision to promote yaml+test from transitive to direct
    # dev-deps stays documented at the rule level, and a future
    # flutter_lints release that drops this rule from "recommended" doesn't
    # silently relax our import hygiene.
    depend_on_referenced_packages: true
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    use_super_parameters: true
    avoid_redundant_argument_values: true
    require_trailing_commas: true
