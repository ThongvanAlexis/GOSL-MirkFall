---
phase: 06-review-gate-gps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-review-gate-gps/06-REVIEW.md
  - .planning/phases/06-review-gate-gps/06-01-SUMMARY.md
autonomous: false
requirements: []
success_criteria:
  - SC#3
must_haves:
  truths:
    - "`06-REVIEW.md` exists on `main` with a 5-section skeleton (User-observed findings + sub-§1b POC evidence review / Claude audit findings / Triage decisions / Adversarial evidence / CI-green confirmation) — `grep -c '^## [1-5]\\.' 06-REVIEW.md` returns exactly 5"
    - "§1 contains either the user's verbatim IDE observations OR the explicit `Aucune observation utilisateur` marker (Phase 04 precedent), committed BEFORE any later plan reads POC artefacts or spawns sub-agents"
    - "§1b placeholder sub-section exists in the skeleton so Plan 06-02 can fill it without re-editing the upstream structure (mirrors Phase 04 §1b but renamed `POC evidence review` per CONTEXT user decision 2026-04-20 — no fresh runtime walk this gate)"
    - "§2 contains a `Pre-known from CONTEXT` sub-section placeholder so Plan 06-03 fills 8 entries (iOS duration / POC artefact path drift / battery waiver / OEM deferral / iOS auto-resume deferral / store-EN deferral / pumpAndSettle watch / dart format drift watch) BEFORE spawning the 4 sub-agents"
    - "§2 contains an empty `SC#4 OEM workaround plan` sub-section placeholder so Plan 06-03 can build the OemFamily table inline without restructuring §2"
    - "§4 contains 6 empty sub-blocks (Test 1 MethodChannel sync / Test 2 Permission cascade / Test 3 OemDetector ambiguous / Test 4 Platform manifests / Test 5 Android boot receiver contract / Test 6 adversarial CI run on tool/check_platform_manifests.dart) — mirrors Phase 04 §4 contract with GPS-shaped ingredients"
    - "Two atomic commits on main: `docs(06-rev): scaffold 06-REVIEW.md 5-section skeleton` + `docs(06-rev): capture user-observed findings into 06-REVIEW.md §1`"
  artifacts:
    - path: ".planning/phases/06-review-gate-gps/06-REVIEW.md"
      provides: "5-section review artifact with §1 User-observed findings filled and §1b + §2 pre-class + §2 OEM workaround + §4 six-test placeholders scaffolded"
      contains: "# Phase 06: Review Gate — GPS Review"
      min_lines: 90
  key_links:
    - from: "Claude-main"
      to: "User (IDE review session on Phase 05 artefacts — sessions/GPS/notifications/permissions/UI/native/POC tooling)"
      via: "CLAUDE.md §Code Review Phases protocol step 1 — solicitation message"
      pattern: "qu'as-tu vu|findings?|IDE"
    - from: "`06-REVIEW.md` §1"
      to: "Plan 06-02 Wave 2 POC evidence review + Plan 06-03 Wave 3 agent spawn trigger"
      via: "§1 must be non-empty (verbatim user capture or explicit marker) before 06-02 / 06-03 — strict user-first protocol"
      pattern: "## 1\\. User-observed findings"
    - from: "`06-REVIEW.md` §2 sub-section `Pre-known from CONTEXT`"
      to: "Plan 06-03 Task 1 pre-classification before agent spawn"
      via: "CONTEXT §POC evidence acceptance + dart format watch + pumpAndSettle watch — pre-class saves redundant agent discovery cycles"
      pattern: "### Pre-known from CONTEXT"
---

<objective>
Encode the CLAUDE.md §Code Review Phases protocol as a hard ordering gate for Phase 06: user's IDE observations on Phase 05 GPS & Session Lifecycle artefacts are captured into `06-REVIEW.md` §1 BEFORE Claude reads any POC artefact or spawns any audit sub-agent. This plan also scaffolds the 5-section `06-REVIEW.md` artifact (including the new §1b POC evidence review sub-section, the §2 `Pre-known from CONTEXT` 8-item slot, the §2 SC#4 OEM workaround table slot, and the §4 six-test placeholders) that subsequent plans will fill.

Purpose: The user-first rule is non-negotiable (explicitly anti-parallelized in CONTEXT §Ordering strict user-first protocol Phase 02 + Phase 04). Encoding it as Plan 06-01 with a blocking checkpoint makes the protocol enforceable, not cosmetic. If the user is biased by Claude's findings before posting their own observations on Phase 05 (GPS infra, sealed ActiveSessionState, permission cascade, native bridges, POC POC evidence), the review's value is destroyed. Phase 06 reuses the Phase 02 + 04 review-gate template with three deliberate divergences captured by `06-CONTEXT.md`: (1) §1b becomes `POC evidence review` (no fresh runtime walk per user decision 2026-04-20), (2) §2 has 8 pre-class items + an SC#4 OEM workaround table sub-section, (3) §4 has 6 sub-blocks (5 permanent unit tests + 1 adversarial CI evidence block).

Output: `06-REVIEW.md` committed with a complete 5-section skeleton (with §1b POC evidence review, §2 pre-class + OEM workaround placeholders, §4 six-test placeholders mirroring Phase 04 exemplar) and §1 populated with the user's verbatim IDE observations (or explicit "Aucune observation utilisateur" if the user has nothing to add).
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-gate-gps/06-CONTEXT.md
@.planning/phases/06-review-gate-gps/06-RESEARCH.md
@.planning/phases/06-review-gate-gps/06-VALIDATION.md
@.planning/phases/04-review-gate-persistence/04-01-PLAN.md
@.planning/phases/04-review-gate-persistence/04-REVIEW.md
@.planning/phases/02-review-gate-foundation/02-REVIEW.md
@CLAUDE.md

<interfaces>
<!-- Key rules this plan MUST encode. No codebase exploration needed. -->

From CLAUDE.md §Code Review Phases (verbatim binding constraint):
```
**Protocole :**

- Tu me demandes d'abord ce que j'ai vu qui a besoin d'être revu
- Ensuite tu fais ton audit
- Tu affiches uniquement les **titres** avec une courte explication, et tu me laisses choisir ce qu'on corrige
```

From 06-CONTEXT.md §Ordering strict user-first protocol Phase 02 + Phase 04:
```
- User poste ses findings IDE en chat AVANT que Claude spawn quoi que ce soit
- Claude capture verbatim dans 06-REVIEW.md §1 (ou 'Aucune observation utilisateur' marker si user n'en a aucune — précédent Phase 04)
- ENSUITE POC evidence review §1b par Agent #4 (pas un runtime walk fresh, lecture des POC artifacts committed)
- ENSUITE pre-class §2 des 8 handoff items connus
- ENSUITE spawn les 4 sub-agents en single tool-use message
- Parallèle (user tape pendant que agents tournent) explicitement rejeté
```

From 06-RESEARCH.md §Pattern 2 — `06-REVIEW.md` 5-section skeleton contract (adapted from 02-REVIEW.md + 04-REVIEW.md exemplars):
```
## 1. User-observed findings (IDE review)
### 1b. POC evidence review
## 2. Claude audit findings
### Pre-known from CONTEXT
### SC#4 OEM workaround plan
### Agent #1 — GPS infra + notifications
### Agent #2 — Controller + permissions + state
### Agent #3 — UI + routing
### Agent #4 — Boot watchdog + native bridges + POC tooling
## 3. Triage decisions
## 4. Adversarial evidence
### Test 1: MethodChannel triple-source drift regression guard (permanent unit test)
### Test 2: Permission cascade regression guard (permanent unit test)
### Test 3: OemDetector ambiguous match regression guard (permanent unit test)
### Test 4: Platform manifest drift regression guard (permanent unit test)
### Test 5: Android BootCompletedReceiver contract test (permanent unit test)
### Test 6: tool/check_platform_manifests.dart adversarial CI run (throwaway branch adversarial/06-manifest-drift)
## 5. CI-green confirmation
```

GOSL header policy: `tool/check_headers.dart` targets `.dart` files only. `.md` review artefacts are EXEMPT — do NOT prepend the GOSL header to `06-REVIEW.md`. Consistent with Phase 02 + 04 precedent (STATE.md: "GOSL Dart copyright header NOT prepended to .md review artifacts").

8 pre-class items expected in §2 (CONTEXT §POC evidence acceptance):
1. iOS walk duration 13.5 min vs 30 min target — Noted (PASS-with-caveat accepted)
2. POC artefact location drift (.planning/pocs/phase-05/ → docs/qual-01-02-poc.md + docs/poc-artifacts/) — Should (fix in loop: amend ROADMAP SC#1)
3. SC#2 battery measurement < 15%/h waiver — Noted (extract from POC if present, else inline waiver with fix-cadence proxy)
4. Xiaomi/Samsung/Huawei/OnePlus OEM coverage deferred — Noted (already accepted Phase 05)
5. Auto-resume-post-kill iOS unvalidated (FlutterImplicitEngineDelegate stripped Xcode 26) — Noted (Android covered, iOS Phase 15)
6. Store rationale English copy final polish — Noted (French defended-by-reviewer, English Phase 15)
7. Flaky widget-test pumpAndSettle races — Should (pre-flag known-pattern; agents may find more or confirm none)
8. dart format drift regression watch — Noted (monitor; if drift found becomes Should fix in loop)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the 5-section 06-REVIEW.md skeleton (with §1b POC evidence review + §2 pre-class + §2 OEM workaround + §4 six-test placeholders)</name>
  <files>.planning/phases/06-review-gate-gps/06-REVIEW.md</files>
  <action>
    Create `.planning/phases/06-review-gate-gps/06-REVIEW.md` with the 5-section skeleton matching the format locked in 02-REVIEW.md + 04-REVIEW.md but adapted for Phase 06's GPS / session-lifecycle / native-bridge / POC-evidence concerns. Sections MUST be in this order and named exactly (gsd-verifier greps `^## [1-5]\.` to confirm all 5 present):

    Use the Write tool to create the file with the following contents verbatim (no placeholder substitution; literal text):

    ```
    # Phase 06: Review Gate — GPS Review

    **Opened:** 2026-04-20
    **Status:** open
    **Closed:** (pending)

    ## 1. User-observed findings (IDE review)

    *Captured verbatim at phase start, BEFORE Claude reads any POC artefact and BEFORE Claude spawns any audit sub-agent.*

    (awaiting user input — Task 2 of this plan fills this section)

    ### 1b. POC evidence review

    *Filled by Plan 06-02 after extracting `docs/qual-01-02-poc.md` + `docs/poc-artifacts/test2-full.png` + `docs/store-review-rationale.md` snapshot. Replaces the "Runtime walk" sub-heading from Phase 04 §1b — user decision 2026-04-20: POC artefacts ARE the runtime observation; no fresh `flutter run` walk this gate.*

    <details>
    <summary>Pixel 4a (Android 14) walk extract</summary>
    (pending — filled by Plan 06-02)
    </details>

    <details>
    <summary>iPhone 17 Pro (iOS 26) walk extract</summary>
    (pending — filled by Plan 06-02)
    </details>

    <details>
    <summary>POC plot — docs/poc-artifacts/test2-full.png</summary>
    (pending — filled by Plan 06-02)
    </details>

    **Battery delta extraction:** *(pending — filled by Plan 06-02; if absent from POC, inline waiver per pre-class item 3)*

    **QUAL-03 store rationale snapshot:** *(pending — filled by Plan 06-02 from docs/store-review-rationale.md)*

    ## 2. Claude audit findings

    *Filled by Plan 06-03: first the 8 pre-classified CONTEXT handoff items + the SC#4 OEM workaround plan table, then the 4 parallel sub-agents in ONE tool-use message.*

    Format: `[severity] Title — 1-line explanation — file:line`. Severities: Blocker / Should / Could / Noted.

    ### Pre-known from CONTEXT

    *Filled by Plan 06-03 Task 1 BEFORE spawning sub-agents. Source: 06-CONTEXT.md §POC evidence acceptance (pre-class §2 items). Committed as `docs(06-rev): pre-class 8 CONTEXT handoff items into §2` before any Agent tool call.*

    (pending — 8 entries: iOS duration Noted | POC artefact path drift Should | battery waiver Noted | OEM coverage Noted | iOS auto-resume Noted | store-EN Noted | pumpAndSettle watch Should | dart format drift watch Noted)

    ### SC#4 OEM workaround plan

    *Filled by Plan 06-03 Task 2 from `lib/presentation/screens/oem_guidance_screen.dart::_copyFor` switch + `lib/infrastructure/platform/oem_detector.dart` OemFamily variants + `permission_handler.openAppSettings` reachability + dontkillmyapp.com URLs. The "Tracking interrompu on next launch" banner is explicitly DEFERRED to Phase 15 SC#4 recovery flow per CONTEXT.md.*

    | OemFamily | OemGuidanceScreen copy summary | dontkillmyapp.com URL | openLocationSettings reachability | Pre-class severity |
    |-----------|--------------------------------|-----------------------|----------------------------------|-------------------|
    | (pending) | | | | |

    ### Agent #1 — GPS infra + notifications + Drift V3 + manifest declarations
    (pending)

    ### Agent #2 — Controller + permissions + Riverpod state
    (pending)

    ### Agent #3 — UI + routing + banner widget
    (pending)

    ### Agent #4 — Boot watchdog + native bridges + POC tooling + CLAUDE.md sweep
    (pending)

    <details>
    <summary>Audit Notes (narrative appendix, per agent)</summary>
    (pending)
    </details>

    ## 3. Triage decisions

    *Filled by Plan 06-03 Task 4 after user selects what to fix. Every Blocker MUST be `fix` (waiver forbidden per CONTEXT.md). Every Should MUST be either `fix` or `waived` with inline rationale.*

    | # | Finding | Severity | Decision | Rationale | Commit hash |
    |---|---------|----------|----------|-----------|-------------|
    | (pending) | | | | | |

    ## 4. Adversarial evidence

    *Filled by Plan 06-04. Five permanent unit-test evidence blocks (Tests #1-#5) + one adversarial CI evidence block (Test #6 — throwaway branch `adversarial/06-manifest-drift` exercising `tool/check_platform_manifests.dart`).*

    ### Test 1: MethodChannel triple-source drift regression guard (permanent unit test)
    *File `test/infrastructure/platform/method_channel_sync_test.dart` — scans Kotlin `BootCompletedReceiver.kt` + Dart `boot_completed_watchdog.dart` + Dart `ios_significant_change_watchdog.dart` (and Swift `AppDelegate.swift` IF the literal still exists post-Xcode 26 strip — Open Question 1 from RESEARCH) for `'app.gosl.mirkfall/boot_watchdog'` verbatim. Inertness guard verifies all listed source files exist on disk before asserting content.*

    (pending)

    ### Test 2: Permission cascade regression guard (permanent unit test)
    *File `test/application/permissions/location_permission_cascade_test.dart` — drives `requestLocationAlways` through 4 scenarios (denied → permanentlyDenied → restricted → granted) with `PermissionRequester` typedef seam fake capturing invocations. Inertness guard asserts the fake received N expected invocations before checking the outcome.*

    (pending)

    ### Test 3: OemDetector ambiguous match regression guard (permanent unit test)
    *File `test/infrastructure/platform/oem_detector_ambiguous_test.dart` — 3-5 ambiguous AndroidDeviceInfo fixtures (e.g. manufacturer=aosp brand=oneplus, manufacturer=xiaomi brand=redmi build=miui, manufacturer=huawei brand=honor) assert `OemDetector.detect()` returns deterministic OemFamily resolution. Inertness guard asserts the fake DeviceInfoPlugin was consumed.*

    (pending)

    ### Test 4: Platform manifest drift regression guard (permanent unit test)
    *File `test/tooling/platform_manifests_test.dart` — parses `android/app/src/main/AndroidManifest.xml` + `ios/Runner/Info.plist`, asserts all required uses-permission entries (ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION / ACCESS_BACKGROUND_LOCATION / FOREGROUND_SERVICE / FOREGROUND_SERVICE_LOCATION / WAKE_LOCK / POST_NOTIFICATIONS / RECEIVE_BOOT_COMPLETED) + BootCompletedReceiver declaration + Info.plist required keys (NSLocationWhenInUseUsageDescription / NSLocationAlwaysAndWhenInUseUsageDescription) + UIBackgroundModes location array entry. Inertness guard verifies both manifest files exist + parse OK before asserting content.*

    (pending)

    ### Test 5: Android BootCompletedReceiver contract test (permanent unit test)
    *File `test/infrastructure/platform/android_boot_receiver_contract_test.dart` — Android-scoped complement to Test #1: parses AndroidManifest.xml + greps BootCompletedReceiver.kt + asserts MethodChannel string literal in Kotlin matches Dart constant verbatim. Inertness guard verifies both source files exist on disk.*

    (pending)

    ### Test 6: tool/check_platform_manifests.dart adversarial CI run (throwaway branch adversarial/06-manifest-drift)
    *Branch `adversarial/06-manifest-drift`: poison commit removes `ACCESS_BACKGROUND_LOCATION` from AndroidManifest.xml OR removes `<string>location</string>` from Info.plist UIBackgroundModes array. CI step `Check platform manifests (Android + iOS)` (added to .github/workflows/ci.yml `gates` job in Plan 06-04) MUST fail with exit 1 and stderr identifying file + missing entry. Branch deleted local + remote post-archivage; main `on.push.branches` stays `[main]`-only.*

    (pending)

    ## 5. CI-green confirmation

    *Filled by Plan 06-05 Task 2 after all Blocker + non-waived Should fixes are applied and CI is green.*

    - **Final commit on main:** (pending)
    - **CI run URL:** (pending)
    - **Status:** (pending)
    - **Date:** (pending)

    ---
    _Phase 06 closed: (pending)_
    _Phase 07 unblocked._
    ```

    GOSL header policy: do NOT prepend the GOSL Dart copyright header to this `.md` file. `tool/check_headers.dart` targets `.dart` files only. Markdown review artefacts are EXEMPT (Phase 02 STATE.md decision: "GOSL Dart copyright header NOT prepended to .md review artifacts").

    After writing, verify the 5 top-level sections are present:
    ```bash
    grep -c "^## [1-5]\\." .planning/phases/06-review-gate-gps/06-REVIEW.md
    # must return 5
    ```

    Verify the §1b + §2 sub-sections + §4 six-test placeholders + SC#4 OEM workaround sub-section all exist:
    ```bash
    grep -q "^### 1b\\. POC evidence review" .planning/phases/06-review-gate-gps/06-REVIEW.md
    grep -q "^### Pre-known from CONTEXT" .planning/phases/06-review-gate-gps/06-REVIEW.md
    grep -q "^### SC#4 OEM workaround plan" .planning/phases/06-review-gate-gps/06-REVIEW.md
    grep -c "^### Test [1-6]:" .planning/phases/06-review-gate-gps/06-REVIEW.md
    # last grep must return 6
    ```

    Then commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(06-rev): scaffold 06-REVIEW.md 5-section skeleton" --files .planning/phases/06-review-gate-gps/06-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/06-review-gate-gps/06-REVIEW.md && [ "$(grep -c "^## [1-5]\\." .planning/phases/06-review-gate-gps/06-REVIEW.md)" = "5" ] && grep -q "^### 1b\\. POC evidence review" .planning/phases/06-review-gate-gps/06-REVIEW.md && grep -q "^### Pre-known from CONTEXT" .planning/phases/06-review-gate-gps/06-REVIEW.md && grep -q "^### SC#4 OEM workaround plan" .planning/phases/06-review-gate-gps/06-REVIEW.md && [ "$(grep -c "^### Test [1-6]:" .planning/phases/06-review-gate-gps/06-REVIEW.md)" = "6" ]'</automated>
  </verify>
  <done>`06-REVIEW.md` exists, all 5 top-level sections present (grep count = 5), §1b POC evidence review sub-section present, §2 `Pre-known from CONTEXT` sub-section present, §2 `SC#4 OEM workaround plan` sub-section present, §4 has the 6 Phase-06-specific test placeholders (MethodChannel sync / Permission cascade / OemDetector ambiguous / Platform manifests / Android boot receiver / adversarial CI), `**Status:** open`, sections 1-5 in correct order, skeleton committed as `docs(06-rev): scaffold 06-REVIEW.md 5-section skeleton`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-first IDE review — solicit, capture, commit into §1</name>
  <files>.planning/phases/06-review-gate-gps/06-REVIEW.md</files>
  <action>
    This is the CLAUDE.md §Code Review Phases protocol gate for Phase 06. Strict ordering: Claude asks FIRST, user answers, Claude captures verbatim, THEN Plan 06-02 POC evidence review AND Plan 06-03 agent spawn unblock.

    Claude-main posts in chat as the first non-automated action of this wave:

    > "Avant que je lance la POC evidence review (Plan 06-02) puis les 4 sub-agents d'audit (Plan 06-03), qu'as-tu vu dans ton IDE qui mérite d'être revu sur la Phase 05 GPS & Session Lifecycle ?
    >
    > Scope audit suggéré de ton côté :
    > - `lib/domain/sessions/` + `lib/domain/gps/` (Fix entity, FixStore port, GpsError, LocationStream port)
    > - `lib/infrastructure/gps/` (GeolocatorLocationStream, LocationSettingsFactory, distanceFilter int typing)
    > - `lib/infrastructure/notifications/` (SessionNotificationService, LocalNotificationsPort, FlutterLocalNotificationsAdapter)
    > - `lib/infrastructure/platform/` (OemDetector, BootCompletedWatchdog, IosSignificantChangeWatchdog)
    > - `lib/infrastructure/db/app_database.dart` delta V2→V3 (t_fixes table + indexes)
    > - `lib/application/controllers/active_session_controller.dart` (sealed ActiveSessionState : Idle/Starting/Tracking/Stopping/ErrorState)
    > - `lib/application/permissions/location_permission_flow.dart` (PermissionRequester typedef seam, two-step whenInUse→always cascade)
    > - `lib/application/providers/` + `lib/application/settings/session_settings.dart` + `lib/application/routing/router.dart` (rootNavigatorKey)
    > - `lib/presentation/screens/session_list_screen.dart`, `session_detail_screen.dart`, `settings_screen.dart`, `oem_guidance_screen.dart`, `permission_rationale_screen.dart`, `permission_denied_screen.dart`
    > - `lib/presentation/widgets/active_session_banner.dart` (banner InkWell split pattern)
    > - `android/app/src/main/AndroidManifest.xml` (8 permissions + BootCompletedReceiver declaration)
    > - `android/app/src/main/kotlin/app/gosl/mirkfall/BootCompletedReceiver.kt` (Kotlin receiver + MethodChannel string literal)
    > - `ios/Runner/AppDelegate.swift` (FlutterImplicitEngineDelegate stripped post-Xcode 26 — verify state)
    > - `ios/Runner/Info.plist` (NSLocationWhenInUseUsageDescription, NSLocationAlwaysAndWhenInUseUsageDescription, UIBackgroundModes location)
    > - `test/domain/gps/**`, `test/infrastructure/gps/**`, `test/infrastructure/notifications/**`, `test/infrastructure/platform/**`, `test/application/permissions/**`, `test/application/controllers/**`, `test/application/settings/**`, `test/presentation/**` Phase 05 widget tests
    > - `tool/plot_session_fixes.py` + `tool/requirements.txt`
    > - `docs/store-review-rationale.md` + `docs/qual-01-02-poc.md` + `docs/poc-artifacts/test2-full.png`
    > - `pubspec.yaml` deltas Phase 05 (geolocator 14.0.2 / flutter_local_notifications 21.0.0 / permission_handler 12.0.1 / device_info_plus 12.4.0)
    > - `DEPENDENCIES.md` Phase 05 entries
    > - `lib/config/constants.dart` deltas Phase 05 (kDistanceFilterMeters, kFixBatchFlushInterval, GPS timeouts)
    >
    > Poste ta liste telle quelle — je la capture verbatim dans `06-REVIEW.md` §1 avant de lancer POC review + audit. Si tu n'as rien vu, dis-le explicitement — je note 'Aucune observation utilisateur' et on enchaîne sur Plan 06-02 (POC evidence review)."

    User responds with either:
    - A list of findings (any format — bullets, prose, file references) → Claude captures verbatim into §1
    - "rien vu" / "aucune observation" / equivalent → Claude writes `*Aucune observation utilisateur — l'user n'a pas identifié de point à revoir dans son IDE.*` into §1 (Phase 04 precedent)

    Either way, Claude then:
    1. Edits `06-REVIEW.md` §1 to replace `(awaiting user input — Task 2 of this plan fills this section)` with the captured content. Leave §1b placeholder untouched (Plan 06-02 fills it).
    2. Commits:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(06-rev): capture user-observed findings into 06-REVIEW.md §1" --files .planning/phases/06-review-gate-gps/06-REVIEW.md
       ```
    3. Confirms to user: "§1 capturé et committé. J'enchaîne sur Plan 06-02 POC evidence review dès que tu valides (réponse : `OK POC` ou équivalent)."

    Anti-pattern guard: Do NOT spawn any Agent/Task tool call AND do NOT read POC artefacts in this wave before §1 is committed. The whole point of the checkpoint is to enforce user-first — CONTEXT.md explicitly rejects the parallel approach (user typing while agents/POC review run).
  </action>
  <verify>
    <automated>bash -c '! grep -q "awaiting user input" .planning/phases/06-review-gate-gps/06-REVIEW.md && git log --oneline -5 -- .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -q "capture user-observed findings"'</automated>
  </verify>
  <done>
    `06-REVIEW.md` §1 contains the user's verbatim IDE observations on Phase 05 artefacts (or explicit "Aucune observation utilisateur" marker). The placeholder `awaiting user input` no longer appears anywhere in the file. A commit `docs(06-rev): capture user-observed findings into 06-REVIEW.md §1` is on `main`. User has signaled "OK POC" (or equivalent) explicitly to unblock Plan 06-02 POC evidence review.
  </done>

  <what-built>
    Nothing automated. This checkpoint IS the protocol gate itself: Claude asks the user what they saw in their IDE on Phase 05, captures the verbatim answer (or explicit "rien vu"), writes it into `06-REVIEW.md` §1, and commits. This MUST happen before Plan 06-02 reads any POC artefact AND before Plan 06-03 spawns any audit sub-agent.
  </what-built>
  <how-to-verify>
    After Claude has posted the solicitation and user has responded:
    - `grep -A2 "## 1\\. User-observed findings" .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -v "awaiting user input"` — should NOT show the placeholder
    - `git log --oneline -1 -- .planning/phases/06-review-gate-gps/06-REVIEW.md` — should show the capture commit
    - §1b POC evidence review placeholder remains intact (Plan 06-02 fills it later)
  </how-to-verify>
  <resume-signal>
    User types "OK POC" or "§1 validé, go" to signal that Plan 06-02 may run the POC evidence review.
    If user re-opens IDE findings after seeing this prompt, Claude appends (not replaces) to §1 and re-commits before proceeding to Plan 06-02.
  </resume-signal>
</task>

</tasks>

<verification>
- `06-REVIEW.md` exists with 5 sections in correct order (grep `^## [1-5]\.` returns 5)
- §1b POC evidence review sub-section present (grep `^### 1b\. POC evidence review` matches)
- §2 `Pre-known from CONTEXT` sub-section present (grep matches)
- §2 `SC#4 OEM workaround plan` sub-section present (grep matches)
- §4 contains the 6 Phase-06-specific test placeholders (grep `^### Test [1-6]:` returns 6)
- §1 is populated (no placeholder `awaiting user input` remains)
- Two commits on `main`: scaffold + user capture
- User has explicitly signaled "OK POC" (or equivalent) for Plan 06-02 to proceed
</verification>

<success_criteria>
Plan 06-01 complete when:
1. `.planning/phases/06-review-gate-gps/06-REVIEW.md` exists on `main` with the 5-section skeleton + §1b POC evidence review + §2 pre-class + §2 OEM workaround placeholders + §4 six-test placeholders
2. §1 contains either the user's verbatim IDE findings OR an explicit "Aucune observation utilisateur" marker
3. Two atomic commits logged (scaffold + capture) with `docs(06-rev):` prefix
4. CI green (markdown-only changes, guard scripts not affected — scaffold + capture commits should both trigger green CI, confirming nothing else regressed)
5. User has explicitly unblocked Plan 06-02 via "OK POC" or equivalent signal
</success_criteria>

<output>
After completion, create `.planning/phases/06-review-gate-gps/06-01-SUMMARY.md` summarizing:
- Skeleton commit hash
- §1 capture commit hash
- Verbatim excerpt of user observations (or "Aucune observation utilisateur")
- Timestamp of user unblock signal received
- Confirmation that §1b, §2 pre-class, §2 OEM workaround, §4 six-test placeholders are intact and ready for Plans 06-02 / 06-03 / 06-04 to fill
</output>
