---
phase: 06-review-gate-gps
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - .planning/phases/06-review-gate-gps/06-REVIEW.md
  - .planning/phases/06-review-gate-gps/06-03-SUMMARY.md
autonomous: false
requirements: []
success_criteria:
  - SC#3
  - SC#4
must_haves:
  truths:
    - "BEFORE any Agent tool call is spawned, §2 sub-section `Pre-known from CONTEXT` contains 8 entries verbatim per CONTEXT.md §POC evidence acceptance (iOS 13.5 min duration Noted | POC artefact path drift Should | battery <15%/h waiver Noted | OEM Xiaomi/Samsung/Huawei/OnePlus deferral Noted | iOS auto-resume Noted | store-EN Noted | pumpAndSettle watch Should | dart format drift watch Noted), AND committed as `docs(06-rev): pre-class 8 CONTEXT handoff items into §2`"
    - "BEFORE any Agent tool call is spawned, §2 sub-section `SC#4 OEM workaround plan` contains a populated 5-column markdown table with one row per OemFamily variant (Xiaomi / Samsung / Huawei / OnePlus / Oppo / OtherOem / IosDevice — 7 rows), columns: OemFamily / OemGuidanceScreen copy summary / dontkillmyapp.com URL / openLocationSettings reachability / Pre-class severity, AND committed as `docs(06-rev): build SC#4 OEM workaround plan table in §2`"
    - "Task 2 enforces the `_copyFor()` 7-variant switch precondition: if `lib/presentation/screens/oem_guidance_screen.dart::_copyFor()` does NOT switch on all 7 OemFamily variants (Xiaomi / Samsung / Huawei / OnePlus / Oppo / OtherOem / IosDevice), the affected row(s) in the SC#4 OEM workaround table escalate from baseline `Noted (covered)` to `Should (gap)` with the missing-or-default copy noted inline in the row's severity cell"
    - "4 sub-agents (ALL `general-purpose` per Phase 02 + 04 Accumulated Decision) spawned in a SINGLE tool-use message — not 4 serial messages — preserving the wall-clock parallelism that validated 54 + 86 findings in Phases 02 + 04 respectively"
    - "Each sub-agent scope matches CONTEXT §Sub-agent slicing verbatim: Agent #1 GPS infra + notifications + Drift V3 + manifest; Agent #2 Controller + permissions + Riverpod state; Agent #3 UI + routing + banner; Agent #4 Boot watchdog + native bridges + POC tooling + CLAUDE.md sweep"
    - "Each sub-agent returned a structured findings list (`[severity] Title — 1-line explanation — file:line`) + a narrative appendix (collapsible `<details>` block in §2 Audit Notes)"
    - "Claude-main preserved cross-lens overlap findings (Phase 04 convention): same finding surfaced by 2 agents under different lenses is preserved under BOTH with explicit cross-reference, NOT deduplicated"
    - "§2 populated with the 4-agent findings; user presented with TITLES + 1-line explanations only (never diffs or code blocks) per CLAUDE.md §Code Review Phases"
    - "§3 triage table populated: every Blocker = `fix` (waiver forbidden), every Should = `fix` OR `waived (rationale)` (rationale non-empty), every Could = `fix` / `waived` / `defer-to-phase-X` / `won't-fix`, every Noted kept as observation"
    - "06-03-SUMMARY.md carries forward the adversarial readiness checklist for Plan 06-04 (5 unit tests target paths verified, OemDetector ambiguous fixtures sketched, MethodChannel literal Swift presence/absence verified per RESEARCH Open Question 1)"
  artifacts:
    - path: ".planning/phases/06-review-gate-gps/06-REVIEW.md"
      provides: "Sections 2 (8 pre-class + SC#4 OEM workaround table + 4-agent findings + narrative appendix) and 3 (triage table) fully populated"
      contains: "### Agent #1 — GPS infra + notifications + Drift V3 + manifest declarations"
      min_lines: 280
    - path: ".planning/phases/06-review-gate-gps/06-03-SUMMARY.md"
      provides: "Wave 3 summary: agent type selection, finding counts per severity per agent, triage summary, adversarial readiness checklist for Plan 06-04 (Test #1 Swift presence verification, Test #3 OemDetector ambiguous fixtures sketch, ROADMAP SC#1 amendment text draft)"
      contains: "Adversarial readiness checklist"
      min_lines: 50
  key_links:
    - from: "Plan 06-03 Task 1 commit (pre-class 8 §2)"
      to: "Plan 06-03 Task 2 commit (SC#4 OEM workaround table) → Task 3 Agent spawn"
      via: "git log grep `pre-class 8 CONTEXT` AND `build SC#4 OEM workaround` MUST precede 4 Agent tool calls — protocol gate enforces pre-class first"
      pattern: "docs\\(06-rev\\): (pre-class 8|build SC#4)"
    - from: "Claude-main"
      to: "4 general-purpose sub-agents"
      via: "single tool-use message containing 4 parallel Agent tool calls"
      pattern: "Agent #[1-4]"
    - from: "Agent structured outputs"
      to: "`06-REVIEW.md` §2 sub-sections (one per agent)"
      via: "main-agent assembly + cross-lens preservation (no dedup)"
      pattern: "\\[(Blocker|Should|Could|Noted)\\]"
    - from: "`06-REVIEW.md` §2"
      to: "user triage chat → `06-REVIEW.md` §3 table"
      via: "titles+1-line explanation presentation (never diffs) → user decides per-finding → Claude records"
      pattern: "\\| fix \\||\\| waived \\||\\| defer-to-phase-"
    - from: "`OemDetector` (`lib/infrastructure/platform/oem_detector.dart`) + `OemGuidanceScreen` (`lib/presentation/screens/oem_guidance_screen.dart::_copyFor`)"
      to: "§2 `SC#4 OEM workaround plan` table"
      via: "Agent #4 reads both + extracts per-OemFamily title / steps / learnMoreUrl + reachability of openAppSettings → table cells; if _copyFor() does not cover all 7 variants, severity escalates"
      pattern: "OemFamily|dontkillmyapp\\.com"
---

<objective>
Pre-class the 8 known CONTEXT items + the SC#4 OEM workaround plan table into §2 FIRST, commit, THEN spawn the 4 parallel sub-agents in ONE tool-use message, consolidate their findings into §2, present them to the user as titles + 1-line explanations (not diffs), and capture the user's triage decisions into §3.

Purpose: This is the core audit activity of Phase 06. The 8 pre-classifications save 8 cycles of redundant agent discovery (each known item would otherwise be re-found by 2-4 agents) — Phase 04 had 3 pre-class items that already proved this pattern. The SC#4 OEM workaround table is a pre-known scope baseline (not a finding surfaced BY agent), so it lives in §2 not §3 per CONTEXT.md `§SC#4 OEM workaround gate-closure`. Spawning all 4 agents in a single tool-use message preserves the wall-clock parallelism that validated Phase 02 (54 findings) + Phase 04 (86 findings). Presenting titles-only to the user is the CLAUDE.md contract that lets the user choose what to fix without being biased by diffs. The split between "pre-class / OEM table / agents / triage" mirrors 04-03-PLAN.md adapted to Phase 06's GPS concerns with two adjustments: (1) 8 pre-class items vs Phase 04's 3, (2) SC#4 OEM workaround table sub-section added to §2 BEFORE agents (gate-closure scope baseline).

Output: `06-REVIEW.md` §2 populated (pre-class 8 + SC#4 OEM workaround table + 4-agent findings + narrative appendices) + §3 populated (user triage decisions) + 06-03-SUMMARY.md with adversarial readiness checklist for Plan 06-04 (Test #1 Swift presence verification, Test #3 OemDetector ambiguous fixtures sketch, ROADMAP SC#1 amendment text draft).
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-review-gate-gps/06-CONTEXT.md
@.planning/phases/06-review-gate-gps/06-RESEARCH.md
@.planning/phases/06-review-gate-gps/06-VALIDATION.md
@.planning/phases/06-review-gate-gps/06-REVIEW.md
@.planning/phases/06-review-gate-gps/06-01-SUMMARY.md
@.planning/phases/06-review-gate-gps/06-02-SUMMARY.md
@.planning/phases/05-gps-session-lifecycle/05-01-SUMMARY.md
@.planning/phases/05-gps-session-lifecycle/05-02-SUMMARY.md
@.planning/phases/05-gps-session-lifecycle/05-03-SUMMARY.md
@.planning/phases/05-gps-session-lifecycle/05-04-SUMMARY.md
@.planning/phases/05-gps-session-lifecycle/05-05-SUMMARY.md
@.planning/phases/05-gps-session-lifecycle/05-06-SUMMARY.md
@.planning/phases/04-review-gate-persistence/04-03-PLAN.md
@CLAUDE.md
@DEPENDENCIES.md

<interfaces>
<!-- 4 sub-agent scope contract, verbatim from CONTEXT §Sub-agent slicing layer-based. Agents MUST NOT silently expand scope. -->
<!-- All 4 agents are general-purpose per Phase 02 + 04 Accumulated Decision. -->

**Agent #1 — GPS infra + notifications + Drift V3 + manifest declarations**
Scope (files to audit):
- `lib/domain/gps/` — Fix entity, FixStore port, GpsError, LocationStream port (sealed types + value objects)
- `lib/infrastructure/gps/` — GeolocatorLocationStream, LocationSettingsFactory, distanceFilter int typing (regression locked Phase 05 STATE.md), location_settings.factory.dart
- `lib/infrastructure/notifications/` — SessionNotificationService, LocalNotificationsPort, FlutterLocalNotificationsAdapter, Android channel creation, iOS permission requests
- `lib/infrastructure/db/app_database.dart` delta V2→V3 (t_fixes table + indexes per Phase 05 STATE.md `Drift V2 to V3 uses m.createTable(db.fixes) + explicit createIndex per @TableIndex.sql`)
- `lib/infrastructure/db/migrations/v2_to_v3_fixes.dart` if exists (else inline in app_database.dart)
- `android/app/src/main/AndroidManifest.xml` (8 permissions: ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION / ACCESS_BACKGROUND_LOCATION / FOREGROUND_SERVICE / FOREGROUND_SERVICE_LOCATION / WAKE_LOCK / POST_NOTIFICATIONS / RECEIVE_BOOT_COMPLETED + BootCompletedReceiver declaration with intent-filter)
- `ios/Runner/Info.plist` (NSLocationWhenInUseUsageDescription / NSLocationAlwaysAndWhenInUseUsageDescription / UIBackgroundModes location + fetch / NSUserActivityTypes if applicable)
- `test/domain/gps/**`, `test/infrastructure/gps/**`, `test/infrastructure/notifications/**`, `test/infrastructure/db/migration_v2_to_v3*test.dart` if exists

Focus:
- distanceFilter int typing regression locked Phase 05 (STATE.md `distanceFilter is int not double`) — verify no drift
- Pragma cohérence (V3 uses same WAL + foreign_keys + busy_timeout setup from Phase 03)
- Permissions déclarées = permissions demandées (cross-check AndroidManifest.xml uses-permission entries against `permission_handler` Permission.* constants used in `lib/application/permissions/`)
- LocationStream port purity (no Flutter/Drift imports in lib/domain/gps/)
- FlutterLocalNotificationsAdapter seam: factory-singleton plugin wrapped behind LocalNotificationsPort (Phase 05 STATE.md `LocalNotificationsPort + FlutterLocalNotificationsAdapter seam`)
- Drift V2→V3 migration uses m.createTable + explicit createIndex per @TableIndex.sql (Phase 05 STATE.md Pitfall #7) — verify no drift
- BootCompletedReceiver Android manifest declaration matches Kotlin class path (the receiver itself is Agent #4's scope, but the manifest declaration is Agent #1)
- AndroidManifest.xml Android 14 SecurityException avoidance (Phase 05 STATE.md `BootCompletedReceiver fires notification only, NEVER starts the geolocator foreground service directly`)

Output format:
- Structured findings list: `[Blocker|Should|Could|Noted] Title — 1-line explanation — file:line`
- Narrative appendix (prose audit notes for §2 Audit Notes collapsible)

**Agent #2 — Controller + permissions + Riverpod state**
Scope:
- `lib/application/controllers/active_session_controller.dart` — ActiveSessionController, sealed ActiveSessionState : Idle / Starting / Tracking / Stopping / ErrorState, start() / stop() / resume() lifecycle, ConcurrentActivationException propagation
- `lib/application/permissions/location_permission_flow.dart` — requestLocationAlways, PermissionRequester typedef seam, two-step whenInUse→always cascade (Phase 05 STATE.md `PermissionRequester typedef seam`), Android 13+ POST_NOTIFICATIONS first
- `lib/application/providers/` — session_settings_provider, active_session_controller_provider, oem_detector_provider, fix_store_provider, location_stream_provider, session_notification_provider, etc.
- `lib/application/settings/session_settings.dart` — distanceFilter, displayName defaults
- `test/application/controllers/**`, `test/application/permissions/**`, `test/application/settings/**`

Focus:
- cancelOnError: false on subscription (Phase 05 STATE.md decision — single recoverable GpsError flips ErrorState but keeps subscription alive)
- ConcurrentActivationException propagates untyped via AsyncError, NOT as ErrorState(GpsError) (Phase 05 STATE.md decision)
- AsyncValue.value (Riverpod 3.x), NOT valueOrNull (Phase 05 STATE.md decision — Riverpod 3.x removed valueOrNull)
- Two-step permission invariant (whenInUse before always; POST_NOTIFICATIONS first on Android 13+)
- ActiveSessionController builds synchronously returning Idle() with DB/settings lazy in start() (Phase 05 STATE.md `ActiveSessionController builds synchronously`)
- All 7 @Riverpod providers keepAlive: true (Phase 05 STATE.md `All 7 @Riverpod providers use keepAlive: true`) — re-verify
- IosSignificantChangeWatchdog wrapper: controller hooks on every platform; wrapper no-ops on non-iOS (Phase 05 STATE.md `Controller hooks iOS watchdog on every platform — wrapper class no-ops on non-iOS`)
- PermissionRequester fake injection patterns (closure captures invocation order + returns programmed statuses)

Output format: same as Agent #1.

**Agent #3 — UI + routing + banner widget**
Scope:
- `lib/presentation/screens/session_list_screen.dart`, `session_detail_screen.dart`, `settings_screen.dart`, `oem_guidance_screen.dart`, `permission_rationale_screen.dart`, `permission_denied_screen.dart`
- `lib/presentation/widgets/active_session_banner.dart` — banner InkWell split: inner InkWell(title-only) + peer IconButton(stop) (Phase 05 STATE.md `Banner InkWell split`)
- `lib/application/routing/router.dart` + `rootNavigatorKey` top-level (NOT inside @riverpod — Phase 05 STATE.md `rootNavigatorKey lives at the top level of router.dart`)
- `lib/main.dart` — runZonedGuarded option (b) post-Phase 04 P4 fix (Phase 04 STATE.md `Plan 04-05 strategy P4 Zone mismatch resolved via option-b`); rootNavigatorKey wiring; flutter_local_notifications init (Phase 05 STATE.md `flutter_local_notifications 21.0.0 initialize requires named settings parameter`)
- `test/presentation/**` Phase 05 widget tests (session_list_screen_test, session_detail_screen_test, settings_screen_test, oem_guidance_screen_test, permission_rationale_screen_test, permission_denied_screen_test, active_session_banner_test)

Focus:
- canPop() ? pop() : go('/') discipline on deep-linkable screens (OemGuidanceScreen — Phase 05 STATE.md `OemGuidanceScreen._onDone uses canPop() ? pop() : go('/')`)
- pumpAndSettle avoidance on screens with Stream.periodic (_ChronoCard 1s tick — Phase 05 STATE.md `Widget tests must avoid pumpAndSettle when the live Tracking dashboard is rendered`) — this is pre-class §2 item 7 (Should). Re-confirm or surface NEW occurrences as Should fixes.
- ProviderScope overrides inline (Override not exported by flutter_riverpod 3.3.x — Phase 05 STATE.md `Override is NOT publicly exported`)
- WidgetsBinding.instance.addPostFrameCallback for TextEditingController.dispose() during animations (Phase 05 STATE.md `Deferred TextEditingController.dispose()`)
- Smoke test overrides sessionStoreProvider with in-memory fake (Phase 05 STATE.md `Smoke test overrides sessionStoreProvider with in-memory fake`)
- Banner gesture-arena split (inner InkWell + peer IconButton — no ancestor InkWell wrapping the whole Row)
- const constructors per CLAUDE.md §Widgets

Output format: same as Agent #1.

**Agent #4 — Boot watchdog + native bridges + POC tooling + CLAUDE.md sweep**
Scope (LARGEST agent — combined CLAUDE.md anti-patterns sweep + POC review + native bridges + tooling):
- `lib/infrastructure/platform/boot_completed_watchdog.dart` (Dart logic, runBootWatchdogEntryPoint entry point, channel constant `'app.gosl.mirkfall/boot_watchdog'`, DB reopening via buildAppDatabase factory, mandatory engine.destroy + DB close try/finally per Phase 05 STATE.md `Mini-engine DB close is mandatory`)
- `lib/infrastructure/platform/ios_significant_change_watchdog.dart` (wrapper class no-op non-iOS, channel constant mirror, wraps controller hook)
- `lib/infrastructure/platform/oem_detector.dart` (sealed OemFamily 7 variants, regex resolution chain Xiaomi → Samsung → Huawei → OnePlus → Oppo → Other, isIosOverride / isAndroidOverride seam — Phase 05 STATE.md `OemDetector.detect takes isIosOverride/isAndroidOverride`)
- `android/app/src/main/kotlin/app/gosl/mirkfall/BootCompletedReceiver.kt` (Android receiver, notification-only NOT fg-service start from boot per Phase 05 STATE.md, channel string literal MUST match Dart constant verbatim)
- `ios/Runner/AppDelegate.swift` — verify whether the channel literal `'app.gosl.mirkfall/boot_watchdog'` is STILL present post-Xcode 26 strip (Phase 05 STATE.md `iOS FlutterImplicitEngineDelegate bridge stripped after Xcode 26 move`). RESEARCH Open Question 1 — Plan 06-04 Test #1 inertness guard depends on this verification.
- `tool/plot_session_fixes.py` + `tool/requirements.txt` (staticmap 0.5.x + Pillow deps audit — tool-side scope per Phase 05 STATE.md `Python POC tool deps live in tool/requirements.txt only — DEPENDENCIES.md is binary-ship-scoped`)
- `docs/store-review-rationale.md` (QUAL-03 content + English polish scope — pre-class §2 item 6)
- `docs/qual-01-02-poc.md` + `docs/poc-artifacts/*` (POC evidence completeness — already extracted by Plan 06-02; Agent #4 confirms no overlooked items)
- `pubspec.yaml` deltas Phase 05 (geolocator 14.0.2 / flutter_local_notifications 21.0.0 / permission_handler 12.0.1 / device_info_plus 12.4.0 — verify pin justification re. win32 conflict per Phase 05 STATE.md `device_info_plus pinned to 12.4.0`)
- `DEPENDENCIES.md` Phase 05 entries (geolocator / flutter_local_notifications / permission_handler / device_info_plus / share_plus rows complete with telemetry audit + license + date)
- `lib/config/constants.dart` deltas Phase 05 (kDistanceFilterMeters / kFixBatchFlushInterval / GPS timeouts — verify no magic numbers leaked elsewhere)
- CLAUDE.md anti-patterns sweep on ALL Phase 05 code (magic numbers outside `lib/config/constants.dart`, naming conventions xxxFilename / xxxFileName / xxxBasename / xxxDir, valueByKey Maps, xxxSet Sets, xxxs Lists, DTOs without distinct semantic, delegation wrappers, narrative comments, context.mounted post-await discipline, const constructors, p.join over manual `/` `\\`)

Focus:
- MethodChannel triple-source-of-truth verification (Kotlin BootCompletedReceiver.kt + Dart boot_completed_watchdog.dart + Dart ios_significant_change_watchdog.dart + IF Swift AppDelegate.swift) — string literal MUST match `'app.gosl.mirkfall/boot_watchdog'` verbatim
- DEPENDENCIES.md entries Phase 05 complete (no missing rows for any Phase 05 direct dep)
- tool/ Python deps in tool/requirements.txt scope-correct (NOT in DEPENDENCIES.md per Phase 05 STATE.md — DEPENDENCIES.md is binary-ship-scoped)
- dart format drift check: `dart format --line-length 160 --set-exit-if-changed lib/ test/ tool/` returns 0 — pre-class §2 item 8 (Noted; if drift found, becomes Should fix)
- POC artefact location drift (.planning/pocs/phase-05/ vs docs/qual-01-02-poc.md + docs/poc-artifacts/) — pre-class §2 item 2 (Should; ROADMAP amendment in Plan 06-05 fix loop)
- AndroidManifest.xml + Info.plist crosscheck against `tool/check_platform_manifests.dart` requirements (anticipates Plan 06-04)
- BootCompletedWatchdog 4 existing unit tests cover active / none / idempotent / error-swallow — pure-Dart per Phase 05 STATE.md `BootCompletedWatchdog is PURE DART`

Output format: same as Agent #1, PLUS a dedicated **adversarial readiness checklist** sub-section in the narrative appendix:

```markdown
#### Agent #4 — Adversarial readiness checklist for Plan 06-04

- [ ] Test #1 MethodChannel sync: Swift channel literal in `ios/Runner/AppDelegate.swift` is **present** / **absent** (mark one). If absent, Test #1 file map must EXCLUDE the Swift entry (Open Question 1 from RESEARCH).
- [ ] Test #3 OemDetector ambiguous fixtures: 3-5 ambiguous AndroidDeviceInfo fixtures sketched (e.g. manufacturer=aosp brand=oneplus, manufacturer=xiaomi brand=redmi build=miui, manufacturer=huawei brand=honor) — confirm each resolves to a deterministic OemFamily per current OemDetector regex order.
- [ ] Test #4 Platform manifests: list of required AndroidManifest uses-permission entries + Info.plist required keys cross-checked against current state (any drift becomes a Should fix in loop).
- [ ] Test #5 Android boot receiver contract: BootCompletedReceiver.kt class path + Kotlin channel literal + AndroidManifest.xml `<receiver android:name=".BootCompletedReceiver">` declaration confirmed aligned.
- [ ] Test #6 Adversarial branch CI: confirm `.github/workflows/ci.yml` `on.push.branches: [main]` (current state) — Plan 06-04 will inline-expand to `[main, 'adversarial/**']` on the throwaway branch only.
- [ ] ROADMAP SC#1 amendment text drafted (pre-class §2 item 2): change `.planning/pocs/phase-05/` → `docs/qual-01-02-poc.md + docs/poc-artifacts/`.
- [ ] dart format drift watch (pre-class §2 item 8): local run of `dart format --line-length 160 --set-exit-if-changed lib/ test/ tool/` returns exit code <0 or 1>.
```

This checklist becomes Plan 06-04's prerequisite — without it, Plan 06-04 might author Test #1 with a stale Swift assumption.

**Pre-class 8 items text (CONTEXT.md §POC evidence acceptance verbatim):**

```markdown
### Pre-known from CONTEXT

*Filled by Plan 06-03 Task 1 BEFORE spawning sub-agents. Source: 06-CONTEXT.md §POC evidence acceptance + §Adversarial wave + §SC#4 OEM workaround. Committed as `docs(06-rev): pre-class 8 CONTEXT handoff items into §2`.*

1. **[Noted] iOS walk duration 13.5 min vs 30 min target** — Plan 05-06 PASS-with-caveat accepted (CONTEXT.md). Convergent same-day Android evidence (Pixel 4a 28.6 min PASS) supports extrapolation; stable cadence throughout iOS walk indicates no background suspension. Phase 06 closes without re-walk; user may request 30-min top-up Phase 15 release-confidence.
2. **[Should] POC artefact location drift** — ROADMAP SC#1 says `.planning/pocs/phase-05/`, actual artefacts live in `docs/qual-01-02-poc.md` + `docs/poc-artifacts/`. Fix in Plan 06-05 loop: 1 atomic commit `docs(06-rev): amend ROADMAP.md SC#1 to match docs/ artifact location`.
3. **[Noted] SC#2 battery measurement < 15%/h waiver** — extracted from POC if present (see §1b Battery delta), else inline waiver with fix-cadence proxy argument. Full dumpsys battery_stats deferred Phase 15 release-confidence per ROADMAP.
4. **[Noted] Xiaomi / Samsung / Huawei / OnePlus OEM coverage deferred** — already accepted Phase 05 (ROADMAP SC#1 annotated "partial"). Phase 06 does not re-litigate.
5. **[Noted] Auto-resume-post-kill iOS unvalidated** — FlutterImplicitEngineDelegate bridge stripped after Xcode 26 move per Phase 05 STATE.md. Android covered by 4 BootCompletedWatchdog unit tests + Plan 05-05. iOS rewire deferred Phase 15.
6. **[Noted] Store rationale English copy final polish** — `docs/store-review-rationale.md` French copy is defended-by-reviewer-quality (Plan 05-06 SUMMARY). English Phase 15 polish per CONTEXT.md.
7. **[Should] Flaky widget-test pumpAndSettle races** — pre-flag known-pattern (Phase 05 STATE.md `Widget tests must avoid pumpAndSettle`). Agent #3 verifies no `pumpAndSettle()` in `test/presentation/**` Phase 05 tests touching `_ChronoCard`. If any new occurrence found, becomes Should fix in loop.
8. **[Noted] dart format drift regression watch** — `dart format --line-length 160 --set-exit-if-changed` CI gate active since Plan 04-05. Agent #4 runs locally to confirm zero drift; if drift found, becomes Should fix in loop (Phase 04 surprise Blocker precedent).
```

**SC#4 OEM workaround plan table template (CONTEXT.md §SC#4 OEM workaround gate-closure):**

```markdown
### SC#4 OEM workaround plan

*Built from `lib/presentation/screens/oem_guidance_screen.dart::_copyFor` + `lib/infrastructure/platform/oem_detector.dart` OemFamily variants + `permission_handler.openAppSettings` reachability + dontkillmyapp.com URLs. Self-contained: future maintainer reads §2 and understands the Phase 06 signed-off OEM workaround baseline. Source linked: `docs/store-review-rationale.md` if content overlap exists.*

| OemFamily | OemGuidanceScreen copy summary | dontkillmyapp.com URL | openLocationSettings reachability | Pre-class severity |
|-----------|--------------------------------|-----------------------|-----------------------------------|-------------------|
| Xiaomi | <2-line summary from _copyFor() Xiaomi case> | https://dontkillmyapp.com/xiaomi | reachable via permission_denied_screen.dart → openAppSettings() | Noted (covered) |
| Samsung | <2-line summary> | https://dontkillmyapp.com/samsung | reachable | Noted (covered) |
| Huawei | <2-line summary> | https://dontkillmyapp.com/huawei | reachable | Noted (covered) |
| OnePlus | <2-line summary> | https://dontkillmyapp.com/oneplus | reachable | Noted (covered) |
| Oppo | <2-line summary> | https://dontkillmyapp.com/oppo | reachable | Noted (covered) |
| OtherOem | <2-line summary — generic guidance> | https://dontkillmyapp.com/ (root) | reachable | Noted (covered) |
| IosDevice | <2-line summary — iOS-specific guidance, no dontkillmyapp.com link applicable> | n/a | reachable via Settings app deep-link | Noted (covered) |

**Deferred Phase 15 (Noted):**
- "Tracking interrompu on next launch" banner — Phase 15 SC#4 recovery flow (overlaps Phase 15 plan).
- Native per-OEM battery-settings intent deep-links (MIUI Security / Huawei PhoneManager / Samsung DeviceCare / OnePlus Battery) — maintenance drift across OS versions; dontkillmyapp.com link suffices V1.0.
- Second iOS POC walk reaching 30 min target (also pre-class item 1).
```

The `<2-line summary>` cells are filled by Agent #4 during the audit pass; this template provides the structure but Agent #4 is the source of truth for cell content.

**Severity-disagreement cross-lens preservation rule (Phase 04 convention, locked Phase 06):**
A finding surfaced by 2 agents under different lenses with different severities is preserved under BOTH lens sub-sections with explicit cross-reference (e.g. `(also surfaced by Agent #3 as Should — see §2 Agent #3 finding #N)`), NOT deduplicated. Audit transparency over compression.

**Triage table format (§3) — verbatim from Phase 04:**

```markdown
## 3. Triage decisions

| # | Finding | Severity | Decision | Rationale | Commit hash |
|---|---------|----------|----------|-----------|-------------|
| 1 | Pre-class #1 — iOS walk duration 13.5 min | Noted | observation | PASS-with-caveat accepted CONTEXT.md | n/a |
| 2 | Pre-class #2 — POC artefact location drift | Should | fix | ROADMAP SC#1 amendment in Plan 06-05 loop | (pending Plan 06-05) |
| 3 | Pre-class #3 — battery <15%/h waiver | Noted | observation | fix-cadence proxy + Phase 15 dumpsys | n/a |
| ... | ... | ... | ... | ... | ... |
```

Every Blocker MUST be `fix` (waiver forbidden per CONTEXT.md). Every Should MUST be `fix` OR `waived (rationale)` (rationale non-empty). Every Could MUST be `fix` / `waived` / `defer-to-phase-15` / `won't-fix`. Every Noted is kept as observation (no decision needed; row tracks for audit transparency).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-class 8 CONTEXT handoff items into §2 (BEFORE agent spawn)</name>
  <files>.planning/phases/06-review-gate-gps/06-REVIEW.md</files>
  <action>
    Use the Edit tool to replace the §2 `Pre-known from CONTEXT` placeholder block with the populated 8-item list. Boundary discipline: edit ONLY the `### Pre-known from CONTEXT` sub-section — leave §1 (Plan 06-01) + §1b (Plan 06-02) + the rest of §2/§3/§4/§5 untouched.

    Anchor the Edit on the placeholder text from Plan 06-01's scaffold:

    OLD (Plan 06-01 placeholder):
    ```
    ### Pre-known from CONTEXT

    *Filled by Plan 06-03 Task 1 BEFORE spawning sub-agents. Source: 06-CONTEXT.md §POC evidence acceptance (pre-class §2 items). Committed as `docs(06-rev): pre-class 8 CONTEXT handoff items into §2` before any Agent tool call.*

    (pending — 8 entries: iOS duration Noted | POC artefact path drift Should | battery waiver Noted | OEM coverage Noted | iOS auto-resume Noted | store-EN Noted | pumpAndSettle watch Should | dart format drift watch Noted)
    ```

    NEW (use the 8-item template from `<interfaces>` above verbatim — copy-paste).

    Verify §2 sub-section populated:
    ```bash
    awk '/^### Pre-known from CONTEXT/{flag=1; next} /^### SC#4/{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "(pending"
    # must return 0

    grep -c "^[0-9]\\+\\.\\s\\+\\*\\*\\[" .planning/phases/06-review-gate-gps/06-REVIEW.md
    # must return at least 8 (one per pre-class item) — check is approximate; visual inspection authoritative
    ```

    Then commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(06-rev): pre-class 8 CONTEXT handoff items into §2" --files .planning/phases/06-review-gate-gps/06-REVIEW.md
    ```

    **CRITICAL anti-pattern guard:** Do NOT spawn any Agent tool call in this task. Pre-class commit MUST land on `main` BEFORE Task 3 spawns the 4-agent wave. Phase 04 inaugural pattern.
  </action>
  <verify>
    <automated>bash -c 'grep -q "^### Pre-known from CONTEXT" .planning/phases/06-review-gate-gps/06-REVIEW.md && [ "$(awk "/^### Pre-known from CONTEXT/{flag=1; next} /^### SC#4/{flag=0} flag" .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "(pending")" = "0" ] && git log --oneline -5 -- .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -q "pre-class 8 CONTEXT handoff"'</automated>
  </verify>
  <done>§2 `Pre-known from CONTEXT` sub-section populated with 8 entries verbatim from CONTEXT.md §POC evidence acceptance. No `(pending` markers remain in the sub-section range. One atomic commit `docs(06-rev): pre-class 8 CONTEXT handoff items into §2` on `main`. §1 + §1b + rest of §2 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build SC#4 OEM workaround plan table in §2 (BEFORE agent spawn)</name>
  <files>.planning/phases/06-review-gate-gps/06-REVIEW.md</files>
  <action>
    First, read the source files to extract per-OemFamily content:

    ```bash
    cat lib/infrastructure/platform/oem_detector.dart  # confirm 7 OemFamily variants
    cat lib/presentation/screens/oem_guidance_screen.dart  # extract _copyFor() switch content
    cat lib/presentation/screens/permission_denied_screen.dart  # confirm openAppSettings() reachability
    cat docs/store-review-rationale.md  # check for content overlap with the OEM table
    ```

    **Precondition — `_copyFor()` 7-variant switch coverage check (revision 2026-04-20 W3):**

    Before populating the table, verify that `lib/presentation/screens/oem_guidance_screen.dart::_copyFor()` switches on ALL 7 OemFamily variants (Xiaomi / Samsung / Huawei / OnePlus / Oppo / OtherOem / IosDevice). Use the OemDetector source as the canonical list:

    ```bash
    # Extract the OemFamily sealed variants from oem_detector.dart
    grep -E "^\s*(class|case)\s+(Xiaomi|Samsung|Huawei|OnePlus|Oppo|OtherOem|IosDevice)" lib/infrastructure/platform/oem_detector.dart

    # Extract the _copyFor() switch cases
    awk '/_copyFor\(/{flag=1} flag{print} /^  }$/{if(flag){flag=0}}' lib/presentation/screens/oem_guidance_screen.dart
    ```

    For each OemFamily variant, determine the `_copyFor()` coverage:
    - **Variant has explicit `case OemFamily.X():` arm** → row severity baseline = `Noted (covered)`
    - **Variant falls through default / wildcard** → row severity escalates to `Should (gap)` with the missing-or-default copy noted inline (e.g. "OemGuidanceScreen copy column: 'falls through to OtherOem default — explicit case missing'")
    - **Variant absent entirely (impossible if Dart sealed exhaustiveness check is on; if it ever happens it's a Blocker)** → row severity = `Blocker (gap)` and add a CLAUDE.md violation finding to Agent #4's structured findings list at Task 3

    Document the coverage check result inline in the table's introductory paragraph (e.g. "_copyFor() coverage check: 7/7 variants explicitly handled" OR "_copyFor() coverage check: 6/7 variants explicitly handled — IosDevice falls through to OtherOem default, escalated to Should (gap)").

    For each of the 7 OemFamily variants (Xiaomi / Samsung / Huawei / OnePlus / Oppo / OtherOem / IosDevice), extract:
    1. **OemGuidanceScreen copy summary** — 2-line summary of the title + steps from `_copyFor()` switch case (paraphrase, not verbatim — table cell readability)
    2. **dontkillmyapp.com URL** — exact `learnMoreUrl` from `_copyFor()` (n/a for IosDevice)
    3. **openLocationSettings reachability** — confirm the path: most likely `permission_denied_screen.dart` → `openAppSettings()` from `permission_handler`. Some OemFamily variants may also expose direct settings deep-link via `OemGuidanceScreen` reuse.
    4. **Pre-class severity** — default `Noted (covered)` if all 3 columns are filled with concrete content AND `_copyFor()` has an explicit case for this variant (per precondition above). If any column is missing/incomplete OR `_copyFor()` falls through to default, escalate to `Should (gap)` with the gap description.

    Use the Edit tool to replace the §2 `SC#4 OEM workaround plan` placeholder block:

    OLD (Plan 06-01 placeholder):
    ```
    ### SC#4 OEM workaround plan

    *Filled by Plan 06-03 Task 2 from `lib/presentation/screens/oem_guidance_screen.dart::_copyFor` switch + `lib/infrastructure/platform/oem_detector.dart` OemFamily variants + `permission_handler.openAppSettings` reachability + dontkillmyapp.com URLs. The "Tracking interrompu on next launch" banner is explicitly DEFERRED to Phase 15 SC#4 recovery flow per CONTEXT.md.*

    | OemFamily | OemGuidanceScreen copy summary | dontkillmyapp.com URL | openLocationSettings reachability | Pre-class severity |
    |-----------|--------------------------------|-----------------------|----------------------------------|-------------------|
    | (pending) | | | | |
    ```

    NEW (use the SC#4 OEM workaround plan table template from `<interfaces>` above with the per-OemFamily cells filled from the source extraction; include the "Deferred Phase 15 (Noted)" sub-list at the bottom verbatim; add the "_copyFor() coverage check" line at the top of the introductory paragraph per the precondition above).

    Verify table populated with 7 rows + deferred sub-list:
    ```bash
    awk '/^### SC#4 OEM workaround plan/{flag=1; next} /^### Agent #1/{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "(pending"
    # must return 0

    awk '/^### SC#4 OEM workaround plan/{flag=1; next} /^### Agent #1/{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "^| "
    # must return at least 9 (1 header + 1 separator + 7 OemFamily rows = 9)

    grep -q "_copyFor() coverage check" .planning/phases/06-review-gate-gps/06-REVIEW.md
    # must match — confirms precondition coverage check was documented
    ```

    Then commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(06-rev): build SC#4 OEM workaround plan table in §2" --files .planning/phases/06-review-gate-gps/06-REVIEW.md
    ```

    **CRITICAL anti-pattern guard:** Do NOT spawn any Agent tool call in this task. Both pre-class commits (Task 1 + Task 2) MUST land on `main` BEFORE Task 3 spawns the 4-agent wave.
  </action>
  <verify>
    <automated>bash -c 'grep -q "^### SC#4 OEM workaround plan" .planning/phases/06-review-gate-gps/06-REVIEW.md && [ "$(awk "/^### SC#4 OEM workaround plan/{flag=1; next} /^### Agent #1/{flag=0} flag" .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "(pending")" = "0" ] && grep -q "dontkillmyapp\\.com" .planning/phases/06-review-gate-gps/06-REVIEW.md && grep -q "_copyFor() coverage check" .planning/phases/06-review-gate-gps/06-REVIEW.md && git log --oneline -5 -- .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -q "SC#4 OEM workaround"'</automated>
  </verify>
  <done>§2 `SC#4 OEM workaround plan` sub-section populated with 7 OemFamily rows + deferred Phase 15 sub-list + `_copyFor() coverage check` line in the intro paragraph (X/7 variants explicitly handled). dontkillmyapp.com URLs cited per OemFamily. Pre-class severity column filled (default `Noted (covered)`; escalations to `Should (gap)` if any column incomplete OR `_copyFor()` falls through to default for that variant). One atomic commit `docs(06-rev): build SC#4 OEM workaround plan table in §2` on `main`. §1 + §1b + Pre-known from CONTEXT + rest of §2/§3/§4/§5 untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Spawn 4 sub-agents (general-purpose) in SINGLE tool-use message + assemble findings into §2</name>
  <files>.planning/phases/06-review-gate-gps/06-REVIEW.md</files>
  <action>
    PREREQUISITE GATE — verify both pre-class commits are on `main` BEFORE proceeding:
    ```bash
    git log --oneline -10 | grep -E "(pre-class 8 CONTEXT|build SC#4 OEM workaround)"
    # MUST show both commits. If either is missing, abort and complete Task 1/2 first.
    ```

    Spawn **4 Agent tool calls in a SINGLE tool-use message** (parallel — not 4 sequential messages). Each Agent invocation uses `subagent_type: general-purpose` per Phase 02 + 04 Accumulated Decision (locked rule).

    Agent prompt skeleton — use this for all 4, substituting Agent-specific scope from `<interfaces>` above:

    ```
    You are sub-agent #N for Phase 06 Review Gate — GPS audit of Phase 05 GPS & Session Lifecycle artefacts.

    Your scope (DO NOT silently expand):
    <Agent #N scope verbatim from interfaces — files + focus + output format>

    Read every file in your scope ligne-à-ligne. Apply the focus criteria to surface findings.

    Output:
    1. Structured findings list — markdown bullet per finding:
       `- [Blocker|Should|Could|Noted] Title — 1-line explanation — file:line`
       Severities (Phase 02 + 04 definitions):
       - Blocker: must be fixed before Phase 07 opens (CLAUDE.md violation, runtime bug, broken invariant, missing test for stated invariant)
       - Should: would be Blocker but has a workaround OR is non-runtime (style, deferrable hygiene)
       - Could: nice-to-have polish or future-proofing
       - Noted: observation only, no fix expected (decision documented elsewhere)
    2. Narrative appendix — prose audit notes for context (will be archived in §2 collapsible Audit Notes block).
    3. Agent #4 ONLY: dedicated "Adversarial readiness checklist for Plan 06-04" sub-section in narrative appendix (use template from interfaces).

    Pre-class items already in §2 (DO NOT re-surface as Blocker/Should — they're already classified Noted/Should with rationale):
    1. iOS walk duration 13.5 min — Noted
    2. POC artefact path drift — Should (fix Plan 06-05)
    3. Battery <15%/h waiver — Noted
    4. OEM Xiaomi/Samsung/Huawei/OnePlus deferral — Noted
    5. iOS auto-resume deferral — Noted
    6. Store-EN polish — Noted
    7. pumpAndSettle widget-test races — Should (you may surface ADDITIONAL occurrences as Should)
    8. dart format drift watch — Noted (you may upgrade to Should if drift detected)

    Cross-lens preservation: if you suspect another agent's lens overlaps, mention it in your finding (e.g. "may also surface in Agent #2 controller lens"). Do NOT dedupe.

    Return ONLY the findings list + narrative appendix. Do NOT propose fixes — that's the user's triage decision in §3.
    ```

    Wait for all 4 agents to return. Assemble:
    1. Open `06-REVIEW.md` for Edit
    2. Replace the `### Agent #N — ...` placeholders (one per agent) with the structured findings list returned by each agent
    3. Replace the `<details><summary>Audit Notes (narrative appendix, per agent)</summary>` block with the 4 narrative appendices (one `#### Agent #N Narrative` sub-heading per agent), preserving the Agent #4 adversarial readiness checklist sub-section verbatim
    4. Cross-lens preservation: if Agent #N flagged a finding "may also surface in Agent #M lens" AND Agent #M ALSO flagged it, preserve under BOTH with `(also surfaced by Agent #M as <severity> — see §2 Agent #M finding #X)` cross-reference. Do NOT dedupe.

    Verify §2 fully populated (no `(pending` markers anywhere in §2 range):
    ```bash
    awk '/^## 2\\./{flag=1; next} /^## 3\\./{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "(pending"
    # must return 0

    awk '/^## 2\\./{flag=1; next} /^## 3\\./{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "^### Agent #"
    # must return 4
    ```

    Commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(06-rev): consolidate 4-agent audit findings into 06-REVIEW.md §2" --files .planning/phases/06-review-gate-gps/06-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c '[ "$(awk "/^## 2\\./{flag=1; next} /^## 3\\./{flag=0} flag" .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "(pending")" = "0" ] && [ "$(awk "/^## 2\\./{flag=1; next} /^## 3\\./{flag=0} flag" .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -c "^### Agent #")" = "4" ] && git log --oneline -5 -- .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -q "consolidate 4-agent audit"'</automated>
  </verify>
  <done>4 sub-agents spawned in SINGLE tool-use message (parallel). Each returned structured findings list + narrative appendix. §2 populated: pre-class + SC#4 OEM table + 4 Agent sub-sections + collapsible Audit Notes with 4 per-agent narrative + Agent #4 adversarial readiness checklist. Cross-lens findings preserved (no dedup). One atomic commit `docs(06-rev): consolidate 4-agent audit findings into 06-REVIEW.md §2` on `main`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Present findings to user as titles + 1-line explanations + capture triage into §3</name>
  <files>.planning/phases/06-review-gate-gps/06-REVIEW.md</files>
  <action>
    Per CLAUDE.md §Code Review Phases: present the user with **titles + 1-line explanations** ONLY (no diffs, no code blocks, no prose audit notes) so the user can choose what to fix without bias.

    Claude-main posts in chat:

    > "Audit Phase 06 terminé — 4 sub-agents + 8 pre-class + SC#4 OEM workaround. Findings count par sévérité :
    >
    > **Pre-class §2 (8):** <count Blocker / Should / Could / Noted from §2 Pre-known sub-section>
    > **SC#4 OEM table (7):** <count escalations to Should (gap) if any>
    > **Agent #1 GPS infra:** <count Blocker / Should / Could / Noted>
    > **Agent #2 Controller + permissions:** <same>
    > **Agent #3 UI + routing:** <same>
    > **Agent #4 Boot watchdog + native + tooling + CLAUDE.md sweep:** <same>
    >
    > **TOTAL:** <X> Blockers / <Y> Shoulds / <Z> Coulds / <W> Noteds
    >
    > Findings list (titles + 1-line explanations only; full file:line + narrative dispo dans `06-REVIEW.md` §2 Audit Notes si tu veux creuser un point) :
    >
    > ### Blockers (<X>)
    > 1. [Agent #N] <Title> — <1-line explanation>
    > 2. ...
    >
    > ### Shoulds (<Y>)
    > 1. [Pre-class #2] POC artefact location drift — ROADMAP SC#1 amendment in Plan 06-05 loop
    > 2. [Pre-class #7] pumpAndSettle widget-test races — Agent #3 found <N additional>: <list>
    > 3. [Agent #N] <Title> — <1-line explanation>
    > ...
    >
    > ### Coulds (<Z>)
    > 1. [Agent #N] <Title> — <1-line explanation>
    > ...
    >
    > ### Noteds (<W>)
    > 1. [Pre-class #1] iOS walk duration 13.5 min — PASS-with-caveat accepted CONTEXT.md
    > ...
    >
    > Décide pour chaque finding : `fix` / `waived (rationale)` / `defer-to-phase-15` / `won't-fix` / (Noted = observation only).
    >
    > Rappel CONTEXT.md gate-closed criteria :
    > - Tous Blockers MUST fix (waiver forbidden)
    > - Tous Shoulds MUST fix OR waive avec rationale non-vide
    > - Coulds : ton choix
    > - Noteds : observations, no decision
    >
    > Tu peux répondre :
    > (a) **Per-finding** (`#1 fix / #2 waived parce que X / ...`) — Phase 02 / 04 default
    > (b) **Blanket-decision** (`fix tous Blockers + Shoulds, defer Coulds, Noteds observations`) — Phase 04 precedent (user said `let's fix blocker and should`)
    >
    > Quelle stratégie de triage ?"

    User responds with per-finding decisions OR blanket decision.

    Claude builds the §3 triage table per the verbatim format from `<interfaces>` (rows for every finding including pre-class items + SC#4 OEM table escalations + 4-agent findings; Decision + Rationale + Commit hash placeholder columns). Use the Edit tool to replace the §3 placeholder block (the scaffold has `| (pending) | | | | | |` as the placeholder row — anchor on that).

    Example final triage table row format:
    ```
    | 1 | [Pre-class #1] iOS walk duration 13.5 min | Noted | observation | PASS-with-caveat accepted CONTEXT.md | n/a |
    | 2 | [Pre-class #2] POC artefact location drift | Should | fix | ROADMAP SC#1 amendment | (pending Plan 06-05) |
    | 3 | [Agent #1] distanceFilter int typing — verified | Noted | observation | regression locked Phase 05 STATE.md | n/a |
    | 4 | [Agent #2] AsyncValue.value pattern — verified | Noted | observation | Phase 05 STATE.md | n/a |
    | ... | ... | ... | ... | ... | ... |
    ```

    Verify §3 populated (every Blocker has Decision=`fix`; every Should has Decision=`fix` OR `waived` with non-empty Rationale; the scaffold placeholder row `| (pending) | | | | | |` is no longer present):
    ```bash
    # Confirm the scaffold placeholder row is gone (anchor matches the actual scaffold from Plan 06-01 line 215: `| (pending) | | | | | |`)
    awk '/^## 3\\./{flag=1; next} /^## 4\\./{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -cE '^\\| \\(pending\\) \\|'
    # must return 0

    # Count Blockers in §3 with Decision != fix (must be 0)
    awk '/^## 3\\./{flag=1; next} /^## 4\\./{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -E "\\| Blocker \\| (waived|defer|won.t-fix|observation)" | wc -l
    # must be 0

    # Count Shoulds in §3 with empty Rationale (must be 0)
    awk '/^## 3\\./{flag=1; next} /^## 4\\./{flag=0} flag' .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -E "\\| Should \\| (waived) \\|  \\|" | wc -l
    # must be 0
    ```

    Commit §3:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(06-rev): capture user triage decisions into 06-REVIEW.md §3" --files .planning/phases/06-review-gate-gps/06-REVIEW.md
    ```

    Confirm to user: "§3 triage capturé et committé. Plan 06-04 démarre l'adversarial wave (5 unit tests + 1 nouveau CI gate + 1 throwaway branch). Je laisse Plan 06-04 prendre le relais — confirme avec `OK adversarial`."
  </action>
  <verify>
    <automated>bash -c '[ "$(awk "/^## 3\\./{flag=1; next} /^## 4\\./{flag=0} flag" .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -cE "^\\| \\(pending\\) \\|")" = "0" ] && grep -q "^| 1 |" .planning/phases/06-review-gate-gps/06-REVIEW.md && git log --oneline -5 -- .planning/phases/06-review-gate-gps/06-REVIEW.md | grep -q "user triage decisions"'</automated>
  </verify>
  <done>§3 triage table populated with one row per finding (pre-class 8 + SC#4 OEM 7 + 4-agent findings). The scaffold placeholder row `| (pending) | | | | | |` (from Plan 06-01 line 215) is no longer present. Every Blocker = `fix`. Every Should = `fix` OR `waived (rationale)`. User has explicitly chosen triage strategy (per-finding OR blanket). One atomic commit `docs(06-rev): capture user triage decisions into 06-REVIEW.md §3` on `main`. User signaled "OK adversarial" to unblock Plan 06-04.</done>

  <what-built>
    Nothing automated. This checkpoint IS the CLAUDE.md §Code Review Phases protocol step "tu affiches uniquement les **titres** avec une courte explication, et tu me laisses choisir ce qu'on corrige". User receives the synthesized findings list (titles + 1-line explanations only — no diffs, no code blocks) and decides per-finding (or blanket) what to fix.
  </what-built>
  <how-to-verify>
    After Claude posts findings + user triages:
    - §3 has one row per finding with Decision column populated
    - The scaffold placeholder row `| (pending) | | | | | |` no longer exists in §3
    - No Blocker has Decision other than `fix`
    - No Should with Decision=`waived` has empty Rationale
    - User signal "OK adversarial" received in chat
  </how-to-verify>
  <resume-signal>
    User types "OK adversarial" or "§3 triagé, go" to signal Plan 06-04 may start the adversarial wave.
    User may also re-open the triage if findings spark new questions — Claude updates §3 and re-commits before proceeding to Plan 06-04.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write 06-03-SUMMARY.md with adversarial readiness checklist for Plan 06-04</name>
  <files>.planning/phases/06-review-gate-gps/06-03-SUMMARY.md</files>
  <action>
    Create `.planning/phases/06-review-gate-gps/06-03-SUMMARY.md` (do NOT prepend GOSL header — markdown file) summarizing Wave 3:

    ```markdown
    # Phase 06 Plan 03 — Summary

    **Completed:** 2026-04-XX
    **Commits:** 4 atomic — pre-class §2 / SC#4 OEM table / 4-agent findings consolidation / §3 triage capture

    ## Agent dispatch — single tool-use message

    All 4 agents spawned in ONE tool-use message (parallel):
    - Agent #1 (general-purpose) — GPS infra + notifications + Drift V3 + manifest declarations
    - Agent #2 (general-purpose) — Controller + permissions + Riverpod state
    - Agent #3 (general-purpose) — UI + routing + banner widget
    - Agent #4 (general-purpose) — Boot watchdog + native bridges + POC tooling + CLAUDE.md sweep

    Wall-clock: <X> min total (bounded by slowest agent — record per-agent timing).

    ## SC#4 OEM workaround — _copyFor() coverage check

    - **OemFamily variants in oem_detector.dart:** 7 (Xiaomi / Samsung / Huawei / OnePlus / Oppo / OtherOem / IosDevice)
    - **_copyFor() switch coverage:** <X>/7 explicit cases
    - **Escalations:** <list any variant rows escalated from `Noted (covered)` to `Should (gap)` due to default fallthrough; if none, "all 7 variants explicitly handled — no escalations">

    ## Findings counts

    | Source | Blocker | Should | Could | Noted |
    |--------|---------|--------|-------|-------|
    | Pre-class §2 (8 items) | 0 | <Y> | 0 | <8-Y> |
    | SC#4 OEM table escalations | 0 | <Z> | 0 | <7-Z> |
    | Agent #1 GPS infra | <a1B> | <a1S> | <a1C> | <a1N> |
    | Agent #2 Controller | <a2B> | <a2S> | <a2C> | <a2N> |
    | Agent #3 UI | <a3B> | <a3S> | <a3C> | <a3N> |
    | Agent #4 Boot/native/POC/sweep | <a4B> | <a4S> | <a4C> | <a4N> |
    | **TOTAL** | <X> | <Y> | <Z> | <W> |

    ## Triage strategy

    - User chose: **per-finding** OR **blanket-fix-Blockers+Shoulds-defer-Coulds** (record verbatim user decision)
    - Cross-lens preservations: <N> findings preserved under 2+ agents with cross-references (Phase 04 convention)

    ## Adversarial readiness checklist for Plan 06-04 (from Agent #4 narrative)

    - [x] / [ ] **Test #1 MethodChannel sync** — Swift channel literal in `ios/Runner/AppDelegate.swift` is **<present|absent>** (record actual; Open Question 1 from RESEARCH closed). If absent, Test #1 file map EXCLUDES Swift entry; Test #5 covers Android-side cleanly.
    - [x] / [ ] **Test #3 OemDetector ambiguous fixtures** — sketched 3-5 fixtures: <list>. Each resolves to deterministic OemFamily per current OemDetector regex order Xiaomi → Samsung → Huawei → OnePlus → Oppo → Other.
    - [x] / [ ] **Test #4 Platform manifests** — required AndroidManifest entries (<list>) + Info.plist required keys (<list>) cross-checked. Drift status: <none / N items>.
    - [x] / [ ] **Test #5 Android boot receiver contract** — BootCompletedReceiver.kt class path = `android/app/src/main/kotlin/app/gosl/mirkfall/BootCompletedReceiver.kt` (verified). MethodChannel literal verbatim match: <yes/no>.
    - [x] / [ ] **Test #6 Adversarial branch CI** — current `.github/workflows/ci.yml` `on.push.branches: [main]` (verified). Plan 06-04 will inline-expand to `[main, 'adversarial/**']` on the throwaway branch only.
    - [x] / [ ] **ROADMAP SC#1 amendment text drafted** (pre-class §2 item 2): change `.planning/pocs/phase-05/` → `docs/qual-01-02-poc.md + docs/poc-artifacts/`. Suggested commit message: `docs(06-rev): amend ROADMAP.md SC#1 to match docs/ artifact location`.
    - [x] / [ ] **dart format drift watch** (pre-class §2 item 8): local `dart format --line-length 160 --set-exit-if-changed lib/ test/ tool/` returns exit code <0 / 1>. If 1, list of drifted files: <list>. Becomes Should fix in Plan 06-05 first commit.

    ## Surprise findings (if any)

    <list any unexpected Blocker findings that emerged during agent audit and are NOT in pre-class — these are the most important rows for Plan 06-05 fix loop>

    ## Handoff to Plan 06-04

    Plan 06-04 reads:
    - §3 triage table (which Shoulds need fixing → Plan 06-05; which Should fix-loops can be batched per Plan 04-05 precedent)
    - This summary's adversarial readiness checklist (Test #1 Swift presence, Test #3 OemDetector fixtures, Test #6 CI trigger expansion timing)
    ```

    Commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(06-rev): summarize Wave 3 audit + adversarial readiness checklist (06-03-SUMMARY.md)" --files .planning/phases/06-review-gate-gps/06-03-SUMMARY.md
    ```
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/06-review-gate-gps/06-03-SUMMARY.md && grep -q "Adversarial readiness checklist" .planning/phases/06-review-gate-gps/06-03-SUMMARY.md && grep -q "Test #1 MethodChannel sync" .planning/phases/06-review-gate-gps/06-03-SUMMARY.md && grep -q "ROADMAP SC#1 amendment text drafted" .planning/phases/06-review-gate-gps/06-03-SUMMARY.md && grep -q "_copyFor() coverage check" .planning/phases/06-review-gate-gps/06-03-SUMMARY.md'</automated>
  </verify>
  <done>`06-03-SUMMARY.md` exists on `main` with finding counts table, _copyFor() coverage check section, triage strategy record, adversarial readiness checklist for Plan 06-04 (7 items), surprise findings section, handoff section. One atomic commit `docs(06-rev): summarize Wave 3 audit + adversarial readiness checklist`. Plan 06-04 has all the inputs it needs.</done>
</task>

</tasks>

<verification>
- §2 `Pre-known from CONTEXT` sub-section populated with 8 items verbatim from CONTEXT.md
- §2 `SC#4 OEM workaround plan` table populated with 7 OemFamily rows + Deferred Phase 15 sub-list + `_copyFor() coverage check` line in intro
- §2 has 4 `### Agent #N` sub-sections with structured findings lists + collapsible narrative appendices
- §2 cross-lens findings preserved (no dedup; explicit cross-references)
- §3 triage table populated with one row per finding (every Blocker = fix, every Should = fix or waived(rationale)); scaffold placeholder row `| (pending) | | | | | |` no longer present
- 4 atomic commits on `main`: pre-class 8 / SC#4 OEM table / consolidate findings / triage
- 06-03-SUMMARY.md exists with adversarial readiness checklist (7 items: Swift presence, OemDetector fixtures, manifest drift, boot receiver contract, CI trigger, ROADMAP amendment text, dart format drift) AND _copyFor() coverage check section
- User signaled "OK adversarial" to unblock Plan 06-04
</verification>

<success_criteria>
Plan 06-03 complete when:
1. §2 populated: 8 pre-class + SC#4 OEM table (with `_copyFor()` coverage check inline) + 4 Agent sub-sections + Audit Notes narrative appendix
2. §3 triage table populated with all Blockers = fix, all Shoulds = fix OR waived(rationale); scaffold placeholder row gone
3. 4 atomic `docs(06-rev):` commits on `main` (pre-class / SC#4 / 4-agent findings / triage)
4. 06-03-SUMMARY.md committed with adversarial readiness checklist + _copyFor() coverage record
5. CI green on each commit (markdown-only changes; no source code touched in this plan)
6. User has explicitly unblocked Plan 06-04 via "OK adversarial"
</success_criteria>

<output>
After completion, ensure `06-03-SUMMARY.md` includes:
- 4 commit hashes (pre-class 8 / SC#4 OEM / 4-agent findings consolidation / §3 triage)
- Per-agent timing (wall-clock for parallelism validation)
- _copyFor() coverage check (X/7 OemFamily variants explicitly handled + escalation list if any)
- Finding counts per severity per source (pre-class / SC#4 / 4 agents / total)
- User triage strategy (per-finding vs blanket)
- Adversarial readiness checklist (7 items above) — Plan 06-04 prerequisite
- Surprise findings (Blocker discoveries not in pre-class — most important rows for Plan 06-05)
- Handoff section pointing Plan 06-04 to §3 triage + this summary's checklist
</output>
