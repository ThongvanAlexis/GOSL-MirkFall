---
phase: 02-review-gate-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-review-gate-foundation/02-REVIEW.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "`02-REVIEW.md` exists with the 5-section skeleton (User-observed findings, Claude audit findings, Triage decisions, Adversarial evidence, CI-green confirmation)"
    - "User has posted their IDE review verbatim (or explicit 'rien vu') into §1 BEFORE any Claude audit sub-agent is spawned"
    - "The §1 capture is committed to git, establishing a hard ordering gate for Plan 02-02"
  artifacts:
    - path: ".planning/phases/02-review-gate-foundation/02-REVIEW.md"
      provides: "5-section review artifact with §1 User-observed findings filled"
      contains: "# Phase 02: Review Gate — Foundation Review"
      min_lines: 40
  key_links:
    - from: "Claude (main agent)"
      to: "User (IDE review session)"
      via: "CLAUDE.md §Code Review Phases protocol step 1 — solicitation message"
      pattern: "qu'as-tu vu ?|findings?|IDE"
    - from: "`02-REVIEW.md` §1"
      to: "Plan 02-02 Wave 2 audit trigger"
      via: "§1 must be non-empty (verbatim user capture or explicit 'none') before 02-02 runs"
      pattern: "## 1\\. User-observed findings"
---

<objective>
Encode the CLAUDE.md §Code Review Phases protocol as a hard ordering gate: user's IDE observations are captured into `02-REVIEW.md` §1 BEFORE Claude spawns any audit sub-agent. This plan also scaffolds the 5-section `02-REVIEW.md` artifact that subsequent plans will fill in.

Purpose: The user-first rule is non-negotiable (explicitly anti-parallelized in CONTEXT.md). Encoding it as Plan 01 with a blocking checkpoint makes the protocol enforceable, not cosmetic. If the user is biased by Claude's findings before posting their own, the review's value is destroyed.

Output: `02-REVIEW.md` committed with a complete 5-section skeleton and §1 populated with the user's verbatim IDE observations (or explicit "rien vu" if the user has nothing to add).
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-review-gate-foundation/02-CONTEXT.md
@.planning/phases/02-review-gate-foundation/02-RESEARCH.md
@.planning/phases/02-review-gate-foundation/02-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key rules this plan MUST encode. No codebase exploration needed. -->

From CLAUDE.md §Code Review Phases (verbatim binding constraint):
```
**Protocole :**

- Tu me demandes d'abord ce que j'ai vu qui a besoin d'être revu
- Ensuite tu fais ton audit
- Tu affiches uniquement les **titres** avec une courte explication, et tu me laisses choisir ce qu'on corrige
```

From 02-CONTEXT.md §decisions (Ordering):
```
Ordering : user d'abord, puis Claude. Protocole CLAUDE.md §Code Review Phases strict :
user poste ses findings IDE en chat, Claude les capture, ensuite spawn les 4 sub-agents.
Parallèle (user tape pendant que agents tournent) explicitement rejeté.
```

From 02-RESEARCH.md Example 4 — `02-REVIEW.md` 5-section skeleton contract:
```
## 1. User-observed findings (IDE review)
## 2. Claude audit findings
## 3. Triage decisions
## 4. Adversarial evidence
## 5. CI-green confirmation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the 5-section 02-REVIEW.md skeleton</name>
  <files>.planning/phases/02-review-gate-foundation/02-REVIEW.md</files>
  <action>
    Create `.planning/phases/02-review-gate-foundation/02-REVIEW.md` with the 5-section skeleton exactly matching the format locked in 02-RESEARCH.md Example 4. Sections MUST be in this order and named exactly (the gsd-verifier will look for them):

    ```
    # Phase 02: Review Gate — Foundation Review

    **Opened:** 2026-04-17
    **Status:** open
    **Closed:** (pending)

    ## 1. User-observed findings (IDE review)

    *Captured verbatim at phase start, BEFORE Claude's audit.*

    (awaiting user input — Task 2 of this plan fills this section)

    ## 2. Claude audit findings

    *Filled by Plan 02-02 Wave 2 after the 4 parallel sub-agents return.*

    Format: `[severity] Title — 1-line explanation — file:line`. Severities: Blocker / Should / Could / Noted.

    ### Agent #1 — CI gate scripts + adversarial design
    (pending)

    ### Agent #2 — Bootstrap runtime
    (pending)

    ### Agent #3 — Code quality sweep
    (pending)

    ### Agent #4 — Tests + tooling + CI workflow
    (pending)

    <details>
    <summary>Audit Notes (narrative appendix, per agent)</summary>
    (pending)
    </details>

    ## 3. Triage decisions

    *Filled by Plan 02-02 after user selects what to fix. Every Blocker MUST be `fix`. Every Should MUST be either `fix` or `waived` with inline rationale.*

    | # | Finding | Severity | Decision | Rationale |
    |---|---------|----------|----------|-----------|
    | (pending) | | | | |

    ## 4. Adversarial evidence

    *Filled by Plan 02-03 Wave 3. One block per guardrail test.*

    ### Test 1: License GPL scan
    (pending)

    ### Test 2: Missing GOSL header
    (pending)

    ### Test 3: Missing DEPENDENCIES.md entry
    (pending)

    ## 5. CI-green confirmation

    *Filled by Plan 02-04 Wave 4 after all Blocker + non-waived Should fixes are applied.*

    - **Final commit on main:** (pending)
    - **CI run URL:** (pending)
    - **Status:** (pending)
    - **Date:** (pending)

    ---
    _Phase 02 closed: (pending)_
    _Phase 03 unblocked._
    ```

    Prepend the GOSL header is NOT required for markdown files (the check_headers.dart scanner only targets .dart files per `tool/check_headers.dart` — verified in Phase 01). Do NOT add the Dart copyright header to this file.

    Commit: `docs(02-rev): scaffold 02-REVIEW.md 5-section skeleton`
  </action>
  <verify>
    <automated>test -f .planning/phases/02-review-gate-foundation/02-REVIEW.md &amp;&amp; grep -c "^## [1-5]\\." .planning/phases/02-review-gate-foundation/02-REVIEW.md</automated>
  </verify>
  <done>`02-REVIEW.md` exists, all 5 top-level sections present (grep count = 5), status `open`, sections 1-5 in correct order, skeleton committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-first IDE review — solicit, capture, commit into §1</name>
  <files>.planning/phases/02-review-gate-foundation/02-REVIEW.md</files>
  <action>
    This is the CLAUDE.md §Code Review Phases protocol gate. Strict ordering: Claude asks FIRST, user answers, Claude captures verbatim, THEN unblocks Plan 02-02.

    Claude-main posts in chat as the first non-automated action of this wave:

    > "Avant que je lance les 4 sub-agents d'audit, qu'as-tu vu dans ton IDE qui mérite d'être revu sur la Phase 01 ? (code `lib/`, tests, outils `tool/`, workflow CI, `DEPENDENCIES.md`, `pubspec.yaml`, `analysis_options.yaml`, layer READMEs, platform stubs iOS/Android). Poste ta liste telle quelle — je la capture verbatim dans `02-REVIEW.md` §1 avant de déclencher mon propre audit. Si tu n'as rien vu, dis-le explicitement — je note 'aucune observation utilisateur' et on enchaîne."

    User responds with either:
    - A list of findings (any format — bullets, prose, file references) → Claude captures verbatim into §1
    - "rien vu" / "aucune observation" / equivalent → Claude writes `*Aucune observation utilisateur — l'user n'a pas identifié de point à revoir dans son IDE.*` into §1

    Either way, Claude then:
    1. Edits `02-REVIEW.md` §1 to replace `(awaiting user input — Task 2 of this plan fills this section)` with the captured content
    2. Commits: `docs(02-rev): capture user-observed findings into 02-REVIEW.md §1`
    3. Confirms to user: "§1 capturé et committé. J'enchaîne sur les 4 sub-agents d'audit dès que tu valides."

    Anti-pattern guard: Do NOT spawn any Agent/Task tool call in this wave before §1 is committed. The whole point of the checkpoint is to enforce user-first.
  </action>
  <verify>
    <automated>! grep -q "awaiting user input" .planning/phases/02-review-gate-foundation/02-REVIEW.md &amp;&amp; git log --oneline -5 -- .planning/phases/02-review-gate-foundation/02-REVIEW.md | grep -q "capture user-observed findings"</automated>
  </verify>
  <done>
    `02-REVIEW.md` §1 contains the user's verbatim IDE observations (or explicit "aucune observation utilisateur" marker). The placeholder `awaiting user input` no longer appears anywhere in the file. A commit `docs(02-rev): capture user-observed findings into 02-REVIEW.md §1` is on main. User has signaled "OK lance les agents" (or equivalent) explicitly to unblock Plan 02-02.
  </done>

  <what-built>
    Nothing automated. This checkpoint is the protocol gate itself: Claude asks the user what they saw in their IDE, captures the verbatim answer (or explicit "rien vu"), writes it into `02-REVIEW.md` §1, and commits. This MUST happen before Plan 02-02 spawns any audit sub-agent.
  </what-built>
  <how-to-verify>
    After Claude has posted the solicitation and user has responded:
    - `grep -A2 "## 1\\. User-observed findings" .planning/phases/02-review-gate-foundation/02-REVIEW.md | grep -v "awaiting user input"` — should NOT show the placeholder
    - `git log --oneline -1 -- .planning/phases/02-review-gate-foundation/02-REVIEW.md` — should show the capture commit
  </how-to-verify>
  <resume-signal>
    User types "OK lance les agents" or "§1 validé, go" to signal that Plan 02-02 may spawn sub-agents.
    If user re-opens IDE findings after seeing this prompt, Claude appends (not replaces) to §1 and re-commits before proceeding.
  </resume-signal>
</task>

</tasks>

<verification>
- `02-REVIEW.md` exists with 5 sections in correct order (grep `^## [1-5]\\.` returns 5)
- §1 is populated (no placeholder `awaiting user input` remains)
- Two commits on `main`: scaffold + user capture
- User has explicitly signaled "go" for Plan 02-02 to proceed
</verification>

<success_criteria>
Plan 02-01 complete when:
1. `.planning/phases/02-review-gate-foundation/02-REVIEW.md` exists on `main` with the 5-section skeleton
2. §1 contains either the user's verbatim IDE findings OR an explicit "aucune observation utilisateur" marker
3. Two atomic commits logged (scaffold + capture)
4. CI green (markdown-only changes, guard scripts not affected, but a green CI on the capture commit confirms nothing else regressed)
5. User has explicitly unblocked Plan 02-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-review-gate-foundation/02-01-SUMMARY.md` summarizing:
- Skeleton committed with hash
- §1 capture committed with hash
- Verbatim excerpt of user observations (or "aucune observation")
- User unblock signal received at (timestamp)
</output>
