---
phase: 09.1-port-back-same-canvas-fog-flutter-map-migration
plan: 04
subsystem: mirk-rendering
tags: [fog-layer, same-canvas, flutter_map, map-camera, custom-painter, ticker, clip-path, mirk-paint-context, frame-delta-probe, fog-transform-logger, verbose-logging, tdd, widget-test]

# Dependency graph
requires:
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/03
    provides: "12-field `MirkPaintContext` (+ `ScreenProjector` / `MetersToPixels`), `applyPlatformShaderCorrections`, `FogShaderRenderer` seam, `RecordingFogShaderRenderer`, `FakeMirkRenderer`"
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/02
    provides: "flutter_map 7.0.2 engine, `FlutterMapMapViewWidget.fogLayers` slot, rotation disabled"
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/01
    provides: "`kMirkFogReferenceZoom`, `kMirkFogNoiseTilePx`, `kMirkFogDiag*` constants, `check_avoid_flutter_map_leak` perimeter (fog_layer.dart / fog_clip_path.dart allow-listed)"
  - phase: poc-mirk-poc-debug@90c9321
    provides: "`FogLayer` / `fog_clip_path.dart` / 3 diagnostic loggers / 13 `fog_*` widget tests + 3 logger tests to port"
provides:
  - "`FogLayer` (lib/presentation/widgets/fog_layer.dart) — pure StatefulWidget child of `FlutterMap`: ONE `MapCamera.of` per build (FOG-07, `debugOnCameraRead` seam), Ticker → `CustomPainter(repaint:)`, live Stopwatch by reference (invariant 10), delegates painting to the injected `MirkRenderer`"
  - "`_FogPainter` — single `getTransform()` (FOG-12), `save` + `translate(-canvasOffset)` FIRST (FOG-13), `clipPath(computeFogClipPath)` without canvasOffset (FOG-06 / 03.1-08-FIX), `applyPlatformShaderCorrections` once on the raw pixelOrigin (FOG-21/23), `zoomScale = 2^(zoom - kMirkFogReferenceZoom)` (FOG-19), raw pixelOrigin (FOG-18), extended `MirkPaintContext`, `renderer.update` + `renderer.paint`, `restore`"
  - "`fog_clip_path.dart` (perimeter) — `cameraScreenProjector`, `cameraMetersToPixels` (1 m latitude probe), `computeFogClipPath`"
  - "`fog_clip_geometry.dart` (pure) — `buildFogClipPath(size, discs, projectToScreen, metersToPixels, canvasOffset)`"
  - "`FrameDeltaProbe` (PERF-07), `FogTransformLogger` (FOG-10), `DevMarkerLogger` — verbose-only (`isLoggable(Level.FINE)`), `record*` no-op before `start()`"
  - "Test toolkit: `fakeMapCamera()` + `CameraAccessCounter`, `pumpFogLayerInFlutterMap`, `findFogPainter`, `SpyMirkRenderer`, `IdentityCanvasFake`, `RecordingCanvasFake`, `pumpAtmosphericFogLayer` + `pumpUntilShaderRendered`"
  - "16 test files + 3 helpers / 64 tests: FOG-07 keystone ×2 (CI regression gate), FOG-04/05/06/09/11/12/13/17/18/19 widget tests, 4 infra suites"
affects: [09.1-05 wisps (projectToScreen / metersToPixels through the context), 09.1-06 clip removal from the renderers (FogLayer owns the clip), 09.1-07 FogLayerConnector + MirkOverlay deletion (FogLayer constructor contract, loggers to start/stop, DevMarkerLogger.mark for the debug menu), 09.1-08 closure, 10 review-gate-fog]

# Tech tracking
tech-stack:
  added: []
  patterns: [single camera snapshot per build passed by constructor to the painter, matrix-level single snapshot (one getTransform reused for translate + diagnostics), translate-to-identity-before-clip, verbose-gated diagnostic loggers with armed 1 Hz timers, spy renderer that draws one rect so a recording canvas orders the renderer call, real-event-loop SDF settle under tester.runAsync, clean git worktree for cross-plan verification]

key-files:
  created:
    - lib/presentation/widgets/fog_layer.dart
    - lib/presentation/widgets/fog_clip_path.dart
    - lib/infrastructure/mirk/fog_clip_geometry.dart
    - lib/infrastructure/mirk/frame_delta_probe.dart
    - lib/infrastructure/mirk/fog_transform_logger.dart
    - lib/infrastructure/mirk/dev_marker_logger.dart
    - test/_helpers/fake_map_camera.dart
    - test/_helpers/fog_layer_test_harness.dart
    - test/_helpers/atmospheric_fog_layer_harness.dart
    - test/infrastructure/mirk/frame_delta_probe_test.dart
    - test/infrastructure/mirk/fog_transform_logger_test.dart
    - test/infrastructure/mirk/dev_marker_logger_test.dart
    - test/infrastructure/mirk/fog_clip_geometry_test.dart
    - test/presentation/widgets/fog_layer_camera_snapshot_test.dart
    - test/presentation/widgets/fog_layer_single_camera_snapshot_test.dart
    - test/presentation/widgets/fog_clip_path_test.dart
    - test/presentation/widgets/fog_canvas_frame_alignment_test.dart
    - test/presentation/widgets/fog_rect_viewport_coverage_test.dart
    - test/presentation/widgets/fog_pan_translation_test.dart
    - test/presentation/widgets/fog_layer_test.dart
    - test/presentation/widgets/fog_pixel_origin_decomposition_test.dart
    - test/presentation/widgets/fog_smooth_noise_test.dart
    - test/presentation/widgets/fog_tile_period_invariant_test.dart
    - test/presentation/widgets/fog_world_coordinate_noise_test.dart
    - test/presentation/widgets/fog_zoom_invariant_basis_test.dart
  modified: []

key-decisions:
  - "Task 1 RED is compilable: `fog_layer.dart` shipped as a skeleton whose `build` throws `UnimplementedError` (09.1-02 precedent) so `flutter analyze` stays green at every commit and the parallel executor never sees a compile-failing test file; the plan's 'file does not exist' wording was traded for that"
  - "`FogTransformLogger.recordPaint` takes `GeoPoint` (not `LatLng`) and reads the four scale / shear scalars from the matrix itself: the file is outside the flutter_map import perimeter and the POC's four extra parameters were redundant with `canvasTransform`; the 47 JSONL keys (`uOffsetX*` included) are unchanged"
  - "`DevMarkerLogger` is an instance (`const DevMarkerLogger().mark(label)`) per the plan's `<interfaces>` block and CLAUDE.md DI, not the POC static `emit(tag:)`; the JSONL payload keeps the `tag` key"
  - "Verbose gating = early return in `record*` AND in the rollup callback / `stop()` flush, timer left armed; `debugRecordRawDelta` (test seam) bypasses the running gate but never the emission gate"
  - "Painter-level tests use `SpyMirkRenderer` (records size + context, draws one rect) instead of the POC's shader recorder; the four uniform / noise tests keep the production `AtmosphericMirkRenderer` + `RecordingFogShaderRenderer` end-to-end"
  - "SDF settle idiom for renderer-backed widget tests: `tester.runAsync` + real 20 ms delays (bounded ×30), NOT fake-time `pump(duration)` — the SDF builder finishes through `ui.decodeImageFromPixels`, which never completes under FakeAsync; renderer disposed in the test body (its timers would trip the pending-timer check before `addTearDown` runs)"
  - "Widget tests size the test screen to the requested viewport (`tester.view.physicalSize`, dpr 1): the default 800×600 screen silently clamped the POC's 400×800 / 390×844 boxes"
  - "Cross-plan verification through a detached `git worktree` at my HEAD: 09.1-05's uncommitted wisp rewrite breaks `atmospheric_mirk_renderer.dart` in the shared tree for the whole wave; the worktree gives a consistent committed tree without touching their files or the shared index"

patterns-established:
  - "`FogLayer` contract for 09.1-07: `FogLayer(renderer:, discs:, currentFix:, frameDeltaProbe:, fogTransformLogger:, isAndroid:, pixelRatio:)` — renderer owned by its provider (never disposed by the layer), loggers owned by the connector (start / stop there)"
  - "Every `fog_*` widget test pumps through `pumpFogLayerInFlutterMap` and drives the private painter through `findFogPainter(tester).paint(fake, size)`; canvas ordering / argument assertions go through `RecordingCanvasFake.firstIndexByOp`"
  - "Diagnostic loggers: `Logger('infrastructure.mirk.<name>')`, `bool get _isVerbose => _log.isLoggable(Level.FINE)`, `isRunning` getter, JSONL at INFO only when verbose"

requirements-completed: [MIRK-01, MIRK-04, MIRK-05]

# Metrics
duration: ~3h 25m wall-clock (2026-09-09T11:12:42Z → 14:38Z, including a rate-limit cutoff; ~2h active)
completed: 2026-09-09
---

# Phase 09.1 Plan 04: FogLayer same-canvas + clip geometry + verbose-only diagnostics Summary

**`FogLayer` ported from the POC as a pure `FlutterMap` child that snapshots `MapCamera` once per build, normalises the canvas to the identity frame before clipping, builds the 12-field `MirkPaintContext` (platform corrections, raw `pixelOrigin`, `zoomScale`, projection closures) and delegates the paint to the injected `MirkRenderer`; the clip geometry split into a pure function + a camera bridge, the three POC diagnostics gated on verbose logging, and the 12 POC widget tests (FOG-07 keystone ×2 as CI gate) + 4 infra suites green — 64 tests.**

## Performance

- **Duration:** ~3h 25m wall-clock (a rate-limit cutoff interrupted Task 3; ~2h of active execution)
- **Started:** 2026-09-09T11:12:42Z
- **Completed:** 2026-09-09T14:38Z
- **Tasks:** 3 (Task 1 and Task 2 TDD)
- **Files modified:** 25 created, 0 modified

## Accomplishments

- **`FogLayer` (290 lines, ≤ 350 target):** constructor exactly as the plan's `<interfaces>` block. `build()` = `debugOnCameraRead?.call()` → `MapCamera.of(context)` (the only occurrence in the file) → `frameDeltaProbe.recordCameraSnapshot()` → `pixelRatio ?? MediaQuery.devicePixelRatioOf` → `MobileLayerTransformer(CustomPaint(_FogPainter(...), size: Size.infinite))`. `isAndroid ?? Platform.isAndroid` resolved once in `initState`; Ticker → `_Repaint.tick()` → `repaint:` Listenable; `dispose` releases only the ticker and the notifier. `grep "RepaintBoundary\|setState"` → empty, `grep -c "MapCamera.of(context)"` → 1.
- **`_FogPainter.paint` in the locked order:** `getTransform()` once → `save` → `translate(-tx, -ty)` → `clipPath(computeFogClipPath(camera, discs))` → `applyPlatformShaderCorrections(raw pixelOrigin)` → `zoomScale = 2^(zoom − 13)` → `sessionElapsed` from the live Stopwatch → `MirkPaintContext(…, canvasOffset, cameraScreenProjector, cameraMetersToPixels)` → probe + transform logger → `renderer.update(sessionElapsed)` → `renderer.paint(canvas, size, context)` → `restore`. `shouldRepaint` on camera / discs identity / renderer / currentFix / pixelRatio / isAndroid.
- **Clip geometry:** `buildFogClipPath` (dart:ui + domain only) and the perimeter bridge `fog_clip_path.dart`; `check_avoid_flutter_map_leak` OK (195 files). Metric radius verified against the Web Mercator ground resolution (0.0791 px/m at z13 / 48.54° N, ×4 at z15) and by ±0.5 px probes on all four axes of a 100 m hole.
- **Diagnostics:** `FrameDeltaProbe` / `FogTransformLogger` / `DevMarkerLogger` ported with `kMirkFogDiag*`; under root level INFO no `LogRecord` and no stream rollup after N records + elapsed windows + `stop()` (fake_async); under ALL one JSONL line per window; `record*` before `start()` dropped; `stop()` idempotent.
- **Tests:** 64 new (29 infra in 4 files, 35 widget in 12 files). FOG-07 keystone `readCount` 1 → 2 → 3 with `FakeMirkRenderer` AND with `AtmosphericMirkRenderer(shaderRenderer: RecordingFogShaderRenderer())`. FOG-12/13 through `RecordingCanvasFake` (order `getTransform → save → translate → clipPath → drawRect → restore`, single translate `(-757.35, -319.46)`, clip bounds invariant under canvasOffset) plus a REAL pre-translated canvas: the renderer observes tx = ty = 0, `size == camera.size` (390×844) and a local clip covering exactly the viewport. FOG-09 pan: `Δ pixelOrigin == Δ camera.pixelOrigin` (≈ 86 / 91 px at z13 — the POC comment's "~411 / ~205" was wrong) and `projectToScreen(P)` moves by −Δ. FOG-05: 20 tunables, 31 observed slots, identity `sdfRect`, strictly increasing `uTime`. FOG-18: seam value `==` `camera.pixelOrigin` at z15 (~2.1 M). FOG-11: 10 gentle pans, deltas ≪ 2000 px. FOG-19: 1.0 → 4.0 (z15) → 0.5 (z12). FOG-17: `.frag` formulation + `kNoiseTilePx` lockstep + no Dart-side period, numerical harness zero wraps at 1.06 M / 4.26 M / 17.04 M.
- **Verification at HEAD 40c930d (clean worktree — see Issues):** full-tree `flutter analyze --fatal-infos --fatal-warnings` clean, `dart format --line-length 160 --set-exit-if-changed .` clean (480 files), 9/9 `tool/check_*` OK (`check_avoid_flutter_map_leak` 196 files, `check_headers` 419, `check_domain_purity` 57, `check_mirk_variant_file_count` 6), `dart test tool/test/` 96/96, `flutter test` 1184 passed / 8 failed — all 8 outside this plan's scope: 7 × `test/infrastructure/mirk/wisp/wisp_transform_logger_test.dart` (09.1-05 RED commit `6f17494`) + 1 × `test/presentation/widgets/mirk_overlay_feather_test.dart` ("A Timer is still pending": the 1 s periodic `SdfRebuildLogger()..start()` that 09.1-05's `AtmosphericMirkRenderer` default constructs since `c2e98aa` — reproduced at `c2e98aa` in the worktree without any of my Task 2 / Task 3 commits).

## Task Commits

1. **Task 1: diagnostics + helpers + FOG-07 keystones RED** — `09a8609` (test; loggers GREEN 24/24, keystones RED at runtime against a compilable skeleton)
2. **Task 2: clip geometry + `FogLayer` GREEN + frame tests** — `797d3d8` (feat: keystone GREEN) → `acc518d` (test: FOG-06/09/12/13 ports)
3. **Task 3: uniform / noise widget tests** — `40c930d` (test: FOG-04/05/11/17/18/19 + `MapCamera.of` docstring trim)

**Plan metadata:** see final docs commit.

## Files Created/Modified

- `lib/presentation/widgets/fog_layer.dart` — `FogLayer`, `_FogLayerState`, `_Repaint`, `_FogPainter`, `_viewportFromCamera`, `_kZoomScaleBase`
- `lib/presentation/widgets/fog_clip_path.dart` — `cameraScreenProjector`, `cameraMetersToPixels`, `computeFogClipPath` (flutter_map + latlong2 `show LatLng`)
- `lib/infrastructure/mirk/fog_clip_geometry.dart` — `buildFogClipPath` (pure)
- `lib/infrastructure/mirk/frame_delta_probe.dart` — `FrameDeltaRollup`, `FrameDeltaProbe` (`isRunning`, `_pushDelta`, named `_kP95Quantile` / `_kMsDecimals`)
- `lib/infrastructure/mirk/fog_transform_logger.dart` — `FogTransformLogger`, `kCanvasTransform*Index` slot constants (shared with the painter), `_FogTransformField` enum driving the 45 stat keys
- `lib/infrastructure/mirk/dev_marker_logger.dart` — `DevMarkerLogger.mark`
- `test/_helpers/fake_map_camera.dart` — `kTestMapCenter` (Melun), `kTestMapZoom`, `kTestMapViewportSize`, `fakeMapCamera()`, `CameraAccessCounter`
- `test/_helpers/fog_layer_test_harness.dart` — `pumpFogLayerInFlutterMap` (sized screen), `findFogPainter`, `centreTestDisc`, `SpyMirkRenderer`, `IdentityCanvasFake`, `RecordingCanvasFake`, `identityMatrixWithTranslation`, tolerances
- `test/_helpers/atmospheric_fog_layer_harness.dart` — `pumpAtmosphericFogLayer`, `pumpUntilShaderRendered` (runAsync settle)
- `test/infrastructure/mirk/{frame_delta_probe,fog_transform_logger,dev_marker_logger,fog_clip_geometry}_test.dart`
- `test/presentation/widgets/fog_*_test.dart` (12 files, each headed by `// Porté de mirk-poc-debug@90c9321 … — invariant FOG-xx`)

### Tests ported vs rewritten

| File | Status |
|------|--------|
| `frame_delta_probe_test`, `fog_transform_logger_test`, `dev_marker_logger_test` | ported (7 / 4 / 3 cases) + 4 / 4 / 1 gating cases; `Logger.root.level = ALL` in `setUp` (the rollup is now gated) |
| `fog_layer_camera_snapshot_test`, `fog_layer_single_camera_snapshot_test` | ported quasi verbatim (`CameraAccessCounter`, MirkFall constructor; the second uses the atmospheric renderer instead of the POC wisp system) |
| `fog_clip_path_test` | ported (4) + 3 bridge cases (projector, Mercator resolution, ×4 zoom) |
| `fog_canvas_frame_alignment_test`, `fog_rect_viewport_coverage_test` | ported (1 / 3) via `RecordingCanvasFake`; + `canvasOffset` / `update` case, + REAL canvas case |
| `fog_pan_translation_test` | rewritten on `SpyMirkRenderer` (delta equality + projection), POC `skip: true` RED-guard dropped (referenced a POC SHA) |
| `fog_layer_test` | FOG-04 ported + FOG-05 seam case added (plan) |
| `fog_pixel_origin_decomposition_test` | rewritten: STATIC (no `%` / period / `truncateToDouble` in the painter, corrections called once), NUMERICAL through `applyPlatformShaderCorrections`, BEHAVIOURAL `==` at z15 |
| `fog_smooth_noise_test`, `fog_zoom_invariant_basis_test` | ported on the atmospheric harness; zoom test drives 13 → 15 → 12 with `move` as the plan asks |
| `fog_tile_period_invariant_test` | ported minus the debug-spiral cases (shader absent from MirkFall) + lockstep and "no Dart-side period" cases |
| `fog_world_coordinate_noise_test` | ported (4); forward path = `applyPlatformShaderCorrections` |
| `fog_clip_geometry_test` | new (5, pure) |

No numerical expectation from the POC changed except the FOG-09 comment above; `kTestCanvasTransformEpsilon = 1e-6` and `kTestSubPixelTolerance = 1e-3` replace the POC `kPocCanvasTransformEpsilon` / local tolerance.

## Decisions Made

See `key-decisions` in the frontmatter. Additional notes:

- `renderer.update(sessionElapsed)` is called right before `paint` as the plan's `<interfaces>` block prescribes (the overlay never called `update`; the renderers treat it as a no-op today).
- `fog_layer.dart` keeps ~40 % rationale (condensed from the POC's ~60 %): FOG-07 lock, no repaint boundary / no per-frame rebuild, invariant 10, FOG-12/13, 03.1-08-FIX, FOG-21/23 pointer, FOG-18/19. Docstrings in English, consistent with 09.1-02 / 09.1-03.
- `MapCamera` compared with `!=` in `shouldRepaint` (flutter_map overrides `==` on crs / centre / zoom / rotation / size).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Compilable RED for Task 1 (skeleton instead of a missing file)**
- **Found during:** Task 1
- **Issue:** The plan wanted `fog_layer.dart` absent at the RED commit; a compile-failing test file would break `flutter analyze` (prompt success criteria: green at every commit) and pollute the parallel executor's runs.
- **Fix:** Skeleton `FogLayer` with the final constructor and a `build` throwing `UnimplementedError`; both keystones fail at runtime.
- **Committed in:** 09a8609

**2. [Rule 3 - Blocking] `LatLng` cannot appear in `lib/infrastructure/mirk/`**
- **Found during:** Task 1 (`FogTransformLogger.recordPaint(cameraCenter: LatLng)` in the POC)
- **Issue:** `check_avoid_flutter_map_leak` forbids `latlong2` outside the perimeter.
- **Fix:** `cameraCenter: GeoPoint`; the painter converts `camera.center` at the boundary.
- **Committed in:** 09a8609

**3. [Rule 1 - Bug] Test screen smaller than the requested viewport**
- **Found during:** Task 2 (`fog_rect_viewport_coverage_test`, 390×844 box → camera.size 390×600)
- **Fix:** `pumpFogLayerInFlutterMap` sets `tester.view.physicalSize` / `devicePixelRatio = 1` with reset teardowns.
- **Committed in:** acc518d

**4. [Rule 1 - Bug] Renderer-backed tests never reached the seam under fake-time pumps**
- **Found during:** Task 3 (`recorder.renders` empty after 20 × `pump(100 ms)`)
- **Issue:** The plan suggested bounded `tester.pump(duration)`; `ui.decodeImageFromPixels` only completes on the real event loop.
- **Fix:** `pumpUntilShaderRendered` runs under `tester.runAsync` with real 20 ms delays (POC idiom), bounded ×30.
- **Committed in:** 40c930d

**5. [Rule 1 - Bug] Lints / format side effects**
- `initialZoom: 13` redundant with the `MapOptions` default, `DateTime.utc(2026, 5, 1)` redundant day, unused / unnecessary imports, float32 rounding of a real canvas matrix (`closeTo` instead of `equals`), a `dart format` line wrap that broke a literal substring assertion (regex now).
- **Committed in:** 09a8609 / acc518d / 40c930d

**6. [Rule 2 - Missing critical] `SpyMirkRenderer` + `fog_layer_test_harness.dart` + `atmospheric_fog_layer_harness.dart` (not in `files_modified`)**
- **Found during:** Task 2 / Task 3
- **Issue:** The plan asked for a `_SpyMirkRenderer` per file; 9 widget files share the pump / find / fake-canvas idioms, and the atmospheric harness must stay out of the painter-level harness so those tests do not inherit the renderer's compile state during the wave.
- **Committed in:** acc518d / 40c930d

### Cross-plan (09.1-05 in parallel) — nothing edited on their side

- **Transient compile failure of the renderer-backed tests in the shared tree.** From 09.1-05's Task 2 on, their uncommitted `wisp_particle_system.dart` removed `render` / `spawnAtPosition` while their committed `atmospheric_mirk_renderer.dart` (`c2e98aa`) still calls them, so `fog_layer_single_camera_snapshot_test`, `fog_layer_test`, `fog_pixel_origin_decomposition_test`, `fog_smooth_noise_test` and `fog_zoom_invariant_basis_test` could not compile in the working tree. Verified instead in a detached `git worktree` at my HEAD (`C:\claude_checkouts\mirkfall-0904-wt`, removed after the run): 11/11 green there, then the full suite (see Accomplishments). Their RED test commit `6f17494` is part of HEAD, so the full suite carries their expected failures — listed below, none in my scope.
- **Coupling to watch for 09.1-05 / 09.1-06:** the four renderer-backed tests construct `AtmosphericMirkRenderer(const AtmosphericConfig(), shaderRenderer: recorder)` (plan-prescribed) and dispose it in-body; if the constructor gains a required parameter or the default `SdfCache` stops resolving through `decodeImageFromPixels`, `atmospheric_fog_layer_harness.dart` is the single place to adapt.

### Handoff to 09.1-05

- **`test/presentation/widgets/mirk_overlay_feather_test.dart` fails since `c2e98aa`** ("A Timer is still pending even after the widget tree was disposed", periodic 1 s): the `AtmosphericMirkRenderer` default `SdfCache(rebuildLogger: SdfRebuildLogger()..start())` arms a periodic timer in the constructor and the overlay test never disposes the renderer in-body. Either start the rebuild logger lazily (first `getOrBuild`) / gate the timer like `FrameDeltaProbe`, or the overlay test must `await renderer.dispose()` before returning (the overlay itself is deleted in 09.1-07). Not edited here — both files are 09.1-05 / Phase 09 scope.
- Nothing else required. (For information: `FogTransformLogger` exports `kCanvasTransform*Index`; `SdfRebuildLogger` / `WispTransformLogger` could reuse the `_isVerbose` + `isRunning` gating shape.)

---

**Total deviations:** 6 auto-fixed (3 blocking, 2 bugs, 1 missing critical) + 1 cross-plan note. No scope change; 4 task commits (plan target 3-4).

## Issues Encountered

- **Rate-limit cutoff** mid-Task 3 (after the lint pass on the Task 2 tests); resumed on the coordinator's message with the tree exactly as left (three commits + the uncommitted Task 3 files).
- **Parallel-tree verification:** `flutter test` on anything importing `atmospheric_mirk_renderer.dart` fails in the shared working tree for as long as 09.1-05 is between its RED and GREEN — used a worktree rather than waiting or editing their files (rules). `git worktree add --detach` touches only `.git/worktrees`, not the shared index.
- The POC's `fog_pan_translation_test` comment claimed ~411 / ~205 px for the Melun → (48.5500, 2.6700) trajectory at z13; the actual Mercator delta is ≈ 86 / 91 px — the port asserts against the live camera and a 50 px floor.
- `tester.view.physicalSize` matters: several POC widget tests believed they ran at 400×800 while the map was 400×600.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **09.1-05 (wisps):** `context.projectToScreen` / `metersToPixels` are populated by the layer from the camera snapshot; wisps must not re-project through the engine. `AtmosphericMirkRenderer` keeps its own `buildViewportFogClipPathFromDiscs` clip until 09.1-06 (double clip harmless, already exercised by the keystone).
- **09.1-06:** remove the renderer-side clip; `FogLayer` already clips before `renderer.paint`.
- **09.1-07 (connector):** mount `FogLayer` through `FlutterMapMapViewWidget.fogLayers`; wire `activeMirkRendererProvider`, the disc snapshot provider, `currentFix`, construct + `start()` the `FrameDeltaProbe` / `FogTransformLogger` in the connector (and `stop()` / `dispose()` on unmount), `const DevMarkerLogger().mark(label)` from the debug menu; do not pass `isAndroid` / `pixelRatio` in production.
- **CI:** `test/presentation/widgets/fog_layer_camera_snapshot_test.dart` is the named FOG-07 regression gate (VALIDATION quick run). The keystone with the atmospheric renderer and the FOG-05/11/18/19 tests need `flutter test` (asset bundle + real event loop) — not the plain-Dart CI step.
- MIRK-01 / MIRK-04 / MIRK-05 re-validated under the same-canvas layer (marking is idempotent).

---
*Phase: 09.1-port-back-same-canvas-fog-flutter-map-migration*
*Completed: 2026-09-09*

## Self-Check: PASSED

- 25 created files present; commits 09a8609 / 797d3d8 / acc518d / 40c930d present; artifact checks: all satisfied (`debugOnCameraRead` seam, fog_layer.dart ≥ 200 lines, logger names, keystone ≥ 60 lines, `renderer.paint(canvas, size`, `buildFogClipPath` link).
