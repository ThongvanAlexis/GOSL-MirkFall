# Phase 09.1 — Deferred items

Out-of-scope discoveries logged by executors (CLAUDE.md scope boundary: only issues directly caused by the current task are fixed inline). Owner column = the plan expected to pick the item up.

| # | Logged by | Item | Why deferred | Owner |
|---|-----------|------|--------------|-------|
| 1 | 09.1-02 | Historical MapLibre mentions still in prose outside the plan's file list: `lib/domain/mirk/mirk_viewport_bbox.dart` (docstring), `lib/infrastructure/mirk/mirk_projection.dart`, `lib/infrastructure/mirk/atmospheric_mirk_renderer.dart:298`, `lib/infrastructure/mirk/README.md` (§Rendering Strategy), `lib/presentation/widgets/mirk_overlay.dart:28` ("sibling of the MapLibre platform view"), `test/infrastructure/mirk/mirk_projection_test.dart`, `test/presentation/widgets/mirk_overlay_pointer_passthrough_test.dart`, `test/fixtures/pmtiles/README.md`, `tool/check_style_no_external_url.dart` + `tool/generate_tiny_pmtiles.dart` docstrings. All describe the Phase 07/09 engine; none references a deleted symbol. | Mirk-side files are rewritten by 09.1-04 (FogLayer) / 09.1-07 (MirkOverlay removal); tool docstrings are cosmetic. | 09.1-07 / 09.1-08 final scrub |
| 2 | 09.1-02 | `check_avoid_flutter_map_leak` keeps `maplibre_gl` in its regex and its paired test keeps a `package:maplibre_gl` fixture line (`tool/test/check_avoid_flutter_map_leak_test.dart:49`) — the only `package:maplibre_gl` match left in the tree. | Deliberate 09.1-01 decision (the gate must still flag a re-introduction); the 09.1-02 "grep returns nothing" criterion is met for `lib/`, `test/`, `integration_test/`. | none (keep) |
| 3 | 09.1-02 | `vector_map_tiles 8.0.0` raises `CancellationException` (owned by its transitive `executor_lib`) for tile jobs cancelled at layer unmount / re-key; some escape as uncaught async errors. Handled by type-name matching in `lib/main.dart` (`isBenignTileCancellation`) and in `test/infrastructure/map/flutter_map_map_view_test.dart` (filter + settle). | Importing `executor_lib` directly would need a pin + audit row for a single `is` check; revisit if a later `vector_map_tiles` release re-exports the type or stops leaking the error. | 09.1-08 (dependency review) |
| 4 | 09.1-02 | Sprite / icon pipeline for `pois` (`icon-image` dropped from `style.json`; `vector_tile_renderer` needs a `SpriteStyle` + local sprite sheet). `tool/prepare_style.dart` (MapLibre glyph + sprite fetcher) was deleted. | No sprite rendering in V1.0; a local `SpriteStyle` pipeline can return with the Phase 13 creative themes (second `style.json`). | Phase 13 |
| 5 | 09.1-02 | `MapThemeLoader.assetPathFor(MapThemeRpgParchment)` returns the standard style (documented provisional stub, tested). | Second JSON is Phase 13 scope; `setTheme` has no caller today (RESEARCH Open Question 7). | Phase 13 |
