---
phase: 09.1
plan: 08
type: execute
wave: 6
depends_on: ["09.1-07"]
files_modified:
  - docs/phase-09.1-uat.md
  - docs/phase09-bug-tracking/BUG-014-sdf-rect-offset-axes.md
  - docs/POC-flutter-map-mirk.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/deferred-items.md
  - .planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-VALIDATION.md
  - lib/infrastructure/mirk/README.md
  - lib/domain/map/README.md
  - lib/infrastructure/map/README.md
  - lib/domain/mirk/README.md
  - tool/README.md
autonomous: false
requirements: [MIRK-01, MIRK-02, MIRK-03, MIRK-04, MIRK-05, MAP-01, MAP-05, MAP-06, MAP-07, MAP-08]

must_haves:
  truths:
    - "UAT iPhone (cible primaire, sideload SideStore) approuvé par l'utilisateur : zéro déplacement visible du fog pendant pan, zoom et gestes combinés, sur une session avec disques persistés ; le fog réapparaît au même endroit après kill + relance de l'app"
    - "UAT Pixel 4a (APK debug) : aucune stripe horizontale (FOG-23), halo SDF non miroir (FOG-21), médiane fps informative notée (rollup `frame_delta` en verbose) ; backend GPU noté (Impeller-Vulkan / GLES) depuis logcat"
    - "Régressions Phase 07 vérifiées sur device : mode avion (carte + fog fonctionnels, zéro tuile grise), hot-swap monde ↔ pays téléchargé, follow-me, téléchargement d'un pays"
    - "BUG-014 est fermé dans `docs/phase09-bug-tracking/BUG-014-sdf-rect-offset-axes.md` (status RESOLVED, référence Phase 09.1 + POC `90c9321`, résumé des 13 correctifs portés) ; `PROJECT.md` Key Decisions enregistre « moteur flutter_map 7.0.2 same-canvas remplace maplibre_gl » et marque la ligne `maplibre_gl` comme superseded ; `docs/POC-flutter-map-mirk.md` est annoté « réalisé, voir mirk-poc-debug »"
    - "Les README `lib/infrastructure/mirk`, `lib/domain/map`, `lib/infrastructure/map`, `lib/domain/mirk` décrivent l'architecture réelle (C9 : plus de référence à des fichiers supprimés) ; `deferred-items.md` liste les reports (WalkSimulator, WispRadiusBasis, rpgParchment JSON, sprites Phase 13, index spatial SDF Phase 10, AB Impeller Phase 15, MapEventSource echo)"
    - "REQUIREMENTS.md : MAP-01 / MAP-04 / MAP-05 / MAP-06 amendés (vocabulaire flutter_map, gate `avoid_flutter_map_leak`) ; ROADMAP Phase 09.1 cochée avec date ; VALIDATION.md sign-off"
  artifacts:
    - path: "docs/phase-09.1-uat.md"
      provides: "Checklist UAT remplie (iPhone + Pixel 4a) avec mesures frame_delta / sdf et backend GPU"
      min_lines: 60
    - path: "docs/phase09-bug-tracking/BUG-014-sdf-rect-offset-axes.md"
      provides: "Tracker fermé"
      contains: "Phase 09.1"
    - path: ".planning/PROJECT.md"
      provides: "Key Decision moteur same-canvas"
      contains: "flutter_map 7.0.2"
    - path: "lib/infrastructure/mirk/README.md"
      provides: "Layout final (FogLayer / renderers / sdf / wisp / shader / diag) + doctrine MIRK-05 / seam"
      contains: "FogLayer"
    - path: ".planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/deferred-items.md"
      provides: "Reports explicites"
      contains: "WalkSimulator"
  key_links:
    - from: "docs/phase09-bug-tracking/BUG-014-sdf-rect-offset-axes.md"
      to: ".planning/ROADMAP.md (Phase 09.1)"
      via: "référence croisée de clôture"
      pattern: "09\\.1"
    - from: ".planning/PROJECT.md"
      to: "DEPENDENCIES.md"
      via: "décision moteur ↔ lignes d'audit des 6 packages"
      pattern: "vector_map_tiles"
---

<objective>
Valider sur device le résultat de la phase (critère de succès 3 — la perception du lag inter-pipeline n'est pas capturable en widget test) puis clore la documentation (critère 7). L'automatisation précède le checkpoint : récupération des artefacts CI, préparation d'une checklist UAT, puis un unique checkpoint humain (iPhone primaire + Pixel 4a), puis la clôture documentaire qui consigne les mesures.

Purpose : SC#3 (UAT iPhone / Pixel 4a), SC#4 (régressions Phase 07 sur device), SC#7 (BUG-014 fermé, PROJECT.md), entrée de la Phase 10 (courbe rebuild SDF vs volume de disques, budget frame PERF-07).

Output : UAT approuvé et consigné, BUG-014 fermé, décisions enregistrées, README + deferred-items à jour, ROADMAP / REQUIREMENTS / VALIDATION clos.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-CONTEXT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-RESEARCH.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-VALIDATION.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-07-SUMMARY.md
@.planning/phases/09-fog-rendering/deferred-items.md
@CLAUDE.md
@docs/phase09-bug-tracking/BUG-014-sdf-rect-offset-axes.md
@docs/phase09-bug-tracking/BUG-010-cell-grid-resolution-blocky.md
@docs/POC-flutter-map-mirk.md
@lib/infrastructure/mirk/README.md
@lib/domain/map/README.md
@lib/infrastructure/map/README.md
@lib/domain/mirk/README.md
@download_builds.py

Source externe (lecture seule) : `C:\claude_checkouts\mirk-poc-debug\VERDICT.md` (§caveats 1-11), `PORTBACK.md` §9 (caveats inheritance), `walk-evidence/pixel4a-walk-2/` (format des captures / logcat de référence).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Artefacts CI + checklist UAT prête à remplir</name>
  <files>docs/phase-09.1-uat.md</files>
  <action>
1. `gh run list --branch main --limit 5` → identifier le run vert du commit de clôture 09.1-07 ; `gh run download <id> -n mirkfall-ios-unsigned-ipa -D GH_builds/09.1/` et `-n mirkfall-android-debug-apk` (le script `download_builds.py` à la racine fait la même chose — l'utiliser si plus simple : `python download_builds.py`). Noter le SHA (`kGitCommitSha` est affiché au démarrage et dans le debug menu — l'UAT doit confirmer que le build installé porte ce SHA).
2. Créer `docs/phase-09.1-uat.md` (checklist en français, à remplir pendant le checkpoint) avec : en-tête (date, SHA, run CI, devices : iPhone 17 Pro iOS version, Pixel 4a Android 13) ; **pré-requis** : activer « Verbose logging » dans le debug menu AVANT la session (les rollups `frame_delta` / `fog_transform` / `sdf` / `wisp` n'émettent qu'en verbose) ; **section A — iPhone (primaire, SideStore)** : A1 session avec disques persistés (ouvrir une session existante d'un walk précédent, ou en créer une et marcher 2 min) ; A2 pan lent / pan rapide / pinch-zoom / pan+zoom combiné — observer halo des disques + noise : « zéro déplacement visible » oui / non ; A3 kill de l'app + relance → les disques réapparaissent au même endroit ; A4 changer de style (picker) ×4 → chaque variant peint dans le même Canvas ; A5 tuner : un slider change le rendu en direct ; A6 mode avion : carte + fog fonctionnels, aucune tuile grise ; A7 follow-me ; A8 hot-swap monde ↔ pays téléchargé (télécharger un petit pays, ex. Aruba) ; **section B — Pixel 4a (APK debug)** : B1 mêmes gestes → aucune stripe horizontale (FOG-23), halo SDF centré sur le trou (FOG-21) ; B2 backend GPU : `adb logcat | grep -i "impeller\|vulkan\|opengl"` au lancement → noter ; B3 rollup `frame_delta` sur 2 min pan+zoom → p50 / p95 ; **section C — mesures pour Phase 10** : rollup `infrastructure.mirk.sdf` (rebuilds/s, ms/rebuild, nb disques) sur la session la plus fournie ; **section D — verdict** : cases APPROUVÉ / REFUSÉ par section + observations libres + captures (`docs/phase-09.1-uat-screenshots/`). Commande de récupération des logs : partage depuis le debug menu (`<app_documents>/logs/yyyymmdd_hhmm.ss_logs.txt`).
Commit `docs(09.1-08): add UAT checklist + fetch CI artifacts`.
  </action>
  <verify>
    <automated>test -f docs/phase-09.1-uat.md && grep -q "Pixel 4a" docs/phase-09.1-uat.md && grep -q "SideStore" docs/phase-09.1-uat.md && ls GH_builds/09.1/ | grep -qi "ipa\|apk"</automated>
  </verify>
  <done>
- IPA + APK du commit de clôture téléchargés localement ; checklist UAT créée avec les 4 sections et les commandes de collecte
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT device — iPhone (primaire) + Pixel 4a</name>
  <files>docs/phase-09.1-uat.md, docs/phase-09.1-uat-screenshots/</files>
  <action>Checkpoint humain bloquant : l'utilisateur déroule la checklist `docs/phase-09.1-uat.md` sur l'iPhone (cible primaire, sideload SideStore) puis sur le Pixel 4a (APK debug), remplit les sections A à D (verdict, mesures, captures) et répond « approved » ou décrit les écarts. Aucune action Claude ici hormis attendre le signal ; en cas de refus, reprise en gap closure (`/gsd:plan-phase 09.1 --gaps`).</action>
  <what-built>Phase 09.1 complète sur `main` : carte flutter_map 7.0.2 + fog same-canvas (`FogLayer` enfant du `FlutterMap`, fix bundle POC FOG-06/07/12/13/18/19/21/23, disques Drift, wisps monde, 4 variants), CI verte. Artefacts dans `GH_builds/09.1/`.</what-built>
  <how-to-verify>
    1. Sideloader l'IPA sur l'iPhone (SideStore) ; installer l'APK sur le Pixel 4a (`adb install -r GH_builds/09.1/*.apk`). Vérifier dans « Debug menu → Build commit » que le SHA correspond au run CI noté dans `docs/phase-09.1-uat.md`.
    2. Activer « Verbose logging » dans le debug menu sur les deux devices.
    3. Dérouler `docs/phase-09.1-uat.md` section A sur l'iPhone (A1 → A8). Critère bloquant : A2 « zéro déplacement visible du fog pendant pan, zoom et gestes combinés » et A3 « fog conservé après redémarrage ».
    4. Dérouler la section B sur le Pixel 4a. Critère bloquant : B1 « aucune stripe horizontale, halo non miroir ». B3 informatif (médiane ≥ 30 fps attendue, p95 dépend du device).
    5. Récupérer les logs (partage depuis le debug menu) et noter les mesures C (rollups `sdf` / `frame_delta`).
    6. Remplir la section D du fichier UAT (cases + observations + captures dans `docs/phase-09.1-uat-screenshots/`).
    En cas d'échec d'un critère bloquant : décrire précisément le geste, le device, le style actif, joindre capture + log ; le plan sera repris en mode gap closure (`/gsd:plan-phase 09.1 --gaps`).
  </how-to-verify>
  <resume-signal>Tapez « approved » (sections A et B critères bloquants OK, fichier UAT rempli) ou décrivez les écarts observés.</resume-signal>
  <verify>Manuel (voir Manual-Only Verifications de 09.1-VALIDATION.md) : section D de `docs/phase-09.1-uat.md` renseignée avec les cases APPROUVÉ cochées pour A2, A3, B1 ; captures présentes dans `docs/phase-09.1-uat-screenshots/`.</verify>
  <done>Signal « approved » reçu ; `docs/phase-09.1-uat.md` contient le verdict par section, les mesures p50/p95 Pixel 4a, le rollup SDF et le backend GPU observé.</done>
</task>

<task type="auto">
  <name>Task 3: Clôture documentaire — BUG-014, PROJECT.md, POC doc, README (C9), deferred-items, REQUIREMENTS / ROADMAP / VALIDATION</name>
  <files>docs/phase09-bug-tracking/BUG-014-sdf-rect-offset-axes.md, docs/POC-flutter-map-mirk.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/deferred-items.md, .planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-VALIDATION.md, lib/infrastructure/mirk/README.md, lib/domain/map/README.md, lib/infrastructure/map/README.md, lib/domain/mirk/README.md, tool/README.md</files>
  <action>
1. **BUG-014** : status → `🟢 RESOLVED — 2026-09-XX (Phase 09.1, port-back POC mirk-poc-debug @ 90c9321)` ; section « Résolution » : architecture same-canvas (FogLayer enfant du FlutterMap), tableau des 13 correctifs portés (PORTBACK §6) et leur fichier MirkFall, invariants, résultat UAT (iPhone / Pixel 4a, mesures), lien vers `docs/phase-09.1-uat.md` ; « Tested on » : iOS + Android. Ajouter la même note de clôture croisée dans BUG-010 / BUG-012 / BUG-013 / BUG-015 si leur statut référençait BUG-014 comme bloquant (une ligne chacun).
2. **`docs/POC-flutter-map-mirk.md`** : bandeau en tête « Réalisé — POC clôturé 2026-05-31 (`C:\claude_checkouts\mirk-poc-debug`, VERDICT PORT BACK), porté dans MirkFall par la Phase 09.1 (2026-09). Document conservé comme historique. »
3. **`.planning/PROJECT.md` Key Decisions** : nouvelle ligne « **Moteur carte V1.0 = `flutter_map 7.0.2` + `vector_map_tiles 8.0.0` + `vector_map_tiles_pmtiles 1.5.0` (same-canvas)** | BUG-014 (fog qui ne suit pas la caméra) est structurel avec une platform view : deux pipelines de rendu. Le POC `mirk-poc-debug` a validé le rendu du fog comme enfant du widget carte (même Canvas, même frame) sur iPhone 17 Pro + Pixel 4a. Pins stricts, parité POC ; 8.x → V1.x avec re-validation. | — Recorded 2026-09-09 » ; sur la ligne `Renderer V1.0 = maplibre_gl 0.25.0`, remplacer l'Outcome par « Superseded 2026-09-09 (Phase 09.1) » sans supprimer la ligne (historique).
4. **`REQUIREMENTS.md`** : amender le texte de MAP-01 (« consommé via une URI `pmtiles:///<path>` par `maplibre_gl` » → « lu directement par `PmTilesVectorTileProvider` (`pmtiles 1.2.0`, `FileAt`) »), MAP-04 (« s'intègre au rendu vectoriel MapLibre comme un layer natif … RepaintBoundary » → « est peint comme enfant du `FlutterMap` dans le même Canvas que les tuiles (`FogLayer`), sans référencer flutter_map au-dessus de `lib/infrastructure/map/` hors du périmètre explicite du gate »), MAP-05 (« URI locales `pmtiles:///<path>` » → « chemins de fichiers locaux ; lint `avoid_remote_pmtiles` étendu à `fromUri` / `fromSource('http`) »), MAP-06 (types SDK : `MapController`, `MapCamera`, `LatLng`, `VectorTileLayer` ; lint `avoid_flutter_map_leak` ; périmètre + 3 fichiers). Marquer chaque amendement `(amendé 2026-09-09 Phase 09.1)`. Table de traçabilité : ajouter une ligne « Phase 09.1 | MIRK-01..05, MAP-01, MAP-05..08 (re-validés) | 10 ».
5. **README** (C9) : réécrire `lib/infrastructure/mirk/README.md` (layout final : `*_mirk_renderer.dart` ×6, `sdf/{revealed_sdf_builder,sdf_cache}`, `wisp/{wisp_particle,wisp_particle_system,wisp_transform_logger}`, `shader/{fog_shader_service,fog_shader_uniforms,fog_shader_renderer,fog_platform_corrections}`, `fog_clip_geometry.dart`, `{frame_delta_probe,fog_transform_logger,sdf_rebuild_logger,dev_marker_logger}.dart` verbose-only, `mirk_projection.dart` (SDF seulement) ; doctrine MIRK-05 : contexte étendu une fois, 3 membres, clip possédé par FogLayer, renderers peignent le repère identité clippé ; tableau des gardes structurelles : keystone FOG-07, firewall wisp/SDF, gate variant count, gate import ; retirer toute mention de `map_screen_viewport_filtering_test`, `fog_50k_tiles_perf_test`, `check_mirk_fixture_fresh`, `MirkOverlay`) ; `lib/domain/map/README.md` (port à 13 membres, flutter_map derrière l'adapter) ; `lib/infrastructure/map/README.md` (`flutter_map_map_view.dart`, `map_theme_loader.dart`, `pmtiles_source.dart` chemins, hot-swap + cache par archive, FOG-16 (b) rotation off) ; `lib/domain/mirk/README.md` (contexte étendu, `GeoPoint`) ; `tool/README.md` déjà touché par 09.1-01/02 — relire.
6. **`deferred-items.md`** (phase 09.1, nouveau) : `WalkSimulator` (hors budget ; utile pour l'UAT indoor ; si porté : derrière le debug menu, alimentant `locationStreamProvider` en `Stream<Fix>`) ; `WispRadiusBasis` + `*RadiusMeters` (axe A/B cosmétique POC, non porté) ; second JSON `rpgParchment` (Phase 13) ; pipeline `SpriteStyle` local pour `icon-image` (Phase 13) ; index spatial `RevealedSdfBuilder` si la courbe rebuild UAT l'exige (Phase 10) ; AB test Impeller / Skia Adreno 6xx + matrice OEM (Phase 15) ; discrimination d'écho via `MapEventSource.mapController` au lieu du debounce 1 s (`kMapCameraPendingMoveDebounce`) ; animation de `moveCameraTo` (tween dans l'adapter) ; `flutter_map 8.x` (V1.x) ; flakes hors périmètre héritées de la Phase 09.
7. **ROADMAP.md** : cocher Phase 09.1 (`- [x]`), `**Plans:** 8 plans` avec les 8 `[x]`, ajouter la date de clôture dans le pied de page ; Phase 10 : ajouter dans son en-tête les entrées UAT (courbe SDF, p50/p95 Pixel 4a, backend GPU). **VALIDATION.md** : passer le statut des lignes de la Per-Task Verification Map à ✅ pour les tâches exécutées, `wave_0_complete: true`, `status: complete`, `Approval: approved <date>`.
Commit `docs(09.1-08): close BUG-014, record same-canvas engine decision, rewrite mirk/map READMEs, deferred items, requirements amendments`.
  </action>
  <verify>
    <automated>grep -q "RESOLVED" docs/phase09-bug-tracking/BUG-014-sdf-rect-offset-axes.md && grep -q "flutter_map 7.0.2" .planning/PROJECT.md && grep -q "Phase 09.1" docs/POC-flutter-map-mirk.md && ! grep -n "map_screen_viewport_filtering_test\|fog_50k_tiles_perf_test\|check_mirk_fixture_fresh\|MirkOverlay" lib/infrastructure/mirk/README.md && grep -q "WalkSimulator" .planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/deferred-items.md && grep -q "avoid_flutter_map_leak" .planning/REQUIREMENTS.md && dart run tool/check_headers.dart && flutter analyze --fatal-infos --fatal-warnings</automated>
  </verify>
  <done>
- BUG-014 fermé avec référence Phase 09.1 + mesures UAT ; PROJECT.md décision enregistrée + maplibre superseded ; POC doc annoté
- 4 README à jour (aucune référence à un fichier supprimé) ; deferred-items complet ; REQUIREMENTS amendés ; ROADMAP + VALIDATION clos
- Commit poussé ; CI verte (docs only → job `gates` vert)
  </done>
</task>

</tasks>

<verification>
- UAT approuvé (sections A + B critères bloquants) et consigné dans `docs/phase-09.1-uat.md` avec captures + mesures
- `git log --oneline` de la phase : commits atomiques `chore/test/feat/refactor/docs(09.1-0X):`, tous verts en CI
- `/gsd:verify-work 09.1` peut être lancé : les 7 critères de succès du ROADMAP ont une preuve (tests nommés, gates, UAT)
</verification>

<success_criteria>
- SC#3 : UAT iPhone zéro déplacement + persistance ; Pixel 4a zéro stripe ; médiane fps notée
- SC#4 : régressions Phase 07 vérifiées sur device (mode avion, hot-swap, follow-me, téléchargement, thème)
- SC#7 : BUG-014 fermé, décision moteur enregistrée
- Entrées Phase 10 disponibles (courbe SDF vs disques, PERF-07 device, backend GPU, seam audité)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-08-SUMMARY.md` (verdict UAT par section, mesures brutes p50/p95 + rebuild SDF, backend GPU Pixel 4a, liste des documents mis à jour, items reportés).
</output>
