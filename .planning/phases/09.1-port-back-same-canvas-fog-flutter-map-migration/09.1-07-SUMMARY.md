---
phase: 09.1-port-back-same-canvas-fog-flutter-map-migration
plan: 07
subsystem: presentation-mirk-composition
tags: [fog-layer-connector, same-canvas, flutter_map, map-screen, mirk-overlay-deletion, bbox-padding, bug-012, bug-014, airplane-mode, integration-test, dev-marker, debug-menu, tdd, widget-test, ci]

# Dependency graph
requires:
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/02
    provides: "`FlutterMapMapViewWidget.fogLayers` slot between `VectorTileLayer` and the puck, `MapViewWidgetBuilder({onReady, fogLayers})`, real-engine widget-test idiom (world.pmtiles in a temp app-support dir, `MapThemeLoader(bundle:)`, `cacheFolderOverride`, tile-cancellation filter)"
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/04
    provides: "`FogLayer(renderer:, discs:, currentFix:, frameDeltaProbe:, fogTransformLogger:)`, `FrameDeltaProbe` / `FogTransformLogger` / `DevMarkerLogger`, `fog_layer_test_harness` (`pumpFogLayerInFlutterMap`, `findFogPainter`, `RecordingCanvasFake`, `SpyMirkRenderer`), `atmospheric_fog_layer_harness` settle constants"
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/05
    provides: "`WispParticleSystem(wallClock:)` + `spawnAtNewDisc`, `wispTransformLogger:` / `sdfCache:` / `wispSystem:` renderer seams, `FakeStopwatch`, `immediateStubSdfCache()`"
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/06
    provides: "`FogLayer` owns the single clip; the interim `MirkOverlay._MirkPainter` clip that had to disappear with the overlay"
provides:
  - "`FogLayerConnector` (lib/presentation/widgets/fog_layer_connector.dart) — ConsumerStatefulWidget with NO flutter_map / latlong2 import: watches `activeMirkRendererProvider`, `mapViewportProvider`, `discsInViewportProvider(viewport: padMirkViewportBbox(bbox, kMirkFogDiscQueryPaddingFactor))`, `activeSessionControllerProvider`; keeps `_lastKnownDiscs` across a reload (BUG-012); `AsyncData` → `FogLayer`, `AsyncError` → shrink + ONE warning, loading → shrink; owns + starts / stops the two verbose-only diagnostics"
  - "`padMirkViewportBbox(bbox, factor)` (lib/domain/mirk/mirk_viewport_bbox.dart) — latitude clamped to `TileMath.maxLatMercator`, longitude wrapped into [-180, 180) (identity inside the range), spans ≥ 360° or unrepresentable wrapped pairs degrade to the full longitude range"
  - "`MapScreen._buildMapStack` passes `fogLayers: const [MirkInitialRevealFade(child: FogLayerConnector())]` to `FlutterMapMapViewWidget` AND to the test builder; the `Positioned.fill(IgnorePointer(RepaintBoundary(MirkInitialRevealFade(MirkOverlay()))))` entry is gone — the fog is painted in the SAME canvas / frame as the tiles, between them and the puck (SC#2, BUG-014 structurally closed)"
  - "`MirkOverlay` deleted with its 4 widget suites + `map_screen_repaint_boundary_test`; `grep -rn 'MirkOverlay\\|mirk_overlay' lib/ test/ integration_test/` → empty; `MirkProjection` has no consumer under `lib/` any more"
  - "Debug menu entry « Marquer une anomalie (dev marker) » → `DevMarkerLogger.mark('manual')` (constructor-injected), snackbar tells whether verbose is on"
  - "Tests: `mirk_viewport_bbox_padding_test` (9, plain Dart), `fog_layer_connector_test` (7), `map_screen_fog_composition_test` (6: FogLayer child of FlutterMap without RepaintBoundary / IgnorePointer, source order tiles → fog → puck, drag pans the camera, in-session renderer swap, Ticker frames never rebuild siblings), `fog_layer_wisp_render_test` (2, POC WISP-04 port), `mirk_initial_reveal_fade_test` +1 (MapCamera.of through the FadeTransition inside a FlutterMap), `debug_menu_screen_test` +1; `_harness.dart` + `map_screen_test` on a real tile-less `FlutterMap`"
  - "`integration_test/airplane_mode_test.dart` second scenario: MapScreen on the PRODUCTION `FlutterMapMapViewWidget` + `world.pmtiles` from disk + `FogLayer` child, first render / 3 moves / `showMap(null)` / unmount under `HttpOverrides` — zero `HttpClient` constructed (MAP-01 on the real engine); `test/_helpers/tile_cancellation_noise.dart` shared helpers"
  - "Repository free of `MapLibre` / `MirkOverlay` / `StyleRewriter` prose under lib/ test/ tool/ integration_test/ (deferred-items #1 resolved)"
affects: [09.1-08 UAT (device confirmation of BUG-014, desktop smoke not run here), 10 review-gate-fog (deferred-items #6 helper de-duplication, #7 mirk_projection relocation, brief FogLayer remount on renderer swap)]

# Tech tracking
tech-stack:
  added: []
  patterns: [Riverpod connector as a thin boundary widget with no engine import (watch → compose → FogLayer), DB query keyed on the padded throttled bbox never on the per-frame camera, last-known-list retention across a family re-key, sealed AsyncValue switch (AsyncData / AsyncError / loading) with log-once error flag, test host builders taking named seeds instead of an override list (`Override` is not exported by flutter_riverpod), `ProviderScope(retry:)` disabled in error-path widget tests, real tile-less FlutterMap as the engine stand-in in screen tests, source-order reflection for composition invariants, one integration file per `flutter test` invocation]

key-files:
  created:
    - lib/presentation/widgets/fog_layer_connector.dart
    - test/domain/mirk/mirk_viewport_bbox_padding_test.dart
    - test/presentation/widgets/fog_layer_connector_test.dart
    - test/presentation/screens/map_screen_fog_composition_test.dart
    - test/presentation/widgets/fog_layer_wisp_render_test.dart
    - test/_helpers/tile_cancellation_noise.dart
  modified:
    - lib/domain/mirk/mirk_viewport_bbox.dart
    - lib/presentation/screens/map_screen.dart
    - lib/presentation/screens/debug_menu_screen.dart
    - lib/presentation/widgets/mirk_initial_reveal_fade.dart
    - lib/application/providers/active_mirk_renderer_provider.dart
    - lib/infrastructure/mirk/README.md
    - lib/infrastructure/mirk/mirk_projection.dart
    - lib/infrastructure/map/map_theme_loader.dart
    - lib/infrastructure/map/style_layer_order.dart
    - test/presentation/_harness.dart
    - test/presentation/screens/map_screen_test.dart
    - test/presentation/widgets/mirk_initial_reveal_fade_test.dart
    - test/debug_menu_screen_test.dart
    - test/_helpers/mirk_paint_context_builder.dart
    - test/infrastructure/mirk/renderer_rounded_reveal_test.dart
    - test/infrastructure/mirk/mirk_projection_test.dart
    - test/infrastructure/map/pmtiles_source_test.dart
    - test/fixtures/pmtiles/README.md
    - tool/check_avoid_remote_pmtiles.dart
    - tool/check_style_no_external_url.dart
    - tool/generate_tiny_pmtiles.dart
    - integration_test/airplane_mode_test.dart
  deleted:
    - lib/presentation/widgets/mirk_overlay.dart
    - test/presentation/widgets/mirk_overlay_composition_test.dart
    - test/presentation/widgets/mirk_overlay_feather_test.dart
    - test/presentation/widgets/mirk_overlay_pointer_passthrough_test.dart
    - test/presentation/widgets/mirk_overlay_swap_test.dart
    - test/presentation/map_screen_repaint_boundary_test.dart

key-decisions:
  - "Renderer availability is matched on the sealed `AsyncValue` (`AsyncData` → FogLayer, `AsyncError` → shrink + one warning, anything else → shrink): during a session start / style swap the provider passes through `AsyncLoading` and the `FogLayer` is unmounted for a frame or two instead of painting with the already-disposed previous renderer (which the overlay did — and which paints nothing anyway); the remount resets the layer's mount stopwatch, harmless because the renderer is new too"
  - "`padMirkViewportBbox` degrades to the whole longitude range when the padded pair is not representable under the `MirkViewportBbox` wrap convention (span ≥ 360°, or a wrapped west > 0 with east ≥ 0 — a zoom-2 viewport hugging the antimeridian): over-fetching the ring is harmless, an `AssertionError` on a frame is not. Longitudes already inside [-180, 180) are returned untouched so the family key of an identical query stays bit-exact"
  - "`viewport == null` (adapter not published yet) → `FogLayer` with `discs: const []` and NO query (the plan's behaviour), so the fog covers the map from the very first frame instead of waiting for the throttle"
  - "The `FogLayer`'s diagnostics are owned by the connector: `FrameDeltaProbe.start()` / `FogTransformLogger.start()` in `initState`, `stop()` / `dispose()` in `dispose()` — their 1 Hz timers are cancelled when the tree unmounts, which happens BEFORE flutter_test's pending-timer check, so no widget test needs a special teardown"
  - "`map_screen_fog_composition_test` replaces the deleted overlay suites with what still has meaning on the same-canvas architecture: pass-through is proved by a `tester.drag` that moves `MapController.camera.center`, the swap by `container.invalidate` changing `FogLayer.renderer` from `FakeMirkRenderer` to `SpyMirkRenderer` with the first disposed once, sibling isolation by the counters of the rewritten `TestMapScreenHarness` (no RepaintBoundary any more — the Ticker drives the painter's `repaint:` Listenable, never `setState`)"
  - "The plan's prescribed map_screen.dart comment named `RepaintBoundary` / `IgnorePointer` while its own verification greps that file for the literal — the comment says 'repaint-boundary widget' / 'pointer-ignoring wrapper' instead"
  - "The real-engine airplane proof lives in `airplane_mode_test.dart` (tag `integration`) as a second `testWidgets` pumping `MapScreen` with a `mapViewBuilderForTest` that returns the PRODUCTION `FlutterMapMapViewWidget` (test seams `themeLoader` / `cacheFolderOverride`) — it exercises `MapScreen` → `fogLayers` → `FogLayerConnector` → `FogLayer` → `MapCamera.of` on real tiles, not just the adapter; it counts `HttpClient` CONSTRUCTIONS (0) on top of invocations (0)"
  - "`DevMarkerLogger` is constructor-injected into `DebugMenuScreen` (default `const DevMarkerLogger()`), the entry writes `mark('manual')` and a snackbar says whether verbose logging is on — a marker tapped with verbose off is never silently lost"

patterns-established:
  - "Screen tests stand the engine in with a REAL tile-less `FlutterMap` mounting the `fogLayers` the screen passes (`_FakeMapWidget`); the fog then reads `MapCamera.of` exactly as in production, and no disk / network is touched"
  - "Widget-test hosts take named seeds (`renderer`, `viewport`, `discs`, `session`) and build the override list internally — `Override` is not exported by `flutter_riverpod`, and a seeded `MapViewport` subclass exposes `push(bbox)` to simulate a pan"
  - "Real-engine tests import `test/_helpers/tile_cancellation_noise.dart` (`installTileCancellationFilterForBody` at the top of the body, `settleTiles` before every unmount / swap, `drainTileCancellationNoise` after `runAsync`)"
  - "Integration files run ONE per `flutter test` invocation: several `integration_test/*.dart` on one command line make the tool build a Windows app and look for a device"

requirements-completed: [MIRK-01, MIRK-02, MIRK-03, MIRK-04, MAP-01, MAP-06, MAP-08]

# Metrics
duration: ~35 min of task commits (2026-09-09T15:51:14Z → 16:25Z) + docs / CI watch
completed: 2026-09-09
---

# Phase 09.1 Plan 07: FogLayer inside the FlutterMap, MirkOverlay deleted, CI green Summary

**The fog is now a child of the `FlutterMap`: `MapScreen` passes `MirkInitialRevealFade(child: FogLayerConnector())` through `fogLayers`, the thin Riverpod connector (no engine import) feeds the `FogLayer` with the provider renderer, the disc list queried on the PADDED throttled bbox (kept across reloads, BUG-012) and the session fix, and `MirkOverlay` with everything that only existed for the overlay architecture (RepaintBoundary, IgnorePointer, 5 suites) is gone — the fog and the tiles are painted in the same canvas, same frame, from the same camera snapshot (BUG-014 structurally closed), proved on the real engine by the airplane-mode test and locked by 26 new tests with the full suite (1263) and all gates green.**

## Performance

- **Duration:** ~35 min from the first RED commit to the last task commit (15:51:14Z → 16:25Z), then docs + CI watch
- **Started:** 2026-09-09T15:51:14Z
- **Completed:** 2026-09-09
- **Tasks:** 3 (Task 1 TDD: RED + GREEN; Task 2 one refactor commit; Task 3 one test commit + one chore commit)
- **Files modified:** 34 (6 created, 22 modified, 6 deleted)

## Accomplishments

- **Task 1 — `padMirkViewportBbox` + `FogLayerConnector`.** Domain padding: factor × (height, width) on each side, latitude clamped to `TileMath.maxLatMercator` (reused, no new constant), longitude wrapped with an exact identity inside the range, unrepresentable results degraded to the full longitude range — 9 plain-Dart cases (Paris, identity, both poles, already-wrapped seam, wrap across +180 and −180, ≥ 360°, unrepresentable pair). Connector: `ref.watch` × 4, `discsInViewportProvider(viewport: padMirkViewportBbox(bbox, kMirkFogDiscQueryPaddingFactor))`, `_lastKnownDiscs` retained while a re-keyed query loads (same list instance, no painter churn), `switch (rendererAsync)` on the sealed `AsyncValue`, FINE log on disc-count change, INFO on renderer-state transition, WARNING once on error. 7 widget cases under a real `FlutterMap` (renderer / 2 discs / fix reach the `FogLayer`; the spy family override records the padded key and never the raw viewport; last-known discs across a `push(shiftedViewport)` then the fresh result once the completer resolves; loading → `SizedBox`; error → `SizedBox` + exactly one warning across three rebuilds with Riverpod retry disabled; `viewport == null` → empty discs and zero queries; source-reflection MAP-06 guard). `check_domain_purity` 57, `check_avoid_flutter_map_leak` 196 / 197 files OK.
- **Task 2 — composition + deletion.** `map_screen.dart`: `const fogLayers = [MirkInitialRevealFade(child: FogLayerConnector())]` handed to both the production widget and the test builder; overlay `Positioned.fill` block removed; class docstring lists the fog under layer 1. `mirk_overlay.dart` + 4 suites + `map_screen_repaint_boundary_test.dart` deleted. `map_screen_fog_composition_test` (6): FogLayer / connector / fade are descendants of `FlutterMap`, the widgets between `FogLayer` and `MirkInitialRevealFade` contain no `RepaintBoundary` / `IgnorePointer` and no such wrapper has a fog widget as direct child; source order `VectorTileLayer(` < `...widget.fogLayers` < `CircleLayer<Object>(` in the engine widget; `map_screen.dart` contains the fade + connector import and no `RepaintBoundary` / `IgnorePointer`; `tester.drag(FlutterMap, (−120, 0))` moves `camera.center` east at constant latitude; `container.invalidate(activeMirkRendererProvider)` swaps `FogLayer.renderer` (Fake → Spy), first disposed once, second painted; 10 Ticker frames repaint the fog (`paintCallCount` grows) and rebuild none of the 4 sibling builders. `_harness.dart` rewritten on the new composition; `map_screen_test` `_FakeMapWidget` mounts `fogLayers` in a tile-less `FlutterMap` (5/5 unchanged assertions). `fog_layer_wisp_render_test` (2): production `AtmosphericMirkRenderer` with a counting drawing seam, `immediateStubSdfCache()`, a `WispParticleSystem(wallClock: FakeStopwatch(6000))` pre-spawned on the centre disc (18..22 wisps), SDF settled on the real event loop → recording canvas: `clipPath` < `drawRect` < first `drawCircle` < `restore`, one circle per active wisp, every centre inside the 400×800 viewport, `recordPaint` seen with the active count; empty system → zero circles, zero `recordPaint`, clip / rect / restore still there. `mirk_initial_reveal_fade_test` +1: `FogLayer` under the fade inside a `FlutterMap` builds without throwing (`MapCamera.of` resolves through the `FadeTransition`), opacity 1.0 after 600 ms, then paints. Debug menu: « Marquer une anomalie (dev marker) » + test with a spy logger (`labels == ['manual']`, "Verbose désactivé — marqueur ignoré" snackbar). Docstrings: provider, fade, README (table row + §Rendering strategy rewritten for the same-canvas architecture), test helpers. Verify scope 163/163.
- **Task 3 — integration + full suite + CI.** `airplane_mode_test` real-engine scenario: `world.pmtiles` copied to `<tmp>/maps/world.pmtiles`, `PmtilesSource(fakeRepo, tmp)`, `MapThemeLoader(bundle: _FileAssetBundle())`, `MapScreen` with the production widget through the test builder under `HttpOverrides.runZoned` + `tester.runAsync`: `onReady` within 20 s, `VectorTileLayer` present, `FogLayer` a descendant of `FlutterMap`, 3 × `moveCameraTo`, `showMap(null)`, unmount inside the zone — `clientConstructionCount == 0`, `invocationCount == 0` (≈ 4 s). Fake scenario kept (mounts the fog layers too). `map_end_to_end_test` 2/2, `first_launch_world_copy_test` 3/3 unchanged. Full VALIDATION chain at HEAD `f347eeb`: `dart format --line-length 160 --set-exit-if-changed .` clean (482 files), `flutter analyze --fatal-infos --fatal-warnings` clean, 9/9 `tool/check_*` OK (`check_headers` 421, `check_licenses` / `check_dependencies_md` 201, `check_domain_purity` 57, `check_platform_manifests`, `check_avoid_flutter_map_leak` 196, `check_avoid_remote_pmtiles` 661, `check_style_no_external_url`, `check_mirk_variant_file_count` 6), `dart test tool/test/` 96/96, `flutter test` **1263/1263** (1251 → 1263), `flutter test --dart-define=DEBUG=true test/file_logger_debug_define_test.dart` 1/1, integration 2 + 2 + 3. MapLibre / StyleRewriter prose scrubbed (deferred-items #1 resolved). Pushed; CI run **34376643891** on `f347eeb` — see "CI" below.

## Task Commits

1. **Task 1: `padMirkViewportBbox` + `FogLayerConnector`** — `8c52ac3` (test, RED: compilable stubs, 16 runtime failures) → `73b24d9` (feat, GREEN: 17/17 + 9/9 under plain `dart test`)
2. **Task 2: composition, deletions, harness, wisp render test, debug menu** — `d4feb53` (refactor)
3. **Task 3: real-engine airplane proof + integration suite** — `c9a2d69` (test) ; MapLibre prose scrub — `f347eeb` (chore)

**Plan metadata:** see final docs commit.

## Files Created/Modified

- `lib/presentation/widgets/fog_layer_connector.dart` — `FogLayerConnector`, `_FogLayerConnectorState` (`_watchDiscs`, `_buildFogLayer`, `_rendererLoading`, `_rendererUnavailable`, `_logDiscCount`, `_logRendererState`)
- `lib/domain/mirk/mirk_viewport_bbox.dart` — `padMirkViewportBbox`, `_longitudeSpanDegrees`, `_wrapLongitude`, `_fullCircleDeg` / `_halfCircleDeg`; docstring scrubbed
- `lib/presentation/screens/map_screen.dart` — `fogLayers` composition, overlay entry removed, docstrings
- `lib/presentation/screens/debug_menu_screen.dart` — `devMarkerLogger` (injected), `_onMarkAnomaly`, `_manualMarkerLabel`, the ListTile
- `lib/presentation/widgets/mirk_initial_reveal_fade.dart`, `lib/application/providers/active_mirk_renderer_provider.dart`, `lib/infrastructure/mirk/README.md`, `lib/infrastructure/mirk/mirk_projection.dart`, `lib/infrastructure/map/map_theme_loader.dart`, `lib/infrastructure/map/style_layer_order.dart` — prose only
- `test/presentation/_harness.dart` — `TestMapScreenHarness` on a real `FlutterMap` + `kHarnessMapCentre` / `kHarnessMapZoom`
- `test/presentation/screens/map_screen_test.dart` — `_FakeMapWidget(fogLayers:)` → `FlutterMap`
- `test/presentation/screens/map_screen_fog_composition_test.dart`, `test/presentation/widgets/fog_layer_connector_test.dart`, `test/presentation/widgets/fog_layer_wisp_render_test.dart`, `test/domain/mirk/mirk_viewport_bbox_padding_test.dart` — new suites
- `test/presentation/widgets/mirk_initial_reveal_fade_test.dart`, `test/debug_menu_screen_test.dart` — +1 case each
- `test/_helpers/tile_cancellation_noise.dart` — `isTileCancellation`, `installTileCancellationFilterForBody`, `drainTileCancellationNoise`, `useSmallViewport`, `settleTiles`, `kTileSettleTime` / `kTilePumpStep` / `kSmallTileViewport`
- `integration_test/airplane_mode_test.dart` — real-engine scenario, `_FileAssetBundle`, fake widget on a tile-less `FlutterMap`
- `test/_helpers/mirk_paint_context_builder.dart`, `test/infrastructure/mirk/renderer_rounded_reveal_test.dart`, `test/infrastructure/mirk/mirk_projection_test.dart`, `test/infrastructure/map/pmtiles_source_test.dart`, `test/fixtures/pmtiles/README.md`, `tool/check_avoid_remote_pmtiles.dart`, `tool/check_style_no_external_url.dart`, `tool/generate_tiny_pmtiles.dart` — prose only
- Deleted: `lib/presentation/widgets/mirk_overlay.dart`, `test/presentation/widgets/mirk_overlay_{composition,feather,pointer_passthrough,swap}_test.dart`, `test/presentation/map_screen_repaint_boundary_test.dart`

### Overlay-era behaviours ported vs dropped

| Deleted suite | Fate |
|---------------|------|
| `mirk_overlay_pointer_passthrough_test` (tap reaches a detector below an IgnorePointer) | ported as a `tester.drag` on the `FlutterMap` that moves the camera — the fog is a sibling of the tiles under the map's own gesture layer, nothing to shield |
| `mirk_overlay_swap_test` (invalidate → old disposed, new paints) | ported on `MapScreen` with the renderer changing type (`FakeMirkRenderer` → `SpyMirkRenderer`) |
| `map_screen_repaint_boundary_test` (structural RepaintBoundary + siblings not rebuilt) | structural half inverted (NO RepaintBoundary around the fog); behavioural half ported on the rewritten harness |
| `mirk_overlay_composition_test` (shrink while Idle / viewport null) | superseded by the connector cases (loading / error → shrink; `viewport == null` → FogLayer with empty discs — the fog covers the map from frame one) |
| `mirk_overlay_feather_test` (paints via the active renderer) | superseded by the connector case 1 (`paintCallCount > 0`) and the 09.1-06 renderer suites |

## Decisions Made

See `key-decisions` in the frontmatter. Additional notes:

- **`_FakeMapWidget` = real tile-less `FlutterMap`** in `map_screen_test`, `_harness.dart`, the composition test and the fake airplane scenario: the plan asked for it, and it is the only way the connector can mount a `FogLayer` (which needs `MapCamera.of`) without disk / network.
- **Wisp render test toolkit** exactly as 09.1-05 handed over (`FakeStopwatch`, `immediateStubSdfCache`, `RecordingCanvasFake` extended with `drawCircle`); the shader seam is a local counting `DrawRect` variant so the settle loop knows when the SDF resolved without a `RecordingFogShaderRenderer`.
- **`_render_helpers.dart` imported from `test/presentation/widgets/`** for `immediateStubSdfCache` (the file's "do not import outside test/infrastructure/mirk/" note is a soft convention; 09.1-05's handoff named it as the toolkit for this very test).
- **Desktop smoke (`flutter run -d windows`) NOT run** — non-blocking in the plan; an agent cannot evaluate the visual; the real-engine airplane scenario renders the same `VectorTileLayer` + `FogLayer` composition on `world.pmtiles`. To do at the start of 09.1-08 UAT.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] `padMirkViewportBbox` degrades to the full longitude range when the padded pair is not representable**
- **Found during:** Task 1 design
- **Issue:** clamp + wrap alone turn a world-overview viewport hugging the antimeridian (west −179.9°, east −39.9°, factor 0.5) into west 110.1° / east 30.1° — both positive with west > east, which the `MirkViewportBbox` `@Assert` rejects on every frame.
- **Fix:** ≥ 360° spans and unrepresentable wrapped pairs return `west: -180, east: 180`; tested.
- **Committed in:** 73b24d9

**2. [Rule 1 - Bug] `_wrapLongitude` float noise on the identity**
- **Found during:** Task 1 GREEN (`factor 0 is the identity` failed: `2.3 → 2.3000000000000114`)
- **Fix:** longitudes already inside [-180, 180) are returned untouched — also keeps the family key of an identical query bit-exact.
- **Committed in:** 73b24d9

**3. [Rule 3 - Blocking] `Override` is not exported by `flutter_riverpod`**
- **Found during:** Task 1 RED (`List<Override>` in the test host)
- **Fix:** the host takes named seeds and builds the overrides internally.
- **Committed in:** 8c52ac3

**4. [Rule 2 - Missing critical functionality] Riverpod 3 automatic retry in the error-path test**
- **Found during:** Task 1 design
- **Issue:** a failing provider is retried on a backoff `Timer` by default — pending timers at test end and repeated loading / error flips.
- **Fix:** `ProviderScope(retry: (_, _) => null)` in the connector test host.
- **Committed in:** 8c52ac3

**5. [Rule 3 - Blocking] Plan-prescribed comment vs plan verification grep**
- **Found during:** Task 2 (`map_screen.dart passes … carries no overlay scaffolding` failed on the literal `RepaintBoundary`)
- **Fix:** the composition comment says "repaint-boundary widget" / "pointer-ignoring wrapper"; the plan's `grep -rn RepaintBoundary lib/presentation/screens/map_screen.dart` is now empty.
- **Committed in:** d4feb53

**6. [Rule 3 - Blocking] `FadeTransition` finders inside a `FlutterMap`**
- **Found during:** Task 2 (`Bad state: Too many elements` — flutter_map has `FadeTransition`s of its own)
- **Fix:** finders scoped to the `MirkInitialRevealFade` subtree.
- **Committed in:** d4feb53

**7. [Rule 3 - Blocking] Files outside the plan list, literal greps**
- `lib/infrastructure/mirk/README.md` (table row on the deleted suite + §Rendering strategy), `test/infrastructure/mirk/renderer_rounded_reveal_test.dart`, `test/_helpers/mirk_paint_context_builder.dart` mentioned `MirkOverlay` / `mirk_overlay` and would have failed the plan's done-grep; `test/debug_menu_screen_test.dart` is the existing debug-menu suite (the plan expected `test/presentation/screens/`); `lib/infrastructure/map/map_theme_loader.dart` carried the last `StyleRewriter` mention; `lib/domain/mirk/mirk_paint_context.dart` had no overlay reference (nothing to edit). `test/_helpers/tile_cancellation_noise.dart` created for the real-engine scenario (the 09.1-02 helpers were local functions of `flutter_map_map_view_test.dart`, left as-is → deferred-items #6).
- **Committed in:** d4feb53, c9a2d69, f347eeb

**8. [Rule 3 - Blocking] Integration files run one per invocation**
- **Found during:** Task 3 (`flutter test integration_test/a.dart integration_test/b.dart …` → "Building Windows application… Unable to start the app on the device" for the 2nd and 3rd files, the 1st had already passed)
- **Fix:** three separate invocations (same finding as 09.1-02); recorded as a pattern.

### Plan expectations corrected

- "Le `CustomPaint` du `FogLayer` ne capture pas les pointeurs (`hitTest` renvoie false)": a `CustomPaint` with a painter IS hit-testable in Flutter; what makes the gesture reach the map is that the fog is now a CHILD of the `FlutterMap`, whose gesture layer is an ancestor of all its children — the drag test proves the behaviour, the mechanism is the composition, not a hit-test override.
- "`viewport == null` → SizedBox.shrink" was the overlay's behaviour; the plan's connector spec (`FogLayer` with `discs: const []`) is what shipped and what the test locks.

---

**Total deviations:** 8 auto-fixed (2 missing-critical — unrepresentable padding, retry timers; 1 bug — wrap identity; 5 blocking — `Override` type, plan comment vs grep, fade finders, out-of-list files, integration invocation) + 2 corrected plan expectations. No scope change. 5 task commits (plan target: 3 + optional extra).

## Issues Encountered

- A bash heredoc carrying the Task 2 patch broke on the tool's shell quoting (the trap noted by 09.1-03 / 09.1-05 / 09.1-06); every multi-file patch went through a scratchpad Python script executed by path.
- The gate loop's `grep -v "Running build hooks"` silently dropped the OK lines (they share a line with the hook message) — re-run with `sed` and explicit exit codes; all 9 OK.
- Desktop smoke not run (see Decisions).

## CI

- Run **34376643891** on `f347eeb` (push of the 5 task commits): **green on the 3 jobs** — `Lint / Licence / Headers / Deps` (format, analyze, 9 gates, tool tests, `flutter test`, DEBUG-define, plain-Dart domain step), `Build Android APK (debug)`, `Build iOS (no-codesign)`; watched with `gh run watch --exit-status` (exit 0). Previous run on `d7d97d0` (09.1-06 docs): green on the 3 jobs.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **09.1-08 (UAT + closure):** BUG-014 is structurally closed at HEAD — device confirmation on the iPhone / Pixel 4a walk is the remaining step; start with the desktop smoke (`flutter run -d windows`, pan / zoom with the mouse, fog locked to the tiles) that this plan did not run. The debug menu now has the dev-marker entry for the walk (verbose on → JSONL `dev_marker` lines next to the `frame_delta` / `fog_transform` rollups). `deferred-items.md` #1 resolved here; #2–#5 unchanged; #6 (tile-noise helper de-duplication in `flutter_map_map_view_test.dart`) and #7 (`mirk_projection.dart` has no `lib/` consumer) added for Phase 10.
- **Phase 10 review:** the connector unmounts the `FogLayer` for a frame or two on every renderer swap (session start / style change) — acceptable today, worth a look if a cross-fade between styles is ever wanted; `FrameDeltaProbe` / `FogTransformLogger` keep a 1 Hz timer armed while the map is open (emission gated on verbose) whereas the 09.1-05 loggers arm lazily — harmonising is optional.
- MIRK-01/02/03/04, MAP-01/06/08 re-validated under the same-canvas composition (marking is idempotent).

---
*Phase: 09.1-port-back-same-canvas-fog-flutter-map-migration*
*Completed: 2026-09-09*

## Self-Check: PASSED

- 6 created files present (`fog_layer_connector.dart`, `tile_cancellation_noise.dart`, the 4 new suites); the 6 deleted files absent (`mirk_overlay.dart`, its 4 suites, `map_screen_repaint_boundary_test.dart`); 5 task commits present (8c52ac3 / 73b24d9 / d4feb53 / c9a2d69 / f347eeb); artifact checks: `fog_layer_connector.dart` 159 lines (≥ 80) containing `discsInViewportProvider` / `padMirkViewportBbox` / `FogLayer(`, `fog_layer_connector_test.dart` 273 lines (≥ 60), `map_screen.dart` contains `fogLayers:` + `FogLayerConnector`, `padMirkViewportBbox` exported by `mirk_viewport_bbox.dart`; `grep -rn "MirkOverlay\|mirk_overlay" lib/ test/ integration_test/` empty, `grep -rn MapLibre lib/ test/ tool/ integration_test/` empty.
