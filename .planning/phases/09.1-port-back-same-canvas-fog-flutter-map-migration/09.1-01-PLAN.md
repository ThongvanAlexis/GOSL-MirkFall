---
phase: 09.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - DEPENDENCIES.md
  - tool/check_avoid_flutter_map_leak.dart
  - tool/test/check_avoid_flutter_map_leak_test.dart
  - tool/check_avoid_maplibre_leak.dart
  - tool/test/check_avoid_maplibre_leak_test.dart
  - tool/README.md
  - .github/workflows/ci.yml
  - lib/config/constants.dart
  - test/constants_test.dart
autonomous: true
requirements: [MAP-01, MAP-06, MIRK-04]

must_haves:
  truths:
    - "`flutter pub get` résout les 6 packages pinnés (flutter_map 7.0.2, vector_map_tiles 8.0.0, vector_map_tiles_pmtiles 1.5.0, vector_tile_renderer 5.2.0, pmtiles 1.2.0, latlong2 0.9.1) sans override, et `pubspec.lock` est committé"
    - "`DEPENDENCIES.md` contient une ligne d'audit datée 2026-09-09 pour chacun des 6 packages directs ET pour chaque nouvelle transitive du lock (12 attendues) ; `check_dependencies_md` et `check_licenses` passent"
    - "`tool/check_avoid_flutter_map_leak.dart` remplace `check_avoid_maplibre_leak.dart` : tout import de flutter_map / latlong2 / vector_map_tiles / vector_map_tiles_pmtiles / vector_tile_renderer / pmtiles / maplibre_gl hors du périmètre autorisé fait échouer le gate (exit 1) ; test apparié vert ; step CI renommé"
    - "Les constantes Phase 09.1 (`kMirkFogReferenceZoom = 13.0`, `kMirkFogNoiseTilePx = 384.0`, `kMirkWisp*`, `kMapMinZoom = 2.0`, `kMapMaxZoom = 20.0`, `kMapWorldOverviewZoom = 2.0`, `kMapStyleSourceKey = 'mirkfall_map'`, `kMirkFogDiag*`, `kMirkFogDiscQueryPaddingFactor`) existent dans `lib/config/constants.dart` et sont verrouillées par `test/constants_test.dart`"
    - "À chaque commit de ce plan, `flutter analyze --fatal-infos --fatal-warnings` et `flutter test` sont verts (maplibre_gl est CONSERVÉ jusqu'au plan 09.1-02 Task 2 — coexistence volontaire)"
  artifacts:
    - path: "pubspec.yaml"
      provides: "6 pins flutter_map stack + maplibre_gl encore présent"
      contains: "flutter_map: 7.0.2"
    - path: "DEPENDENCIES.md"
      provides: "Audit des 6 directs + transitives"
      contains: "vector_map_tiles_pmtiles | 1.5.0"
    - path: "tool/check_avoid_flutter_map_leak.dart"
      provides: "Gate d'import MAP-06 (périmètre explicite)"
      exports: ["runCheck"]
    - path: "tool/test/check_avoid_flutter_map_leak_test.dart"
      provides: "Test apparié du gate (0 / 1 / 2, allowlist, fichiers générés)"
      min_lines: 80
    - path: "lib/config/constants.dart"
      provides: "Bloc Phase 09.1 constantes fog same-canvas"
      contains: "kMirkFogReferenceZoom"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tool/check_avoid_flutter_map_leak.dart"
      via: "step `Check avoid_flutter_map_leak`"
      pattern: "check_avoid_flutter_map_leak"
    - from: "tool/check_dependencies_md.dart"
      to: "DEPENDENCIES.md"
      via: "croisement nom/version pubspec.lock ↔ sections *dependencies"
      pattern: "flutter_map \\| 7\\.0\\.2"
---

<objective>
Wave 0 de la Phase 09.1 : poser les fondations SANS toucher au code de rendu. Trois livrables cohésifs :

1. **Dépendances** — ajouter les 6 packages validés par le POC `mirk-poc-debug` @ `90c9321`, strictement pinnés, avec l'audit `DEPENDENCIES.md` complet (directs + transitives, licences + télémétrie + plateformes), en réutilisant l'audit POC du 2026-05-05 comme base re-datée 2026-09-09.
2. **Gate d'import** — `tool/check_avoid_flutter_map_leak.dart` + test apparié remplacent `check_avoid_maplibre_leak.dart` ; step CI renommé.
3. **Constantes** — le bloc Phase 09.1 dans `lib/config/constants.dart` (renommage des `kPoc*` du POC en convention MirkFall, zooms produit, clé de source du style, constantes de diagnostic), verrouillé par `test/constants_test.dart`.

**Stratégie de compilation (explicite, à respecter) :** ce plan est purement ADDITIF. `maplibre_gl 0.25.0` reste dans `pubspec.yaml` et `lib/infrastructure/map/maplibre_map_view.dart` reste intact : les deux moteurs coexistent dans le graphe de dépendances le temps d'une wave (le projet a déjà connu `flutter_map 8.3.0` + `latlong2` avant Phase 07, aucun conflit de résolution attendu). Le retrait de `maplibre_gl` et la suppression de l'adapter MapLibre se font dans le plan 09.1-02 Task 2, dans le même commit atomique que la mise en service de l'adapter flutter_map, pour que `flutter analyze` reste vert à chaque commit. Le nouveau gate inclut `maplibre_gl` dans son regex : l'import restant de `maplibre_map_view.dart` est sous `infrastructure/map/`, donc autorisé — et après 09.1-02 le regex protège contre toute réintroduction.

Purpose : SC#1 (6 packages pinnés et audités, gates verts) et SC#6 (gate d'import flutter_map remplace `check_avoid_maplibre_leak`). Débloque les plans 09.1-02 (adapter) et 09.1-03 (seam + ABI) qui tournent en parallèle en Wave 2.

Output : `pubspec.yaml` / `pubspec.lock` / `DEPENDENCIES.md` à jour, nouveau gate + test, CI renommée, constantes + test.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-CONTEXT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-RESEARCH.md
@CLAUDE.md
@pubspec.yaml
@DEPENDENCIES.md
@tool/check_avoid_maplibre_leak.dart
@tool/test/check_avoid_maplibre_leak_test.dart
@tool/check_dependencies_md.dart
@.github/workflows/ci.yml
@lib/config/constants.dart
@test/constants_test.dart

Source externe (lecture seule) : `C:\claude_checkouts\mirk-poc-debug` — `pubspec.yaml`, `pubspec.lock`, `DEPENDENCIES.md` (lignes 21-26 : audit des 6 packages, 2026-05-05), `lib/config/constants.dart` (blocs `kPocFog*`, `kMirkPocWisp*`, `kPocFrameDelta*`, `kPocFogTransform*`, `kPocSdfLogRollupSeconds`, `kPocWispTransform*`).

<interfaces>
<!-- Contrat CLI des gates `tool/check_*.dart` (Phase 01), à reproduire à l'identique. -->

From tool/check_avoid_maplibre_leak.dart (modèle à copier) :
```dart
const List<String> _excludedSuffixes = <String>['.g.dart', '.freezed.dart', '.gr.dart', '.config.dart', '.mocks.dart'];
final RegExp _leakPattern = RegExp(r"""^\s*import\s+['"]package:maplibre_gl(?:/|['"])""");
const String _allowedPrefix = 'infrastructure/map/';
Future<int> runCheck({String? rootPath}) async { /* scan lib/**.dart, rel normalisé '/', exit 0/1/2 */ }
Future<void> main(List<String> args) async { exitCode = await runCheck(); }
```

From tool/check_dependencies_md.dart :
```dart
// Sections dont le titre contient (insensible à la casse) l'une de :
const Set<String> pubspecSections = <String>{'direct dependencies', 'dev dependencies', 'transitive dependencies'};
// Chaque ligne `| <name> | <version> | ...` d'une telle section est croisée avec pubspec.lock :
//   missing (lock → md), extra (md → lock), mismatched version ⇒ exit 1.
```

Format des lignes DEPENDENCIES.md :
```
## Direct dependencies        → | Package | Version | License | Source | Telemetry audit | Date |
## Transitive dependencies    → | Package | Version | License | Pulled in by | Notes | Date |
```

Transitives NOUVELLES attendues (RESEARCH §Standard Stack, à confirmer par `flutter pub deps --style=compact` après pub get — le lock MirkFall peut résoudre des versions légèrement différentes du POC) :
dart_earcut 1.2.0 (MIT), polylabel 1.0.1 (BSD-3), proj4dart 2.1.0 (MIT), mgrs_dart 2.0.0 (MIT), wkt_parser 2.0.0 (MIT), unicode 0.3.1 (BSD-3), lists 1.0.1 (BSD-3), logger 2.7.0 (MIT), intl 0.20.2 (BSD-3), executor_lib 1.1.2 (BSD-3), vector_tile 2.0.1 (MIT), protobuf 3.1.0 (BSD-3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les 6 packages pinnés + audit DEPENDENCIES.md (maplibre_gl conservé)</name>
  <files>pubspec.yaml, pubspec.lock, DEPENDENCIES.md</files>
  <action>
**pubspec.yaml — section `dependencies`** : ajouter, juste après le bloc `maplibre_gl: 0.25.0` / `crypto: 3.0.7`, un bloc commenté puis les 6 pins stricts :

```yaml
  # Phase 09.1 — moteur flutter_map same-canvas (POC mirk-poc-debug @ 90c9321,
  # VERDICT.md : PORT BACK). Remplace maplibre_gl 0.25.0 — retiré au plan
  # 09.1-02 Task 2 dans le même commit que l'adapter flutter_map (coexistence
  # d'une wave pour garder `flutter analyze` vert à chaque commit). Pins
  # stricts = parité exacte avec le code validé sur iPhone 17 Pro + Pixel 4a.
  # Audit (licences BSD-3 / MIT / BSD-2 / Apache-2.0, zéro télémétrie, zéro
  # réseau hors NetworkTileProvider / HttpAt non instanciés) dans DEPENDENCIES.md.
  flutter_map: 7.0.2
  vector_map_tiles: 8.0.0
  vector_map_tiles_pmtiles: 1.5.0
  vector_tile_renderer: 5.2.0
  pmtiles: 1.2.0
  latlong2: 0.9.1
```

NE PAS retirer `maplibre_gl` ici. Lancer `flutter pub get` ; si le solveur refuse (conflit de contrainte entre `maplibre_gl` et la pile flutter_map — non attendu d'après le POC + l'historique Phase 07), NE PAS ajouter de `dependency_overrides` : documenter le conflit exact dans le SUMMARY et basculer sur la stratégie de repli « retrait de `maplibre_gl` + stub compilable `lib/infrastructure/map/maplibre_map_view.dart` réduit à `CameraLatLngZoom` + un `MapLibreMapViewWidget` qui rend un `ColoredBox` et appelle `onReady(FakeMapView-like)` » — uniquement dans ce cas de repli.

**Capturer le delta de transitives** : avant `pub get`, `git show HEAD:pubspec.lock > $TEMP/lock_before.lock` ; après, `flutter pub deps --style=compact > $TEMP/deps_after.txt`. Les nouvelles entrées du lock sont exactement celles à documenter (attendues : les 12 du bloc `<interfaces>`). Vérifier la licence de chacune en lisant le fichier `LICENSE` dans `%LOCALAPPDATA%\Pub\Cache\hosted\pub.dev\<pkg>-<version>\` (ne pas se fier au seul champ pub.dev — règle CLAUDE.md §Audit). Grep réseau dans chaque package direct : `grep -rn "package:http\|HttpClient\|dart:io" lib/` dans le cache — reporter le résultat verbatim dans la colonne audit (flutter_map : `http` uniquement dans `NetworkTileProvider` ; vector_map_tiles : `NetworkVectorTileProvider` + `StyleReader` ; pmtiles : `HttpAt` seulement via `fromUri` ; les 3 autres : zéro).

**DEPENDENCIES.md** :
- Section `## Direct dependencies` : 6 nouvelles lignes, colonnes `| Package | Version | License | Source | Telemetry audit | Date |`, date `2026-09-09`. Reprendre le texte d'audit du POC (`mirk-poc-debug/DEPENDENCIES.md` lignes 21-26) en l'enrichissant : raison de l'ajout (« Phase 09.1 — same-canvas fog, remplace maplibre_gl, cf. BUG-014 »), résultat du grep réseau, plateformes (iOS + Android + Windows desktop — bonus : la carte tourne désormais en `flutter run -d windows`), maintenance (flutter_map 7.x gelé ~22 mois / 8.x actif, pin volontaire pour parité POC ; vector_map_tiles 9.x beta ; pmtiles 2.x disponible).
- Section `## Transitive dependencies` : une ligne par nouvelle transitive, colonne `Pulled in by` exacte (cf. tableau), date `2026-09-09`.
- NE PAS toucher aux 4 lignes MapLibre (`maplibre_gl`, `maplibre_gl_platform_interface`, `maplibre_gl_web`, `image`) : elles partent au plan 09.1-02 Task 2 avec le retrait du pin.
- Mettre à jour le paragraphe d'en-tête de DEPENDENCIES.md s'il énonce « renderer = maplibre_gl » : ajouter une phrase « Phase 09.1 (2026-09-09) : migration vers flutter_map same-canvas, voir ROADMAP Phase 09.1 ».

Commit : `chore(09.1-01): add flutter_map 7.0.2 stack (6 pinned packages) + DEPENDENCIES.md audit`.
  </action>
  <verify>
    <automated>flutter pub get && dart run tool/check_dependencies_md.dart && dart run tool/check_licenses.dart && flutter analyze --fatal-infos --fatal-warnings && git diff --quiet HEAD -- pubspec.lock || echo "lock changed (expected before commit)"</automated>
  </verify>
  <done>
- `pubspec.yaml` porte les 6 pins stricts ET encore `maplibre_gl: 0.25.0`
- `pubspec.lock` résolu sans `dependency_overrides` supplémentaire
- `check_dependencies_md` : OK (zéro missing / extra / mismatch)
- `check_licenses` : OK (aucun package hors allowlist)
- Chaque nouvelle ligne DEPENDENCIES.md a une licence lue dans le fichier LICENSE du cache + un verdict réseau explicite
- `flutter analyze` zéro issue
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate `check_avoid_flutter_map_leak` (périmètre explicite) + test apparié + CI</name>
  <files>tool/check_avoid_flutter_map_leak.dart, tool/test/check_avoid_flutter_map_leak_test.dart, tool/check_avoid_maplibre_leak.dart, tool/test/check_avoid_maplibre_leak_test.dart, tool/README.md, .github/workflows/ci.yml</files>
  <behavior>
    - Arbre vide de tout import SDK → exit 0
    - `import 'package:flutter_map/flutter_map.dart';` dans `lib/infrastructure/map/x.dart` → exit 0 (préfixe autorisé)
    - Même import dans `lib/presentation/widgets/fog_layer.dart`, `lib/presentation/widgets/fog_clip_path.dart`, `lib/presentation/screens/map_screen.dart` → exit 0 (fichiers exacts autorisés)
    - `import 'package:latlong2/latlong.dart';` dans `lib/infrastructure/mirk/wisp/wisp_particle.dart` → exit 1 (C2 option A : les wisps utilisent le record domaine, pas latlong2)
    - `import "package:vector_map_tiles/vector_map_tiles.dart";` (guillemets doubles) dans `lib/application/x.dart` → exit 1
    - `import 'package:pmtiles/pmtiles.dart';` dans `lib/domain/x.dart` → exit 1
    - `import 'package:maplibre_gl/maplibre_gl.dart';` dans `lib/infrastructure/map/legacy.dart` → exit 0 (transition 09.1-01 → 09.1-02) et dans `lib/presentation/x.dart` → exit 1
    - `lib/presentation/widgets/fog_layer.g.dart` contenant un import interdit → exit 0 (suffixes générés exemptés)
    - `lib/presentation/widgets/fog_layer_connector.dart` important flutter_map → exit 1 (le connecteur Riverpod n'est PAS dans le périmètre : il ne doit consommer que `FogLayer`)
    - Racine inexistante → exit 2
    - Mutation guard : arbre propre 0 → ajout d'une violation → 1
  </behavior>
  <action>
Créer `tool/check_avoid_flutter_map_leak.dart` par copie adaptée de `tool/check_avoid_maplibre_leak.dart` (même header GOSL, même contrat CLI 0/1/2, même exclusion des suffixes générés, même normalisation `\` → `/`, même signature publique `Future<int> runCheck({String? rootPath})`). Différences :

```dart
/// Packages du moteur de carte confinés au périmètre (MAP-06). `maplibre_gl` reste
/// listé après sa suppression (plan 09.1-02) pour interdire toute réintroduction.
final RegExp _leakPattern = RegExp(
  r"""^\s*import\s+['"]package:(flutter_map|latlong2|vector_map_tiles|vector_map_tiles_pmtiles|vector_tile_renderer|pmtiles|maplibre_gl)(?:/|['"])""",
);

/// Préfixe autorisé (tout `lib/infrastructure/map/**`).
const String _allowedPrefix = 'infrastructure/map/';

/// Fichiers exacts autorisés hors du préfixe — décision 09.1-CONTEXT §Périmètre
/// d'import : l'invariant FOG-07 impose de lire `MapCamera.of(context)` dans le
/// `build()` d'un enfant de `FlutterMap`, donc `FogLayer` est structurellement un
/// consommateur flutter_map ; on l'assume et on le liste plutôt que de
/// l'abstraire artificiellement.
const Set<String> _allowedExactPaths = <String>{
  'presentation/widgets/fog_layer.dart',
  'presentation/widgets/fog_clip_path.dart',
  'presentation/screens/map_screen.dart',
};
```

Condition d'exemption : `rel.startsWith(_allowedPrefix) || _allowedExactPaths.contains(rel)`. Messages stdout/stderr préfixés `check_avoid_flutter_map_leak:` ; le message de règle cite le périmètre complet (préfixe + 3 fichiers) et pointe vers `lib/domain/map/map_view.dart` (« dépendre du port MapView ») et `lib/domain/mirk/mirk_paint_context.dart` (« les renderers reçoivent une projection typée, jamais MapCamera »).

Créer `tool/test/check_avoid_flutter_map_leak_test.dart` par copie adaptée de `tool/test/check_avoid_maplibre_leak_test.dart` (fixture `Directory.systemTemp.createTemp`, helper d'écriture de fichier, `import '../check_avoid_flutter_map_leak.dart' as check_avoid_flutter_map_leak;`), un `test(...)` par ligne du bloc `<behavior>`. RED d'abord (le fichier source n'existe pas → échec de compilation du test = RED acceptable ; commit `test(09.1-01): add failing check_avoid_flutter_map_leak gate tests`), puis GREEN (`feat(09.1-01): add check_avoid_flutter_map_leak gate replacing maplibre gate`).

Supprimer `tool/check_avoid_maplibre_leak.dart` et `tool/test/check_avoid_maplibre_leak_test.dart` (`git rm`). Mettre à jour `tool/README.md` (ligne du gate : nom, périmètre, requirement MAP-06).

`.github/workflows/ci.yml` job `gates` : remplacer le step

```yaml
      - name: Check avoid_maplibre_leak
        run: dart run tool/check_avoid_maplibre_leak.dart
```
par
```yaml
      - name: Check avoid_flutter_map_leak
        run: dart run tool/check_avoid_flutter_map_leak.dart
```
et réécrire le bloc de commentaire au-dessus (lignes ~113-122) : « Phase 09.1 — `check_avoid_flutter_map_leak` enforces MAP-06 : flutter_map / latlong2 / vector_map_tiles / vector_tile_renderer / pmtiles (and the retired maplibre_gl) imports MUST live under lib/infrastructure/map/ or in the 3 explicitly allow-listed presentation files (FogLayer boundary, see 09.1-CONTEXT). Everything else depends only on the MapView domain port + MirkPaintContext. » Garder le paragraphe `check_avoid_remote_pmtiles` tel quel.

Vérifier que le gate passe sur l'arbre réel : le seul import matché aujourd'hui est `package:maplibre_gl` dans `lib/infrastructure/map/maplibre_map_view.dart` (autorisé).
  </action>
  <verify>
    <automated>dart test tool/test/check_avoid_flutter_map_leak_test.dart && dart run tool/check_avoid_flutter_map_leak.dart && test ! -f tool/check_avoid_maplibre_leak.dart && test ! -f tool/test/check_avoid_maplibre_leak_test.dart && grep -q "check_avoid_flutter_map_leak" .github/workflows/ci.yml && ! grep -q "check_avoid_maplibre_leak" .github/workflows/ci.yml && dart run tool/check_headers.dart</automated>
  </verify>
  <done>
- Gate présent, test apparié couvre les 11 cas du bloc `<behavior>`, tous verts
- `dart run tool/check_avoid_flutter_map_leak.dart` → exit 0 sur l'arbre réel (`maplibre_map_view.dart` toléré)
- Ancien gate + ancien test supprimés du dépôt ; aucune référence résiduelle (`grep -rn check_avoid_maplibre_leak tool/ .github/ docs/ lib/` ne renvoie que des mentions historiques dans `.planning/` ou `docs/phase*`)
- Step CI renommé, commentaire à jour
- `dart test tool/test/` complet vert
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bloc constantes Phase 09.1 dans `lib/config/constants.dart` + verrous dans `test/constants_test.dart`</name>
  <files>lib/config/constants.dart, test/constants_test.dart</files>
  <behavior>
    - `kMirkFogReferenceZoom == 13.0` (double) ; `kMirkFogNoiseTilePx == 384.0` (double)
    - Le lockstep `kMirkFogNoiseTilePx` ↔ `const float kNoiseTilePx = 384.0;` du `.frag` n'est PAS testé ici (le `.frag` MirkFall ne contient pas encore cette constante) : aucun test `skip` n'est créé dans `constants_test.dart` ; le test de lockstep est écrit, directement vert, par 09.1-03 Task 2 dans `test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart` au moment où le `.frag` POC est porté (garde `constants_test.dart` hors des fichiers de 09.1-03, qui tourne en parallèle de 09.1-02 — propriétaire exclusif de `constants.dart` / `constants_test.dart` en Wave 2)
    - `kMirkWispDriftMetersPerSecond == 1.5`, `kMirkWispCurlAccelMetersPerSecondSquared == 0.5`, `kMirkWispDragPerSecond == 0.30`, `kMirkWispMaxDtSeconds == 0.1`, `kMirkWispCurlInputScale == 50.0`
    - `kMirkWispTintAtmosphericArgb == 0xFFE0E6F0`, `kMirkWispTintHeavenlyArgb == 0xFFF8F0E2`
    - `kMapMinZoom == 2.0`, `kMapMaxZoom == 20.0`, `kMapWorldOverviewZoom == 2.0`, `kMapMinZoom <= kMapWorldOverviewZoom`, `kInitialSessionMapZoom == 15` inchangé, `kMapMaxZoom >= kInitialSessionMapZoom`
    - `kMapStyleSourceKey == 'mirkfall_map'` et est égal à l'unique clé de `sources` dans `assets/maps/style.json` (test lit le JSON)
    - `kMapBackgroundColorArgb == 0xFFF5F1E8` (= `background-color` `#f5f1e8` du style)
    - `kMapVectorTileCacheDirName == '.vector_map'`
    - `kPmtilesArchiveOpenTimeout == Duration(seconds: 10)`
    - `kMirkFogDiscQueryPaddingFactor == 0.5`
    - `kMirkFogDiagRollupSeconds == 1` et les autres `kMirkFogDiag*` portées ont les valeurs POC (voir action)
    - Les constantes existantes `kMirkFogWispMaxCount == 200`, `kMirkFogWispLifeSeconds == 2.5`, `kMirkFogMetersPerWisp == 8.0`, `kMirkFogWispWarmUpSeconds == 5.0`, `kMirkFogWispPeakAlpha == 0.35`, `kMirkFogWispBirthRadiusPx == 6`, `kMirkFogWispDeathRadiusPx == 22`, `kMirkFogSdfViewportDebounceMs == 200` sont INCHANGÉES (le POC les a héritées du donor)
  </behavior>
  <action>
Ajouter dans `lib/config/constants.dart` un bloc `// ===== Phase 09.1 — Port-back same-canvas fog (flutter_map) =====` après le bloc BUG-009, avec docstring `///` par constante (QUOI + POURQUOI, référence à l'invariant PORTBACK ou au fix POC concerné). Contenu :

```dart
// --- Carte flutter_map (09.1-02) ---
/// Zoom minimal de la carte. À z2 le monde tient en 1024 px : évite le gris hors carte et reste
/// compatible avec `kWorldFallbackZoomCutoff` (le resolver bascule sur le world bundle sous z8).
const double kMapMinZoom = 2.0;
/// Zoom maximal. Le catalogue est cuit à z15 ; `vector_map_tiles` substitue / agrandit au-delà (validé POC F1).
const double kMapMaxZoom = 20.0;
/// Zoom d'ouverture sans session active (vue monde). Hoisté du littéral `zoom: 2` de map_screen.dart.
const double kMapWorldOverviewZoom = 2.0;
/// Clé `sources` du style ET clé de `TileProviders` : DOIVENT être identiques (assert debug-only de
/// vector_map_tiles ; en release une clé fausse = carte grise silencieuse, POC Pitfall 3).
const String kMapStyleSourceKey = 'mirkfall_map';
/// `MapOptions.backgroundColor` = `background-color` du style (#f5f1e8) : pas de gris entre les tuiles.
const int kMapBackgroundColorArgb = 0xFFF5F1E8;
/// Sous-dossier du cache fichier de vector_map_tiles sous `getTemporaryDirectory()` ; un sous-dossier
/// par archive PMTiles (world / <alpha3>) pour que le hot-swap pays ne serve jamais une tuile périmée.
const String kMapVectorTileCacheDirName = '.vector_map';
/// Timeout d'ouverture d'une archive PMTiles (`PmTilesVectorTileProvider.fromSource`) — CLAUDE.md §Timeouts.
const Duration kPmtilesArchiveOpenTimeout = Duration(seconds: 10);

// --- Fog same-canvas (09.1-03 / 09.1-04) ---
/// Zoom de référence du noise : à z13 `uZoomScale = 1.0` et le rendu est bit-identique au shader
/// pré-FOG-19 (invariant PORTBACK §5 n°8).
const double kMirkFogReferenceZoom = 13.0;
/// Période du noise en pixels monde. DOIT rester en lockstep avec `const float kNoiseTilePx` du .frag
/// (constant-foldée côté GLSL pour ne pas consommer de slot uniform).
const double kMirkFogNoiseTilePx = 384.0;
/// Facteur de padding du bbox de requête disques (½ viewport de chaque côté) : les disques en bordure
/// existent déjà quand le pan les fait entrer, entre deux rafraîchissements throttlés (RESEARCH §7).
const double kMirkFogDiscQueryPaddingFactor = 0.5;

// --- Wisps en coordonnées monde (09.1-05) — remplace le basis px/s (piège BUG-014) ---
const double kMirkWispDriftMetersPerSecond = 1.5;
const double kMirkWispCurlAccelMetersPerSecondSquared = 0.5;
const double kMirkWispDragPerSecond = 0.30;
const double kMirkWispMaxDtSeconds = 0.1;
const double kMirkWispCurlInputScale = 50.0;
const int kMirkWispTintAtmosphericArgb = 0xFFE0E6F0;
const int kMirkWispTintHeavenlyArgb = 0xFFF8F0E2;

// --- Diagnostics verbose-only (09.1-04 / 09.1-05) ---
const int kMirkFogDiagRollupSeconds = 1;
// + les autres kPocFrameDelta* / kPocFogTransform* / kPocWispTransform* portées sous kMirkFogDiag*
```

Pour le bloc diagnostics : ouvrir `C:\claude_checkouts\mirk-poc-debug\lib\config\constants.dart`, repérer chaque constante `kPocFrameDelta*`, `kPocFogTransform*`, `kPocSdfLogRollupSeconds`, `kPocWispTransform*` (rollup 1 s, fenêtre d'échantillons, seuils de percentiles en ms) et la porter à valeur identique sous le préfixe `kMirkFogDiag` (ex. `kPocFrameDeltaSampleWindow` → `kMirkFogDiagFrameDeltaSampleWindow`). Ne PAS porter `kPocInitialZoom / kPocMinZoom / kPocMaxZoom` (19 / 10 / 20 — décision CONTEXT : zooms produit ci-dessus), ni `kMelunCenterLat/LonForCurlNoise` (MirkFall est mondial ; le curl est ancré autrement au plan 09.1-05), ni `kPocBbox*`, ni `WispRadiusBasis` / `*RadiusMeters` (axe A/B cosmétique du POC, non porté — noté dans deferred-items par le plan 09.1-08), ni `kPocWalkSimulator*`.

Ce que ce plan NE fait PAS (compilation) : ne pas supprimer `kMirkFogWispInitialSpeedPx` (encore lu par `wisp_particle_system.dart` → suppression au plan 09.1-05 Task 2), ni `kMirkFogMapLayerResolution / UpdateIntervalMs / PaddingFactor` (orphelines — aucun appelant — mais retirées au plan 09.1-02 Task 2 avec les autres vestiges MapLibre / BUG-014, 09.1-02 étant le seul propriétaire de `constants.dart` en Wave 2 ; `offscreen_fog_renderer.dart` lui-même part au plan 09.1-03 Task 1), ni `kUserLocationSourceId / kUserLocationLayerId` (suppression avec l'adapter MapLibre au plan 09.1-02 Task 2). Ajouter un commentaire `// À supprimer au plan 09.1-0X` au-dessus de chacune.

`test/constants_test.dart` : nouveau `group('Phase 09.1 — same-canvas fog constants', ...)` avec un `test` par ligne du bloc `<behavior>` (RED : les symboles n'existent pas → commit `test(09.1-01): add failing Phase 09.1 constants locks` ; GREEN : `feat(09.1-01): add Phase 09.1 map + fog + wisp + diag constants`). Aucun test `skip` : le lockstep `.frag` ↔ `kMirkFogNoiseTilePx` est créé par 09.1-03 Task 2 (voir `<behavior>`). Le test de `kMapStyleSourceKey` lit `assets/maps/style.json` via `File` depuis la racine du repo (même idiome que `test/presentation/map_style_layer_order_test.dart`).
  </action>
  <verify>
    <automated>flutter test test/constants_test.dart && flutter analyze --fatal-infos --fatal-warnings && dart format --line-length 160 --set-exit-if-changed lib/config/constants.dart test/constants_test.dart</automated>
  </verify>
  <done>
- Toutes les constantes du bloc `<behavior>` existent, typées, documentées, sans nombre magique inline ailleurs
- `test/constants_test.dart` vert (nouveau groupe + anciens tests intacts, aucun test `skip`)
- Aucune constante consommée par du code encore vivant n'a été supprimée ; commentaires « à supprimer au plan … » posés
- `flutter analyze` zéro issue ; `flutter test` complet vert
  </done>
</task>

</tasks>

<verification>
- `flutter pub get` reproductible ; `pubspec.lock` committé
- `dart run tool/check_dependencies_md.dart && dart run tool/check_licenses.dart && dart run tool/check_headers.dart && dart run tool/check_avoid_flutter_map_leak.dart && dart test tool/test/` verts
- `flutter analyze --fatal-infos --fatal-warnings` zéro issue ; `flutter test` complet vert (maplibre_gl toujours présent, app inchangée fonctionnellement)
- Push sur `main` uniquement après la suite verte localement ; vérifier ensuite `gh run watch` (job `gates` avec le step renommé)
</verification>

<success_criteria>
- SC#1 partiel : 6 packages pinnés + audités (licences + télémétrie + transitives), gates verts — le retrait effectif de `maplibre_gl` est livré par 09.1-02
- SC#6 partiel : le gate d'import flutter_map remplace `check_avoid_maplibre_leak` en CI
- Contrats de constantes (ABI shader : `kMirkFogReferenceZoom`, `kMirkFogNoiseTilePx`) disponibles pour 09.1-03 / 09.1-04 / 09.1-05
- 3 à 5 commits atomiques `chore/test/feat(09.1-01):`
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-01-SUMMARY.md` (liste exacte des transitives ajoutées avec versions résolues, éventuel écart vs POC, stratégie de coexistence maplibre_gl confirmée ou repli utilisé).
</output>
