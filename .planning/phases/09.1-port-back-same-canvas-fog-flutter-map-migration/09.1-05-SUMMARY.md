---
phase: 09.1-port-back-same-canvas-fog-flutter-map-migration
plan: 05
subsystem: mirk-rendering
tags: [sdf-cache, perf-08, bug-012-debounce, wisps, world-coordinates, geo-point, bug-014, bug-015, warm-up, firewall, diag-loggers, verbose-only, tdd]

# Dependency graph
requires:
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/01
    provides: "`kMirkWisp*` world-basis tunables, `kMirkFogDiagRollupSeconds` / `kMirkFogDiagWispTransformBufferMaxSamples`, removal marker on `kMirkFogWispInitialSpeedPx`"
  - phase: 09.1-port-back-same-canvas-fog-flutter-map-migration/03
    provides: "`MirkPaintContext.projectToScreen` / `sessionElapsed`, `GeoPoint`, `FogShaderRenderer` seam + `RecordingFogShaderRenderer`, `ImmediateStubSdfBuilder`, `buildTestMirkPaintContext`"
  - phase: poc-mirk-poc-debug@90c9321
    provides: "`SdfCache` (FOG-03 / PERF-08), `SdfRebuildLogger`, `WispParticle` / `WispParticleSystem` (WISP-01..05), `WispTransformLogger`, `_FogPainter._renderWisps`, the ported tests"
provides:
  - "`SdfCache` (quantised key: discs 1e-6° / 1 mm ⊕ bbox 1e-4°) owning every image it hands out, `dispose()` invalidating in-flight builds; both shader renderers rewired: 200 ms viewport-only debounce IN FRONT of `getOrBuild`, one build in flight, coalesced follow-up, stale image kept"
  - "`SdfRebuildLogger` (`infrastructure.mirk.sdf`) and `WispTransformLogger` (`infrastructure.mirk.wisp`): 1 Hz JSONL rollups on `kMirkFogDiagRollupSeconds`, wall-clock `epochSecond`, verbose-only, timers armed lazily on the first verbose sample"
  - "Wisps in WORLD coordinates: `WispParticle.position: GeoPoint`, `velocityMetersPerSecond` (dx east, dy north); `WispParticleSystem.spawnAtNewDisc(discId:, disc:)` idempotent + 5 s warm-up (BUG-015), `advanceFromElapsed(Duration)`, curl anchored on the first spawned disc; `kMirkFogWispInitialSpeedPx` deleted"
  - "Renderers spawn on disc emergence through the system and draw wisps AFTER the shader rect via `context.projectToScreen(wisp.position)`; no `_warmingUp`, no reset hook, no `MirkProjection` import"
  - "Structural firewall test: nothing under `lib/infrastructure/mirk/wisp/` imports the SDF cache / builder / rebuild logger, `latlong2` or `flutter_map`"
  - "Test toolkit: `FakeStopwatch`, `immediateStubSdfCache()`, `DrawRectFogShaderRenderer`, `RecordingCanvas` (drawRect / drawCircle order + circle centres / paint)"
affects: [09.1-06 clip removal from renderers (the wisps are already drawn in the identity frame; `tile_cell_iteration.dart` is the last `MirkProjection` user besides candlelight), 09.1-07 MirkOverlay deletion + FogLayer wisp render test (reuses `FakeStopwatch`), 10 review-gate-fog (UAT caveat #6 measurable through the verbose toggle)]

# Tech tracking
tech-stack:
  added: []
  patterns: [cache owns the GPU handle + the injected diagnostic logger (dispose = release image + invalidate in-flight + stop logger), disc-list CONTENT signature in front of a debounce (identity would defeat it since the provider hands out a fresh List per query), single in-flight async build with request coalescing (`_pendingSdfBuild` + `_rebuildRequested`), verbose-only diagnostics with lazily armed / self-disarming periodic timers, domain record (`GeoPoint`) as the particle basis + projection injected through the paint context, warm-up gate keyed on the SYSTEM's construction stopwatch with a `wallClock` seam, Canvas spy through `noSuchMethod` (dart:ui `Canvas` is a plain abstract class), source-reflection guard tests over import lines]

key-files:
  created:
    - lib/infrastructure/mirk/sdf/sdf_cache.dart
    - lib/infrastructure/mirk/sdf_rebuild_logger.dart
    - lib/infrastructure/mirk/wisp/wisp_transform_logger.dart
    - test/_helpers/fake_stopwatch.dart
    - test/infrastructure/mirk/sdf/sdf_cache_test.dart
    - test/infrastructure/mirk/sdf_rebuild_logger_test.dart
    - test/infrastructure/mirk/wisp/wisp_particle_test.dart
    - test/infrastructure/mirk/wisp/wisp_sdf_firewall_test.dart
    - test/infrastructure/mirk/wisp/wisp_transform_logger_test.dart
    - test/wisp/wisp_pan_invariance_test.dart
  modified:
    - lib/config/constants.dart
    - lib/infrastructure/mirk/atmospheric_mirk_renderer.dart
    - lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart
    - lib/infrastructure/mirk/wisp/wisp_particle.dart
    - lib/infrastructure/mirk/wisp/wisp_particle_system.dart
    - test/application/providers/active_mirk_renderer_provider_test.dart
    - test/constants_test.dart
    - test/infrastructure/mirk/_render_helpers.dart
    - test/infrastructure/mirk/atmospheric_mirk_renderer_test.dart
    - test/infrastructure/mirk/heavenly_clouds_mirk_renderer_test.dart
    - test/infrastructure/mirk/sdf_debounce_test.dart
    - test/infrastructure/mirk/wisp/wisp_emergence_test.dart
    - test/infrastructure/mirk/wisp/wisp_particle_system_test.dart

key-decisions:
  - "SdfCache owns every ui.Image it returns AND the injected SdfRebuildLogger: a miss disposes the previous image, dispose() bumps a generation counter so a build still in flight disposes its late image and throws StateError (the renderer logs it FINE), and dispose() stops the logger — the renderer keeps `_currentSdfImage` as a borrowed handle and never disposes it"
  - "The renderer compares a CONTENT signature of the disc list (ids + lat / lon / radius, `Object.hashAll`) rather than `identical` — the provider hands out a fresh List on every query, identity would turn every pan into an immediate rebuild and defeat the 200 ms debounce; the viewport is compared by Freezed value equality"
  - "Diagnostic loggers arm their periodic timer LAZILY on the first verbose sample and disarm on the first rollup that finds verbose off, instead of keeping a 1 Hz timer armed from start(): flutter_test asserts no pending timer BEFORE addTearDown runs, so an always-armed timer created in the renderer constructor failed `mirk_overlay_feather_test` and would have broken every widget test that builds a shader renderer (09.1-04's harness included); the debug-menu toggle still acts live on the next sample and production non-verbose owns zero timers"
  - "`velocityMetersPerSecond.dy` is NORTHWARD (positive = increasing latitude): the POC docstring said 'south' but its integration (`dLat = dy / kMetersPerDegreeLat`) and the plan's own `<behavior>` are north-positive; the perimeter outward normal is `(cos θ, sin θ)` accordingly"
  - "Curl-noise anchor = centre of the FIRST disc the system spawns (fixed once, reset by clear()); replaces the POC's hard-coded Melun constant — the field is translation-invariant, the anchor only keeps hash inputs small"
  - "No renderer-side warm-up state and no reset hook (plan's firm decision, verified): `activeMirkRendererProvider` calls `factory.create(config)` on every rebuild (Tracking transition, `ref.invalidate` on style change) and disposes the previous renderer; proved by the provider test (Tracking(A) → Tracking(B) → distinct renderers, prior disposed once) and by the two-renderer scenario in `wisp_emergence_test`"
  - "TDD shape: compilable RED commits carry throwing stubs (`SdfCache`, `SdfRebuildLogger`, `WispTransformLogger`) + the tests that compile; tests that cannot compile against the old API (GeoPoint position, `spawnAtNewDisc`, `sdfCache:` parameter) were RED-observed locally (compile errors captured in the commit messages) and landed with the GREEN commits — 09.1-01 / 09.1-03 precedent"
  - "Tasks 2 and 3 committed together (plan default): the world-basis system removes `spawnAtPosition` / `render(canvas)` that the renderers still called"

patterns-established:
  - "A renderer that owns diagnostic loggers stops them in dispose(); tests that construct shader renderers inside `testWidgets` at verbose level must `await renderer.dispose()` inside the test body"
  - "Guard tests inspect import lines / class bodies; docstrings that explain a rule must not spell the forbidden literal (the plan's greps are literal)"
  - "Two-renderer parity suites are parametrised over a factory typedef (`(name, factory)` records) instead of duplicating scenarios"

requirements-completed: [MIRK-01, MIRK-03, MIRK-04]

# Metrics
duration: 3h35m wall-clock (11:06Z → 14:42Z, includes a rate-limit pause mid-Task 2; ~1h50m of execution)
completed: 2026-09-09
---

# Phase 09.1 Plan 05: SdfCache + world-anchored wisps Summary

**The POC `SdfCache` (quantised key) now sits behind the two shader renderers' 200 ms viewport-only debounce with one build in flight and the stale image kept, and the wisps are world-anchored — `GeoPoint` position, m/s velocity, spawn on disc emergence through an idempotent 5 s-warm-up system, projected at paint time through `context.projectToScreen` and drawn after the shader rect — with a structural firewall keeping the wisp system away from the SDF pipeline and both diagnostic loggers verbose-only.**

## Performance

- **Duration:** 3h35m wall-clock (11:06:52Z → 14:41:54Z) — a rate-limit cutoff paused execution mid-Task 2; effective execution ≈ 1h50m
- **Started:** 2026-09-09T11:06:52Z
- **Completed:** 2026-09-09T14:41:54Z
- **Tasks:** 3 (all TDD; Tasks 2 + 3 share one GREEN commit)
- **Files modified:** 23 (10 created, 13 modified)

## Accomplishments

- **`SdfCache` (`lib/infrastructure/mirk/sdf/sdf_cache.dart`)** — POC verbatim key (`hash(discs quantised 1e-6° / 1 mm) ⊕ bbox quantised 1e-4°`), plus MirkFall ownership rules: a miss disposes the previous image, `dispose()` releases the current one, bumps a generation counter (a build still in flight disposes its late image and throws `StateError`) and stops the injected rebuild logger; the cache stays reusable afterwards. Docstring states why the debounce stays in front of it with persisted disc volumes (RESEARCH §7, Pitfall 5).
- **Renderers, SDF side** — `_hashDiscList` / `_hashViewport` / `_mix` / `_triggerSdfRebuild` / `_sdfBuilder` / `_sdfImage` / `_sdfBuildInFlight` / `_pendingRebuild*` gone (grep empty). `sdfCache:` replaces `sdfBuilder:` (default `SdfCache(rebuildLogger: SdfRebuildLogger()..start())`). `_refreshSdfIfNeeded(context)`: disc-list content signature changed → `_scheduleSdfBuild` now; viewport-only change → `Timer(kMirkFogSdfViewportDebounceMs)` re-armed with the LATEST viewport; `_scheduleSdfBuild` keeps ONE `getOrBuild` in flight and coalesces later requests into a single follow-up with the most recent inputs; `_currentSdfImage` is a borrowed handle that stays in use during rebuilds. `dispose()` cancels the timer, drops the handle, disposes the cache.
- **Renderers, wisp side** — `_spawnWispsForNewlyEmergedDiscs(context)`: append-only `_seenDiscIdSet` pre-filter → `spawnAtNewDisc(discId:, disc:)`; `_warmingUp`, the `elapsedSec >= kMirkFogWispWarmUpSeconds` gate, `_lastTSec`, `_spawnWispsAlongDiscPerimeter` and the `mirk_projection.dart` import are gone. `_renderWisps(canvas, context)` runs at the END of `paint()` after the shader / fallback path (inside the interim renderer-side clip until 09.1-06): `advanceFromElapsed(context.sessionElapsed)`, hoisted additive `Paint`, `drawCircle(context.projectToScreen(wisp.position), lerp(birth, death, age), tint.withValues(alpha: (1 − age²)·peak·tintAlpha))`, bounds accumulated → ONE `WispTransformLogger.recordPaint` per paint. Tint = `kMirkWispTintAtmosphericArgb` / `kMirkWispTintHeavenlyArgb`. New injectable `wispTransformLogger:` (stopped on dispose).
- **`WispParticle` / `WispParticleSystem`** — final signature:
  ```dart
  class WispParticle { GeoPoint position; Offset velocityMetersPerSecond; /* dx east, dy north */ double life; final double maxLife; bool get isDead; double get age; }
  class WispParticleSystem {
    WispParticleSystem({int maxCount = kMirkFogWispMaxCount, int rngSeed = 1337, Stopwatch? wallClock});
    Iterable<WispParticle> get wisps; int get activeCount;
    void spawnAtNewDisc({required String discId, required RevealDisc disc}); // idempotent per id; inert but id-recording during kMirkFogWispWarmUpSeconds
    void advanceFromElapsed(Duration sessionElapsed);                        // baseline on first call, dt = Δ clamped to [0, kMirkWispMaxDtSeconds]
    void advance(double dt);                                                  // curl accel (anchored degree basis) + drag, m/s·dt → degrees, removeWhere(isDead) AFTER the loop
    double spawnRatePerSecondAndReset({Duration? sinceInterval});
    void clear();
  }
  ```
  ~20 wisps per 25 m disc (`max(1, round(2π·r / kMirkFogMetersPerWisp))`), ±0.5 m / ±20 % jitter, LRU cap by remaining life, polar-floored longitude scale, donor curl-noise helpers verbatim. `spawnAtPosition` (public) and `render(Canvas, Color)` are gone; `WispRadiusBasis` not ported (plan).
- **Loggers** — `SdfRebuildLogger` (`infrastructure.mirk.sdf`, count / p50 / p95 / max) and `WispTransformLogger` (`infrastructure.mirk.wisp`, stats-of-stats, buffer cap `kMirkFogDiagWispTransformBufferMaxSamples`) on `kMirkFogDiagRollupSeconds`, wall-clock `epochSecond` (invariant 6), `record*` and rollup no-ops unless `isLoggable(Level.FINE)`, timers armed lazily / self-disarming (see decisions).
- **Constants** — `kMirkFogWispInitialSpeedPx` and its removal marker deleted; `constants_test` locks the removal and asserts `wisp_particle_system.dart` consumes `kMirkWispDriftMetersPerSecond`, `…CurlAccelMetersPerSecondSquared`, `…DragPerSecond`, `…MaxDtSeconds`, `…CurlInputScale`, `kMirkFogWispWarmUpSeconds`, `kMirkFogMetersPerWisp`.
- **Tests** — suite 1095 → 1217 (with 09.1-04's additions in the same window). Mine: `sdf_cache_test` 7, `sdf_rebuild_logger_test` 5, `sdf_debounce_test` 9 scenarios × 2 renderers (BUG-012 names kept + coalescing, stale image, fresh-list signature, PERF-08 layering), `wisp_particle_test` 4, `wisp_particle_system_test` 15, `wisp_transform_logger_test` 7, `wisp_sdf_firewall_test` 3, `wisp_pan_invariance_test` 1, `wisp_emergence_test` 7 scenarios × 2 renderers + 2 source-reflection tests, renderer suites +2 each (`RecordingCanvas`: every `drawCircle` after the single `drawRect`, centres == `projectToScreen(position)` in order, tint / `BlendMode.plus` / radius range, one `recordPaint`), provider suite: counting factory + the BUG-015 `Tracking(A) → Tracking(B)` restart test, `constants_test` +1. At each of the 4 commits: scoped `flutter analyze --fatal-infos --fatal-warnings` clean; at the last one the full VALIDATION suite is green (format, analyze, 9 gates, `dart test tool/test/` 96, `flutter test` 1217).

## Task Commits

1. **Task 1: `SdfCache` + `SdfRebuildLogger`, renderers rewired behind the 200 ms debounce** — `e539812` (test, RED: throwing stubs, 0/12 at runtime) → `c2e98aa` (feat, GREEN)
2. **Task 2: wisps in world coordinates + `WispTransformLogger` + constant removal** — `6f17494` (test, RED: `FakeStopwatch`, firewall guard, logger stub 0/7 at runtime; the compile-failing suites RED-observed) → `22ac789` (feat, GREEN, shared with Task 3)
3. **Task 3: renderers spawn on emergence + `_renderWisps` after the shader rect; emergence / renderer / provider tests** — `22ac789` (feat, GREEN)

**Plan metadata:** see final docs commit.

## Files Created/Modified

- `lib/infrastructure/mirk/sdf/sdf_cache.dart` — quantised-key cache, image + logger ownership, generation counter (new)
- `lib/infrastructure/mirk/sdf_rebuild_logger.dart` — POC rollup, verbose gate, lazy timer (new)
- `lib/infrastructure/mirk/wisp/wisp_transform_logger.dart` — POC rollup, verbose gate, lazy timer (new)
- `lib/infrastructure/mirk/wisp/wisp_particle.dart` — `GeoPoint` position, `velocityMetersPerSecond`
- `lib/infrastructure/mirk/wisp/wisp_particle_system.dart` — rewritten (see signature above)
- `lib/infrastructure/mirk/atmospheric_mirk_renderer.dart`, `heavenly_clouds_mirk_renderer.dart` — SDF pipeline + wisp spawn / render, `sdfCache:` / `wispTransformLogger:` params, `_discListSignature`
- `lib/config/constants.dart` — `kMirkFogWispInitialSpeedPx` removed
- `test/_helpers/fake_stopwatch.dart` — `FakeStopwatch` (new; reused by 09.1-07)
- `test/infrastructure/mirk/_render_helpers.dart` — `immediateStubSdfCache()`, `DrawRectFogShaderRenderer`, `RecordingCanvas`
- `test/infrastructure/mirk/sdf/sdf_cache_test.dart`, `sdf_rebuild_logger_test.dart`, `wisp/wisp_particle_test.dart`, `wisp/wisp_transform_logger_test.dart`, `wisp/wisp_sdf_firewall_test.dart`, `test/wisp/wisp_pan_invariance_test.dart` — new
- `test/infrastructure/mirk/sdf_debounce_test.dart`, `wisp/wisp_particle_system_test.dart`, `wisp/wisp_emergence_test.dart` — rewritten
- `test/infrastructure/mirk/atmospheric_mirk_renderer_test.dart`, `heavenly_clouds_mirk_renderer_test.dart`, `test/application/providers/active_mirk_renderer_provider_test.dart`, `test/constants_test.dart` — extended

## Decisions Made

- **Curl anchor** — centre of the first spawned disc, fixed once per system (reset by `clear()`); `advance()` asserts the anchor exists whenever wisps do. No product-specific constant (the POC's Melun centre is not ported).
- **Renderer recreation (firm, verified)** — `activeMirkRendererProvider` is autoDispose, watches `activeSessionControllerProvider` and calls `factory.create(config)` on every rebuild while `ref.onDispose(renderer.dispose)` releases the previous one; `MirkStyleSessionController.select()` invalidates it on style change. The BUG-015 warm-up therefore restarts with each session start / resume without any reset hook; the provider test now proves `Tracking(A) → Tracking(B)` yields distinct renderers with the first disposed exactly once (counting factory replaces `_SpyingFactory(stubFor:)`).
- **Disc-list signature vs identity**, **image / logger ownership**, **lazy diagnostic timers**, **dy = north** — see frontmatter `key-decisions`.
- **POC tests ported / merged / dropped:**
  - ported: `sdf_cache_test` (hit / miss / 1e-5 hit / 2e-4 miss with counts), `sdf_rebuild_logger_test`, `wisp_particle_test`, `wisp_particle_system_test` (`advanceFromWallClock` scenario → `advanceFromElapsed`), `wisp_transform_logger_test`, `test/wisp/wisp_pan_invariance_test` (two `projectToScreen` closures instead of two `MapCamera`s — flutter_map stays behind the MAP-06 perimeter);
  - merged: the POC rebuild-counting through the JSONL stream became a counting `SdfRebuildLogger` subclass (no timers, no `Logger.root` juggling); the POC `_FakeStopwatch` became `test/_helpers/fake_stopwatch.dart`;
  - dropped: `test/wisp/wisp_no_fix_warmup_test.dart` (POC `MapScreenServices` / `RevealDiscRepository` wiring that MirkFall does not have — its two invariants are covered by the warm-up tests of the system and the emergence suite, and the "(0, 0) fix" half belongs to the Phase 02 GPS contract); `WispRadiusBasis` A/B axis (plan).
- **Docstrings in English**, consistent with the surrounding files; the plan's French key phrases (WISP-01/02/03, Pitfall 4) are carried as section headings.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Compilable RED commits carry stubs; compile-failing suites RED-observed and landed with GREEN**
- **Found during:** Task 1 and Task 2 RED steps
- **Issue:** `sdf_debounce_test` needs the `sdfCache:` constructor parameter and the wisp suites need the `GeoPoint` field / `spawnAtNewDisc`; neither compiles against the old tree, and a compile-failing commit breaks the "analyze green at every commit" must-have.
- **Fix:** `e539812` / `6f17494` seed throwing stubs for `SdfCache` / `SdfRebuildLogger` / `WispTransformLogger` (their tests fail at runtime: 0/12, 0/7) plus `FakeStopwatch` and the firewall guard; the compile errors of the other suites are recorded in the commit messages; GREEN commits carry them.
- **Committed in:** e539812, 6f17494 (RED) / c2e98aa, 22ac789 (GREEN)

**2. [Rule 2 - Missing critical functionality] `SdfCache` dispose-during-in-flight-build guard and logger ownership**
- **Found during:** Task 1 design
- **Issue:** POC `SdfCache` publishes a build that completes after `dispose()` (GPU image leak) and nobody stops the `SdfRebuildLogger` the renderer constructs by default (a periodic timer per renderer instance, renderers being recreated per session / style).
- **Fix:** generation counter → late image disposed + `StateError` to the awaiting caller (renderer logs it FINE); `SdfCache.dispose()` stops its logger (`stop()` is idempotent). Tested (`dispose() during an in-flight build …`).
- **Files modified:** lib/infrastructure/mirk/sdf/sdf_cache.dart, both renderers
- **Committed in:** c2e98aa

**3. [Rule 1 - Bug] Always-armed diagnostic timers fail widget tests**
- **Found during:** Task 3 verification (full `flutter test`: `mirk_overlay_feather_test` "A Timer is still pending even after the widget tree was disposed")
- **Issue:** `SdfRebuildLogger()..start()` / `WispTransformLogger()..start()` in the renderer constructor created `Timer.periodic`s; `flutter_test` checks pending timers before `addTearDown` runs, so every widget test building a shader renderer fails — including 09.1-04's `atmospheric_fog_layer_harness.dart` / `fog_layer_single_camera_snapshot_test.dart`.
- **Fix:** both loggers arm the timer on the first VERBOSE sample after `start()` and disarm on the first non-verbose rollup (`_running` flag + `_armIfNeeded` / `_onTick`). Zero timers in non-verbose renderers; toggle acts live. No test file touched. This differs from the 09.1-04 plan text ("le Timer 1 Hz reste armé") — see handoff.
- **Files modified:** lib/infrastructure/mirk/sdf_rebuild_logger.dart, lib/infrastructure/mirk/wisp/wisp_transform_logger.dart
- **Verification:** `mirk_overlay_feather_test` + `mirk_overlay_rounded_reveal_test` green; full suite 1217/1217.
- **Committed in:** 22ac789

**4. [Rule 1 - Bug] Literal greps of the plan matched docstring prose / a private name**
- **Found during:** Task 2 / Task 3 verification
- **Issue:** the firewall docstring named the forbidden files, the renderers' docstrings named `resetWarmUp()`, and the system's private `_spawnAtPosition` matched `spawnAtPosition`.
- **Fix:** prose reworded ("SDF cache / builder / rebuild logger", "no reset hook"), private method renamed `_spawnOneWisp`. All plan greps hold literally.
- **Committed in:** 22ac789

**5. [Rule 2 - Missing critical functionality] `wispTransformLogger:` constructor seam**
- **Found during:** Task 3 (behavior "`recordPaint` called at most once per paint")
- **Issue:** the plan's constructor interface has no way to observe the logger.
- **Fix:** optional `WispTransformLogger? wispTransformLogger` on both renderers (default started, stopped on dispose); the renderer suites inject a counting subclass.
- **Committed in:** 22ac789

**6. [Rule 3 - Blocking] Files outside `files_modified` but inside my directory scope**
- `test/infrastructure/mirk/_render_helpers.dart` (helpers `immediateStubSdfCache`, `DrawRectFogShaderRenderer`, `RecordingCanvas`) — the renderer suites' shared helper; not owned by 09.1-04.
- `test/infrastructure/mirk/atmospheric_mirk_renderer_test.dart` / `heavenly_clouds_mirk_renderer_test.dart` were listed for Task 3 only but had to migrate `sdfBuilder:` → `sdfCache:` already in Task 1 (`c2e98aa`).

### Shared working tree (parallel executor 09.1-04)

- No index sweep this wave: every commit used `git add <paths>` + `git commit … -- <paths>`. No transient cross-plan failure observed — the full suite (including 09.1-04's committed FogLayer tests and its uncommitted `fog_layer.dart`) was green at `22ac789`.
- `.planning/STATE.md` / `ROADMAP.md` carried 09.1-04's uncommitted docs edits when I reached the state step; I re-read them fresh and applied mine on top (end state identical whichever docs commit lands first).

### Handoff to 09.1-04

- **Diagnostic timer idiom (recommended, not required):** `FrameDeltaProbe` / `FogTransformLogger` keep their 1 Hz timer armed from `start()`; that is fine while `FogLayer.dispose()` stops them, but any widget test that constructs a `FogLayer` at verbose level and lets the widget tree be torn down before `dispose()` will hit the pending-timer invariant. `SdfRebuildLogger` / `WispTransformLogger` arm lazily on the first verbose sample and disarm on a non-verbose rollup — same live-toggle property, zero timers otherwise. Adopting it is optional; nothing in 09.1-04's committed tests fails today.
- **Renderers built in widget tests:** with the lazy timers, `AtmosphericMirkRenderer(...)` in `atmospheric_fog_layer_harness.dart` owns no timer while `Logger.root.level` is not FINE-loggable. If a FogLayer test raises the level to `ALL` and paints, `await renderer.dispose()` inside the test body (not only `addTearDown`) is required.

### Plan verification notes

- `grep -rn "MirkProjection" lib/infrastructure/mirk/` → `candlelight_mirk_renderer.dart`, `tile_cell_iteration.dart` (the interim clip-path builder, removed with the renderer-side clip in 09.1-06) and `mirk_projection.dart` itself. `revealed_sdf_builder.dart` never used it (the plan's expectation was off by one file); the two shader renderers no longer import it.
- `test/wisp/` is a new directory; the CI plain-Dart step is scoped to explicit directories, so it needs no exclusion (the suite imports `dart:ui` and runs under `flutter test`).

---

**Total deviations:** 6 auto-fixed (2 blocking — TDD commit shape, out-of-list helper files; 2 missing-critical — cache ownership guard, logger seam; 2 bugs — pending timers, literal greps) + 1 optional handoff.
**Impact on plan:** No scope change. 4 task commits (plan target 4-5). All must-haves hold: `SdfCache` behind the 200 ms viewport-only debounce with one build in flight and the stale image kept; `GeoPoint` + m/s wisps, `spawnAtNewDisc` idempotent + warm-up, ~20 wisps / 25 m disc, cap 200; BUG-015 holds across session restarts without a reset hook (provider + renderer tests); 100 m pan invariance tested through the context projector; structural firewall green; both loggers verbose-only on the 1 Hz `epochSecond` cadence.

## Issues Encountered

- A rate-limit cutoff paused execution after the four wisp library files were written (mid-Task 2); resumed from the on-disk state without loss (the coordinator's file list matched mine exactly).
- Bash heredocs with Python triple-quoted strings broke the tool's shell quoting once; patch scripts were written to the scratchpad with the Write tool and executed from there (same lesson as 09.1-03).
- `ui.Canvas` spy: `noSuchMethod` can swallow but not forward calls without mirrors, so `RecordingCanvas` records `drawRect` / `drawCircle` and no-ops everything else — sufficient because only the call order and the circle arguments are under test.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **09.1-06 (clip removal + variants):** the wisps are already drawn in the identity frame after the shader rect; removing `canvas.save(); canvas.clipPath(path); … restore()` around `_renderWisps` and the shader path is the only wisp-related change. `tile_cell_iteration.dart` (`buildViewportFogClipPathFromDiscs`) is the last `MirkProjection` consumer besides candlelight.
- **09.1-07 (FogLayer wisp render test, MirkOverlay deletion):** `FakeStopwatch` (`test/_helpers`), `immediateStubSdfCache()`, `DrawRectFogShaderRenderer` and `RecordingCanvas` (`test/infrastructure/mirk/_render_helpers.dart`) are the toolkit; a renderer built with the defaults owns no timer while the test is not verbose.
- **Phase 10 review / UAT caveat #6:** enabling the verbose toggle (debug menu or `--dart-define=DEBUG=true`) activates `infrastructure.mirk.sdf` rollups (`rebuildCount / medianMs / p95Ms / maxMs / discCount / intersectingDiscCount` per second) and `infrastructure.mirk.wisp` rollups without recreating the renderer.
- MIRK-01 / MIRK-03 / MIRK-04 were already "Complete" from Phase 09; this plan re-validates them under the same-canvas data path (marking is idempotent).

---
*Phase: 09.1-port-back-same-canvas-fog-flutter-map-migration*
*Completed: 2026-09-09*

## Self-Check: PASSED

- 10 created files present, `kMirkFogWispInitialSpeedPx` absent from `lib/config/constants.dart`, 4 task commits present (e539812 / c2e98aa / 6f17494 / 22ac789), artifact checks satisfied: `sdf_cache.dart` exports `SdfCache` (`getOrBuild` / `dispose`), `wisp_particle_system.dart` contains `spawnAtNewDisc`, `wisp_particle.dart` contains `GeoPoint position`, `sdf_rebuild_logger.dart` contains `Logger('infrastructure.mirk.sdf')`, `wisp_sdf_firewall_test.dart` ≥ 20 lines; key links present (`getOrBuild(` and `spawnAtNewDisc(` in `atmospheric_mirk_renderer.dart`, `projectToScreen(wisp.position)` in `_renderWisps`).
