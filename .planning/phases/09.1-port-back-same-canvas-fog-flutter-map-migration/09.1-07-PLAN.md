---
phase: 09.1
plan: 07
type: execute
wave: 5
depends_on: ["09.1-02", "09.1-06"]
files_modified:
  - lib/domain/mirk/mirk_viewport_bbox.dart
  - lib/presentation/widgets/fog_layer_connector.dart
  - lib/presentation/widgets/mirk_overlay.dart
  - lib/presentation/widgets/mirk_initial_reveal_fade.dart
  - lib/presentation/screens/map_screen.dart
  - lib/presentation/screens/debug_menu_screen.dart
  - lib/application/providers/active_mirk_renderer_provider.dart
  - lib/domain/mirk/mirk_paint_context.dart
  - test/domain/mirk/mirk_viewport_bbox_padding_test.dart
  - test/presentation/widgets/fog_layer_connector_test.dart
  - test/presentation/widgets/fog_layer_wisp_render_test.dart
  - test/presentation/widgets/mirk_overlay_composition_test.dart
  - test/presentation/widgets/mirk_overlay_feather_test.dart
  - test/presentation/widgets/mirk_overlay_pointer_passthrough_test.dart
  - test/presentation/widgets/mirk_overlay_swap_test.dart
  - test/presentation/map_screen_repaint_boundary_test.dart
  - test/presentation/_harness.dart
  - test/presentation/screens/map_screen_test.dart
  - test/presentation/screens/map_screen_fog_composition_test.dart
  - test/presentation/widgets/mirk_initial_reveal_fade_test.dart
  - integration_test/airplane_mode_test.dart
  - integration_test/map_end_to_end_test.dart
  - integration_test/first_launch_world_copy_test.dart
autonomous: true
requirements: [MIRK-01, MIRK-02, MIRK-03, MIRK-04, MAP-01, MAP-06, MAP-08]

must_haves:
  truths:
    - "`MapScreen` compose `FlutterMapMapViewWidget(fogLayers: [MirkInitialRevealFade(child: FogLayerConnector())])` : le fog est un enfant du `FlutterMap`, entre `VectorTileLayer` et le `CircleLayer` du puck ; `MirkOverlay`, son `RepaintBoundary`, son `IgnorePointer` et ses 4 tests + `map_screen_repaint_boundary_test.dart` ont disparu"
    - "`FogLayerConnector` (ConsumerStatefulWidget, sans import flutter_map) watch `activeMirkRendererProvider`, `mapViewportProvider`, `discsInViewportProvider(viewport: paddedBbox)`, `activeSessionControllerProvider` ; garde `_lastKnownDiscs` pendant un rechargement (anti-strobe BUG-012) ; rend `FogLayer(...)` ou `SizedBox.shrink()` sans renderer ; la requête DB est clé sur le bbox throttlé paddé (jamais sur la caméra par frame)"
    - "Le fondu initial 500 ms (`MirkInitialRevealFade`) survit : `FadeTransition` autour du `FogLayer` dans les enfants du `FlutterMap` — `MapCamera.of(context)` fonctionne à travers (InheritedModel), keystone inchangé"
    - "Les gestes atteignent la carte : sans `IgnorePointer`, le `CustomPaint` du `FogLayer` ne capture pas les pointeurs (`hitTest` renvoie false / `MobileLayerTransformer` transparent) — test de pass-through porté depuis `mirk_overlay_pointer_passthrough_test`"
    - "Suite complète verte : `map_screen_test`, `_harness`, `airplane_mode_test` (zéro HTTP au pump + pan + swap — de préférence avec un VRAI `FlutterMap` + `PmTilesVectorTileProvider` sur `world.pmtiles` copié en temp), `map_end_to_end_test`, `first_launch_world_copy_test` ; CI 3 jobs verts après push"
    - "Le `DevMarkerLogger` est accessible depuis le debug menu (ListTile « Marquer une anomalie ») et n'émet qu'en verbose"
  artifacts:
    - path: "lib/presentation/widgets/fog_layer_connector.dart"
      provides: "Pont Riverpod → FogLayer (aucun type flutter_map)"
      contains: "discsInViewportProvider"
      min_lines: 80
    - path: "lib/presentation/screens/map_screen.dart"
      provides: "Composition FlutterMap children [VectorTileLayer, MirkInitialRevealFade(FogLayerConnector), CircleLayer]"
      contains: "FogLayerConnector"
    - path: "lib/domain/mirk/mirk_viewport_bbox.dart"
      provides: "padMirkViewportBbox(bbox, factor) — clamp lat ±85.0511, wrap lon"
      exports: ["padMirkViewportBbox"]
    - path: "test/presentation/widgets/fog_layer_connector_test.dart"
      provides: "Connecteur sous ProviderScope : rend FogLayer, garde les derniers disques, bbox paddé"
      min_lines: 60
  key_links:
    - from: "lib/presentation/screens/map_screen.dart"
      to: "lib/infrastructure/map/flutter_map_map_view.dart"
      via: "fogLayers: <Widget>[MirkInitialRevealFade(child: FogLayerConnector())]"
      pattern: "fogLayers:"
    - from: "lib/presentation/widgets/fog_layer_connector.dart"
      to: "lib/application/providers/discs_in_viewport_provider.dart"
      via: "ref.watch(discsInViewportProvider(viewport: padMirkViewportBbox(viewport, kMirkFogDiscQueryPaddingFactor)))"
      pattern: "padMirkViewportBbox"
    - from: "lib/presentation/widgets/fog_layer_connector.dart"
      to: "lib/presentation/widgets/fog_layer.dart"
      via: "FogLayer(renderer:, discs:, currentFix:, frameDeltaProbe:, fogTransformLogger:)"
      pattern: "FogLayer\\("
---

<objective>
Fermer la boucle visuelle : monter le `FogLayer` (09.1-04) comme enfant du `FlutterMap` (09.1-02) via un connecteur Riverpod fin, supprimer `MirkOverlay` et tout ce qui n'existait que pour l'architecture overlay (RepaintBoundary, IgnorePointer, 5 tests), adapter le harnais de test et les tests d'intégration, et obtenir la CI complète verte. C'est le premier moment où le fog et les tuiles sont peints dans le même Canvas, même frame — BUG-014 est structurellement fermé à la fin de ce plan (confirmation device au plan 09.1-08).

Purpose : SC#2 (FogLayer enfant du FlutterMap entre tuiles et blue dot), SC#4 (zéro régression Phase 07, tests d'intégration), SC#3 préparation (fog persisté : disques Drift → provider → FogLayer), SC#1 (tous les gates CI verts sur l'arbre final).

Output : `map_screen.dart` recomposé, `FogLayerConnector`, suppressions, tests adaptés, CI verte.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-CONTEXT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-RESEARCH.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-02-SUMMARY.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-04-SUMMARY.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-06-SUMMARY.md
@CLAUDE.md
@lib/presentation/screens/map_screen.dart
@lib/presentation/widgets/mirk_overlay.dart
@lib/presentation/widgets/mirk_initial_reveal_fade.dart
@lib/presentation/widgets/fog_layer.dart
@lib/infrastructure/map/flutter_map_map_view.dart
@lib/application/providers/discs_in_viewport_provider.dart
@lib/application/providers/map_viewport_provider.dart
@lib/application/providers/active_mirk_renderer_provider.dart
@lib/application/controllers/active_session_controller.dart
@lib/domain/mirk/mirk_viewport_bbox.dart
@lib/presentation/screens/debug_menu_screen.dart
@lib/infrastructure/mirk/dev_marker_logger.dart
@test/presentation/_harness.dart
@test/presentation/screens/map_screen_test.dart
@test/presentation/widgets/mirk_overlay_pointer_passthrough_test.dart
@integration_test/airplane_mode_test.dart

Source externe (lecture seule) : `C:\claude_checkouts\mirk-poc-debug\test\presentation\widgets\fog_layer_wisp_render_test.dart`, `test/presentation/screens/map_screen_fog_test.dart` (à réécrire sur le MapScreen MirkFall), `test/_helpers/swallow_vector_map_tiles_cancellation.dart` (si les widget tests loggent des cancellations de tuiles au démontage).

<interfaces>
<!-- Ce que MirkOverlay faisait (à reproduire dans le connecteur) et ce que FogLayer attend. -->

MirkOverlay (lib/presentation/widgets/mirk_overlay.dart:94-132) :
```dart
final AsyncValue<MirkRenderer> rendererAsync = ref.watch(activeMirkRendererProvider);
final viewport = ref.watch(mapViewportProvider);                 // MirkViewportBbox? (throttle 50 ms)
final sessionState = ref.watch(activeSessionControllerProvider); // Tracking → lastFix
final discsAsync = viewport != null ? ref.watch(discsInViewportProvider(viewport: viewport)) : const AsyncValue.data(<RevealDisc>[]);
if (discsAsync.hasValue) _lastKnownDiscs = discsAsync.value;
final List<RevealDisc>? effectiveDiscs = discsAsync.hasValue ? discsAsync.value : _lastKnownDiscs;   // anti-strobe BUG-012
```
FogLayer (09.1-04) : `FogLayer({required MirkRenderer renderer, required List<RevealDisc> discs, Fix? currentFix, required FrameDeltaProbe frameDeltaProbe, required FogTransformLogger fogTransformLogger, bool? isAndroid, double? pixelRatio})`.

FlutterMapMapViewWidget (09.1-02) : paramètre `List<Widget> fogLayers` inséré entre `VectorTileLayer` et le `CircleLayer` puck ; `MapViewWidgetBuilder = Widget Function({required ValueChanged<MapView> onReady, required List<Widget> fogLayers})`.

Padding domaine :
```dart
/// Élargit [bbox] de `factor × (hauteur, largeur)` de chaque côté. Latitude clampée à ±85.0511°
/// (limite Mercator), longitude wrappée dans [-180, 180] (antiméridien autorisé par MirkViewportBbox).
MirkViewportBbox padMirkViewportBbox(MirkViewportBbox bbox, double factor);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `padMirkViewportBbox` + `FogLayerConnector` (Riverpod → FogLayer, sans flutter_map)</name>
  <files>lib/domain/mirk/mirk_viewport_bbox.dart, lib/presentation/widgets/fog_layer_connector.dart, test/domain/mirk/mirk_viewport_bbox_padding_test.dart, test/presentation/widgets/fog_layer_connector_test.dart</files>
  <behavior>
    - `padMirkViewportBbox(bbox(48.80..48.90, 2.30..2.40), 0.5)` → `(48.75..48.95, 2.25..2.45)` ; près du pôle → `north` clampé à 85.0511 ; près de l'antiméridien (`west 179.9, east -179.9`) → wrap cohérent, aucune `AssertionError`
    - `FogLayerConnector` sous `ProviderScope` avec overrides (`activeMirkRendererProvider` → `FakeMirkRenderer`, `mapViewportProvider` → bbox Paris, `discsInViewportProvider` → 2 disques, session Tracking avec `lastFix`) et monté DANS un `FlutterMap` de test : `find.byType(FogLayer)` trouve un widget dont `discs.length == 2`, `currentFix` = le fix de session, `renderer` = le fake
    - La famille `discsInViewportProvider` est appelée avec le bbox PADDÉ (`padMirkViewportBbox(viewport, kMirkFogDiscQueryPaddingFactor)`) — override espion qui enregistre l'argument
    - Pendant que `discsInViewportProvider` repasse en `loading` (nouvelle clé), `FogLayer.discs` reste la liste précédente (`_lastKnownDiscs`), jamais `[]`
    - Sans renderer résolu (`AsyncValue.loading`) → `SizedBox.shrink()` (pas de `FogLayer`) ; renderer en erreur → `SizedBox.shrink()` + log `warning` une fois
    - `viewport == null` (adapter pas encore publié) → `FogLayer` rendu avec `discs: const []`
    - Le fichier `fog_layer_connector.dart` n'importe ni `flutter_map` ni `latlong2` (couvert par le gate — assertion structurelle dans le test)
  </behavior>
  <action>
1. `lib/domain/mirk/mirk_viewport_bbox.dart` : ajouter la fonction top-level `padMirkViewportBbox` (constante locale nommée `mercatorLatLimitDeg = 85.0511` — réutiliser une constante existante si `tile_math.dart` en expose une) + test `test/domain/mirk/mirk_viewport_bbox_padding_test.dart` (plain-Dart compatible : pas d'import flutter).
2. `lib/presentation/widgets/fog_layer_connector.dart` (nouveau, header GOSL) : `class FogLayerConnector extends ConsumerStatefulWidget` ; state : `List<RevealDisc>? _lastKnownDiscs`, `late final FrameDeltaProbe _frameDeltaProbe = FrameDeltaProbe()..start()`, `late final FogTransformLogger _fogTransformLogger = FogTransformLogger()..start()` (verbose-only : `start()` est bon marché, l'émission est gatée) ; `dispose` → `stop()` / `unawaited(dispose())`. `build` : reproduire la logique `MirkOverlay` du bloc `<interfaces>` avec le bbox paddé, extraire `currentFix` depuis `Tracking(:final lastFix)`, puis `switch (rendererAsync) { AsyncData(:final value) => FogLayer(renderer: value, discs: effectiveDiscs ?? const <RevealDisc>[], currentFix: currentFix, frameDeltaProbe: _frameDeltaProbe, fogTransformLogger: _fogTransformLogger), AsyncError() => (log once) SizedBox.shrink(), _ => SizedBox.shrink() }`. Garder le log throttlé « every 60th frame » de MirkOverlay si utile (sinon `fine` sur changement de nombre de disques uniquement). Docstring : « frontière Riverpod ; ne connaît pas flutter_map ; la requête DB est clé sur `mapViewportProvider` (throttle 50 ms) paddé, JAMAIS sur la caméra par frame (Pitfall 6) ; le clip / la projection exacts viennent du snapshot caméra dans FogLayer ».
3. Tests selon `<behavior>` (harnais : `ProviderScope(overrides: [...], child: MaterialApp(home: FlutterMap(options: MapOptions(initialCenter: LatLng(48.85, 2.35), initialZoom: 15), children: [FogLayerConnector()])))` ; pompes bornées — Ticker). Le contrôleur de session fake : reprendre `_FakeActiveSessionController` de `map_screen_test.dart` (retour SYNCHRONE du seed — décision 09-05).
RED → GREEN : `test(09.1-07): add failing FogLayerConnector + bbox padding tests`, `feat(09.1-07): add FogLayerConnector (Riverpod → FogLayer, padded disc query, last-known discs)`.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && dart run tool/check_domain_purity.dart && dart run tool/check_avoid_flutter_map_leak.dart && flutter test test/domain/mirk/mirk_viewport_bbox_padding_test.dart test/presentation/widgets/fog_layer_connector_test.dart</automated>
  </verify>
  <done>
- `padMirkViewportBbox` testé (pôle, antiméridien) ; `FogLayerConnector` testé (7 cas) ; aucun import flutter_map dans le connecteur
  </done>
</task>

<task type="auto">
  <name>Task 2: Composition `MapScreen` + suppression `MirkOverlay` et de ses tests + harnais + wisp render test + debug menu</name>
  <files>lib/presentation/screens/map_screen.dart, lib/presentation/widgets/mirk_overlay.dart, lib/presentation/widgets/mirk_initial_reveal_fade.dart, lib/presentation/screens/debug_menu_screen.dart, lib/application/providers/active_mirk_renderer_provider.dart, lib/domain/mirk/mirk_paint_context.dart, test/presentation/widgets/mirk_overlay_composition_test.dart, test/presentation/widgets/mirk_overlay_feather_test.dart, test/presentation/widgets/mirk_overlay_pointer_passthrough_test.dart, test/presentation/widgets/mirk_overlay_swap_test.dart, test/presentation/map_screen_repaint_boundary_test.dart, test/presentation/_harness.dart, test/presentation/screens/map_screen_test.dart, test/presentation/screens/map_screen_fog_composition_test.dart, test/presentation/widgets/fog_layer_wisp_render_test.dart, test/presentation/widgets/mirk_initial_reveal_fade_test.dart</files>
  <action>
Ordre d'édition recommandé (état récupérable si la tâche est interrompue) : 1) nouveaux tests (`map_screen_fog_composition_test`, `fog_layer_wisp_render_test`) ; 2) composition `map_screen.dart` + harnais `_harness.dart` / `map_screen_test.dart` ; 3) docstrings (`active_mirk_renderer_provider.dart`, `mirk_paint_context.dart`, `mirk_initial_reveal_fade.dart`) + debug menu ; 4) suppressions (`mirk_overlay.dart` + ses 5 tests) EN DERNIER — tant que `MirkOverlay` existe encore, l'arbre compile à chaque étape.

1. **`map_screen.dart`** : dans `_buildMapStack`, passer `fogLayers: const <Widget>[MirkInitialRevealFade(child: FogLayerConnector())]` au `FlutterMapMapViewWidget` (et au builder de test) ; supprimer le `Positioned.fill(IgnorePointer(RepaintBoundary(MirkInitialRevealFade(MirkOverlay()))))` et son bloc de commentaire (« sibling of the MapLibre platform view … ») ; remplacer par un commentaire de 3 lignes : « Phase 09.1 : le fog est un ENFANT du FlutterMap (`fogLayers`), peint dans le même Canvas / la même frame que les tuiles — c'est la réponse architecturale à BUG-014. Pas de RepaintBoundary (recréerait une frame de retard), pas d'IgnorePointer (le CustomPaint du FogLayer ne capture pas les pointeurs). » Mettre à jour la docstring de classe (liste des couches). Les overlays `Positioned` (menu, back, tuner, FAB column, banner, chip, attribution) restent hors carte, dans le `Stack`.
2. `git rm lib/presentation/widgets/mirk_overlay.dart test/presentation/widgets/mirk_overlay_composition_test.dart test/presentation/widgets/mirk_overlay_feather_test.dart test/presentation/widgets/mirk_overlay_pointer_passthrough_test.dart test/presentation/widgets/mirk_overlay_swap_test.dart test/presentation/map_screen_repaint_boundary_test.dart`. Avant suppression, relire `mirk_overlay_swap_test` (changement de style en session → le nouveau renderer peint) et `pointer_passthrough` : reporter ces DEUX comportements dans `test/presentation/screens/map_screen_fog_composition_test.dart` (nouveau) : (a) `MapScreen` avec builder de test qui monte les `fogLayers` dans un vrai `FlutterMap` → `find.descendant(of: find.byType(FlutterMap), matching: find.byType(FogLayer))` trouve 1 widget, et il est APRÈS `VectorTileLayer` (ou après le premier enfant) dans l'ordre des enfants ; (b) un `tester.drag` sur la carte modifie `MapController.camera.center` (les gestes passent) ; (c) invalider `activeMirkRendererProvider` avec un autre style → le `FogLayer.renderer` change de type ; (d) `find.byType(RepaintBoundary)` n'a aucun ancêtre direct commun entre `FogLayer` et `FlutterMap` (aucun RepaintBoundary inséré par MirkFall entre les deux — flutter_map peut en avoir en interne : cibler « aucun `RepaintBoundary` dont l'enfant direct est `FogLayer` / `MirkInitialRevealFade` / `FogLayerConnector` »).
3. Retirer les références à `MirkOverlay` dans les docstrings de `active_mirk_renderer_provider.dart`, `mirk_paint_context.dart`, `mirk_initial_reveal_fade.dart` (« appliqué au FogLayer dans les enfants du FlutterMap »).
4. **`test/presentation/_harness.dart`** + **`map_screen_test.dart`** : le `_FakeMapWidget` prend `fogLayers` et les monte dans `FlutterMap(options: MapOptions(initialCenter: LatLng(0, 0), initialZoom: kMapWorldOverviewZoom), children: fogLayers)` (flutter_map tourne en widget test ; `onReady(fakeMapView)` appelé en `initState` post-frame comme avant). Les commentaires « MirkOverlay's Ticker runs forever → pas de pumpAndSettle » deviennent « FogLayer's Ticker … ». Si `vector_map_tiles` n'est pas monté ici (pas de `VectorTileLayer` dans le fake), aucune cancellation à avaler ; sinon porter `swallow_vector_map_tiles_cancellation.dart` du POC.
5. Porter `fog_layer_wisp_render_test.dart` du POC : `FogLayer(renderer: AtmosphericMirkRenderer(config, wispSystem: systemWithSpawnedWisps, shaderRenderer: recorder))` → après pompes bornées, un `Canvas` espion (ou `tester.binding` + `debugPaint`) — plus simple : `_SpyCanvas` n'est pas injectable dans un widget ; utiliser `WispTransformLogger` espion (`recordPaint` appelé avec `activeCount > 0`) + `renders.isNotEmpty` pour prouver l'ordre shader → wisps dans la même frame.
6. **Debug menu** : `debug_menu_screen.dart` → `ListTile(leading: Icon(Icons.flag), title: Text('Marquer une anomalie (dev marker)'), subtitle: Text('Écrit un marqueur horodaté dans les logs verbose'), onTap: () => DevMarkerLogger().mark('manual'))` (ou une instance partagée via provider si DevMarkerLogger a un état) ; test dans `test/presentation/screens/debug_menu_screen_test.dart` si un test existe (sinon un `find.text` dans un nouveau petit test).
Commit `refactor(09.1-07): compose FogLayer inside FlutterMap, delete MirkOverlay + overlay-only tests` (+ `test(09.1-07): ...` séparé si naturel).
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && test ! -f lib/presentation/widgets/mirk_overlay.dart && ! grep -rn "MirkOverlay\|mirk_overlay" lib/ test/ integration_test/ && dart run tool/check_avoid_flutter_map_leak.dart && flutter test test/presentation/ test/application/providers/active_mirk_renderer_provider_test.dart</automated>
  </verify>
  <done>
- `MapScreen` compose le fog dans le `FlutterMap` ; `MirkOverlay` + 5 tests supprimés ; comportements swap / pass-through reportés dans `map_screen_fog_composition_test`
- Harnais + `map_screen_test` verts avec un vrai `FlutterMap` de test ; `fog_layer_wisp_render_test` vert ; dev marker dans le debug menu
- Zéro référence `MirkOverlay` dans `lib/`, `test/`, `integration_test/`
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests d'intégration (airplane mode réel flutter_map, e2e, first launch) + suite complète + CI verte</name>
  <files>integration_test/airplane_mode_test.dart, integration_test/map_end_to_end_test.dart, integration_test/first_launch_world_copy_test.dart</files>
  <action>
1. **`integration_test/airplane_mode_test.dart`** : conserver le scénario existant (`HttpOverrides.runZoned` avec un `HttpClient` qui lève sur toute requête ; pump `MapScreen` ; pan ; country switch ; garde d'inertie « `FakeMapView.showMap` invoqué »). Renforcer la preuve MAP-01 (RESEARCH §8) : ajouter un second `testWidgets` qui pompe le VRAI `FlutterMapMapViewWidget` (pas le fake) avec `PmtilesSource` pointant sur `assets/maps/world.pmtiles` copié dans un temp dir, `cacheFolderOverride` temp, `MapThemeLoader(bundle: _FileAssetBundle)`, sous le même `HttpOverrides` ; pomper 10 frames, `MapController.move` ×3, `showMap(null)` ; aucune exception réseau levée = zéro socket ouvert (`PmTilesArchive.from(path)` → `FileAt`). Tag `integration` conservé.
2. **`map_end_to_end_test.dart`** : ne référence ni MapLibre ni StyleRewriter (vérifié) — l'exécuter tel quel ; si le hot-swap est observable côté `FakeMapView.showMap`, rien à changer.
3. **`first_launch_world_copy_test.dart`** : inchangé ; exécuter.
4. Lancer la commande « Full suite » de `09.1-VALIDATION.md` + `flutter test integration_test/` (tag `integration`) + `flutter test --dart-define=DEBUG=true test/file_logger_debug_define_test.dart`. Corriger toute régression dans le périmètre de la phase ; toute flake préexistante hors périmètre (cf. `.planning/phases/09-fog-rendering/deferred-items.md` : `atomic_renamer_test`, `backup_test`, `download_soak_test`) est notée, pas corrigée.
5. Push sur `main` ; `gh run watch` jusqu'au vert des 3 jobs (`gates`, `android`, `ios`) ; si le job `ios` échoue sur `pod install` ou le job `android` sur impellerc, itérer (autorisé par CLAUDE.md §Git & CI) — ne pas fermer le plan tant que la CI n'est pas verte. Noter les numéros de run dans le SUMMARY.
Commit `test(09.1-07): real flutter_map airplane-mode proof (zero sockets on world.pmtiles) + integration suite green`.
  </action>
  <verify>
    <automated>flutter test integration_test/airplane_mode_test.dart integration_test/map_end_to_end_test.dart integration_test/first_launch_world_copy_test.dart && dart format --line-length 160 --set-exit-if-changed . && flutter analyze --fatal-infos --fatal-warnings && dart run tool/check_headers.dart && dart run tool/check_licenses.dart && dart run tool/check_dependencies_md.dart && dart run tool/check_domain_purity.dart && dart test tool/test/ && dart run tool/check_platform_manifests.dart && dart run tool/check_avoid_flutter_map_leak.dart && dart run tool/check_avoid_remote_pmtiles.dart && dart run tool/check_style_no_external_url.dart && dart run tool/check_mirk_variant_file_count.dart && flutter test</automated>
  </verify>
  <done>
- 3 tests d'intégration verts (dont la preuve airplane mode sur le vrai `FlutterMap`)
- Full suite verte localement ; CI 3 jobs verts sur `main` (numéros de run consignés)
- Aucune référence MapLibre / MirkOverlay / StyleRewriter dans `lib/`, `test/`, `tool/`, `integration_test/`
  </done>
</task>

</tasks>

<verification>
- `flutter run -d windows` : la carte s'affiche, le fog (session de test ou aucune session → fog plein) suit strictement pan / zoom à la souris — vérification visuelle desktop rapide avant l'UAT device (non bloquante, mais à noter dans le SUMMARY)
- `grep -c "MapCamera.of(context)" lib/presentation/widgets/fog_layer.dart` → 1 ; `grep -rn "RepaintBoundary" lib/presentation/screens/map_screen.dart lib/presentation/widgets/fog_layer*.dart` → vide
</verification>

<success_criteria>
- SC#2 : `FogLayer` enfant du `FlutterMap` entre tuiles et blue dot en production ; keystone FOG-07 en CI
- SC#4 : airplane mode zéro réseau (fake + réel), hot-swap pays, follow-me, thème standard, pipeline téléchargement — tests verts
- SC#1 / SC#6 : tous les gates CI verts sur l'arbre final ; `MirkOverlay` disparu
- MIRK-01/02/03 : disques Drift (rayon `kDefaultRevealRadiusMeters`, persistance v6) → provider paddé → FogLayer ; `RevealStreamingController` intouché
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-07-SUMMARY.md` (composition finale, comportements MirkOverlay reportés, résultat du smoke desktop, numéros de run CI, flakes hors périmètre observées).
</output>
