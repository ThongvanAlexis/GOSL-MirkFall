---
phase: 09.1
plan: 03
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - lib/domain/geo/geo_point.dart
  - lib/domain/mirk/mirk_paint_context.dart
  - lib/domain/mirk/mirk_paint_context.freezed.dart
  - lib/domain/mirk/mirk_renderer.dart
  - lib/infrastructure/mirk/offscreen_fog_renderer.dart
  - lib/infrastructure/mirk/shader/fog_shader_uniforms.dart
  - lib/infrastructure/mirk/shader/fog_shader_renderer.dart
  - lib/infrastructure/mirk/shader/fog_platform_corrections.dart
  - lib/infrastructure/mirk/atmospheric_mirk_renderer.dart
  - lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart
  - lib/infrastructure/mirk/solid_fill_mirk_renderer.dart
  - lib/infrastructure/mirk/candlelight_mirk_renderer.dart
  - lib/infrastructure/mirk/noop_mirk_renderer.dart
  - lib/infrastructure/mirk/shader_mirk_renderer.dart
  - lib/presentation/widgets/mirk_overlay.dart
  - lib/config/constants.dart
  - assets/shaders/atmospheric_fog.frag
  - test/_helpers/mirk_paint_context_builder.dart
  - test/_helpers/recording_fog_shader_renderer.dart
  - test/domain/mirk/mirk_paint_context_test.dart
  - test/domain/mirk/mirk_renderer_contract_test.dart
  - test/infrastructure/mirk/_render_helpers.dart
  - test/infrastructure/mirk/noise_overlay_test.dart
  - test/infrastructure/mirk/noop_mirk_renderer_test.dart
  - test/infrastructure/mirk/sdf_debounce_test.dart
  - test/infrastructure/mirk/wisp/wisp_emergence_test.dart
  - test/infrastructure/mirk/atmospheric_mirk_renderer_test.dart
  - test/infrastructure/mirk/heavenly_clouds_mirk_renderer_test.dart
  - test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart
  - test/infrastructure/mirk/shader/fog_platform_corrections_test.dart
  - test/presentation/widgets/mirk_overlay_rounded_reveal_test.dart
  - test/constants_test.dart
autonomous: true
requirements: [MIRK-04, MIRK-05]

must_haves:
  truths:
    - "`MirkPaintContext` est étendu UNE SEULE FOIS dans la phase (6 nouveaux champs : `pixelOrigin`, `zoomScale`, `sdfRect`, `canvasOffset`, `projectToScreen`, `metersToPixels`) ; aucun type flutter_map dans `lib/domain/**` ; `check_domain_purity` et `check_avoid_flutter_map_leak` verts"
    - "`MirkRenderer` expose exactement 3 membres (`paint`, `update`, `dispose`) ; le getter `sdfViewport` (vestige BUG-014 it. 6) a disparu du port, des 6 implémentations et du contract test"
    - "`FogShaderUniforms.setAll` pose 42 slots : slots 3-4 `pixelOrigin`, slot 41 `zoomScale`, `totalFloatSlots == 42` ; `assets/shaders/atmospheric_fog.frag` déclare `uPixelOrigin` + `uZoomScale` et échantillonne `worldPx = fragUv * uResolution + uPixelOrigin; noiseUv = worldPx / (kNoiseTilePx * uZoomScale)`"
    - "`applyPlatformShaderCorrections(pixelOrigin:, isAndroid:)` est une fonction pure : iOS → `(x, y)` + `sdfRect (0,0,1,1)` ; Android → `(x, -y)` + `sdfRect (0,1,1,-1)` (FOG-23 + FOG-21, même racine), testée sur Windows sans device"
    - "`AtmosphericMirkRenderer` et `HeavenlyCloudsMirkRenderer` lisent `context.pixelOrigin / zoomScale / sdfRect` et délèguent la population des uniforms à un seam `FogShaderRenderer` injectable (`RecordingFogShaderRenderer` en test) ; `_computeSdfRect` et `_sdfViewport` n'existent plus"
    - "Tous les tests qui construisent un `MirkPaintContext` passent par `buildTestMirkPaintContext(...)` ; `flutter test` complet vert à chaque commit (MirkOverlay construit le contexte étendu de façon provisoire)"
  artifacts:
    - path: "lib/domain/mirk/mirk_paint_context.dart"
      provides: "Contexte de peinture étendu (extension unique Phase 09.1)"
      contains: "projectToScreen"
    - path: "lib/domain/geo/geo_point.dart"
      provides: "typedef GeoPoint = ({double latitude, double longitude})"
      contains: "typedef GeoPoint"
    - path: "lib/domain/mirk/mirk_renderer.dart"
      provides: "Port à 3 membres"
      min_lines: 30
    - path: "lib/infrastructure/mirk/shader/fog_shader_uniforms.dart"
      provides: "ABI 42 slots"
      contains: "totalFloatSlots = 42"
    - path: "lib/infrastructure/mirk/shader/fog_platform_corrections.dart"
      provides: "FOG-21 / FOG-23 en fonction pure"
      exports: ["applyPlatformShaderCorrections"]
    - path: "lib/infrastructure/mirk/shader/fog_shader_renderer.dart"
      provides: "Seam FogShaderRenderer + FragmentShaderFogRenderer (production)"
      exports: ["FogShaderRenderer", "FragmentShaderFogRenderer"]
    - path: "assets/shaders/atmospheric_fog.frag"
      provides: "Shader POC verbatim (uPixelOrigin + uZoomScale)"
      contains: "uniform float uZoomScale"
    - path: "test/_helpers/mirk_paint_context_builder.dart"
      provides: "buildTestMirkPaintContext(...) partagé"
      exports: ["buildTestMirkPaintContext"]
  key_links:
    - from: "lib/infrastructure/mirk/atmospheric_mirk_renderer.dart"
      to: "lib/infrastructure/mirk/shader/fog_shader_renderer.dart"
      via: "shaderRenderer.render(pixelOrigin: context.pixelOrigin, zoomScale: context.zoomScale, sdfRect: context.sdfRect, ...)"
      pattern: "zoomScale:\\s*context\\.zoomScale"
    - from: "lib/infrastructure/mirk/shader/fog_shader_renderer.dart"
      to: "lib/infrastructure/mirk/shader/fog_shader_uniforms.dart"
      via: "FogShaderUniforms.setAll(... pixelOrigin, zoomScale, sdfRect, sdfImage)"
      pattern: "FogShaderUniforms\\.setAll"
    - from: "assets/shaders/atmospheric_fog.frag"
      to: "lib/config/constants.dart (kMirkFogNoiseTilePx)"
      via: "const float kNoiseTilePx = 384.0 en lockstep (test constants_test dé-skippé)"
      pattern: "kNoiseTilePx = 384\\.0"
---

<objective>
Préparer le seam `MirkRenderer` / `MirkPaintContext` et l'ABI shader pour le rendu same-canvas, sans encore créer le `FogLayer` (plan 09.1-04) ni toucher aux données (plan 09.1-05). Deux tâches TDD :

1. **Seam domaine** — extension unique de `MirkPaintContext` (précédent Plan 09-02 : un seul événement d'extension par phase), retrait de `MirkRenderer.sdfViewport` (C3), typedef `GeoPoint`, helper de test partagé, suppression du vestige `offscreen_fog_renderer.dart` (zéro appelant, zéro test) et de ses constantes.
2. **ABI shader 42 slots** — port du `.frag` POC et de `fog_shader_uniforms.dart` (slot 41 `uZoomScale`, slots 3-4 `uPixelOrigin`), corrections plateforme FOG-21 + FOG-23 en fonction pure (C1), seam `FogShaderRenderer` dans les deux renderers shader pour que les tests POC (`RecordingFogShaderRenderer`) restent portables.

Décisions de design (Claude's discretion, alignées sur RESEARCH §6 / Open Questions 2-3) :
- **Le `FogLayer` (09.1-04) possédera le snapshot caméra, le `getTransform`, le `translate(-canvasOffset)` et le `clipPath` unique ; les renderers peignent `Offset.zero & size` dans le repère identité déjà clippé.** Dans CE plan les renderers gardent provisoirement leur clip (`buildViewportFogClipPathFromDiscs`) — retiré au plan 09.1-06.
- **Les tunables restent lus dans les renderers** (`MirkRuntimeTunables.instance` à chaque paint) ; le `FogShaderRenderer` reçoit les 20 floats par une `Map<String, double>` aux clés POC (`driftZFar` … `boundaryDensityBoost`) — c'est ce qui rend `RecordingFogShaderRenderer` portable verbatim.
- **`sessionElapsed` conservé** (décision Plan 09-02) ; le painter 09.1-04 le dérivera d'un `Stopwatch` lu à chaque paint (invariant 10).

Purpose : SC#6 (seam pur, extension unique, 3 membres), SC#2 (ABI 42 slots, invariants 7-9). Tourne en parallèle du plan 09.1-02 (aucun fichier commun : 09.1-02 touche `lib/infrastructure/map/**`, `map_view.dart`, `map_screen.dart` ; ce plan touche `lib/domain/mirk/**`, `lib/infrastructure/mirk/**`, `mirk_overlay.dart`).

Output : contexte étendu + port à 3 membres + ABI 42 slots + fonction de corrections plateforme + seam shader testable.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-CONTEXT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-RESEARCH.md
@.planning/phases/09-fog-rendering/09-02-PLAN.md
@CLAUDE.md
@lib/domain/mirk/mirk_paint_context.dart
@lib/domain/mirk/mirk_renderer.dart
@lib/domain/mirk/mirk_viewport_bbox.dart
@lib/domain/revealed/reveal_disc.dart
@lib/infrastructure/mirk/shader/fog_shader_uniforms.dart
@lib/infrastructure/mirk/shader/fog_shader_service.dart
@lib/infrastructure/mirk/atmospheric_mirk_renderer.dart
@lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart
@lib/infrastructure/mirk/mirk_projection.dart
@lib/infrastructure/mirk/offscreen_fog_renderer.dart
@lib/presentation/widgets/mirk_overlay.dart
@lib/application/tunables/mirk_runtime_tunables.dart
@assets/shaders/atmospheric_fog.frag
@test/infrastructure/mirk/_render_helpers.dart
@test/domain/mirk/mirk_renderer_contract_test.dart
@tool/check_domain_purity.dart

Source externe (lecture seule) : `C:\claude_checkouts\mirk-poc-debug` — `assets/shaders/atmospheric_fog.frag` (452 lignes), `lib/infrastructure/mirk/shader/fog_shader_uniforms.dart` (173 lignes, `setAll` 42 slots), `lib/presentation/widgets/fog_layer.dart` lignes 36-120 (`FogShaderRenderer` + `_FragmentShaderFogRenderer`), 566-605 (FOG-23 rationale `a73a0cc`), 652-698 (FOG-21 rationale), `test/_helpers/recording_fog_shader_renderer.dart`, `test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart`.

<interfaces>
<!-- Shape AVANT (Phase 09) et shape APRÈS (ce plan) — le contrat que 09.1-04 / 05 / 06 consomment. -->

AVANT — lib/domain/mirk/mirk_paint_context.dart :
```dart
factory MirkPaintContext({required double zoomLevel, required double pixelRatio, required Duration sessionElapsed,
  required MirkViewportBbox viewportBbox, required List<RevealDisc> discs, Fix? currentFix}) = _MirkPaintContext;
```
AVANT — lib/domain/mirk/mirk_renderer.dart : `MirkViewportBbox? get sdfViewport;` + `paint` + `update` + `dispose` (4 membres, contract test verrouille 4).

APRÈS — lib/domain/geo/geo_point.dart :
```dart
/// Point géographique en degrés. Record (pas de classe) : même forme que le paramètre historique de
/// `MapView.markVisited`, zéro dépendance latlong2 dans le domaine (C2 option A).
typedef GeoPoint = ({double latitude, double longitude});
```
APRÈS — lib/domain/mirk/mirk_paint_context.dart :
```dart
/// Projette un point géographique en pixels écran dans le repère IDENTITÉ du painter
/// (après `canvas.translate(-canvasOffset)`). Fournie par le FogLayer depuis le snapshot MapCamera unique.
typedef ScreenProjector = Offset Function(GeoPoint point);
/// Convertit une distance métrique en pixels écran à la latitude donnée (2 points à 1 m sur l'axe latitude).
typedef MetersToPixels = double Function(double meters, {required double atLatitude});

factory MirkPaintContext({
  required double zoomLevel, required double pixelRatio, required Duration sessionElapsed,
  required MirkViewportBbox viewportBbox, required List<RevealDisc> discs, Fix? currentFix,
  // --- Phase 09.1, extension unique ---
  required ({double x, double y}) pixelOrigin,        // camera.pixelOrigin ; .y DÉJÀ négatif sur Android (FOG-23)
  required double zoomScale,                           // pow(2, zoomLevel - kMirkFogReferenceZoom)
  required (double, double, double, double) sdfRect,   // iOS (0,0,1,1) ; Android (0,1,1,-1) (FOG-21) — jamais de remapping viewport→SDF
  required ({double dx, double dy}) canvasOffset,      // informatif (loggers) : translation MobileLayerTransformer déjà compensée
  required ScreenProjector projectToScreen,
  required MetersToPixels metersToPixels,
}) = _MirkPaintContext;
```
`dart:ui` `Offset` en domaine : précédent `Canvas` / `Size` dans `mirk_renderer.dart` (autorisé par `check_domain_purity`).

APRÈS — lib/infrastructure/mirk/shader/fog_shader_renderer.dart :
```dart
abstract class FogShaderRenderer {
  /// Peuple les 42 slots + le sampler et dessine `Offset.zero & size`. Retourne `false` si rien n'a été
  /// dessiné (shader null en production → le renderer peint son fallback CPU).
  bool render({required Canvas canvas, required ui.FragmentShader? shader, required Size size, required double timeSeconds,
    required ({double x, double y}) pixelOrigin, required double zoomScale, required (double, double, double, double) sdfRect,
    required ui.Image sdfImage, required int baseArgb, required double baseAlpha, required int highlightArgb, required int shadowArgb,
    required Map<String, double> tunables});
}
class FragmentShaderFogRenderer implements FogShaderRenderer { const FragmentShaderFogRenderer(); /* setAll + drawRect */ }
```
Clés de `tunables` (20, ordre des slots 17-36) : driftZFar, driftZMid, driftZNear, scaleFar, scaleMid, scaleNear, opacityFar, opacityMid, opacityNear, curlAmplitude, curlScale, lightDirRadians, lightOffset, lightStrength, hueNoiseScale, hueStrength, boundarySharpDistance, boundaryBleedDistance, boundaryEdgeBand, boundaryDensityBoost.

APRÈS — lib/infrastructure/mirk/shader/fog_platform_corrections.dart :
```dart
typedef PlatformShaderCorrections = ({({double x, double y}) pixelOrigin, (double, double, double, double) sdfRect});
PlatformShaderCorrections applyPlatformShaderCorrections({required ({double x, double y}) pixelOrigin, required bool isAndroid});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extension unique de `MirkPaintContext` + `MirkRenderer` à 3 membres + helper de test + suppression `offscreen_fog_renderer`</name>
  <files>lib/domain/geo/geo_point.dart, lib/domain/mirk/mirk_paint_context.dart, lib/domain/mirk/mirk_paint_context.freezed.dart, lib/domain/mirk/mirk_renderer.dart, lib/infrastructure/mirk/offscreen_fog_renderer.dart, lib/infrastructure/mirk/atmospheric_mirk_renderer.dart, lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart, lib/infrastructure/mirk/solid_fill_mirk_renderer.dart, lib/infrastructure/mirk/candlelight_mirk_renderer.dart, lib/infrastructure/mirk/noop_mirk_renderer.dart, lib/infrastructure/mirk/shader_mirk_renderer.dart, lib/presentation/widgets/mirk_overlay.dart, lib/config/constants.dart, test/_helpers/mirk_paint_context_builder.dart, test/domain/mirk/mirk_paint_context_test.dart, test/domain/mirk/mirk_renderer_contract_test.dart, test/infrastructure/mirk/_render_helpers.dart, test/infrastructure/mirk/noise_overlay_test.dart, test/infrastructure/mirk/noop_mirk_renderer_test.dart, test/infrastructure/mirk/sdf_debounce_test.dart, test/infrastructure/mirk/wisp/wisp_emergence_test.dart, test/presentation/widgets/mirk_overlay_rounded_reveal_test.dart, test/constants_test.dart</files>
  <behavior>
    - `MirkPaintContext` se construit avec les 12 champs ; l'ancien appel à 6 champs ne compile plus (friction Freezed voulue : revue de chaque site d'appel)
    - `zoomScale` doit être `> 0` (`@Assert`) ; `sdfRect` accepte `(0,0,1,1)` et `(0,1,1,-1)`
    - `projectToScreen((latitude: 48.85, longitude: 2.35))` renvoie l'`Offset` de la closure injectée ; `metersToPixels(25.0, atLatitude: 48.85)` renvoie la valeur de la closure injectée
    - Deux contextes construits avec les MÊMES instances de closures sont `==` ; avec des closures différentes ils ne le sont pas (égalité par identité documentée)
    - `mirk_renderer_contract_test` : la surface publique de `MirkRenderer` est exactement `{paint, update, dispose}` (3 membres) — un test de réflexion sur le fichier source (grep des déclarations abstraites) ou l'assertion existante ramenée de 4 à 3
    - `buildTestMirkPaintContext()` sans argument produit un contexte valide (viewport Paris 256×256, projection linéaire `MirkProjection.latLonToScreen` sur ce viewport, `pixelOrigin (0,0)`, `zoomScale 1.0`, `sdfRect` identité, `canvasOffset (0,0)`) ; tout champ est surchargeable par paramètre nommé
    - `lib/infrastructure/mirk/offscreen_fog_renderer.dart` n'existe plus ; `kMirkFogMapLayerResolution`, `kMirkFogMapLayerUpdateIntervalMs`, `kMirkFogMapLayerPaddingFactor` n'existent plus (et leurs verrous dans `constants_test.dart` sont retirés)
    - `check_domain_purity` vert (aucun `package:flutter` sous `lib/domain`) ; `check_avoid_flutter_map_leak` vert
  </behavior>
  <action>
**RED** (`test(09.1-03): add failing tests for extended MirkPaintContext + 3-member MirkRenderer`) : réécrire `test/domain/mirk/mirk_paint_context_test.dart` selon `<behavior>` ; ramener `mirk_renderer_contract_test.dart` à 3 membres ; créer `test/_helpers/mirk_paint_context_builder.dart` :

```dart
/// Contexte de peinture complet pour les tests renderers / overlay. Par défaut : viewport Paris
/// (48.85 ± 0.01, 2.35 ± 0.01), canvas 256×256, projection linéaire dans le bbox (comme MirkOverlay
/// avant Phase 09.1), aucune correction plateforme. Tout champ est surchargeable.
MirkPaintContext buildTestMirkPaintContext({
  double zoomLevel = 15.0, double pixelRatio = 1.0, Duration sessionElapsed = Duration.zero,
  MirkViewportBbox? viewportBbox, List<RevealDisc> discs = const <RevealDisc>[], Fix? currentFix,
  ({double x, double y}) pixelOrigin = (x: 0.0, y: 0.0), double zoomScale = 1.0,
  (double, double, double, double) sdfRect = (0.0, 0.0, 1.0, 1.0), ({double dx, double dy}) canvasOffset = (dx: 0.0, dy: 0.0),
  Size canvasSize = const Size(256, 256), ScreenProjector? projectToScreen, MetersToPixels? metersToPixels,
});
```
(`metersToPixels` par défaut : `meters * canvasSize.height / ((viewport.north - viewport.south) * kMetersPerDegreeLat)`.)

**GREEN** (`feat(09.1-03): extend MirkPaintContext once (pixelOrigin/zoomScale/sdfRect/canvasOffset/projectToScreen/metersToPixels), MirkRenderer back to 3 members`) :
1. Créer `lib/domain/geo/geo_point.dart` (header GOSL, typedef + docstring du bloc `<interfaces>`).
2. Réécrire `lib/domain/mirk/mirk_paint_context.dart` : les 3 typedefs (`GeoPoint` importé), la factory à 12 champs, `@Assert('zoomScale > 0.0', ...)`, docstring de classe réécrite (« construit par `FogLayer` à chaque paint depuis le snapshot `MapCamera` unique — FOG-07 ; les renderers ne voient jamais flutter_map ; extension unique Phase 09.1, tout besoin futur passe par une revue Phase 10 ») et un paragraphe par nouveau champ (FOG-23 pour `pixelOrigin.y`, FOG-19 pour `zoomScale`, FOG-21 + « jamais de remapping dynamique viewport→SDF » pour `sdfRect`, FOG-12/13 pour `canvasOffset`). `dart run build_runner build --delete-conflicting-outputs`.
3. `lib/domain/mirk/mirk_renderer.dart` : supprimer `sdfViewport` + `import 'mirk_viewport_bbox.dart'` ; docstring « 3 méthodes, gelées Phase 07 ; le getter BUG-014 a été retiré en Phase 09.1 (le FogLayer compense la caméra par construction, plus par transform a posteriori) ».
4. Retirer `@override MirkViewportBbox? get sdfViewport` des 6 renderers (`atmospheric`, `heavenly_clouds`, `solid_fill`, `candlelight`, `noop`, `shader`). Dans `atmospheric` et `heavenly_clouds`, garder pour l'instant le champ privé `_sdfViewport` et `_computeSdfRect` (retirés en Task 2) — seul le getter public part.
5. `git rm lib/infrastructure/mirk/offscreen_fog_renderer.dart` ; supprimer les 3 constantes `kMirkFogMapLayer*` de `constants.dart` + leurs tests.
6. `lib/presentation/widgets/mirk_overlay.dart` (provisoire, supprimé au plan 09.1-07) : compléter la construction du contexte avec `pixelOrigin: (x: 0.0, y: 0.0)`, `zoomScale: 1.0`, `sdfRect: (0.0, 0.0, 1.0, 1.0)`, `canvasOffset: (dx: 0.0, dy: 0.0)`, `projectToScreen: (GeoPoint p) => MirkProjection.latLonToScreen(lat: p.latitude, lon: p.longitude, viewport: viewport, size: size)`, `metersToPixels: (double m, {required double atLatitude}) => m * size.height / ((viewport.north - viewport.south) * kMetersPerDegreeLat)` — avec un commentaire `// Provisoire 09.1-03 → supprimé avec MirkOverlay au plan 09.1-07 (valeurs neutres : pas de correction caméra ici).`
7. Migrer tous les sites de test qui construisent `MirkPaintContext(` (liste : `_render_helpers.dart` → `fakeContext` délègue à `buildTestMirkPaintContext`, `noise_overlay_test.dart`, `noop_mirk_renderer_test.dart`, `sdf_debounce_test.dart`, `wisp_emergence_test.dart`, `mirk_overlay_rounded_reveal_test.dart`, `mirk_renderer_contract_test.dart`) vers le helper.

Commit atomique ; `flutter test` complet vert (les tests renderers existants doivent passer inchangés sémantiquement).
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter analyze --fatal-infos --fatal-warnings && dart run tool/check_domain_purity.dart && dart run tool/check_avoid_flutter_map_leak.dart && dart run tool/check_mirk_variant_file_count.dart && test ! -f lib/infrastructure/mirk/offscreen_fog_renderer.dart && flutter test test/domain/mirk/ test/infrastructure/mirk/ test/presentation/widgets/ test/constants_test.dart</automated>
  </verify>
  <done>
- Contexte à 12 champs, port à 3 membres, `.freezed.dart` régénéré, aucun site d'appel oublié (`flutter analyze` zéro issue)
- `buildTestMirkPaintContext` est l'unique constructeur de contexte côté tests
- `offscreen_fog_renderer.dart` + 3 constantes supprimés
- Gates domaine / import / variant-count verts ; `flutter test` complet vert
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ABI 42 slots — `.frag` POC, `FogShaderUniforms`, `applyPlatformShaderCorrections`, seam `FogShaderRenderer` dans atmospheric + heavenly</name>
  <files>assets/shaders/atmospheric_fog.frag, lib/infrastructure/mirk/shader/fog_shader_uniforms.dart, lib/infrastructure/mirk/shader/fog_shader_renderer.dart, lib/infrastructure/mirk/shader/fog_platform_corrections.dart, lib/infrastructure/mirk/atmospheric_mirk_renderer.dart, lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart, test/_helpers/recording_fog_shader_renderer.dart, test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart, test/infrastructure/mirk/shader/fog_platform_corrections_test.dart, test/infrastructure/mirk/atmospheric_mirk_renderer_test.dart, test/infrastructure/mirk/heavenly_clouds_mirk_renderer_test.dart, test/constants_test.dart</files>
  <behavior>
    - `FogShaderUniforms.totalFloatSlots == 42` ; `setAll(... pixelOrigin: (x: 12.5, y: -7.0), zoomScale: 4.0 ...)` sur un `ui.FragmentShader` factice enregistreur pose `setFloat(3, 12.5)`, `setFloat(4, -7.0)`, `setFloat(41, 4.0)`, `setFloat(37..40, sdfRect)`, `setImageSampler(0, sdfImage)` et exactement 42 `setFloat` distincts (port du test POC `fog_shader_uniforms_test.dart`)
    - `applyPlatformShaderCorrections(pixelOrigin: (x: 100.0, y: 200.0), isAndroid: false)` → `pixelOrigin (100.0, 200.0)`, `sdfRect (0.0, 0.0, 1.0, 1.0)`
    - `applyPlatformShaderCorrections(pixelOrigin: (x: 100.0, y: 200.0), isAndroid: true)` → `pixelOrigin (100.0, -200.0)`, `sdfRect (0.0, 1.0, 1.0, -1.0)`
    - Appliquer deux fois avec `isAndroid: true` n'est PAS idempotent (le signe re-bascule) — test documentaire : la fonction doit être appelée UNE fois par paint sur la valeur brute `camera.pixelOrigin`
    - `AtmosphericMirkRenderer(config, shaderRenderer: recorder)` : après un paint avec SDF résolu, `recorder.renders.last.pixelOrigin == context.pixelOrigin`, `.zoomScale == context.zoomScale`, `.sdfRect == context.sdfRect`, `.namedFloatArgs.length == 20`, `.totalFloatSlotsObserved == 31` ; `.timeSeconds` strictement croissant entre deux paints à `sessionElapsed` différents
    - `HeavenlyCloudsMirkRenderer` : même contrat via son propre `shaderRenderer` (palette heavenly)
    - Un tunable modifié via `MirkRuntimeTunables.instance` entre deux paints change la valeur correspondante dans `renders.last.namedFloatArgs` (le tuner reste actif)
    - Le test de lockstep `kMirkFogNoiseTilePx` ↔ `.frag` de `constants_test.dart` est dé-skippé et vert
  </behavior>
  <action>
**RED** (`test(09.1-03): add failing 42-slot ABI + platform corrections + shader seam tests`) : porter `mirk-poc-debug/test/infrastructure/mirk/shader/fog_shader_uniforms_test.dart` (renommer le package, `offset:` → `pixelOrigin:` record `({double x, double y})`) ; porter `test/_helpers/recording_fog_shader_renderer.dart` du POC en l'adaptant à la signature `FogShaderRenderer.render` du bloc `<interfaces>` (retourne `true`, enregistre `pixelOrigin` en record, `RecordedFogRender` conserve `totalFloatSlotsObserved = 2 + 1 + 2 + 1 + 4 + 1 + namedFloatArgs.length`) ; créer `fog_platform_corrections_test.dart` ; étendre `atmospheric_mirk_renderer_test.dart` + `heavenly_clouds_mirk_renderer_test.dart` avec un `group('09.1 — shader seam')` selon `<behavior>` (SDF résolu : injecter via le seam existant de `RevealedSdfBuilder` ou attendre `await Future<void>.delayed(...)` + `pumpEventQueue()` après le premier paint qui déclenche `_triggerSdfRebuild` — regarder comment `sdf_debounce_test.dart` obtient un SDF résolu et réutiliser le même idiome).

**GREEN** (`feat(09.1-03): port 42-slot shader ABI (uPixelOrigin + uZoomScale), FOG-21/23 pure corrections, FogShaderRenderer seam`) :
1. **`assets/shaders/atmospheric_fog.frag`** : remplacer par le fichier POC verbatim (`copy` du POC), puis : nettoyer le commentaire périmé lignes ~293-300 (FOG-17a / `kPocFogIntegerWrapPeriodPx`, éliminés par FOG-18), remplacer les mentions `kPocFogReferenceZoom` par `kMirkFogReferenceZoom` dans les commentaires, garder `const float kNoiseTilePx = 384.0;` (constant-foldée, pas un uniform), garder `#ifdef IMPELLER_TARGET_OPENGLES fragUv.y = 1.0 - fragUv.y;` et le toggle `MIRK_FOG_DEBUG_OUTPUT_DENSITY`. Vérifier que la déclaration `uniform float uZoomScale;` est bien ENTRE `uSdfRectSizeY` et `sampler2D uSdf` (ordre des slots : le sampler après tous les floats — BUG-014 it. 1).
2. **`fog_shader_uniforms.dart`** : porter la version POC (paramètre `pixelOrigin` en record `({double x, double y})` plutôt que le tuple positionnel POC, pour lisibilité ; `required double zoomScale` ; `setFloat(41, zoomScale)` ; `totalFloatSlots = 42` ; docstring : tableau des 42 slots).
3. **`fog_platform_corrections.dart`** (nouveau) : fonction pure du bloc `<interfaces>` + docstring auto-portante reprenant la rationale POC `a73a0cc` (codegen impellerc → Vulkan/SPIR-V inversant `worldPx.y = fragCoord.y + uPixelOrigin.y` sur Adreno-Vulkan et pas sur Apple-Metal ; Impeller-Vulkan échantillonne la `ui.Image` en V-up d'où `sdfRect (0,1,1,-1)` ; « correction PERMANENTE d'un écart de codegen, pas un workaround temporaire — ne jamais retirer pour nettoyer » ; invariant 9 reformulé : « identité sur iOS, `(0,1,1,-1)` sur Android, jamais de remapping dynamique »).
4. **`fog_shader_renderer.dart`** (nouveau) : `FogShaderRenderer` abstrait + `FragmentShaderFogRenderer` (`const`) : `if (shader == null) return false; FogShaderUniforms.setAll(shader, resolution: size, time: timeSeconds, pixelOrigin: pixelOrigin, baseArgb:, baseAlpha:, highlightArgb:, shadowArgb:, driftZFar: tunables['driftZFar']!, ..., sdfRect: sdfRect, zoomScale: zoomScale, sdfImage: sdfImage); canvas.drawRect(Offset.zero & size, Paint()..shader = shader); return true;`. Les 20 lectures de map utilisent un helper `_requireTunable(tunables, key)` qui lève `ArgumentError` si la clé manque (bug de programmation, laisser propager).
5. **`atmospheric_mirk_renderer.dart`** : constructeur gagne `FogShaderRenderer shaderRenderer = const FragmentShaderFogRenderer()` ; `_paintShaderPath` devient : construire `tunables` (map des 20 clés depuis `MirkRuntimeTunables.instance` + animation curlScale MirkFall existante) puis `final painted = shaderRenderer.render(canvas: canvas, shader: shader /* nullable */, size: size, timeSeconds: context.sessionElapsed.inMicroseconds / 1e6, pixelOrigin: context.pixelOrigin, zoomScale: context.zoomScale, sdfRect: context.sdfRect, sdfImage: sdf, baseArgb: ..., baseAlpha: ..., highlightArgb: ..., shadowArgb: ..., tunables: tunables); if (!painted) _paintFallbackPath(...)`. La condition d'entrée devient « SDF résolu » (le shader peut être null → `render` renvoie `false` → fallback). Supprimer `_computeSdfRect`, `_sdfViewport` et son affectation dans `_triggerSdfRebuild`, `offsetX/offsetY` (calcul d'`uOffset` par viewport). Le clip (`buildViewportFogClipPathFromDiscs`) reste jusqu'au plan 09.1-06.
6. **`heavenly_clouds_mirk_renderer.dart`** : même transformation (palette heavenly, teinte wisp inchangée pour l'instant).
7. `constants_test.dart` : dé-skipper le test lockstep `kMirkFogNoiseTilePx` ↔ `.frag`.

Régression visuelle interim : avec `MirkOverlay` qui passe `pixelOrigin (0,0)` / `zoomScale 1`, le noise est ancré à l'écran comme avant (pas pire qu'aujourd'hui) — c'est attendu jusqu'au plan 09.1-07.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && grep -q "uniform float uZoomScale" assets/shaders/atmospheric_fog.frag && grep -q "totalFloatSlots = 42" lib/infrastructure/mirk/shader/fog_shader_uniforms.dart && flutter test test/infrastructure/mirk/ test/constants_test.dart test/presentation/widgets/mirk_overlay_rounded_reveal_test.dart && dart format --line-length 160 --set-exit-if-changed lib/infrastructure/mirk test/infrastructure/mirk test/_helpers</automated>
  </verify>
  <done>
- `.frag` = POC (à commentaires près), 42 slots, `uZoomScale` déclaré au bon endroit ; l'app compile le shader (`flutter build apk --debug` non requis ici — le job CI android le vérifiera à la wave)
- `FogShaderUniforms` 42 slots testé ; `applyPlatformShaderCorrections` testé (iOS / Android / non-idempotence)
- Les deux renderers shader passent par `FogShaderRenderer`, lisent le contexte étendu, n'ont plus de remapping SDF ; le tuner runtime agit toujours (test)
- `flutter test` complet vert ; test lockstep `.frag` dé-skippé
  </done>
</task>

</tasks>

<verification>
- Commande Full suite de `09.1-VALIDATION.md` verte localement, puis push ; `gh run watch` : job `gates` vert, job `android` vert (impellerc compile le nouveau `.frag` — c'est la vraie preuve que l'ABI 42 slots est acceptée ; un uniform inutilisé ferait échouer le démarrage Impeller), job `ios` vert
- `grep -rn "sdfViewport\|_computeSdfRect\|kMirkFogMapLayer" lib/ test/` → vide
</verification>

<success_criteria>
- SC#6 : `MirkRenderer` pur, 3 membres ; `MirkPaintContext` étendu une seule fois ; aucun type flutter_map dans `lib/domain`
- SC#2 partiel : invariants 7 (noise en coordonnées monde côté shader), 8 (identité à z13 via `uZoomScale = 1`), 9 (reformulé, fonction pure testée) préparés ; le keystone FOG-07 arrive au plan 09.1-04
- Contrat consommé sans réouverture par 09.1-04 / 09.1-05 / 09.1-06
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-03-SUMMARY.md` (shape finale du contexte, décision Freezed vs classe manuelle si changée, idiome utilisé pour obtenir un SDF résolu en test, écarts éventuels entre `.frag` POC et MirkFall).
</output>
