---
phase: 09.1
plan: 05
type: execute
wave: 3
depends_on: ["09.1-03"]
files_modified:
  - lib/infrastructure/mirk/sdf/sdf_cache.dart
  - lib/infrastructure/mirk/sdf_rebuild_logger.dart
  - lib/infrastructure/mirk/wisp/wisp_particle.dart
  - lib/infrastructure/mirk/wisp/wisp_particle_system.dart
  - lib/infrastructure/mirk/wisp/wisp_transform_logger.dart
  - lib/infrastructure/mirk/atmospheric_mirk_renderer.dart
  - lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart
  - lib/config/constants.dart
  - test/infrastructure/mirk/sdf/sdf_cache_test.dart
  - test/infrastructure/mirk/sdf_rebuild_logger_test.dart
  - test/infrastructure/mirk/sdf_debounce_test.dart
  - test/infrastructure/mirk/wisp/wisp_particle_test.dart
  - test/infrastructure/mirk/wisp/wisp_particle_system_test.dart
  - test/infrastructure/mirk/wisp/wisp_transform_logger_test.dart
  - test/infrastructure/mirk/wisp/wisp_emergence_test.dart
  - test/infrastructure/mirk/wisp/wisp_sdf_firewall_test.dart
  - test/wisp/wisp_pan_invariance_test.dart
  - test/constants_test.dart
autonomous: true
requirements: [MIRK-01, MIRK-03, MIRK-04]

must_haves:
  truths:
    - "`SdfCache` (clé quantifiée PERF-08 : disques + bbox 1e-4°) remplace `_hashDiscList / _hashViewport / _triggerSdfRebuild` dans `AtmosphericMirkRenderer` et `HeavenlyCloudsMirkRenderer` ; le debounce 200 ms viewport-only (`kMirkFogSdfViewportDebounceMs`) reste DEVANT `getOrBuild` ; un seul build en vol ; dernière image conservée pendant le rebuild"
    - "`WispParticle.position` est un `GeoPoint` (record domaine, PAS `LatLng` — C2 option A) et `velocityMetersPerSecond` un `Offset` en m/s ; `WispParticleSystem.spawnAtNewDisc(discId:, disc:)` est idempotent par `discId`, inerte pendant les 5 s de warm-up (`kMirkFogWispWarmUpSeconds`), spawne ~20 wisps le long du périmètre d'un disque de 25 m (8 m d'espacement), cap 200 LRU"
    - "Un pan de 100 m de la caméra ne change PAS la position `GeoPoint` d'un wisp ; sa projection écran bouge du delta pixel correspondant (invariance monde — le piège BUG-014 des 18 px/s est éliminé, `kMirkFogWispInitialSpeedPx` n'existe plus)"
    - "`WispParticleSystem` n'importe ni `sdf_cache.dart` ni `revealed_sdf_builder.dart` (firewall structurel testé) ; le rendu des wisps se fait dans le renderer APRÈS le `drawRect` du shader, dans le repère identité clippé du `FogLayer`, via `context.projectToScreen`"
    - "`SdfRebuildLogger` (`infrastructure.mirk.sdf`) et `WispTransformLogger` (`infrastructure.mirk.wisp`) sont verbose-only et alignés 1 Hz sur `epochSecond` (invariant 6)"
  artifacts:
    - path: "lib/infrastructure/mirk/sdf/sdf_cache.dart"
      provides: "SdfCache POC (getOrBuild / dispose / quantisation)"
      exports: ["SdfCache"]
    - path: "lib/infrastructure/mirk/wisp/wisp_particle_system.dart"
      provides: "Wisps m/s en coordonnées monde, spawnAtNewDisc idempotent + warm-up"
      contains: "spawnAtNewDisc"
    - path: "lib/infrastructure/mirk/wisp/wisp_particle.dart"
      provides: "GeoPoint position + Offset velocityMetersPerSecond"
      contains: "GeoPoint position"
    - path: "lib/infrastructure/mirk/sdf_rebuild_logger.dart"
      provides: "Rollup PERF-08 1 Hz verbose-only"
      contains: "Logger('infrastructure.mirk.sdf')"
    - path: "test/infrastructure/mirk/wisp/wisp_sdf_firewall_test.dart"
      provides: "Garde structurelle : aucun import sdf/ depuis wisp/"
      min_lines: 20
  key_links:
    - from: "lib/infrastructure/mirk/atmospheric_mirk_renderer.dart"
      to: "lib/infrastructure/mirk/sdf/sdf_cache.dart"
      via: "_sdfCache.getOrBuild(discs: context.discs, viewport: context.viewportBbox) derrière le debounce 200 ms"
      pattern: "getOrBuild\\("
    - from: "lib/infrastructure/mirk/atmospheric_mirk_renderer.dart"
      to: "lib/infrastructure/mirk/wisp/wisp_particle_system.dart"
      via: "diff d'ids de disques → spawnAtNewDisc ; _renderWisps(canvas, context) après drawRect"
      pattern: "spawnAtNewDisc\\("
    - from: "lib/infrastructure/mirk/atmospheric_mirk_renderer.dart (_renderWisps)"
      to: "MirkPaintContext.projectToScreen"
      via: "context.projectToScreen(wisp.position)"
      pattern: "projectToScreen\\(w(isp)?\\.position\\)"
---

<objective>
Brancher les données du fog same-canvas dans les deux renderers shader : (1) le `SdfCache` POC à clé quantifiée remplace la machinerie de hash / rebuild maison, avec le debounce 200 ms viewport-only conservé devant lui (arbitrage orchestrateur : volumes de disques persistés MirkFall > POC, caveat #6) ; (2) les wisps passent en coordonnées monde (position `GeoPoint`, vitesse m/s, spawn à l'émergence d'un disque, projection à chaque paint via `context.projectToScreen`) — c'est la faute d'origine de BUG-014 (18 px/s ≈ 86 m/s à z15) ; (3) firewall : le système de wisps ne touche jamais le `SdfCache`.

Les disques viennent déjà de `DriftRevealedDiscStore` via `discsInViewportProvider` (aucun repo mémoire parallèle) : ce plan ne touche ni le domaine, ni les stores, ni `RevealStreamingController`. Il tourne en parallèle du plan 09.1-04 (fichiers disjoints).

Purpose : SC#5 (wisps ancrés monde, spawn sur émergence, firewall), SC#3 préparation (fog persisté = disques Drift → SDF), MIRK-01/03 re-validés.

Output : `SdfCache` + 2 loggers portés, wisps réécrits, renderers rebranchés, 8 fichiers de test portés / réécrits.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-CONTEXT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-RESEARCH.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-03-SUMMARY.md
@CLAUDE.md
@lib/domain/mirk/mirk_paint_context.dart
@lib/domain/geo/geo_point.dart
@lib/domain/revealed/reveal_disc.dart
@lib/infrastructure/mirk/sdf/revealed_sdf_builder.dart
@lib/infrastructure/mirk/wisp/wisp_particle.dart
@lib/infrastructure/mirk/wisp/wisp_particle_system.dart
@lib/infrastructure/mirk/atmospheric_mirk_renderer.dart
@lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart
@lib/config/constants.dart
@test/infrastructure/mirk/sdf_debounce_test.dart
@test/infrastructure/mirk/wisp/wisp_emergence_test.dart
@test/_helpers/mirk_paint_context_builder.dart
@docs/phase09-bug-tracking/BUG-015-wisp-burst-on-open.md

Source externe (lecture seule) : `C:\claude_checkouts\mirk-poc-debug\lib\infrastructure\mirk\sdf\sdf_cache.dart` (130 l.), `sdf_rebuild_logger.dart` (82 l.), `wisp/wisp_particle.dart` (70 l.), `wisp/wisp_particle_system.dart` (389 l.), `wisp/wisp_transform_logger.dart` (236 l.), `lib/presentation/widgets/fog_layer.dart` lignes 771-915 (`_renderWisps`, `_resolveWispRadius`, `_derivePxPerMetre`), `test/infrastructure/mirk/sdf/sdf_cache_test.dart`, `sdf_rebuild_logger_test.dart`, `wisp/wisp_particle_test.dart`, `wisp/wisp_particle_system_test.dart`, `wisp/wisp_transform_logger_test.dart`, `test/wisp/wisp_pan_invariance_test.dart`, `test/wisp/wisp_no_fix_warmup_test.dart`.

<interfaces>
<!-- API cible (POC adaptée C2) — consommée par les renderers ici et par 09.1-06 / 09.1-07. -->

```dart
// lib/infrastructure/mirk/sdf/sdf_cache.dart (POC verbatim)
class SdfCache {
  SdfCache({required SdfRebuildLogger rebuildLogger, RevealedSdfBuilder? builder});
  Future<ui.Image> getOrBuild({required List<RevealDisc> discs, required MirkViewportBbox viewport});
  void dispose();
}

// lib/infrastructure/mirk/wisp/wisp_particle.dart
class WispParticle {
  WispParticle({required this.position, required this.velocityMetersPerSecond, required this.life, required this.maxLife});
  GeoPoint position;                 // C2 option A : record domaine, pas latlong2
  Offset velocityMetersPerSecond;    // dx = est, dy = sud
  double life; final double maxLife;
  bool get isDead; double get age;
}

// lib/infrastructure/mirk/wisp/wisp_particle_system.dart
class WispParticleSystem {
  WispParticleSystem({int maxCount = kMirkFogWispMaxCount, int rngSeed = 1337, Stopwatch? wallClock});
  Iterable<WispParticle> get wisps; int get activeCount;
  void spawnAtNewDisc({required String discId, required RevealDisc disc});   // idempotent discId ; no-op pendant warm-up (discId mémorisé)
  void advance(double dt);                                                   // m/s → degrés ; curl-noise CPU ; drag ; cap
  void advanceFromElapsed(Duration sessionElapsed);                          // dt = Δelapsed clampé à kMirkWispMaxDtSeconds ; 1er appel no-op
  double spawnRatePerSecondAndReset({Duration? sinceInterval});
  void clear();
}
```
Ancrage du curl-noise (remplace `kMelunCenterLat/Lon` du POC) : `curlInput = Offset((w.position.longitude - _anchor.longitude) * kMirkWispCurlInputScale, (w.position.latitude - _anchor.latitude) * kMirkWispCurlInputScale)` où `_anchor` = centre du PREMIER disque spawné par le système (fixé une fois, `GeoPoint?`), le champ curl étant invariant par translation. Documenter.

Renderers (atmospheric + heavenly) — après ce plan :
```dart
AtmosphericMirkRenderer(config, {int seed, WispParticleSystem? wispSystem, FogShaderRenderer shaderRenderer = const FragmentShaderFogRenderer(), SdfCache? sdfCache, FogShaderService? shaderService});
// paint(): _refreshSdfIfNeeded(context) [debounce 200 ms viewport-only → _sdfCache.getOrBuild, 1 build en vol, _currentSdfImage conservé]
//          _spawnWispsForNewlyEmergedDiscs(context) [diff d'ids → spawnAtNewDisc]
//          shader path (drawRect) ou fallback
//          _renderWisps(canvas, context) [advanceFromElapsed(context.sessionElapsed) ; drawCircle(context.projectToScreen(w.position), radius, paint additive)]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port `SdfCache` + `SdfRebuildLogger` ; rebrancher atmospheric + heavenly (debounce 200 ms conservé)</name>
  <files>lib/infrastructure/mirk/sdf/sdf_cache.dart, lib/infrastructure/mirk/sdf_rebuild_logger.dart, lib/infrastructure/mirk/atmospheric_mirk_renderer.dart, lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart, test/infrastructure/mirk/sdf/sdf_cache_test.dart, test/infrastructure/mirk/sdf_rebuild_logger_test.dart, test/infrastructure/mirk/sdf_debounce_test.dart</files>
  <behavior>
    - `sdf_cache_test` (port POC) : même `discs` + même bbox quantifié → `identical` image, zéro rebuild ; bbox décalé de 1e-5° → hit ; décalé de 2e-4° → miss + `recordRebuild` appelé une fois avec `discCount` / `intersectingDiscCount` corrects ; nouvelle liste de disques → miss ; `dispose()` libère l'image et un `getOrBuild` suivant rebâtit
    - `sdf_rebuild_logger_test` (port POC + gating verbose comme 09.1-04) : rollup 1 Hz avec `count / p50 / max` ; silencieux hors `Level.FINE`
    - `sdf_debounce_test` (réécrit sur le nouveau point de debounce, mêmes scénarios BUG-012) : 1er paint → build immédiat ; mêmes disques + viewport différent → PAS de build immédiat, build après 200 ms ; nouveau disque → build immédiat même si le viewport a changé ; `dispose()` annule le timer (pas de build tardif) ; deux paints rapprochés pendant un build en vol → un seul `getOrBuild` supplémentaire au plus ; pendant le rebuild, `_currentSdfImage` précédente reste utilisée (le recorder reçoit l'ancienne image)
  </behavior>
  <action>
1. Copier `sdf_cache.dart` et `sdf_rebuild_logger.dart` du POC (imports renommés, `kPocSdfLogRollupSeconds` → `kMirkFogDiagRollupSeconds`, gating verbose `isLoggable(Level.FINE)` sur `recordRebuild` + rollup). Docstring `SdfCache` : mentionner « clé = hash(disques) ⊕ bbox quantifié 1e-4° ; à z15 le bbox change tous les ~2-3 px de pan → le debounce viewport-only côté renderer reste nécessaire avec des volumes persistés (RESEARCH §7, Pitfall 5) ».
2. **Atmospheric** : supprimer `_hashDiscList`, `_hashViewport`, `_mix`, `_triggerSdfRebuild`, `_lastDiscHash / _lastViewportHash` (ou équivalents) ; ajouter `final SdfCache _sdfCache` (constructeur `SdfCache? sdfCache` → défaut `SdfCache(rebuildLogger: SdfRebuildLogger()..start())`), `ui.Image? _currentSdfImage`, `Future<void>? _pendingSdfBuild`, `Timer? _viewportDebounceTimer`. `_refreshSdfIfNeeded(context)` : si la liste de disques a changé (comparer une signature légère : longueur + dernier id, ou `identical` de la liste — le provider fournit une nouvelle liste à chaque requête) → `_scheduleBuild(now)` ; sinon si le viewport a changé → armer / réarmer le `Timer(kMirkFogSdfViewportDebounceMs)` → `_scheduleBuild`. `_scheduleBuild` : `if (_pendingSdfBuild != null) { _rebuildRequested = true; return; }` → `_pendingSdfBuild = _sdfCache.getOrBuild(...).then((img) { _currentSdfImage = img; }).catchError(log severe).whenComplete(() { _pendingSdfBuild = null; if (_rebuildRequested) {…relancer une fois…} })`. Le painter utilise `_currentSdfImage` (stale accepté, jamais absent une fois le 1er build résolu). `dispose()` : timer cancel, `_sdfCache.dispose()`, `_currentSdfImage = null` (l'image est possédée par le cache). Remplacer l'ancien logger de rebuild par `SdfRebuildLogger`.
3. **Heavenly** : même transformation.
4. Tests : porter `sdf_cache_test` + `sdf_rebuild_logger_test` ; réécrire `sdf_debounce_test` (garder les noms de scénarios BUG-012, injecter un `_CountingSdfCache extends SdfCache` ou un `RevealedSdfBuilder` espion via `SdfCache(builder:)` pour compter les builds ; `fake_async` pour le timer). RED → GREEN : `test(09.1-05): add failing SdfCache + debounce tests`, `feat(09.1-05): port SdfCache, rewire atmospheric/heavenly SDF pipeline behind 200 ms viewport debounce`.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && flutter test test/infrastructure/mirk/sdf/ test/infrastructure/mirk/sdf_rebuild_logger_test.dart test/infrastructure/mirk/sdf_debounce_test.dart test/infrastructure/mirk/atmospheric_mirk_renderer_test.dart test/infrastructure/mirk/heavenly_clouds_mirk_renderer_test.dart && ! grep -n "_hashDiscList\|_hashViewport\|_triggerSdfRebuild" lib/infrastructure/mirk/*.dart</automated>
  </verify>
  <done>
- `SdfCache` porté et testé ; les deux renderers n'ont plus de hash maison ; debounce 200 ms viewport-only + un seul build en vol + image stale conservée, testés
- Logger `infrastructure.mirk.sdf` verbose-only (mesure UAT caveat #6 possible en activant le toggle)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wisps en coordonnées monde — `WispParticle` (GeoPoint), `WispParticleSystem` (m/s, spawnAtNewDisc, warm-up), `WispTransformLogger`, suppression de `kMirkFogWispInitialSpeedPx`</name>
  <files>lib/infrastructure/mirk/wisp/wisp_particle.dart, lib/infrastructure/mirk/wisp/wisp_particle_system.dart, lib/infrastructure/mirk/wisp/wisp_transform_logger.dart, lib/config/constants.dart, test/infrastructure/mirk/wisp/wisp_particle_test.dart, test/infrastructure/mirk/wisp/wisp_particle_system_test.dart, test/infrastructure/mirk/wisp/wisp_transform_logger_test.dart, test/infrastructure/mirk/wisp/wisp_sdf_firewall_test.dart, test/wisp/wisp_pan_invariance_test.dart, test/constants_test.dart</files>
  <behavior>
    - `wisp_particle_test` (port POC) : `age` = `1 - life/maxLife` clampé, `isDead` à `life <= 0`, `position` est un `GeoPoint`
    - `wisp_particle_system_test` (port POC) : `spawnAtNewDisc` sur un disque de 25 m après warm-up → ~20 wisps (`2π·25 / 8 ≈ 19.6`, tolérance ±2) ; second appel même `discId` → aucun ajout ; cap 200 LRU (évincés par `life` restante) ; pendant les 5 premières secondes (Stopwatch injecté) → no-op ET `discId` mémorisé (re-appel post-warm-up → no-op) ; `advance(dt)` intègre vitesse m/s → degrés (`dLat = dy_m / kMetersPerDegreeLat`, `dLon = dx_m / (kMetersPerDegreeLat · cos(lat))`) et retire les morts ; `advanceFromElapsed` : 1er appel no-op, dt clampé à `kMirkWispMaxDtSeconds`
    - `wisp_pan_invariance_test` (port POC `test/wisp/`) : une projection linéaire décalée de 100 m ne change pas `wisp.position` ; l'`Offset` projeté bouge du delta pixel attendu
    - `wisp_transform_logger_test` (port POC + gating verbose) : rollup 1 Hz avec `activeCount / meanAge / bounds / spawnRate`, `epochSecond` = `millisecondsSinceEpoch ~/ 1000`
    - `wisp_sdf_firewall_test` (nouveau, structurel) : aucun fichier de `lib/infrastructure/mirk/wisp/` ne contient `import` de `sdf/sdf_cache.dart`, `sdf/revealed_sdf_builder.dart` ni `sdf_rebuild_logger.dart` ; aucun fichier de `wisp/` n'importe `package:latlong2` ni `package:flutter_map`
    - `constants_test` : `kMirkFogWispInitialSpeedPx` n'existe plus (test retiré) ; les `kMirkWisp*` de 09.1-01 sont référencés par `wisp_particle_system.dart`
  </behavior>
  <action>
1. Réécrire `wisp_particle.dart` et `wisp_particle_system.dart` depuis les versions POC : `LatLng` → `GeoPoint` partout (`(latitude: …, longitude: …)`), aucun import `latlong2` ; constantes `kMirkPocWisp*` → `kMirkFogWisp*` (existantes) / `kMirkWisp*` (09.1-01) ; `advanceFromWallClock(Stopwatch)` → `advanceFromElapsed(Duration)` (le `FogLayer` fournit `context.sessionElapsed` par paint — même sémantique « dt frais par paint », clamp `kMirkWispMaxDtSeconds`) ; ancrage curl = premier disque spawné (voir `<interfaces>`) ; supprimer `spawnAtPosition` public et `render(Canvas, Color)` (le rendu passe dans les renderers en Task 3) ; conserver `_curlNoise / _scalarNoise / _hash2` verbatim (donor) ; docstrings : « WISP-01 position monde », « WISP-02 spawn périmètre idempotent », « WISP-03 warm-up 5 s (BUG-015 : évite la rafale à l'ouverture d'une session reprise) », « Pitfall 4 firewall : ne JAMAIS importer le SDF ». Ne PAS porter `WispRadiusBasis` (axe A/B cosmétique POC, défaut screenPx = comportement MirkFall).
2. Copier `wisp_transform_logger.dart` (gating verbose, `kMirkFogDiag*`, bounds en `GeoPoint`).
3. `constants.dart` : supprimer `kMirkFogWispInitialSpeedPx` (+ commentaire « à supprimer ») ; `constants_test.dart` mis à jour.
4. Tests : porter les 4 fichiers POC + écrire `wisp_sdf_firewall_test.dart` (scan `Directory('lib/infrastructure/mirk/wisp')`, regex d'import — même idiome que `tool/check_domain_purity.dart` mais en test Flutter). RED → GREEN : `test(09.1-05): add failing world-anchored wisp tests + SDF firewall`, `feat(09.1-05): rewrite wisps in world coordinates (GeoPoint + m/s, spawnAtNewDisc, warm-up), drop px/s basis`.

Compilation intermédiaire : après cette tâche `atmospheric` / `heavenly` ne compilent plus (ils appellent `spawnAtPosition` / `render`) → **Task 2 et Task 3 se committent ensemble** si nécessaire (un commit `feat` unique) ; sinon garder temporairement les anciennes méthodes marquées `@Deprecated` jusqu'à Task 3 et les retirer dans le commit de Task 3. Choisir la première option (commit unique) par défaut.
  </action>
  <verify>
    <automated>flutter test test/infrastructure/mirk/wisp/ test/wisp/ test/constants_test.dart && ! grep -rn "package:latlong2\|package:flutter_map\|sdf/sdf_cache\|revealed_sdf_builder\|sdf_rebuild_logger" lib/infrastructure/mirk/wisp/ && ! grep -n "kMirkFogWispInitialSpeedPx" lib/config/constants.dart</automated>
  </verify>
  <done>
- Wisps en `GeoPoint` + m/s, spawn idempotent + warm-up, cap LRU, curl ancré sans Melun ; tests POC portés verts ; firewall structurel vert
- `kMirkFogWispInitialSpeedPx` supprimé ; gate d'import vert sur `wisp/`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Renderers — spawn sur émergence de disque + `_renderWisps` après le drawRect via `context.projectToScreen` ; `wisp_emergence_test` réécrit</name>
  <files>lib/infrastructure/mirk/atmospheric_mirk_renderer.dart, lib/infrastructure/mirk/heavenly_clouds_mirk_renderer.dart, test/infrastructure/mirk/wisp/wisp_emergence_test.dart, test/infrastructure/mirk/atmospheric_mirk_renderer_test.dart, test/infrastructure/mirk/heavenly_clouds_mirk_renderer_test.dart</files>
  <behavior>
    - `wisp_emergence_test` (scénarios conservés, réécrits sur `spawnAtNewDisc`) : pendant le warm-up, les disques de seed ne spawnent rien ; après warm-up, même liste → rien ; un nouveau disque → wisps le long de SON périmètre uniquement (toutes les positions à `radiusMeters ± 1 m` du centre, distance haversine ou équirectangulaire) ; BUG-015 : disques qui sortent puis rentrent dans le viewport → aucun spawn (ids déjà vus) ; premier paint à 0 disque puis arrivée pendant le warm-up → rien
    - `atmospheric_mirk_renderer_test` : après spawn, un paint avec `context.projectToScreen` linéaire dessine `activeCount` cercles (Canvas espion `drawCircle`) aux positions `projectToScreen(w.position)` ; l'ordre des appels sur le canvas espion est `drawRect` (shader, via recorder → utiliser un `FogShaderRenderer` espion qui appelle `canvas.drawRect`) PUIS les `drawCircle` — jamais l'inverse ; teinte = `kMirkWispTintAtmosphericArgb` (heavenly : `kMirkWispTintHeavenlyArgb`)
    - `WispTransformLogger.recordPaint` est appelé au plus une fois par paint
  </behavior>
  <action>
Dans `atmospheric_mirk_renderer.dart` et `heavenly_clouds_mirk_renderer.dart` :
1. `_spawnWispsForNewlyEmergedDiscs(context)` : conserver le diff d'ids (`Set<String> _seenDiscIds`), mais pour chaque nouvel id appeler `_wispSystem.spawnAtNewDisc(discId: disc.id, disc: disc)` — le warm-up et l'idempotence vivent dans le système (BUG-015 se résout par le warm-up Stopwatch du système, construit au même moment que le renderer — vérifier que `activeMirkRendererProvider` recrée bien le renderer au démarrage de session ; sinon exposer `WispParticleSystem.resetWarmUp()` et l'appeler depuis `update()` au premier paint). Supprimer `_spawnWispsAlongDiscPerimeter` + l'usage de `MirkProjection` pour les wisps.
2. `_renderWisps(Canvas canvas, MirkPaintContext context)` : port de `_FogPainter._renderWisps` POC (771-854) — `_wispSystem.advanceFromElapsed(context.sessionElapsed)` ; early return si `activeCount == 0` ; `Paint` hoisté (`BlendMode.plus`), teinte constante par renderer ; boucle : `radius = lerp(kMirkFogWispBirthRadiusPx, kMirkFogWispDeathRadiusPx, age)`, `alpha = (1 - age²) · kMirkFogWispPeakAlpha · tintAlpha`, `canvas.drawCircle(context.projectToScreen(w.position), radius, paint)` ; accumulation des bounds → `_wispTransformLogger.recordPaint(...)` une fois. Appeler `_renderWisps` à la FIN de `paint()`, après le chemin shader / fallback (donc après le `drawRect`), avant de rendre la main au `FogLayer` qui fera `restore` : le repère est le repère identité clippé du `FogLayer`, comme dans le POC.
3. Supprimer les anciens `_wispSystem.advance(dt)` basés sur un dt maison et `_wispSystem.render(canvas, tint)`.
4. Réécrire `wisp_emergence_test.dart` ; étendre les deux tests renderers. Commit `feat(09.1-05): spawn wisps on disc emergence + render after shader rect via projectToScreen` (ou fusionné avec Task 2, cf. note).
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && flutter test test/infrastructure/mirk/ test/wisp/ && dart run tool/check_mirk_variant_file_count.dart && dart format --line-length 160 --set-exit-if-changed lib/infrastructure/mirk test/infrastructure/mirk test/wisp</automated>
  </verify>
  <done>
- Les deux renderers spawnent via `spawnAtNewDisc` et rendent les wisps après le drawRect via `context.projectToScreen` ; `MirkProjection` n'est plus utilisé pour les wisps
- `wisp_emergence_test` (6 scénarios BUG-010/BUG-015) vert ; ordre drawRect → drawCircle testé
- `flutter test` complet vert
  </done>
</task>

</tasks>

<verification>
- Commande Full suite de `09.1-VALIDATION.md` verte ; push ; `gh run watch` vert
- `grep -rn "spawnAtPosition\|\.render(canvas" lib/infrastructure/mirk/` → vide ; `grep -rn "MirkProjection" lib/infrastructure/mirk/` → uniquement `revealed_sdf_builder.dart` (+ `candlelight` jusqu'au plan 09.1-06)
</verification>

<success_criteria>
- SC#5 : wisps ancrés monde (record + m/s), pas de dérive au zoom (invariance testée), spawn sur émergence, firewall SdfCache structurel
- SC#3 préparé : SDF construit depuis les disques Drift via `SdfCache` (persistance = `DriftRevealedDiscStore`, inchangée)
- Debounce 200 ms conservé et testé ; loggers `sdf` / `wisp` verbose-only prêts pour la mesure UAT (caveat #6)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-05-SUMMARY.md` (choix d'ancrage du curl, stratégie warm-up vs recréation du renderer, signature finale de `WispParticleSystem`, tests POC portés / fusionnés / abandonnés avec raison).
</output>
