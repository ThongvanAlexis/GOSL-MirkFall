---
phase: 09.1
plan: 04
type: execute
wave: 3
depends_on: ["09.1-03"]
files_modified:
  - lib/infrastructure/mirk/frame_delta_probe.dart
  - lib/infrastructure/mirk/fog_transform_logger.dart
  - lib/infrastructure/mirk/dev_marker_logger.dart
  - lib/infrastructure/mirk/fog_clip_geometry.dart
  - lib/presentation/widgets/fog_clip_path.dart
  - lib/presentation/widgets/fog_layer.dart
  - test/_helpers/fake_map_camera.dart
  - test/infrastructure/mirk/frame_delta_probe_test.dart
  - test/infrastructure/mirk/fog_transform_logger_test.dart
  - test/infrastructure/mirk/dev_marker_logger_test.dart
  - test/infrastructure/mirk/fog_clip_geometry_test.dart
  - test/presentation/widgets/fog_layer_camera_snapshot_test.dart
  - test/presentation/widgets/fog_layer_single_camera_snapshot_test.dart
  - test/presentation/widgets/fog_clip_path_test.dart
  - test/presentation/widgets/fog_canvas_frame_alignment_test.dart
  - test/presentation/widgets/fog_rect_viewport_coverage_test.dart
  - test/presentation/widgets/fog_pan_translation_test.dart
  - test/presentation/widgets/fog_layer_test.dart
  - test/presentation/widgets/fog_pixel_origin_decomposition_test.dart
  - test/presentation/widgets/fog_smooth_noise_test.dart
  - test/presentation/widgets/fog_tile_period_invariant_test.dart
  - test/presentation/widgets/fog_world_coordinate_noise_test.dart
  - test/presentation/widgets/fog_zoom_invariant_basis_test.dart
autonomous: true
requirements: [MIRK-01, MIRK-04, MIRK-05]

must_haves:
  truths:
    - "`FogLayer` est un `StatefulWidget` pur (tout injecté par constructeur, aucun `ref`) qui lit `MapCamera.of(context)` EXACTEMENT une fois par `build` (keystone FOG-07 : `readCount` 1 → 2 → 3 sur 3 builds, avec `FakeMirkRenderer` ET avec `AtmosphericMirkRenderer`)"
    - "`_FogPainter.paint` lit `canvas.getTransform()` une seule fois, fait `canvas.save(); canvas.translate(-canvasOffset)` en tête (FOG-12/13), clippe par `computeFogClipPath(camera, discs)` (FOG-06), construit le `MirkPaintContext` étendu (corrections plateforme, `zoomScale`, `sessionElapsed` depuis un `Stopwatch` tenu par référence — invariant 10), appelle `renderer.update` puis `renderer.paint`, `canvas.restore()`"
    - "Le noise est ancré au monde : deux paints à `pixelOrigin` différents (pan) forwardent la valeur brute sans modulo (FOG-18, continuité < 2000 px entre frames voisines — FOG-11) ; à `camera.zoom == kMirkFogReferenceZoom` le `zoomScale` enregistré vaut 1.0 et à z15 il vaut 4.0 (FOG-19)"
    - "`computeFogClipPath` = rect viewport − ∪ disques projetés via `camera.latLngToScreenPoint`, rayon métrique via `metersToPixels` (2 points à 1 m sur l'axe latitude) ; liste vide → rect plein ; la géométrie vit dans `buildFogClipPath` (`lib/infrastructure/mirk/fog_clip_geometry.dart`, sans flutter_map) et `fog_clip_path.dart` n'est qu'un pont caméra → projecteur"
    - "`FrameDeltaProbe`, `FogTransformLogger`, `DevMarkerLogger` sont portés ; leurs `record*` / rollups sont des no-op tant que `Logger('infrastructure.mirk.*').isLoggable(Level.FINE)` est faux (production = erreurs + events métier uniquement, CLAUDE.md §Logging)"
    - "Aucun `RepaintBoundary` autour du `FogLayer` ; le repaint per-frame passe par `CustomPainter(repaint: Listenable)` notifié par un `Ticker`, pas par `setState`"
  artifacts:
    - path: "lib/presentation/widgets/fog_layer.dart"
      provides: "FogLayer (FOG-07/12/13/21/23) + _FogPainter délégant au MirkRenderer"
      contains: "static void Function()? debugOnCameraRead"
      min_lines: 200
    - path: "lib/presentation/widgets/fog_clip_path.dart"
      provides: "computeFogClipPath(camera, discs) + cameraScreenProjector + cameraMetersToPixels"
      exports: ["computeFogClipPath", "cameraScreenProjector", "cameraMetersToPixels"]
    - path: "lib/infrastructure/mirk/fog_clip_geometry.dart"
      provides: "buildFogClipPath(size, discs, projectToScreen, metersToPixels) — géométrie pure"
      exports: ["buildFogClipPath"]
    - path: "lib/infrastructure/mirk/frame_delta_probe.dart"
      provides: "PERF-07 probe 1 Hz (verbose-only)"
      contains: "Logger('infrastructure.mirk.frame_delta')"
    - path: "lib/infrastructure/mirk/fog_transform_logger.dart"
      provides: "Rollup 1 Hz transform/pixelOrigin/zoom (verbose-only)"
      contains: "Logger('infrastructure.mirk.fog_transform')"
    - path: "test/presentation/widgets/fog_layer_camera_snapshot_test.dart"
      provides: "Keystone FOG-07 (gate de régression CI)"
      min_lines: 60
  key_links:
    - from: "lib/presentation/widgets/fog_layer.dart"
      to: "lib/domain/mirk/mirk_renderer.dart"
      via: "renderer.update(sessionElapsed); renderer.paint(canvas, size, context)"
      pattern: "renderer\\.paint\\(canvas, size"
    - from: "lib/presentation/widgets/fog_layer.dart"
      to: "lib/infrastructure/mirk/shader/fog_platform_corrections.dart"
      via: "applyPlatformShaderCorrections(pixelOrigin: (x: camera.pixelOrigin.x, y: camera.pixelOrigin.y), isAndroid: isAndroid)"
      pattern: "applyPlatformShaderCorrections"
    - from: "lib/presentation/widgets/fog_layer.dart"
      to: "MapCamera.of(context)"
      via: "un seul appel dans build(), précédé de FogLayer.debugOnCameraRead?.call()"
      pattern: "MapCamera\\.of\\(context\\)"
    - from: "lib/presentation/widgets/fog_clip_path.dart"
      to: "lib/infrastructure/mirk/fog_clip_geometry.dart"
      via: "computeFogClipPath → buildFogClipPath(projectToScreen: cameraScreenProjector(camera), ...)"
      pattern: "buildFogClipPath"
---

<objective>
Porter le cœur du POC : le `FogLayer` enfant de `FlutterMap` et son painter, avec le fix bundle complet (FOG-06 clip exact, FOG-07 snapshot caméra unique, FOG-12 `getTransform` unique, FOG-13 translate symétrique, FOG-18 `uPixelOrigin` brut, FOG-19 `uZoomScale`, FOG-21/23 corrections plateforme via la fonction pure du plan 09.1-03), plus les 3 diagnostics verbose-only qu'il alimente. Le `FogLayer` MirkFall diffère du POC sur un point structurel : il **délègue la peinture au `MirkRenderer` actif** (reçu par constructeur) au lieu d'embarquer le shader — le painter prépare le repère (translate + clip), construit le `MirkPaintContext` étendu, et appelle `renderer.paint`. Les 13 tests widget POC sont portés quasi verbatim grâce à cette pureté (pas de `ProviderScope` : le connecteur Riverpod arrive au plan 09.1-07).

Tâches :
1. Diagnostics (`FrameDeltaProbe`, `FogTransformLogger`, `DevMarkerLogger`) + helpers de test + **keystone FOG-07 en RED**.
2. Géométrie de clip (pure + pont caméra) + `FogLayer` GREEN + 4 tests de repère.
3. Port des 6 tests d'uniforms / noise (via `AtmosphericMirkRenderer(shaderRenderer: RecordingFogShaderRenderer())`).

Purpose : SC#2 (FogLayer enfant du FlutterMap, 10 invariants, keystone en CI), SC#5 partiel (projection exacte partagée par le clip et — au plan 09.1-05 — par les wisps). Tourne en parallèle du plan 09.1-05 (fichiers disjoints : 09.1-05 = `sdf/`, `wisp/`, `sdf_rebuild_logger.dart`, les deux renderers shader, constantes wisp).

Output : `fog_layer.dart`, `fog_clip_path.dart`, `fog_clip_geometry.dart`, 3 loggers, 13 tests widget + 4 tests infra verts.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-CONTEXT.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-RESEARCH.md
@.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-03-SUMMARY.md
@CLAUDE.md
@lib/domain/mirk/mirk_paint_context.dart
@lib/domain/mirk/mirk_renderer.dart
@lib/domain/geo/geo_point.dart
@lib/infrastructure/mirk/shader/fog_platform_corrections.dart
@lib/infrastructure/mirk/shader/fog_shader_renderer.dart
@lib/infrastructure/mirk/atmospheric_mirk_renderer.dart
@lib/infrastructure/mirk/tile_cell_iteration.dart
@lib/config/constants.dart
@test/fakes/fake_mirk_renderer.dart
@test/_helpers/recording_fog_shader_renderer.dart
@test/_helpers/mirk_paint_context_builder.dart

Source externe (lecture seule — fichiers de référence à porter) : `C:\claude_checkouts\mirk-poc-debug\lib\presentation\widgets\fog_layer.dart` (954 l. : `FogLayer` 151-224, `_FogLayerState` 226-365, `_Repaint` 370, `_FogPainter` 377-915 dont `paint()` 436-720), `fog_clip_path.dart` (115 l.), `lib/infrastructure/mirk/{frame_delta_probe,fog_transform_logger,dev_marker_logger}.dart`, `test/_helpers/fake_map_camera.dart`, `test/presentation/widgets/fog_*_test.dart` (13 fichiers, hors `fog_debug_spiral_continuity_test.dart` et `fog_layer_wisp_render_test.dart` → 09.1-07), `test/infrastructure/mirk/{frame_delta_probe,fog_transform_logger,dev_marker_logger}_test.dart`, `shader-fix-retrospective.md` §Pitfalls.

<interfaces>
<!-- Contrat du FogLayer MirkFall (à implémenter tel quel — les plans 09.1-05/06/07 s'appuient dessus). -->

```dart
/// Couche fog same-canvas — enfant (direct ou via FadeTransition) de `FlutterMap`.
class FogLayer extends StatefulWidget {
  const FogLayer({
    super.key,
    required this.renderer,             // MirkRenderer actif (activeMirkRendererProvider côté connecteur)
    required this.discs,                // snapshot List<RevealDisc> (provider Drift côté connecteur)
    this.currentFix,                    // Fix? → MirkPaintContext.currentFix (candlelight)
    required this.frameDeltaProbe,      // FrameDeltaProbe (no-op si non loggable)
    required this.fogTransformLogger,   // FogTransformLogger (idem)
    this.isAndroid,                     // bool? — null → Platform.isAndroid ; seam de test (FOG-21/23)
    this.pixelRatio,                    // double? — null → MediaQuery.devicePixelRatioOf(context)
  });
  /// Seam keystone FOG-07 : invoqué juste avant `MapCamera.of(context)`, exactement une fois par build.
  @visibleForTesting
  static void Function()? debugOnCameraRead;
}
```
`_FogLayerState with SingleTickerProviderStateMixin` : `Ticker` → `_Repaint extends ChangeNotifier` (repaint: du painter) ; `final Stopwatch _wallClockSinceMount = Stopwatch()..start();` passé PAR RÉFÉRENCE ; `build()` = `FogLayer.debugOnCameraRead?.call(); final MapCamera camera = MapCamera.of(context); final cameraSnapshotMicros = widget.frameDeltaProbe.recordCameraSnapshot(); return MobileLayerTransformer(child: CustomPaint(painter: _FogPainter(...), size: Size.infinite));`.

`_FogPainter.paint(Canvas canvas, Size size)` — ordre verrouillé (PORTBACK §5 + Pattern 2 RESEARCH) :
```dart
final canvasTransform = canvas.getTransform();                                   // FOG-12 : UNE lecture
final canvasOffset = Offset(canvasTransform[_txIndex], canvasTransform[_tyIndex]); // indices 12 / 13 nommés
canvas.save();
canvas.translate(-canvasOffset.dx, -canvasOffset.dy);                            // FOG-13 : repère identité = repère du CircleLayer
canvas.clipPath(computeFogClipPath(camera: camera, discs: discs));               // FOG-06, canvasOffset laissé à zero (03.1-08-FIX)
final corrections = applyPlatformShaderCorrections(pixelOrigin: (x: camera.pixelOrigin.x, y: camera.pixelOrigin.y), isAndroid: isAndroid); // FOG-23 + FOG-21
final zoomScale = math.pow(2.0, camera.zoom - kMirkFogReferenceZoom).toDouble(); // FOG-19
final sessionElapsed = Duration(microseconds: wallClock.elapsedMicroseconds);     // invariant 10 : lu à CHAQUE paint
final context = MirkPaintContext(zoomLevel: camera.zoom, pixelRatio: pixelRatio, sessionElapsed: sessionElapsed,
  viewportBbox: _viewportFromCamera(camera), discs: discs, currentFix: currentFix,
  pixelOrigin: corrections.pixelOrigin, zoomScale: zoomScale, sdfRect: corrections.sdfRect,
  canvasOffset: (dx: canvasOffset.dx, dy: canvasOffset.dy),
  projectToScreen: cameraScreenProjector(camera), metersToPixels: cameraMetersToPixels(camera));
frameDeltaProbe.recordFogUniformPopulation(cameraSnapshotMicros);
fogTransformLogger.recordPaint(/* canvasTransform, appliedPixelOrigin, zoom, size, epoch */);
renderer.update(sessionElapsed);
renderer.paint(canvas, size, context);                                           // le renderer peint Offset.zero & size
canvas.restore();
```
`shouldRepaint` : `camera != old.camera || !identical(discs, old.discs) || renderer != old.renderer || currentFix != old.currentFix` (le Listenable `repaint:` fait le reste).

`fog_clip_path.dart` (flutter_map autorisé) :
```dart
ui.Path computeFogClipPath({required MapCamera camera, required List<RevealDisc> discs, Offset canvasOffset = Offset.zero});
ScreenProjector cameraScreenProjector(MapCamera camera);      // p → Offset(camera.latLngToScreenPoint(LatLng(p.latitude, p.longitude)).x, .y)
MetersToPixels cameraMetersToPixels(MapCamera camera);        // 2 points à 1 m sur l'axe latitude via latLngToScreenPoint
```
`fog_clip_geometry.dart` (pur, sans flutter_map) :
```dart
ui.Path buildFogClipPath({required Size size, required List<RevealDisc> discs, required ScreenProjector projectToScreen,
  required MetersToPixels metersToPixels, Offset canvasOffset = Offset.zero});
```
Loggers POC (API à conserver) : `FrameDeltaProbe { int recordCameraSnapshot(); void recordFogUniformPopulation(int snapshotMicros); void start(); void stop(); Future<void> dispose(); }`, `FogTransformLogger { void start(); void stop(); void recordPaint({...}); }`, `DevMarkerLogger { void mark(String label); }`. Logger names : `infrastructure.mirk.frame_delta`, `infrastructure.mirk.fog_transform`, `infrastructure.mirk.dev_marker`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port des diagnostics (`FrameDeltaProbe`, `FogTransformLogger`, `DevMarkerLogger`, verbose-only) + helpers de test + keystone FOG-07 RED</name>
  <files>lib/infrastructure/mirk/frame_delta_probe.dart, lib/infrastructure/mirk/fog_transform_logger.dart, lib/infrastructure/mirk/dev_marker_logger.dart, test/infrastructure/mirk/frame_delta_probe_test.dart, test/infrastructure/mirk/fog_transform_logger_test.dart, test/infrastructure/mirk/dev_marker_logger_test.dart, test/_helpers/fake_map_camera.dart, test/presentation/widgets/fog_layer_camera_snapshot_test.dart, test/presentation/widgets/fog_layer_single_camera_snapshot_test.dart</files>
  <behavior>
    - Port des tests POC des 3 loggers (renommage package, `kPoc*` → `kMirkFogDiag*`), plus un cas nouveau par logger : avec `Logger.root.level = Level.INFO`, `start()` puis N `record*` puis l'écoulement d'une fenêtre de rollup n'émet AUCUN `LogRecord` ; avec `Level.ALL`, le rollup émet 1 record JSONL par seconde (idiome `Logger.root.onRecord.listen` + `fake_async`)
    - `stop()` est idempotent ; `record*` avant `start()` est un no-op
    - Keystone `fog_layer_camera_snapshot_test.dart` : `FogLayer(renderer: FakeMirkRenderer(), discs: const [], frameDeltaProbe: probe, fogTransformLogger: logger, isAndroid: false)` sous `FlutterMap(key: ValueKey(rebuildKey), options: MapOptions(initialCenter: LatLng(48.5397, 2.6553), initialZoom: 13))` ; `readCount == 1` après le pump initial, `== 2` puis `== 3` après deux rebuilds forcés (bouton `Key('rebuild-trigger')`) ; jamais plus, jamais moins
    - `fog_layer_single_camera_snapshot_test.dart` : même scénario avec `AtmosphericMirkRenderer(const AtmosphericConfig(), shaderRenderer: RecordingFogShaderRenderer())` (le renderer shader + ses wisps ne doivent pas ajouter de lecture caméra)
    - Les deux tests sont RED à la fin de cette tâche (le fichier `fog_layer.dart` n'existe pas) — commit `test(09.1-04): add failing FOG-07 keystone tests (camera snapshot ×2)` ; ils ne sont PAS poussés seuls
  </behavior>
  <action>
1. Copier depuis le POC `lib/infrastructure/mirk/frame_delta_probe.dart`, `fog_transform_logger.dart`, `dev_marker_logger.dart` (headers GOSL conservés ; imports `package:mirk_poc_debug/` → `package:mirkfall/` ; constantes `kPoc*` → `kMirkFogDiag*` posées par 09.1-01). Adapter le paramètre `appliedUOffset` de `recordPaint` en `appliedPixelOrigin: ({double x, double y})` (garder les clés JSONL `uOffsetX*` pour la compatibilité des outils de grep post-walk, avec un commentaire). **Gating verbose** : dans chaque classe, ajouter `bool get _isVerbose => _log.isLoggable(Level.FINE);` et un early return dans `record*` ET dans le callback du `Timer` de rollup quand `!_isVerbose` (le `Timer` 1 Hz reste armé : négligeable, et le toggle « Verbose logging » du debug menu agit sans remontage). Documenter dans la docstring de classe : « diagnostic POC, actif uniquement en verbose (`--dart-define=DEBUG=true` ou toggle debug menu) — CLAUDE.md §Logging ». `FrameDeltaProbe.dispose()` reste `Future<void>`.
2. Porter les 3 tests POC correspondants + les cas de gating verbose.
3. Porter `test/_helpers/fake_map_camera.dart` du POC (utilisé par `fog_clip_path_test` et les tests de géométrie) — si le POC construit un vrai `MapCamera(crs: const Epsg3857(), center:, zoom:, rotation: 0, nonRotatedSize:)`, garder cet idiome (constructeur public en 7.0.2).
4. Écrire les deux keystones (port quasi verbatim du POC, constructeur MirkFall du bloc `<interfaces>`, `addTearDown(() => FogLayer.debugOnCameraRead = null)`, `FakeMirkRenderer` de `test/fakes/`). Pour le second, l'atmospheric renderer peut logger l'échec de `FragmentProgram.fromAsset` en test (asset bundle absent) : tolérer, il peint son fallback.
5. Vérifier que `flutter analyze` sur les 3 loggers + leurs tests est vert ; les 2 keystones échouent à la compilation (import manquant) — attendu. Commit RED.
  </action>
  <verify>
    <automated>flutter test test/infrastructure/mirk/frame_delta_probe_test.dart test/infrastructure/mirk/fog_transform_logger_test.dart test/infrastructure/mirk/dev_marker_logger_test.dart && flutter analyze lib/infrastructure/mirk/frame_delta_probe.dart lib/infrastructure/mirk/fog_transform_logger.dart lib/infrastructure/mirk/dev_marker_logger.dart && (flutter test test/presentation/widgets/fog_layer_camera_snapshot_test.dart; test $? -ne 0 && echo "keystone RED as expected")</automated>
  </verify>
  <done>
- 3 loggers portés, verbose-only, tests verts (dont gating)
- `fake_map_camera.dart` disponible
- 2 keystones écrits et RED (commit local `test(09.1-04):`, non poussé)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `buildFogClipPath` / `computeFogClipPath` + `FogLayer` GREEN + tests de repère (FOG-06/07/12/13)</name>
  <files>lib/infrastructure/mirk/fog_clip_geometry.dart, lib/presentation/widgets/fog_clip_path.dart, lib/presentation/widgets/fog_layer.dart, test/infrastructure/mirk/fog_clip_geometry_test.dart, test/presentation/widgets/fog_clip_path_test.dart, test/presentation/widgets/fog_canvas_frame_alignment_test.dart, test/presentation/widgets/fog_rect_viewport_coverage_test.dart, test/presentation/widgets/fog_pan_translation_test.dart</files>
  <behavior>
    - `buildFogClipPath(size: 200×100, discs: [], ...)` → `getBounds() == Rect.fromLTWH(0, 0, 200, 100)` ; avec 1 disque projeté au centre de rayon 10 px → `contains(center) == false`, `contains(Offset(5, 5)) == true` ; `canvasOffset: Offset(3, 4)` décale le rect ET le trou de `(-3, -4)`
    - `computeFogClipPath(camera: FakeMapCamera(...), discs: [disc 25 m])` : le trou est centré sur `camera.latLngToScreenPoint(LatLng(disc.lat, disc.lon))` et son rayon vaut `cameraMetersToPixels(camera)(25.0, atLatitude: disc.lat)` à ± 0.5 px (port de `fog_clip_path_test` POC, FOG-06 + BUG-011 rayon métrique)
    - Les deux keystones de Task 1 passent (GREEN)
    - `fog_canvas_frame_alignment_test` (FOG-12/13) : le painter, peint sur un `Canvas` dont la transform porte une translation `(tx, ty)`, appelle `translate(-tx, -ty)` puis `clipPath` puis `renderer.paint` — vérifié via un `Canvas` espion (port POC) ; `getTransform()` invoqué exactement une fois par paint
    - `fog_rect_viewport_coverage_test` : après translate, le renderer reçoit `size == camera.size` et le clip couvre exactement le viewport
    - `fog_pan_translation_test` : deux paints avec `camera.center` décalé → le contexte reçu par le renderer a un `pixelOrigin` qui varie du delta pixel attendu et `projectToScreen(fixedPoint)` bouge du même delta (signe inverse)
  </behavior>
  <action>
1. **`lib/infrastructure/mirk/fog_clip_geometry.dart`** (nouveau, header GOSL, imports `dart:ui` + domaine seulement) : `buildFogClipPath` = port de la boucle POC (`worldPath..addRect(viewportRect)`, `holesPath.addOval(Rect.fromCircle(center: projectToScreen((latitude: disc.lat, longitude: disc.lon)) - canvasOffset, radius: metersToPixels(disc.radiusMeters, atLatitude: disc.lat)))`, `Path.combine(PathOperation.difference, ...)`, liste vide → rect). Docstring : FOG-06 + rayon métrique (BUG-011) + « le paramètre `canvasOffset` n'est PAS utilisé par le painter de production (03.1-08-FIX : le canvas est déjà translaté) ; conservé pour les tests de géométrie ».
2. **`lib/presentation/widgets/fog_clip_path.dart`** (nouveau, dans le périmètre du gate) : `cameraScreenProjector`, `cameraMetersToPixels` (port de `_metersToPixels` POC : `latLngToScreenPoint` à `lat` et `lat + 1 / kMetersPerDegreeLat`, distance euclidienne), `computeFogClipPath` qui délègue à `buildFogClipPath` avec `Size(camera.size.x, camera.size.y)`.
3. **`lib/presentation/widgets/fog_layer.dart`** (nouveau) : implémenter EXACTEMENT le contrat du bloc `<interfaces>`. Porter les commentaires de rationale POC essentiels (FOG-07 lock, FOG-12/13 single read + translate discipline, 03.1-08-FIX double-compensation, FOG-23/21 pointant vers `fog_platform_corrections.dart`, FOG-19, invariant 10 « Stopwatch par référence, jamais capturé au build ») en les condensant (~60 % du fichier POC est du commentaire ; viser un fichier lisible ≤ 350 lignes). `isAndroid ?? Platform.isAndroid` résolu UNE fois dans `initState` (pas dans `paint`). `pixelRatio ?? MediaQuery.devicePixelRatioOf(context)` lu dans `build` (pas de second `MapCamera.of`). `_viewportFromCamera(camera)` = `camera.visibleBounds` → `MirkViewportBbox`. Aucun `setState` dans le tick ; aucun `RepaintBoundary` (anti-pattern POC verrouillé : recrée une frame de retard). `dispose` : `_ticker.dispose(); _repaint.dispose();` (le renderer est possédé par le provider, pas par le widget — ne PAS l'appeler `dispose()` ici).
4. Tests : `fog_clip_geometry_test.dart` (nouveau, pur) ; porter `fog_clip_path_test.dart`, `fog_canvas_frame_alignment_test.dart`, `fog_rect_viewport_coverage_test.dart`, `fog_pan_translation_test.dart` du POC — là où le POC inspecte `renders.last` du recorder, préférer un `_SpyMirkRenderer implements MirkRenderer` qui capture `(canvas.getTransform(), size, context)` : plus direct et indépendant du shader. Là où le POC utilise un `Canvas` espion, le garder.
5. Commit GREEN `feat(09.1-04): add FogLayer same-canvas (FOG-06/07/12/13/18/19/21/23) + fog clip geometry`, puis un commit `test(09.1-04): port POC frame-alignment / coverage / pan / clip tests` si la séparation est naturelle.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && dart run tool/check_avoid_flutter_map_leak.dart && flutter test test/infrastructure/mirk/fog_clip_geometry_test.dart test/presentation/widgets/fog_layer_camera_snapshot_test.dart test/presentation/widgets/fog_layer_single_camera_snapshot_test.dart test/presentation/widgets/fog_clip_path_test.dart test/presentation/widgets/fog_canvas_frame_alignment_test.dart test/presentation/widgets/fog_rect_viewport_coverage_test.dart test/presentation/widgets/fog_pan_translation_test.dart</automated>
  </verify>
  <done>
- Keystones FOG-07 verts (×2) ; tests FOG-06/12/13 + pan verts
- `fog_layer.dart` : un seul `MapCamera.of`, un seul `getTransform`, translate symétrique, clip unique, contexte étendu complet, délégation au renderer, pas de RepaintBoundary, pas de setState per-frame
- Gate d'import vert (seuls `fog_layer.dart` et `fog_clip_path.dart` importent flutter_map côté presentation)
  </done>
</task>

<task type="auto">
  <name>Task 3: Port des tests d'uniforms / noise POC (FOG-05/11/17/18/19) via le seam `RecordingFogShaderRenderer`</name>
  <files>test/presentation/widgets/fog_layer_test.dart, test/presentation/widgets/fog_pixel_origin_decomposition_test.dart, test/presentation/widgets/fog_smooth_noise_test.dart, test/presentation/widgets/fog_tile_period_invariant_test.dart, test/presentation/widgets/fog_world_coordinate_noise_test.dart, test/presentation/widgets/fog_zoom_invariant_basis_test.dart</files>
  <action>
Porter les 6 fichiers POC restants en remplaçant l'injection `FogLayer(shaderRenderer: recorder, shader: null, sdfCache: ..., ...)` par `FogLayer(renderer: AtmosphericMirkRenderer(const AtmosphericConfig(), shaderRenderer: recorder), discs: ..., frameDeltaProbe: FrameDeltaProbe(), fogTransformLogger: FogTransformLogger(), isAndroid: false)` sous le même harnais `FlutterMap` (centre Melun 48.5397 / 2.6553 ou Paris — garder les valeurs POC pour conserver les attendus numériques). Le recorder n'est appelé qu'une fois le SDF du renderer résolu : après le premier `pump`, faire `await tester.pump(const Duration(milliseconds: 300)); await tester.pumpAndSettle... ` NON — le Ticker ne se stabilise jamais : utiliser des pompes bornées (`for (var i = 0; i < 5; i++) { await tester.pump(const Duration(milliseconds: 100)); }`) puis asserter sur `recorder.renders.isNotEmpty`. Attendus à conserver :
- `fog_layer_test` : 20 clés `namedFloatArgs`, `totalFloatSlotsObserved == 31`, `sdfRect == (0,0,1,1)` (isAndroid false), `timeSeconds` croissant entre deux paints.
- `fog_pixel_origin_decomposition_test` (FOG-18) : `renders.last.pixelOrigin == (camera.pixelOrigin.x, camera.pixelOrigin.y)` sans modulo, magnitudes ~1e6 à z13 acceptées.
- `fog_smooth_noise_test` (FOG-11) : entre deux paints séparés d'un pan de quelques pixels (via `MapController.move`), `|Δ pixelOrigin| < kMirkFogDiagSmoothCoordinateMaxDelta` (2000 px ; si la constante n'a pas été portée par 09.1-01, la porter ici dans `constants.dart` avec docstring + verrou dans `constants_test.dart`).
- `fog_tile_period_invariant_test` / `fog_world_coordinate_noise_test` (FOG-17) : le painter ne dérive PLUS de période côté Dart — asserter que `pixelOrigin` est forwardé brut et que le `.frag` contient `worldPx = fragUv * uResolution + uPixelOrigin` (lecture du fichier asset, idiome de `constants_test`).
- `fog_zoom_invariant_basis_test` (FOG-19) : à `initialZoom: 13` → `zoomScale == 1.0` ; `move(center, 15)` → `4.0` ; `move(center, 12)` → `0.5`.
Ajouter en tête de chaque fichier un commentaire d'origine : `// Porté de mirk-poc-debug@90c9321 test/presentation/widgets/<nom>.dart — invariant FOG-xx`. Commit `test(09.1-04): port POC uniform / noise anchoring widget tests (FOG-05/11/17/18/19)`.
  </action>
  <verify>
    <automated>flutter test test/presentation/widgets/ test/infrastructure/mirk/ && flutter analyze --fatal-infos --fatal-warnings && dart format --line-length 160 --set-exit-if-changed lib/presentation/widgets lib/infrastructure/mirk test/presentation/widgets test/_helpers</automated>
  </verify>
  <done>
- 13 tests widget `fog_*` + `fog_layer_*` verts (11 portés du POC + `fog_clip_geometry_test` + keystones)
- Chaque invariant PORTBACK §5 n°1, 2, 3, 5, 7, 8, 9, 10 a au moins un test nommé ; n°4 (rotation off) et n°6 (rollups 1 Hz) sont couverts respectivement par `flutter_map_map_view_test` (09.1-02) et les tests loggers (Task 1)
- `flutter test` complet vert
  </done>
</task>

</tasks>

<verification>
- Commande Full suite de `09.1-VALIDATION.md` verte ; push ; `gh run watch` vert (le `.frag` a déjà été compilé par CI au plan 09.1-03)
- `grep -n "RepaintBoundary\|setState" lib/presentation/widgets/fog_layer.dart` → vide
- `grep -c "MapCamera.of(context)" lib/presentation/widgets/fog_layer.dart` → 1
</verification>

<success_criteria>
- SC#2 : `FogLayer` prêt à être monté enfant du `FlutterMap` (composition au plan 09.1-07) ; keystone FOG-07 en CI comme gate de régression nommé ; 10 invariants préservés et testés
- SC#6 : le `FogLayer` est la frontière — le renderer ne voit qu'un `MirkPaintContext`
- Loggers diag portés, silencieux hors verbose
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-port-back-same-canvas-fog-flutter-map-migration/09.1-04-SUMMARY.md` (constructeur final de `FogLayer`, idiome de pompe utilisé pour attendre le SDF, liste des tests portés vs réécrits, écarts numériques éventuels vs POC).
</output>
