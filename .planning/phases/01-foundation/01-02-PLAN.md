---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/main.dart
  - lib/app.dart
  - lib/infrastructure/logging/file_logger.dart
  - lib/presentation/router.dart
  - lib/presentation/router.g.dart
  - lib/presentation/screens/about_placeholder_screen.dart
  - lib/presentation/screens/debug_menu_screen.dart
  - test/file_logger_test.dart
  - test/file_logger_debug_define_test.dart
  - test/file_logger_prune_test.dart
  - test/debug_menu_screen_test.dart
  - test/smoke_test.dart
autonomous: true
requirements: [FOUND-06]
must_haves:
  truths:
    - "Le logger écrit au démarrage de l'app un fichier `<app_docs>/logs/yyyymmdd_hhmm.ss_logs.txt` en format JSON Lines"
    - "`--dart-define=DEBUG=true` fait passer `Logger.root.level` à `Level.ALL` ; sans le define, le niveau est lu du flag SharedPreferences `debug_logging_enabled` (défaut `Level.INFO`)"
    - "Au démarrage, si la taille totale du dossier `logs/` dépasse `kMaxLogsDirBytes` (10 MB), les plus vieux fichiers sont supprimés jusqu'à ce que la limite soit respectée"
    - "Un écran `/debug` (accessible via 7-tap sur `/about`) affiche : switch verbose logging, liste des log files (date + taille), bouton Share, bouton Clear all"
    - "Un écran `/about` (placeholder) affiche un texte minimal et intercepte 7 taps dans les 3 secondes pour naviguer vers `/debug`"
    - "`runZonedGuarded` + `FlutterError.onError` du `main.dart` forwardent vers le Logger (pas vers `debugPrint`)"
    - "Les 5 tests Phase 02 passent : file_logger (boot + JSONL), file_logger_debug_define, file_logger_prune, debug_menu_screen, smoke_test mis à jour pour mocker path_provider"
  artifacts:
    - path: "lib/infrastructure/logging/file_logger.dart"
      provides: "FileLogger.bootstrap() + JSON Lines sink + size-bound prune"
      exports: ["FileLogger"]
    - path: "lib/presentation/router.dart"
      provides: "GoRouter config as a @riverpod provider — routes /, /about, /debug"
    - path: "lib/presentation/screens/about_placeholder_screen.dart"
      provides: "Placeholder screen with 7-tap easter egg to /debug"
    - path: "lib/presentation/screens/debug_menu_screen.dart"
      provides: "Debug menu UI with verbose toggle, log file list, share, clear"
    - path: "lib/main.dart"
      provides: "Updated main: awaits FileLogger.bootstrap() before runApp + error handlers forward to Logger"
    - path: "lib/app.dart"
      provides: "Updated MirkFallApp using MaterialApp.router(routerConfig: ref.watch(appRouterProvider))"
  key_links:
    - from: "lib/main.dart"
      to: "FileLogger.bootstrap()"
      via: "awaited before runApp"
      pattern: "await FileLogger\\.bootstrap"
    - from: "lib/main.dart"
      to: "Logger('main').shout"
      via: "FlutterError.onError + runZonedGuarded outer"
      pattern: "Logger\\('main'\\)\\.shout"
    - from: "lib/app.dart"
      to: "appRouterProvider"
      via: "ref.watch in MirkFallApp.build"
      pattern: "ref\\.watch\\(appRouterProvider\\)"
    - from: "lib/presentation/screens/about_placeholder_screen.dart"
      to: "context.go('/debug')"
      via: "7-tap detector"
      pattern: "context\\.go\\('/debug'\\)"
    - from: "lib/presentation/screens/debug_menu_screen.dart"
      to: "SharePlus.instance.share"
      via: "share log file button"
      pattern: "SharePlus\\.instance\\.share"
---

<objective>
Câbler le kit logger complet exigé par CONTEXT.md : fichier JSON Lines dans `<app_docs>/logs/`, prune au démarrage borné par `kMaxLogsDirBytes`, toggle via `--dart-define=DEBUG=true` ou flag SharedPreferences, debug menu accessible via 7-tap easter egg sur l'écran À propos placeholder, routing go_router en place avec les 3 routes (`/`, `/about`, `/debug`).

Purpose: FOUND-06 est l'unique requirement de Phase 01 qui produit du code applicatif (vs configuration). Tout le reste de MirkFall loggera via ce système pour le debug POC GPS (Phase 05) et les diagnostics release.
Output: App qui au démarrage crée un fichier de log daté, l'écrit en JSONL, expose un debug menu caché.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@lib/config/constants.dart
@lib/main.dart
@lib/app.dart
@lib/presentation/screens/placeholder_home_screen.dart
@test/smoke_test.dart

<interfaces>
Le Plan 01 a posé la base ; Plan 02 l'augmente. Contrats pertinents :

Depuis `lib/config/constants.dart` (Plan 01) :
```dart
const String kAppName = 'MirkFall';
const int kMaxLogsDirBytes = 10 * 1024 * 1024;
const int kAboutTapsToTriggerDebugMenu = 7;
const int kAboutTapWindowMilliseconds = 3000;
```

Depuis `lib/main.dart` (Plan 01) :
```dart
// Phase 01 avait Logger.root.onRecord.listen qui appelle debugPrint.
// Phase 02 DOIT le remplacer par un await sur FileLogger.bootstrap() AVANT runApp.
```

Depuis `lib/app.dart` (Plan 01) :
```dart
// Phase 01 avait : home: const PlaceholderHomeScreen()
// Phase 02 DOIT passer à : routerConfig: ref.watch(appRouterProvider)
```

Dépendances runtime déjà pinnées en Plan 01 (pubspec.yaml) :
- `logging: 1.3.0` — `Logger`, `Logger.root`, `LogRecord`, `Level`
- `path_provider: 2.1.5` — `getApplicationDocumentsDirectory()`
- `path: 1.9.1` — `p.join(...)`
- `shared_preferences: 2.5.5` — `SharedPreferences.getInstance().getBool/setBool`
- `share_plus: 13.0.0` — `SharePlus.instance.share(ShareParams(files: [XFile(path)]))`
- `go_router: 16.0.0` — `GoRouter`, `GoRoute`, `context.go(...)`
- `flutter_riverpod: 3.3.1` + `riverpod_annotation: 3.0.3` — `@riverpod` + `ConsumerWidget` + `ProviderScope`

Dev deps déjà pinnées :
- `riverpod_generator: 4.0.3` — génère `*.g.dart` pour `@riverpod`
- `build_runner: 2.13.1` — drive codegen via `dart run build_runner build`
</interfaces>
</context>

<file_logger_spec>
`lib/infrastructure/logging/file_logger.dart` — complète la spec du RESEARCH.md §Code Examples :

```dart
// Copyright (c) 2026 THONGVAN Alexis
// Licensed under the Good Old Software License v1.0
// See LICENSE file for details

import 'dart:convert';
import 'dart:io';

import 'package:logging/logging.dart';
import 'package:path/path.dart' as p;
import 'package:path_provider/path_provider.dart';
import 'package:shared_preferences/shared_preferences.dart';

import '../../config/constants.dart';

/// JSON-Lines file sink for the project logger.
///
/// One file per app launch under `<app_docs>/logs/yyyymmdd_hhmm.ss_logs.txt`.
/// At bootstrap, oldest files are pruned until directory size < [kMaxLogsDirBytes].
/// Root log level is set from `--dart-define=DEBUG` or SharedPreferences flag.
class FileLogger {
  static IOSink? _sink;
  static String? _activeFilename;
  static StreamSubscription<LogRecord>? _subscription;

  /// Shared-prefs key for the verbose-logging toggle.
  static const String kDebugLoggingPrefsKey = 'debug_logging_enabled';

  /// Active log file path (absolute), or null before bootstrap.
  static String? get activeFilename => _activeFilename;

  /// Initialise the logger: open today's file, prune, set level, subscribe to LogRecord stream.
  /// MUST be awaited before [runApp].
  /// Idempotent: calling twice closes the previous sink and re-opens cleanly (dev hot-reload path).
  static Future<void> bootstrap() async {
    // 1) Decide level
    const debugDefine = bool.fromEnvironment('DEBUG', defaultValue: false);
    final prefs = await SharedPreferences.getInstance();
    final verboseFromPrefs = prefs.getBool(kDebugLoggingPrefsKey) ?? false;
    Logger.root.level = (debugDefine || verboseFromPrefs) ? Level.ALL : Level.INFO;

    // 2) Resolve logs dir
    final docsDir = await getApplicationDocumentsDirectory();
    final logsDir = Directory(p.join(docsDir.path, 'logs'));
    if (!await logsDir.exists()) {
      await logsDir.create(recursive: true);
    }

    // 3) Prune
    await _pruneToSizeLimit(logsDir);

    // 4) Open today's file
    final now = DateTime.now();
    final timestamp = _formatFilenameTimestamp(now);
    _activeFilename = p.join(logsDir.path, '${timestamp}_logs.txt');
    final file = File(_activeFilename!);

    // Idempotent: close previous sink if any (hot-reload safety)
    await _sink?.flush();
    await _sink?.close();
    await _subscription?.cancel();

    _sink = file.openWrite(mode: FileMode.append);
    _subscription = Logger.root.onRecord.listen(_onRecord);
  }

  /// Toggles the SharedPreferences flag that controls logging verbosity when
  /// `--dart-define=DEBUG` is not set. Takes effect on next app launch.
  /// Returns the new flag value.
  static Future<bool> toggleVerbosePref() async {
    final prefs = await SharedPreferences.getInstance();
    final next = !(prefs.getBool(kDebugLoggingPrefsKey) ?? false);
    await prefs.setBool(kDebugLoggingPrefsKey, next);
    return next;
  }

  /// Reads the current value of the verbose logging preference.
  static Future<bool> readVerbosePref() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getBool(kDebugLoggingPrefsKey) ?? false;
  }

  /// Lists all `*_logs.txt` files in the logs directory, sorted newest-first.
  static Future<List<File>> listLogFiles() async {
    final docsDir = await getApplicationDocumentsDirectory();
    final logsDir = Directory(p.join(docsDir.path, 'logs'));
    if (!await logsDir.exists()) return <File>[];
    final files = <File>[];
    await for (final entity in logsDir.list()) {
      if (entity is File && entity.path.endsWith('_logs.txt')) {
        files.add(entity);
      }
    }
    files.sort((a, b) => b.path.compareTo(a.path)); // newest first (by filename)
    return files;
  }

  /// Deletes all log files and the currently-open sink's file is recreated.
  static Future<void> clearAll() async {
    await _sink?.flush();
    await _sink?.close();
    await _subscription?.cancel();
    _sink = null;
    _subscription = null;

    final files = await listLogFiles();
    for (final f in files) {
      try {
        await f.delete();
      } on FileSystemException {
        // Non-critical: couldn't delete one file (e.g., Windows lock). Skip and continue.
        // Periphery error per CLAUDE.md §Error handling: log-worthy but not fatal.
      }
    }
    _activeFilename = null;
  }

  static Future<void> _onRecord(LogRecord rec) async {
    final entry = <String, Object?>{
      'ts': rec.time.toIso8601String(),
      'level': rec.level.name,
      'logger': rec.loggerName,
      'msg': rec.message,
      if (rec.error != null) 'error': rec.error.toString(),
      if (rec.stackTrace != null) 'stack': rec.stackTrace.toString(),
    };
    final sink = _sink;
    if (sink == null) return;
    sink.writeln(jsonEncode(entry));
    await sink.flush();
  }

  static Future<void> _pruneToSizeLimit(Directory logsDir) async {
    final files = <File>[];
    await for (final entity in logsDir.list()) {
      if (entity is File && entity.path.endsWith('_logs.txt')) {
        files.add(entity);
      }
    }
    files.sort((a, b) => a.path.compareTo(b.path)); // oldest first (chronological)

    int totalBytes = 0;
    final sizes = <int>[];
    for (final f in files) {
      final s = await f.length();
      sizes.add(s);
      totalBytes += s;
    }

    int i = 0;
    while (totalBytes > kMaxLogsDirBytes && i < files.length) {
      try {
        await files[i].delete();
        totalBytes -= sizes[i];
      } on FileSystemException {
        // Skip unlinkable file, keep going
      }
      i++;
    }
  }

  static String _formatFilenameTimestamp(DateTime dt) {
    String pad(int n, int w) => n.toString().padLeft(w, '0');
    return '${pad(dt.year, 4)}${pad(dt.month, 2)}${pad(dt.day, 2)}'
        '_${pad(dt.hour, 2)}${pad(dt.minute, 2)}.${pad(dt.second, 2)}';
  }
}
```

Import nécessaire non-listé au-dessus : `import 'dart:async';` pour `StreamSubscription`.
</file_logger_spec>

<router_spec>
`lib/presentation/router.dart` — GoRouter exposé via `@riverpod` (génère `router.g.dart` via `build_runner`) :

```dart
// Copyright (c) 2026 THONGVAN Alexis
// Licensed under the Good Old Software License v1.0
// See LICENSE file for details

import 'package:go_router/go_router.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';

import 'screens/about_placeholder_screen.dart';
import 'screens/debug_menu_screen.dart';
import 'screens/placeholder_home_screen.dart';

part 'router.g.dart';

/// Root GoRouter exposed via Riverpod so consumers get it through DI.
@riverpod
GoRouter appRouter(Ref ref) {
  return GoRouter(
    initialLocation: '/',
    routes: <GoRoute>[
      GoRoute(
        path: '/',
        builder: (_, __) => const PlaceholderHomeScreen(),
      ),
      GoRoute(
        path: '/about',
        builder: (_, __) => const AboutPlaceholderScreen(),
      ),
      GoRoute(
        path: '/debug',
        builder: (_, __) => const DebugMenuScreen(),
      ),
    ],
  );
}
```

NOTE : Riverpod 3.x utilise `Ref` (unifié) — pas `AppRouterRef`. Le codegen produit `appRouterProvider`.

Le fichier `router.g.dart` est généré par `dart run build_runner build --delete-conflicting-outputs`. Il NE doit PAS être modifié manuellement. Il est exclu de `check_headers.dart` (Plan 03) via le pattern `\.g\.dart$`.
</router_spec>

<about_screen_spec>
`lib/presentation/screens/about_placeholder_screen.dart` :

```dart
// Copyright (c) 2026 THONGVAN Alexis
// Licensed under the Good Old Software License v1.0
// See LICENSE file for details

import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

import '../../config/constants.dart';

class AboutPlaceholderScreen extends StatefulWidget {
  const AboutPlaceholderScreen({super.key});

  @override
  State<AboutPlaceholderScreen> createState() => _AboutPlaceholderScreenState();
}

class _AboutPlaceholderScreenState extends State<AboutPlaceholderScreen> {
  int _tapCount = 0;
  DateTime _lastTap = DateTime.fromMillisecondsSinceEpoch(0);

  void _onTap() {
    final now = DateTime.now();
    if (now.difference(_lastTap).inMilliseconds > kAboutTapWindowMilliseconds) {
      _tapCount = 0;
    }
    _tapCount++;
    _lastTap = now;
    if (_tapCount >= kAboutTapsToTriggerDebugMenu) {
      _tapCount = 0;
      context.go('/debug');
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('À propos')),
      body: GestureDetector(
        behavior: HitTestBehavior.opaque,
        onTap: _onTap,
        child: const Center(
          child: Padding(
            padding: EdgeInsets.all(24),
            child: Text(
              'MirkFall\n\nPlaceholder À propos. '
              'Phase 15 livrera l\'écran complet.',
              textAlign: TextAlign.center,
            ),
          ),
        ),
      ),
    );
  }
}
```
</about_screen_spec>

<debug_menu_spec>
`lib/presentation/screens/debug_menu_screen.dart` :

```dart
// Copyright (c) 2026 THONGVAN Alexis
// Licensed under the Good Old Software License v1.0
// See LICENSE file for details

import 'dart:io';

import 'package:cross_file/cross_file.dart';
import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';
import 'package:logging/logging.dart';
import 'package:share_plus/share_plus.dart';

import '../../infrastructure/logging/file_logger.dart';

class DebugMenuScreen extends StatefulWidget {
  const DebugMenuScreen({super.key});

  @override
  State<DebugMenuScreen> createState() => _DebugMenuScreenState();
}

class _DebugMenuScreenState extends State<DebugMenuScreen> {
  bool _verbose = false;
  List<File> _files = <File>[];
  bool _loading = true;

  static const bool _debugDefine = bool.fromEnvironment('DEBUG', defaultValue: false);

  @override
  void initState() {
    super.initState();
    _refresh();
  }

  Future<void> _refresh() async {
    final verbose = await FileLogger.readVerbosePref();
    final files = await FileLogger.listLogFiles();
    if (!mounted) return;
    setState(() {
      _verbose = verbose;
      _files = files;
      _loading = false;
    });
  }

  Future<void> _onToggleVerbose(bool _) async {
    final next = await FileLogger.toggleVerbosePref();
    if (!mounted) return;
    setState(() {
      _verbose = next;
    });
    // Apply immediately for the current run too (prefs-based path).
    Logger.root.level = (_debugDefine || next) ? Level.ALL : Level.INFO;
  }

  Future<void> _onShare(File f) async {
    await SharePlus.instance.share(ShareParams(files: <XFile>[XFile(f.path)]));
  }

  Future<void> _onClearAll() async {
    final confirmed = await showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        title: const Text('Supprimer tous les logs ?'),
        content: const Text('Cette action est irréversible.'),
        actions: <Widget>[
          TextButton(onPressed: () => Navigator.of(ctx).pop(false), child: const Text('Annuler')),
          TextButton(onPressed: () => Navigator.of(ctx).pop(true), child: const Text('Supprimer')),
        ],
      ),
    );
    if (!mounted) return;
    if (confirmed ?? false) {
      await FileLogger.clearAll();
      if (!mounted) return;
      await _refresh();
    }
  }

  @override
  Widget build(BuildContext context) {
    if (_loading) {
      return const Scaffold(body: Center(child: CircularProgressIndicator()));
    }
    return Scaffold(
      appBar: AppBar(title: const Text('Debug menu')),
      body: ListView(
        children: <Widget>[
          SwitchListTile(
            title: const Text('Verbose logging'),
            subtitle: Text(
              '--dart-define=DEBUG = $_debugDefine · prefs = $_verbose',
            ),
            value: _verbose,
            onChanged: _onToggleVerbose,
          ),
          const Divider(),
          if (_files.isEmpty)
            const ListTile(title: Text('Aucun fichier de log'))
          else
            ..._files.map(
              (f) => ListTile(
                title: Text(f.uri.pathSegments.last),
                subtitle: FutureBuilder<int>(
                  future: f.length(),
                  builder: (_, snap) => Text('${snap.data ?? 0} bytes'),
                ),
                trailing: IconButton(
                  icon: const Icon(Icons.share),
                  onPressed: () => _onShare(f),
                ),
              ),
            ),
          const Divider(),
          ListTile(
            leading: const Icon(Icons.delete_forever),
            title: const Text('Supprimer tous les logs'),
            onTap: _onClearAll,
          ),
          const SizedBox(height: 16),
          Padding(
            padding: const EdgeInsets.all(16),
            child: Text(
              'Active: ${FileLogger.activeFilename ?? "(none)"}',
              style: Theme.of(context).textTheme.bodySmall,
            ),
          ),
        ],
      ),
    );
  }
}
```

NOTE: `cross_file` est une dep transitive de `share_plus` 13.0.0. Vérifier qu'elle est résolue dans `pubspec.lock` — si oui, on peut l'importer directement. Si non, l'ajouter comme dep directe épinglée (audit dans DEPENDENCIES.md Plan 03). Pattern `XFile(path)` est le contract officiel de `ShareParams.files`.
</debug_menu_spec>

<main_update_spec>
`lib/main.dart` — mise à jour pour brancher `FileLogger.bootstrap()` et forwarder vers `Logger` au lieu de `debugPrint` :

```dart
// Copyright (c) 2026 THONGVAN Alexis
// Licensed under the Good Old Software License v1.0
// See LICENSE file for details

import 'dart:async';

import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:logging/logging.dart';

import 'app.dart';
import 'infrastructure/logging/file_logger.dart';

Future<void> main() async {
  await runZonedGuarded<Future<void>>(() async {
    WidgetsFlutterBinding.ensureInitialized();

    // File-sink logger: opens today's JSONL file, prunes, sets root level.
    await FileLogger.bootstrap();

    final log = Logger('main');

    FlutterError.onError = (FlutterErrorDetails details) {
      log.shout('FlutterError', details.exception, details.stack);
      if (kDebugMode) {
        FlutterError.dumpErrorToConsole(details);
      }
    };

    log.info('MirkFall starting — logger armed');
    runApp(const ProviderScope(child: MirkFallApp()));
  }, (Object error, StackTrace stack) {
    Logger('main').shout('uncaughtZoneError', error, stack);
  });
}
```

`lib/app.dart` — passe de `home:` à `routerConfig:` :

```dart
// Copyright (c) 2026 THONGVAN Alexis
// Licensed under the Good Old Software License v1.0
// See LICENSE file for details

import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'config/constants.dart';
import 'presentation/router.dart';

class MirkFallApp extends ConsumerWidget {
  const MirkFallApp({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(appRouterProvider);
    return MaterialApp.router(
      title: kAppName,
      theme: ThemeData.from(
        colorScheme: ColorScheme.fromSeed(
          seedColor: Colors.indigo,
          brightness: Brightness.dark,
        ),
        useMaterial3: true,
      ),
      routerConfig: router,
      debugShowCheckedModeBanner: false,
    );
  }
}
```
</main_update_spec>

<tests_spec>
**Tests à créer :**

1. `test/file_logger_test.dart` — widget/unit test qui :
   - mocke `path_provider` en appelant `PathProviderPlatform.instance = _FakePathProvider(tempDir)` où tempDir est un `Directory` temp du système (via `Directory.systemTemp.createTemp`)
   - appelle `await FileLogger.bootstrap()`
   - assert que `FileLogger.activeFilename` n'est pas null
   - assert que `Directory('<tempDir>/logs').existsSync()` = true
   - log une ligne via `Logger('test').info('hello world')`
   - attend un micro-délai (`await Future<void>.delayed(Duration.zero)`) puis lit le fichier actif
   - assert que le fichier contient au moins une ligne de JSON parseable avec `msg: 'hello world'` et `level: 'INFO'`
   - teardown: supprime le tempDir

2. `test/file_logger_debug_define_test.dart` — même setup que ci-dessus, mais lancé avec `--dart-define=DEBUG=true`. Assert que `Logger.root.level == Level.ALL` après `bootstrap()`. Ce test est PILOTÉ par la commande de run : `flutter test --dart-define=DEBUG=true test/file_logger_debug_define_test.dart`.

3. `test/file_logger_prune_test.dart` — :
   - mock path_provider comme ci-dessus
   - crée manuellement 6 fichiers `20260417_100000.01_logs.txt` à `20260417_100000.06_logs.txt` de 2 MB chacun dans le tempDir/logs (total 12 MB, au-dessus de kMaxLogsDirBytes=10 MB)
   - appelle `FileLogger.bootstrap()`
   - assert que la taille totale du dossier est maintenant < kMaxLogsDirBytes
   - assert que les plus vieux fichiers ont été supprimés (le plus récent doit être conservé)

4. `test/debug_menu_screen_test.dart` — widget test smoke :
   - mock path_provider
   - mock `SharedPreferences.setMockInitialValues(<String, Object>{})`
   - `await FileLogger.bootstrap()` en setUp
   - pumpWidget un `MaterialApp(home: DebugMenuScreen())` avec un `ProviderScope`
   - assert : find `Verbose logging` SwitchListTile
   - assert : find `Supprimer tous les logs` ListTile
   - tap le switch → assert que le toggle a changé la valeur prefs

5. `test/smoke_test.dart` — **mis à jour** (existait en Plan 01, mais maintenant `MirkFallApp` dépend de `FileLogger` indirectement via le router) :
   - mock path_provider + SharedPreferences
   - `await FileLogger.bootstrap()` en setUp
   - pumpWidget `ProviderScope(child: MirkFallApp())`
   - assert que `find.text('MirkFall — bootstrap OK')` trouve 1 widget (on est toujours sur `/`)

**Mock `path_provider` :** utiliser le pattern officiel `path_provider_platform_interface` avec un `PathProviderPlatform` fake. Skeleton :

```dart
class _FakePathProvider extends PathProviderPlatform {
  _FakePathProvider(this._root);
  final Directory _root;

  @override
  Future<String?> getApplicationDocumentsPath() async => _root.path;
  // Autres méthodes: return null ou _root.path selon le besoin.
}

// Dans setUp:
final tempDir = await Directory.systemTemp.createTemp('mirkfall_test_');
PathProviderPlatform.instance = _FakePathProvider(tempDir);
addTearDown(() async {
  if (tempDir.existsSync()) await tempDir.delete(recursive: true);
});
```

Chaque test file porte le header GOSL v1.0 en tête (scanné par `check_headers.dart` Plan 03).
</tests_spec>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FileLogger + update main/app + add router + screens</name>
  <files>
    lib/infrastructure/logging/file_logger.dart,
    lib/presentation/router.dart, lib/presentation/router.g.dart,
    lib/presentation/screens/about_placeholder_screen.dart,
    lib/presentation/screens/debug_menu_screen.dart,
    lib/main.dart, lib/app.dart,
    test/file_logger_test.dart,
    test/file_logger_prune_test.dart
  </files>
  <behavior>
    - `FileLogger.bootstrap()` crée `<appdocs>/logs/` si absent, open une IOSink en append sur un fichier `yyyymmdd_hhmm.ss_logs.txt`, subscribe `Logger.root.onRecord` pour sérialiser JSONL.
    - Level = `Level.ALL` si `--dart-define=DEBUG=true` OU prefs `debug_logging_enabled=true`, sinon `Level.INFO`.
    - `_pruneToSizeLimit` supprime les fichiers les plus anciens (ordre alphabétique = chronologique) jusqu'à ce que la taille totale < `kMaxLogsDirBytes`.
    - `FileLogger.bootstrap()` est idempotent : appelée deux fois, la seconde closes la première `IOSink` et re-subscribe.
    - `main.dart` : `runZonedGuarded` + `FlutterError.onError` forwardent vers `Logger('main').shout`, pas vers `debugPrint`.
    - `app.dart` : utilise `MaterialApp.router(routerConfig: ref.watch(appRouterProvider))`, plus de `home:`.
    - `router.dart` : `@riverpod GoRouter appRouter(Ref ref)` expose 3 routes (`/`, `/about`, `/debug`). `router.g.dart` est généré par `build_runner`.
    - Tests file_logger et file_logger_prune passent.
  </behavior>
  <action>
1. Créer `lib/infrastructure/logging/file_logger.dart` avec le contenu de `<file_logger_spec>`.

2. Créer `lib/presentation/router.dart` avec le contenu de `<router_spec>`.

3. Créer `lib/presentation/screens/about_placeholder_screen.dart` avec le contenu de `<about_screen_spec>`.

4. Créer `lib/presentation/screens/debug_menu_screen.dart` avec le contenu de `<debug_menu_spec>`.

5. Vérifier que `cross_file` est bien résolu par `share_plus` dans `pubspec.lock`. Si non, ajouter `cross_file` en dep directe pinnée (noter la version résolue dans `pubspec.lock`).

6. Mettre à jour `lib/main.dart` avec le contenu de `<main_update_spec>`.

7. Mettre à jour `lib/app.dart` avec le contenu de `<main_update_spec>` (section `lib/app.dart`).

8. Générer le code Riverpod : `dart run build_runner build --delete-conflicting-outputs`. Cela crée `lib/presentation/router.g.dart`. **Ce fichier .g.dart ne doit PAS contenir le header GOSL** — il est généré, et `check_headers.dart` (Plan 03) l'exclut via le pattern `\.g\.dart$`.

9. Créer les 2 tests suivants (les 3 autres dans Task 2) :
   - `test/file_logger_test.dart` — selon `<tests_spec>` §1
   - `test/file_logger_prune_test.dart` — selon `<tests_spec>` §3

10. Lancer la suite complète :
    - `flutter analyze --fatal-infos --fatal-warnings` → 0 warnings (ignore les `*.g.dart` excluded)
    - `dart format --set-exit-if-changed .` → exit 0
    - `flutter test test/file_logger_test.dart` → vert
    - `flutter test test/file_logger_prune_test.dart` → vert

**Important: avoid_print enforcement.** L'analyzer est en mode `avoid_print: error`. Le `file_logger.dart` n'utilise pas `print` — uniquement `sink.writeln`. Si un test utilise `print` pour debug, le remplacer par `debugPrint` (qui n'est pas banni).

**Important: strict-casts.** `SharedPreferences.getBool` retourne `bool?`, le coalesce avec `?? false` est nécessaire. `PathProviderPlatform` retourne `String?`, à traiter.

**NOTE main.dart:** le commit précédent (Plan 01) avait un listener qui appelait `debugPrint` en dev. Ce listener est maintenant REMPLACÉ par le listener interne à `FileLogger._onRecord` — ne PAS laisser les deux, sinon chaque log est écrit deux fois.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && dart format --set-exit-if-changed . && flutter test test/file_logger_test.dart test/file_logger_prune_test.dart</automated>
  </verify>
  <done>
    - `lib/infrastructure/logging/file_logger.dart` existe, compile, tests file_logger_test + file_logger_prune_test verts.
    - `lib/presentation/router.dart` existe, `router.g.dart` généré et valide.
    - `lib/presentation/screens/about_placeholder_screen.dart` + `debug_menu_screen.dart` existent et compilent.
    - `lib/main.dart` await `FileLogger.bootstrap()` avant `runApp` ; `FlutterError.onError` forwarde vers `Logger`.
    - `lib/app.dart` utilise `MaterialApp.router(routerConfig: ref.watch(appRouterProvider))`.
    - `flutter analyze` = 0 warnings. `dart format` = no-op.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete test suite + smoke test update + DEBUG define test + debug menu widget test</name>
  <files>
    test/smoke_test.dart,
    test/file_logger_debug_define_test.dart,
    test/debug_menu_screen_test.dart
  </files>
  <behavior>
    - `test/smoke_test.dart` mis à jour : mock path_provider, await `FileLogger.bootstrap()`, pumpWidget `ProviderScope(child: MirkFallApp())`, assert `find.text('MirkFall — bootstrap OK')` trouve 1 widget.
    - `test/file_logger_debug_define_test.dart` : avec `--dart-define=DEBUG=true`, après `bootstrap()`, `Logger.root.level == Level.ALL`.
    - `test/debug_menu_screen_test.dart` : mock path_provider + SharedPreferences, pumpWidget `DebugMenuScreen`, assert les 3 éléments UI présents (Verbose switch, Supprimer tous les logs, Active filename), tap sur le switch toggle la pref.
  </behavior>
  <action>
1. Mettre à jour `test/smoke_test.dart` pour ajouter le setup mock path_provider + FileLogger.bootstrap avant pumpWidget. Assertion inchangée : `find.text('MirkFall — bootstrap OK')`.

2. Créer `test/file_logger_debug_define_test.dart` selon `<tests_spec>` §2. Le test n'a besoin que d'un mock path_provider minimal et d'un SharedPreferences mock vide (pour que `verboseFromPrefs` soit `false` — le test vérifie que le DEBUG define seul fait basculer à ALL).

3. Créer `test/debug_menu_screen_test.dart` selon `<tests_spec>` §4. Mocker `share_plus` n'est pas nécessaire car le test ne tap pas sur le bouton Share — seulement sur le switch.

4. Lancer les tests :
   - `flutter test test/smoke_test.dart` → vert
   - `flutter test --dart-define=DEBUG=true test/file_logger_debug_define_test.dart` → vert
   - `flutter test test/debug_menu_screen_test.dart` → vert

5. Lancer la suite complète avec couverture :
   - `flutter test` → tout vert (suite de base, sans le DEBUG define test qui a besoin d'un flag de commande dédié)
   - `flutter test --dart-define=DEBUG=true test/file_logger_debug_define_test.dart` → vert (séparé car option de build)

**NOTE:** Le test DEBUG define est un test isolé qui requiert le flag `--dart-define=DEBUG=true` pour valider. Il est exclu du `flutter test` nu pour éviter les faux succès. La CI (Plan 04) devra l'inclure dans une étape dédiée.

**SharedPreferences mock:** utiliser `SharedPreferences.setMockInitialValues(<String, Object>{})` avant chaque test. Cela évite toute IO réelle.
  </action>
  <verify>
    <automated>flutter test test/smoke_test.dart test/debug_menu_screen_test.dart && flutter test --dart-define=DEBUG=true test/file_logger_debug_define_test.dart</automated>
  </verify>
  <done>
    - `test/smoke_test.dart` mis à jour, passe.
    - `test/file_logger_debug_define_test.dart` existe, passe avec le flag DEBUG.
    - `test/debug_menu_screen_test.dart` existe, passe.
    - `flutter test` (sans flag) → toute la suite verte.
  </done>
</task>

</tasks>

<verification>
- [ ] `flutter analyze --fatal-infos --fatal-warnings` → 0 warnings (FOUND-08 maintenu)
- [ ] `dart format --set-exit-if-changed .` → no-op
- [ ] `flutter test` : smoke, file_logger, file_logger_prune, debug_menu_screen tous verts
- [ ] `flutter test --dart-define=DEBUG=true test/file_logger_debug_define_test.dart` → vert
- [ ] `dart run build_runner build --delete-conflicting-outputs` idempotent (re-run produit 0 diff)
- [ ] Lancement réel : `flutter run -d windows` (desktop) démarre l'app, crée un fichier `logs/yyyymmdd_hhmm.ss_logs.txt` dans le app docs dir, écrit au moins une ligne `"msg":"MirkFall starting — logger armed"`
- [ ] 7-tap sur `/about` navigue vers `/debug` (test manuel en desktop run)
- [ ] Debug menu affiche le fichier actif, la taille, et les 3 boutons (switch / share / clear all)
</verification>

<success_criteria>
- [ ] FOUND-06 validé end-to-end : fichier log créé, JSONL, toggle `--dart-define=DEBUG`, toggle prefs, prune taille, debug menu full UI
- [ ] App démarre sans exception avec le logger armé ; un `log.info(...)` en `main()` écrit bien une ligne JSONL au disque
- [ ] Tous les tests Phase 02 sont verts (5 tests : file_logger, file_logger_prune, debug_menu_screen, smoke_test mis à jour, file_logger_debug_define)
- [ ] Les `FlutterError.onError` + `runZonedGuarded` forwardent vers `Logger('main').shout` (à vérifier en provoquant délibérément une exception dans un widget lors d'une vérification manuelle future)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- Final FileLogger surface (public methods + their contracts)
- Whether `cross_file` needed to be added as a direct dep (yes/no + version)
- Mock pattern used for path_provider in tests (reusable in later phases)
- Confirm the 7-tap easter egg works on desktop run (manual observation noted)
- Size of `logs/*_logs.txt` after a short manual run (sanity check that JSONL is written)
- Any code-gen quirks (router.g.dart signature, if Riverpod 3.x API differed from spec)
</output>
