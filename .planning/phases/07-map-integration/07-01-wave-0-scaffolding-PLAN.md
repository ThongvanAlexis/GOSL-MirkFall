---
phase: 07-map-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - DEPENDENCIES.md
  - android/app/src/main/AndroidManifest.xml
  - lib/config/constants.dart
  - lib/config/world_bundle_sha256.dart
  - assets/maps/world.pmtiles
  - assets/maps/catalog.json
  - assets/maps/style.json
  - assets/maps/glyphs/
  - assets/maps/sprites/
  - assets/maps/polygons/
  - tool/check_avoid_maplibre_leak.dart
  - tool/check_avoid_remote_pmtiles.dart
  - tool/generate_world_sha256.dart
  - tool/prepare_style.dart
  - tool/test/check_avoid_maplibre_leak_test.dart
  - tool/test/check_avoid_remote_pmtiles_test.dart
  - test/fakes/fake_map_view.dart
  - test/fakes/fake_pmtiles_source.dart
  - test/fakes/fake_installed_manifest_repository.dart
  - test/fakes/fake_download_controller.dart
  - test/fakes/fake_country_resolver.dart
  - test/fixtures/catalogs/mini_catalog.json
  - test/fixtures/polygons/
  - test/fixtures/pmtiles/tiny.pmtiles
  - test/fixtures/chunks/
  - .github/workflows/ci.yml
  - tool/check_platform_manifests.dart
autonomous: true
requirements:
  - MAP-05
  - MAP-06
  - MAP-07
  - MAP-08
  - MAP-09
user_setup: []

must_haves:
  truths:
    - "pubspec.yaml drops flutter_map + latlong2 and pins maplibre_gl 0.25.0 + crypto 3.0.7 + shelf (dev) with a committed pubspec.lock"
    - "DEPENDENCIES.md contains audit rows for maplibre_gl, crypto (promoted), shelf (promoted), and a new 'Bundled assets' section for Protomaps basemaps-assets (fonts OFL + sprites CC0 + data ODbL)"
    - "Two new CI gates (`check_avoid_maplibre_leak.dart`, `check_avoid_remote_pmtiles.dart`) exit 0 on clean tree, exit 1 on violation, exit 2 on misconfig; paired tests green in `tool/test/`"
    - "Android manifest declares INTERNET permission; `tool/check_platform_manifests.dart` still green"
    - "`assets/maps/` contains world.pmtiles + catalog.json + style.json (placeholder pmtiles URI, asset:// glyphs+sprites) + glyphs + sprites + polygons; `assets/maps/` is listed under `flutter.assets` in pubspec.yaml"
    - "`lib/config/constants.dart` carries every Phase 07 constant slot listed in CONTEXT.md §Existing Code Insights"
    - "`lib/config/world_bundle_sha256.dart` exports `kWorldBundleSha256` generated by `tool/generate_world_sha256.dart`"
    - "Test fakes (`FakeMapView`, `FakePmtilesSource`, `FakeInstalledManifestRepository`, `FakeDownloadController`, `FakeCountryResolver`) compile against Phase 07 domain interfaces (created as empty shells here, filled as downstream plans materialise the interfaces)"
    - "Test fixtures exist for catalog, polygons, pmtiles stub, chunk bytes with recorded sha256"
    - "CI `gates` job runs both new lints + their tool/test/ suites; existing gates stay green"
  artifacts:
    - path: "pubspec.yaml"
      provides: "maplibre_gl 0.25.0 + crypto 3.0.7 + shelf dev pin, flutter_map + latlong2 removed"
      contains: "maplibre_gl: 0.25.0"
    - path: "pubspec.lock"
      provides: "reproducible lockfile post-swap"
    - path: "DEPENDENCIES.md"
      provides: "audit rows for Phase 07 additions"
      contains: "maplibre_gl"
    - path: "tool/check_avoid_maplibre_leak.dart"
      provides: "CI gate enforcing MAP-06 seam (exit 0/1/2)"
    - path: "tool/check_avoid_remote_pmtiles.dart"
      provides: "CI gate enforcing MAP-05 seam (exit 0/1/2)"
    - path: "tool/test/check_avoid_maplibre_leak_test.dart"
      provides: "paired unit test with clean + violation + misconfig fixtures"
    - path: "tool/test/check_avoid_remote_pmtiles_test.dart"
      provides: "paired unit test"
    - path: "assets/maps/world.pmtiles"
      provides: "Day-1 world bundle (z0-2, 856 KB)"
    - path: "assets/maps/catalog.json"
      provides: "bundled country catalog (192+ entries)"
    - path: "assets/maps/style.json"
      provides: "Protomaps neutral style with asset:// glyphs/sprites + pmtiles placeholder + frozen 8-layer order"
      contains: "mirk_fog"
    - path: "lib/config/constants.dart"
      provides: "Phase 07 constant slots"
      contains: "kMapCatalogAssetPath"
    - path: "lib/config/world_bundle_sha256.dart"
      provides: "build-time kWorldBundleSha256 constant"
      contains: "kWorldBundleSha256"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "INTERNET permission declared"
      contains: "android.permission.INTERNET"
    - path: ".github/workflows/ci.yml"
      provides: "CI gates for two new lint scripts"
      contains: "check_avoid_maplibre_leak"
  key_links:
    - from: "pubspec.yaml"
      to: "DEPENDENCIES.md"
      via: "every new pin has a matching audit row"
      pattern: "maplibre_gl 0.25.0"
    - from: ".github/workflows/ci.yml"
      to: "tool/check_avoid_maplibre_leak.dart"
      via: "gates job runs `dart run tool/check_avoid_maplibre_leak.dart`"
      pattern: "check_avoid_maplibre_leak"
    - from: ".github/workflows/ci.yml"
      to: "tool/check_avoid_remote_pmtiles.dart"
      via: "gates job runs `dart run tool/check_avoid_remote_pmtiles.dart`"
      pattern: "check_avoid_remote_pmtiles"
    - from: "lib/config/world_bundle_sha256.dart"
      to: "assets/maps/world.pmtiles"
      via: "`tool/generate_world_sha256.dart` streams the asset and writes the constant"
      pattern: "kWorldBundleSha256"
---

<objective>
Establish the Wave 0 foundation for Phase 07 without landing any production map feature code. Every fake, fixture, lint script, asset, and dependency rewrite is done here so downstream plans (domain, infrastructure, controllers, presentation) can execute in parallel against a known-good baseline.

Purpose: Phase 03/05 convention — Wave 0 scaffolding BEFORE any impl. Closes RESEARCH.md Open Question #5 (world bundle sha256 via build-time constant) and #6 (hand-roll disk-space; no `disk_space_plus`). Removes the flutter_map/latlong2 day-1 pins now that maplibre_gl replaces them.
Output: A green tree with maplibre_gl wired, assets present, lints live in CI, fakes compile against placeholder interfaces, fixtures ready for downstream suites.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-map-integration/07-CONTEXT.md
@.planning/phases/07-map-integration/07-RESEARCH.md
@.planning/phases/07-map-integration/07-VALIDATION.md
@CLAUDE.md
@DEPENDENCIES.md
@pubspec.yaml
@lib/config/constants.dart
@android/app/src/main/AndroidManifest.xml
@.github/workflows/ci.yml
@tool/check_domain_purity.dart
@tool/check_platform_manifests.dart
@tool/test/check_platform_manifests_test.dart

<interfaces>
<!-- Patterns Wave 0 scripts must follow. Extracted from existing tool/ CI gates. -->

Contract for every `tool/check_*.dart` (validated 5 times from Phase 01 through Phase 06):
- `Future<int> runCheck({String? rootPath})` returns exit code
- `Future<void> main(List<String> args) async { exitCode = await runCheck(); }`
- Exit 0 = clean, exit 1 = policy violation (actionable by developer), exit 2 = misconfiguration (broken run)
- GOSL v1.0 header prepended to every .dart file in tool/
- Paired test in `tool/test/<name>_test.dart` with at minimum: clean fixture, violation fixture, misconfig fixture; each asserts exit code explicitly.

Existing constants.dart slots (Phase 05 + day-1) — do not reorder, extend at the commented Phase 07 block:
```dart
const int kHttpTimeout = 30000; // Phase 05
// Phase 07 additions go here:
// const String kMapCatalogAssetPath = 'assets/maps/catalog.json';
// const String kWorldPmtilesAssetPath = 'assets/maps/world.pmtiles';
// ...
```

Fake test pattern — see `test/fakes/fake_location_stream.dart` (Phase 05):
- Implements domain port literally (no inheritance from real impl)
- In-memory state tracked via public observable getters (`List<String> methodCallsObserved`, `Map<String, int> counterByMethod`)
- GOSL header prepended
- Empty-shell is fine for Wave 0 — the real tests will exercise them in downstream plans
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pubspec swap + DEPENDENCIES.md audits + Android manifest + constants + world sha256 + Phase 07 assets</name>
  <files>
    pubspec.yaml,
    pubspec.lock,
    DEPENDENCIES.md,
    android/app/src/main/AndroidManifest.xml,
    lib/config/constants.dart,
    lib/config/world_bundle_sha256.dart,
    assets/maps/world.pmtiles,
    assets/maps/catalog.json,
    assets/maps/style.json,
    assets/maps/glyphs/,
    assets/maps/sprites/,
    assets/maps/polygons/,
    tool/generate_world_sha256.dart,
    tool/prepare_style.dart,
    tool/check_platform_manifests.dart,
    tool/test/check_platform_manifests_test.dart
  </files>
  <behavior>
    - `flutter pub get` succeeds with maplibre_gl 0.25.0, crypto 3.0.7 (promoted from transitive → direct runtime dep), shelf 1.4.2 (promoted from transitive → direct dev dep)
    - flutter_map 8.3.0 + latlong2 0.9.1 + their 7 transitives (dart_earcut, dart_polylabel2, mgrs_dart, proj4dart, wkt_parser, simple_sparse_list, intl if unreferenced) removed from pubspec.lock
    - `flutter pub get` followed by `flutter pub deps --json` contains `maplibre_gl: 0.25.0` and none of the removed pins
    - `test/pubspec_pinned_test.dart` (existing Phase 01 test) passes — every entry still strictly pinned
    - DEPENDENCIES.md audit rows present for: maplibre_gl 0.25.0 (BSD-3, verified publisher, telemetry audit note "zero HTTP when pmtiles://file:// enforced"), crypto 3.0.7 (BSD-3, Dart-team, pure Dart), shelf 1.4.2 (BSD-3, dev, Dart-team), Protomaps basemaps-assets under new "Bundled assets" section (fonts OFL, sprites CC0, data ODbL, pinned by commit SHA)
    - `dart run tool/check_dependencies_md.dart` exits 0 (pubspec.lock ↔ DEPENDENCIES.md reconciled)
    - `dart run tool/check_licenses.dart` exits 0 (no forbidden license in any new transitive)
    - AndroidManifest.xml declares `<uses-permission android:name="android.permission.INTERNET"/>` before `<application>`
    - `tool/check_platform_manifests.dart` updated allowlist includes INTERNET; exits 0; paired test updated with INTERNET fixture covering present+absent
    - `lib/config/constants.dart` carries every Phase 07 slot verbatim: `kHttpTimeout` bumped comment to reference both Phase 05 (GPS) and Phase 07 (chunk downloads, raised to 60000), `kMapCatalogAssetPath = 'assets/maps/catalog.json'`, `kWorldPmtilesAssetPath = 'assets/maps/world.pmtiles'`, `kWorldPmtilesInternalPath = 'maps/world.pmtiles'`, `kCountriesDir = 'maps/countries'`, `kStagingDir = 'maps/staging'`, `kInstalledManifestPath = 'maps/installed.json'`, `kCountryPolygonsAssetPath = 'assets/maps/polygons'`, `kStyleJsonAssetPath = 'assets/maps/style.json'`, `kInitialSessionMapZoom = 13`, `kInitialRevealRadiusMeters = 20`, `kDiskSpaceSafetyMarginMultiplier = 1.1`, `kDownloadRetryAttempts = 3`, `kDownloadRetryBaseDelayMs = 1000`. All constants tested in `test/constants_test.dart` (extend existing file)
    - `lib/config/world_bundle_sha256.dart` exports `const String kWorldBundleSha256 = '<hex>';` generated by `tool/generate_world_sha256.dart`
    - `tool/generate_world_sha256.dart` is a one-shot script: reads `assets/maps/world.pmtiles`, streams sha256 via `package:crypto`, writes `lib/config/world_bundle_sha256.dart` with GOSL header. Fail-safe: if the asset doesn't exist, exit 2 (misconfig), don't write a stub constant.
    - `assets/maps/world.pmtiles` copied byte-identical from `C:\claude_checkouts\files_for_mirkfall\world-z2.pmtiles`; sha256 recorded in world_bundle_sha256.dart
    - `assets/maps/catalog.json` copied byte-identical from `C:\claude_checkouts\files_for_mirkfall\catalog.json`
    - `assets/maps/style.json` contains the 8 frozen layers in order (background, landcover, water, boundaries, roads, pois, mirk_fog, user_location) with `"glyphs": "asset:///assets/maps/glyphs/{fontstack}/{range}.pbf"`, `"sprite": "asset:///assets/maps/sprites/sprite"`, and ONE source `mirkfall_map` with `"url": "pmtiles://file:///YOUR_PMTILES_PATH_PLACEHOLDER"`. The `mirk_fog` layer has `"type": "background"` with `"paint": {"background-color": "#000000", "background-opacity": 0}` (stub — Phase 09 will replace with a real `fill`-from-GeoJSON layer wired to the mirk renderer source). Rationale: MapLibre fill/fill-extrusion/line/symbol layers require a `source`; using a `background` layer avoids the need for a dummy empty-GeoJSON source in Phase 07 (zero-byte runtime cost, style validates cleanly). Document this in a comment at the top of the style.json or in the 07-01 SUMMARY.
    - `assets/maps/glyphs/` and `assets/maps/sprites/` populated by `tool/prepare_style.dart` one-shot from github.com/protomaps/basemaps-assets (pinned commit SHA, cloned one-shot, no runtime fetch)
    - `assets/maps/polygons/` contains simplified GeoJSON for every alpha3 referenced by catalog.json (or an aggregate `polygons.json` — planner discretion, 5 MB budget). Simplification via `mapshaper` CLI or equivalent one-shot; tool documents the command used in a header comment of `tool/simplify_polygons.dart` if that path is taken
    - pubspec.yaml `flutter:` block declares `assets:` list including `assets/maps/` (recursive)
    - `dart run tool/generate_world_sha256.dart` is invoked once here and the emitted file is committed
    - No new transitive violates CLAUDE.md license allowlist (verified by check_licenses.dart green)
    - Zero `// TODO` markers left in the new files (except for honest placeholders like the pmtiles URI in style.json which is intentional)
  </behavior>
  <action>
    0. **Pre-flight** (informational, no commits):
       - Run `dart run tool/check_licenses.dart` and `dart run tool/check_dependencies_md.dart` to confirm current-main baseline green.
       - Inspect files in `C:\claude_checkouts\files_for_mirkfall\` to confirm sources exist (`world-z2.pmtiles`, `catalog.json`).
       - Validate `assets/maps/world.pmtiles` will be 856 KB and catalog.json will be ~132 KB.

    1. **Pubspec swap**:
       - Remove `flutter_map: 8.3.0` and `latlong2: 0.9.1` from `dependencies:`.
       - Add `maplibre_gl: 0.25.0` (production dep, BSD-3, publisher maplibre.org verified per RESEARCH §Standard Stack).
       - Add `crypto: 3.0.7` (production dep — previously transitive via build_runner; we need it at runtime for chunk sha256 verification).
       - Add `shelf: 1.4.2` under `dev_dependencies:` (previously transitive via `test`; explicit for `depend_on_referenced_packages` compliance per Phase 03 convention).
       - Run `flutter pub get`; commit the regenerated `pubspec.lock`.
       - Delete `ios/Podfile.lock` same commit (Phase 01 Option A — maplibre_gl adds a CocoaPod, CI macOS will regenerate). Per RESEARCH Pitfall #10.
       - Do NOT add `archive` (chunks are raw binary per CONTEXT.md). Do NOT add `disk_space_plus` (RESEARCH §Don't Hand-Roll decision — platform channel in Plan 03).
       - Add `assets/maps/` (recursive) to `pubspec.yaml` `flutter:` `assets:` block. Also add `assets/maps/glyphs/` and `assets/maps/sprites/` explicitly if recursion semantics are fragile; test with `flutter pub get` followed by an asset-load test.
       - Test: `flutter test test/pubspec_pinned_test.dart` passes + `flutter pub deps --no-dev` shows `maplibre_gl` direct + `crypto` direct.

    2. **DEPENDENCIES.md audits**:
       - Add a row for `maplibre_gl 0.25.0` under Direct dependencies: SPDX `BSD-3-Clause`, publisher `maplibre.org` (verified on pub.dev), telemetry audit note: "No built-in analytics or crash reporting. Tile fetches only when style source URL is HTTP(S); Phase 07 enforces pmtiles://file:// local-only via `tool/check_avoid_remote_pmtiles.dart`. Native SDKs: MapLibre Native Android 12.3.0 (BSD-2-Clause) + iOS 6.14.0 (BSD-2-Clause). Audit date: <today>."
       - Add a row for `crypto 3.0.7` (Direct, promoted): BSD-3-Clause, Dart-team, pure Dart, no network. Move from "Transitive" to "Direct".
       - Add a row for `shelf 1.4.2` (Dev, promoted): BSD-3-Clause, Dart-team, dev-only MockHTTPServer for download soak tests.
       - Drop rows for `flutter_map 8.3.0`, `latlong2 0.9.1` and each dropped transitive (`dart_earcut`, `dart_polylabel2`, `mgrs_dart`, `proj4dart`, `wkt_parser`, `simple_sparse_list`) — confirm drop via `flutter pub deps`. If `intl` survives (retained by flutter_local_notifications), keep its row.
       - Add a new top-level section "Bundled assets (non-pub)" under the existing Transitive section. Populate with: (a) Protomaps basemaps-assets — fonts OFL (SIL Open Font License 1.1), sprites CC0-1.0, pinned by commit SHA, inspect `fonts/OFL.txt` + repo-root LICENSE at pin-time; (b) Protomaps basemaps style (the style.json skeleton) — BSD-3-Clause (software), CC0-1.0 (map design); (c) OSM data (bundled inside world.pmtiles + country .pmtiles via Protomaps build pipeline) — ODbL 1.0 attribution required (MAP-03). Record audit date.
       - Run `dart run tool/check_dependencies_md.dart` — MUST exit 0.
       - Run `dart run tool/check_licenses.dart` — MUST exit 0.

    3. **Android manifest**:
       - Add `<uses-permission android:name="android.permission.INTERNET"/>` above `<application>` in `android/app/src/main/AndroidManifest.xml` (required per RESEARCH Pitfall #6).
       - Extend `tool/check_platform_manifests.dart`: add `android.permission.INTERNET` to the required-or-allowed list; add it to the expected green set. Update the paired test `tool/test/check_platform_manifests_test.dart` with a fixture that includes INTERNET + a mutation-test fixture that removes INTERNET and asserts exit 1.
       - `dart run tool/check_platform_manifests.dart` MUST exit 0 against the live tree.

    4. **`lib/config/constants.dart` extension**:
       - Append a `// Phase 07 — Map integration` section after the Phase 05 block (keep Phase 05 constants untouched — they are referenced by running tests).
       - Emit every slot listed in CONTEXT.md §Existing Code Insights (see `<behavior>` list). Each constant has a one-line docstring explaining purpose + unit.
       - Bump `kHttpTimeout` comment: mention both GPS (Phase 05) and chunk downloads (Phase 07). Value stays 60000 (60s) — cover 1.5 GB chunks on 4G.
       - Extend `test/constants_test.dart` with assertions for every new constant (value + type).
       - `flutter test test/constants_test.dart` green.

    5. **World bundle sha256 pipeline**:
       - Copy `C:\claude_checkouts\files_for_mirkfall\world-z2.pmtiles` to `assets/maps/world.pmtiles` byte-identical (use `File.copy` via Dart one-shot script or `cp -a` equivalent on Windows). Verify byte count matches source (~856 KB).
       - Copy `C:\claude_checkouts\files_for_mirkfall\catalog.json` to `assets/maps/catalog.json` byte-identical.
       - Create `tool/generate_world_sha256.dart`:
         - GOSL v1.0 header
         - `Future<int> runCheck({String? rootPath})` stream-reads `assets/maps/world.pmtiles` via `package:crypto` AccumulatorSink pattern (RESEARCH Example 2), computes sha256, writes `lib/config/world_bundle_sha256.dart` with content `// Copyright … / …\nconst String kWorldBundleSha256 = '<hex>';`
         - Exit 2 if asset missing.
       - Run `dart run tool/generate_world_sha256.dart`, commit both the asset and the generated file.
       - Unit test optional (the constant is tested downstream in first_launch_world_copier; the script itself is build-time).

    6. **`assets/maps/style.json` + glyphs + sprites + polygons**:
       - Create `tool/prepare_style.dart` one-shot:
         - GOSL header
         - `Future<int> main(List<String> args)`: clones `github.com/protomaps/basemaps-assets` at a pinned commit SHA to a temp dir (via `git clone --depth=1 --branch <sha>` shell out), copies `fonts/` → `assets/maps/glyphs/` and `sprites/` → `assets/maps/sprites/` (including sprite.json + sprite.png + sprite@2x.png).
         - Downloads Protomaps basemaps style.json template (or vendored subset at pinned commit), rewrites: `"glyphs"` → `asset:///assets/maps/glyphs/{fontstack}/{range}.pbf`, `"sprite"` → `asset:///assets/maps/sprites/sprite`, adds/replaces one source `mirkfall_map` with `"url": "pmtiles://file:///YOUR_PMTILES_PATH_PLACEHOLDER"`, rewrites the `layers` array to the frozen 8-layer spec (background / landcover / water / boundaries / roads / pois / mirk_fog / user_location). The mirk_fog layer is `{"id": "mirk_fog", "type": "background", "paint": {"background-color": "#000000", "background-opacity": 0}}` (no `source` needed — `background` layers never bind a source; Phase 09 will replace this layer with a real `type: "fill"` + source reference when the mirk renderer lands).
         - Writes `assets/maps/style.json` pretty-printed.
         - Exit 2 on any upstream failure.
       - Run once, commit the outputs. Do NOT commit `tool/prepare_style.dart` invocations to CI — it's a one-shot maintenance script. Document the invocation in a README comment at the top of the file.
       - Simplified polygons — planner discretion between per-country files (`assets/maps/polygons/<alpha3>.geo.json`) or aggregate (`assets/maps/polygons.json`). Simplify via `mapshaper` CLI one-shot (documented in a `tool/simplify_polygons.dart` README block if that path is used). 5 MB total budget; record actual total in the commit message.
       - Verify: `flutter test test/fixtures_exist_test.dart` (NEW, Wave 0 tiny test) asserts `File('assets/maps/style.json').existsSync()` and `File('assets/maps/world.pmtiles').existsSync()` and `File('assets/maps/catalog.json').existsSync()` and polygon assets > 0 bytes. Layer-order assertion lives in Plan 07-06 (presentation test).
  </action>
  <verify>
    <automated>
      flutter pub get &&
      flutter test test/pubspec_pinned_test.dart test/constants_test.dart test/fixtures_exist_test.dart &&
      dart run tool/check_dependencies_md.dart &&
      dart run tool/check_licenses.dart &&
      dart run tool/check_platform_manifests.dart &&
      dart run tool/check_headers.dart &&
      dart test tool/test/check_platform_manifests_test.dart
    </automated>
  </verify>
  <done>
    pubspec swap clean + DEPENDENCIES.md reconciled + Android manifest has INTERNET + constants.dart has every Phase 07 slot + kWorldBundleSha256 committed + assets/maps/ populated + all existing CI gate scripts exit 0 + pubspec.lock committed + ios/Podfile.lock deleted (CI macOS regenerates).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two new lint scripts (avoid_maplibre_leak + avoid_remote_pmtiles) + paired tests + CI wiring</name>
  <files>
    tool/check_avoid_maplibre_leak.dart,
    tool/check_avoid_remote_pmtiles.dart,
    tool/test/check_avoid_maplibre_leak_test.dart,
    tool/test/check_avoid_remote_pmtiles_test.dart,
    .github/workflows/ci.yml
  </files>
  <behavior>
    - `tool/check_avoid_maplibre_leak.dart`:
      - Scans `lib/**/*.dart` recursively
      - Pattern: any `import 'package:maplibre_gl/...'` or `import "package:maplibre_gl/..."`
      - Allowed paths: `lib/infrastructure/map/**` only
      - Exit 0 if no violation. Exit 1 with stderr listing each violating file + line. Exit 2 on missing lib/ root.
      - Note: Phase 07 `lib/infrastructure/map/` does not exist yet — the script must handle "no maplibre_gl imports anywhere" as green (exit 0).
    - `tool/check_avoid_remote_pmtiles.dart`:
      - Scans `lib/**/*.dart`, `test/**/*.dart`, `assets/**/*.json` recursively
      - Pattern: `pmtiles://http:` or `pmtiles://https:` (case-insensitive)
      - Exit 0 if no violation. Exit 1 with stderr listing each violating file + line + offending string. Exit 2 on missing scan root.
    - Both scripts follow the 5-time-validated contract: GOSL header + `Future<int> runCheck()` + main wrapper + docstrings.
    - Paired tests under `tool/test/`:
      - Clean fixture (scan tree with no violations) → exit 0
      - Violation fixture (maplibre_gl import in `lib/domain/foo.dart`; pmtiles://https URI in `assets/maps/bad.json`) → exit 1 + stderr contains fixture filename + line number
      - Allowed fixture for maplibre_gl (import inside `lib/infrastructure/map/`) → exit 0
      - Misconfig fixture (non-existent scan root) → exit 2
      - Mutation assertion: test deliberately mutates the fixture mid-test to prove exit code flips (Phase 04 inertness-guard idiom).
    - CI: two new steps under the `gates` job after `Check platform manifests`, named identically to existing steps (`Check avoid_maplibre_leak`, `Check avoid_remote_pmtiles`).
    - Existing gates job stays green (no step reordering).
  </behavior>
  <action>
    1. **`tool/check_avoid_maplibre_leak.dart`** (model from `tool/check_domain_purity.dart`):
       - GOSL header
       - const `_leakPattern = RegExp(r"""^\s*import\s+['"]package:maplibre_gl(/|['"])""")`
       - const `_allowedPrefix = 'lib/infrastructure/map/'` (forward slashes; the scanner normalises backslashes via `p.normalize` + `p.split`)
       - Recurse `lib/` via `Directory.listSync(recursive: true)`; skip `.dart_tool/`, `build/`, `*.g.dart`, `*.freezed.dart`
       - For every .dart file, check every line for `_leakPattern`. If path does not start with `_allowedPrefix`, record violation.
       - Emit `int runCheck({String? rootPath})`: return 2 on missing root, 1 with stderr listing, 0 on clean.
       - `main()` wires `exitCode = await runCheck();`.

    2. **`tool/check_avoid_remote_pmtiles.dart`** (model from `tool/check_headers.dart` scan structure):
       - GOSL header
       - const `_remote = RegExp(r'pmtiles://https?:', caseSensitive: false)`
       - Recurse `lib/`, `test/`, `assets/`. For .dart files inspect line-by-line. For .json files read whole file, scan. Skip `.dart_tool/`, `build/`.
       - Exit contract identical to Task 1 script.

    3. **Paired tests** `tool/test/check_avoid_maplibre_leak_test.dart` and `tool/test/check_avoid_remote_pmtiles_test.dart`:
       - GOSL header
       - Use `Directory.systemTemp` scratch pattern (same as `tool/test/check_headers_test.dart`)
       - Test cases (each its own `test(...)` block):
         a. "clean tree returns 0" — empty scan root with only headers.dart baseline
         b. "violation in forbidden file returns 1" — seed `lib/domain/foo.dart` with `import 'package:maplibre_gl/maplibre_gl.dart';` (or pmtiles://https://... for the other script)
         c. "violation in allowed dir returns 0" (maplibre test only) — seed `lib/infrastructure/map/adapter.dart` with the maplibre_gl import
         d. "misconfig returns 2" — nonexistent scan root
         e. "mutation guard" — seed clean tree, assert exit 0; mutate to add violation; assert exit 1 (Phase 04 inertness-guard idiom: prevents the test silently becoming a no-op if the scanner breaks)
       - Every `expect` covers exit code AND stderr substring (filename + line number)

    4. **CI wiring** `.github/workflows/ci.yml`:
       - Locate the `gates` job, the step `Check platform manifests`. Add two new steps immediately after:
         - `- name: Check avoid_maplibre_leak\n  run: dart run tool/check_avoid_maplibre_leak.dart`
         - `- name: Check avoid_remote_pmtiles\n  run: dart run tool/check_avoid_remote_pmtiles.dart`
       - Confirm the `Tool scripts unit tests` step (`dart test tool/test/`) auto-picks up the new tests (existing glob).
       - Do not reorder existing steps. Do not touch android / ios jobs.

    5. **Smoke locally**:
       - `dart run tool/check_avoid_maplibre_leak.dart` → exit 0 (no offenders; Phase 07 infra doesn't exist yet)
       - `dart run tool/check_avoid_remote_pmtiles.dart` → exit 0 (style.json uses pmtiles://file:// placeholder, catalog.json uses https:// for parts[].url which is HTTP not pmtiles:// so it does NOT match the `pmtiles://http` pattern)
       - `dart test tool/test/` → all tests green, including 2 new files
  </action>
  <verify>
    <automated>
      dart run tool/check_avoid_maplibre_leak.dart &&
      dart run tool/check_avoid_remote_pmtiles.dart &&
      dart test tool/test/check_avoid_maplibre_leak_test.dart tool/test/check_avoid_remote_pmtiles_test.dart
    </automated>
  </verify>
  <done>
    Both scripts exit 0 on main, exit 1 on seeded violation (covered by paired tests), exit 2 on misconfig. Paired tests green locally (CI will re-run via existing `dart test tool/test/` glob). `.github/workflows/ci.yml` gates job declares both new steps.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test fakes + fixtures scaffold (empty shells, compile only)</name>
  <files>
    test/fakes/fake_map_view.dart,
    test/fakes/fake_pmtiles_source.dart,
    test/fakes/fake_installed_manifest_repository.dart,
    test/fakes/fake_download_controller.dart,
    test/fakes/fake_country_resolver.dart,
    test/fixtures/catalogs/mini_catalog.json,
    test/fixtures/polygons/fra.geo.json,
    test/fixtures/polygons/deu.geo.json,
    test/fixtures/polygons/esp.geo.json,
    test/fixtures/polygons/gbr.geo.json,
    test/fixtures/polygons/usa.geo.json,
    test/fixtures/pmtiles/tiny.pmtiles,
    test/fixtures/chunks/README.md
  </files>
  <behavior>
    - Each fake file is GOSL-headered and compiles under `flutter analyze --fatal-infos`.
    - Fakes are thin "shells" here — they declare the class with `// TODO: implement in Plan 07-02 (domain) once MapView / PmtilesSource / InstalledManifestRepository / DownloadController interfaces materialise` stubs. The stubs MUST NOT be `UnimplementedError()`-throwing method bodies until the domain plan fills the interfaces — instead, the fake classes live as *forward-declared class files* whose body will be filled by Plan 07-02 Task 2 (the FakeMapView in Plan 07-02 is the authoritative one).
    - To avoid a compile break in the meantime, this task creates each file as a commented-out doc-only shell: GOSL header + a top-of-file doc block explaining the forward-declaration intent + a single-line `// Deferred to Plan 07-02 — this file is a placeholder so downstream plans can list it in files_modified.` Nothing else.
    - `test/fixtures/catalogs/mini_catalog.json`: 5-country catalog (FRA 3 parts, DEU 2 parts, ESP 1 part, GBR 1 part, USA 4 parts) with fabricated `sha256` values matching the bytes in `test/fixtures/chunks/`. Compatible with the real Freezed `CountryCatalog` shape planned by 07-02.
    - `test/fixtures/polygons/<alpha3>.geo.json`: coarse 4-point bounding polygon per country (e.g. FRA = `[[-5,41],[9,41],[9,51],[-5,51],[-5,41]]` closed). Purpose: give the country_resolver unit test in 07-03 deterministic fixtures.
    - `test/fixtures/pmtiles/tiny.pmtiles`: 1 KB file with a valid PMTiles magic header (`"PMTiles"` 7-byte ASCII + version + default header fields) followed by zeros. Generated via `tool/generate_tiny_pmtiles.dart` one-shot OR hand-crafted byte array; documented in `test/fixtures/pmtiles/README.md` (create stub).
    - `test/fixtures/chunks/README.md`: documents the chunk fixture convention for Plan 07-04 download soak tests — expected layout, sha256 seed recipe, reference to the atomic-protocol specification.
  </behavior>
  <action>
    1. **Forward-declare fake files**:
       - For each fake path in files_modified, create a .dart file with:
         ```
         // Copyright (c) 2026 THONGVAN Alexis
         // Licensed under the Good Old Software License v1.0
         // See LICENSE file for details

         /// Forward declaration for Plan 07-02 (domain interfaces) / 07-03 (map infra) / 07-04 (downloads).
         ///
         /// This file is intentionally minimal — it will be populated in the plan that
         /// introduces the corresponding domain port so that downstream plans can list it
         /// in `files_modified` without the git diff being a two-step creation dance.
         library;
         ```
       - Every file MUST compile — verify via `flutter analyze lib/ test/`. The `library;` declaration + doc comment is valid Dart.
       - No premature test code. No stubs that would need refactoring once the real interface arrives.

    2. **`test/fixtures/catalogs/mini_catalog.json`**:
       - Write JSON literal:
         ```json
         {
           "countries": [
             {
               "alpha3": "aru",
               "name": "Aruba",
               "parts": [ { "sha256": "<4 MB single part sha>", "size": 4194304, "url": "https://example.test/aru.part01" } ],
               "reassembled": { "sha256": "<same as single-part>", "size": 4194304 }
             },
             {"alpha3": "esp", "name": "Spain", "parts": [/* 1 part 10 MB */], "reassembled": {...}},
             {"alpha3": "deu", "name": "Germany", "parts": [/* 2 parts 20 MB + 20 MB */], "reassembled": {...}},
             {"alpha3": "gbr", "name": "United Kingdom", "parts": [/* 1 part 30 MB */], "reassembled": {...}},
             {"alpha3": "fra", "name": "France", "parts": [/* 3 parts 40+40+40 MB */], "reassembled": {...}},
             {"alpha3": "usa", "name": "United States", "parts": [/* 4 parts 50 MB each */], "reassembled": {...}}
           ]
         }
         ```
       - Sha256 values are synthetic, matching hashes computed from the chunk fixture byte patterns defined in the next sub-step.
       - Goal: cover happy-1-part (ARU), 2-part concat (DEU), 3-part (FRA), 4-part (USA) soak test cases in 07-04.

    3. **Polygon fixtures**: 5 coarse bounding-box closed polygons in GeoJSON FeatureCollection shape per country. Not representative of real borders — deterministic for point-in-polygon unit tests in 07-03.

    4. **`test/fixtures/pmtiles/tiny.pmtiles`**:
       - Create `tool/generate_tiny_pmtiles.dart` one-shot script writing a 1 KB byte array starting with the PMTiles magic `"PMTiles"` + version byte + zeros. Commit the resulting binary.
       - Purpose: Plan 07-04 download soak tests use this for "concat assembled file looks sane" smoke — real MapLibre won't load it but the binary_concatenator + sha256 passes.

    5. **`test/fixtures/chunks/README.md`**: documents the fixture layout. Actual chunk binaries are deferred to Plan 07-04 (they match the synthetic sha256 values in mini_catalog.json).

    6. Run `flutter analyze` — MUST be clean with zero warnings. `dart run tool/check_headers.dart` MUST exit 0 (every .dart file, including the forward-declared fakes, has the GOSL header).
  </action>
  <verify>
    <automated>
      flutter analyze --fatal-infos --fatal-warnings &&
      dart run tool/check_headers.dart &&
      flutter test test/fixtures_exist_test.dart
    </automated>
  </verify>
  <done>
    All 5 fake shell files compile + header check green + 5 polygon fixtures + mini_catalog.json + tiny.pmtiles all committed. Downstream plans can now list these paths in their `files_modified` and fill the implementations without re-creating the files.
  </done>
</task>

</tasks>

<verification>
Full-suite quick run after all 3 tasks:

```
flutter pub get &&
flutter analyze --fatal-infos --fatal-warnings &&
flutter test --reporter=expanded --exclude-tags=soak &&
dart test tool/test/ &&
dart run tool/check_headers.dart &&
dart run tool/check_licenses.dart &&
dart run tool/check_dependencies_md.dart &&
dart run tool/check_platform_manifests.dart &&
dart run tool/check_domain_purity.dart &&
dart run tool/check_avoid_maplibre_leak.dart &&
dart run tool/check_avoid_remote_pmtiles.dart
```

All steps MUST exit 0. CI will re-run on push; local pre-verify saves CI minutes per Phase 02 patterns.
</verification>

<success_criteria>
- pubspec swap lands cleanly: maplibre_gl 0.25.0 in, flutter_map + latlong2 out, pubspec.lock committed, flutter pub get reproducible
- DEPENDENCIES.md accurately describes the new dependency graph + Protomaps bundled assets
- Two new CI gates live and paired-tested with inertness-guard fixtures
- Android manifest declares INTERNET; platform-manifests guard extended
- `lib/config/constants.dart` carries every Phase 07 slot + `lib/config/world_bundle_sha256.dart` generated
- `assets/maps/` populated with world.pmtiles + catalog.json + style.json (placeholder URI + frozen 8-layer order + asset:// glyphs & sprites) + glyphs + sprites + polygons (≤ 5 MB total)
- Test fakes forward-declared + fixtures present — downstream plans can consume without re-creation
- All 7 existing + 2 new CI gate scripts exit 0 on main; all existing tests still green
</success_criteria>

<output>
After completion, create `.planning/phases/07-map-integration/07-01-SUMMARY.md` documenting:
- Deltas to pubspec.yaml + exact pubspec.lock churn (list of packages dropped + added)
- DEPENDENCIES.md rows added / removed
- Any deviations from plan (e.g. if mapshaper polygon simplification budget exceeded 5 MB)
- Commit hashes for each atomic commit in this plan
- World bundle sha256 (hex) recorded for Plan 07-03 consumption
- Catalog version tag extracted from the first `parts[].url` (e.g. `v20260419`)
- Whether polygons shipped per-file or aggregate + total byte count
- Paths of all new files + any unexpected surface discovered during the swap
</output>
