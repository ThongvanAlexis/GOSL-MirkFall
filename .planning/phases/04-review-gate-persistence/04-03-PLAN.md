---
phase: 04-review-gate-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - .planning/phases/04-review-gate-persistence/04-REVIEW.md
  - .planning/phases/04-review-gate-persistence/04-03-SUMMARY.md
autonomous: false
requirements:
  - SC#2
  - SC#3
must_haves:
  truths:
    - "Before ANY Agent tool call is spawned, §2 sub-section `Pre-known from VERIFICATION` contains 3 entries verbatim (flaky `backup_test.dart::rotate` Blocker / `custom_lint` silently degraded Noted / `computeRevealMask` UnimplementedError Should), PLUS any runtime walk deviations flagged as Blockers by Plan 04-02, and this pre-class is committed to `main` as `docs(04-rev): pre-class VERIFICATION candidates into §2`"
    - "4 sub-agents (ALL `general-purpose` per Phase 02 Accumulated Decision) spawned in a SINGLE tool-use message — not 4 serial messages — preserving the wall-clock parallelism that validated 54 findings in Phase 02"
    - "Each sub-agent scope matches CONTEXT §Sub-agent slicing verbatim: Agent #1 Schema+migrations+backup / Agent #2 Domain+pureté / Agent #3 Stores+factory+providers / Agent #4 Tests+fixtures+tooling+CLAUDE.md sweep"
    - "Each sub-agent returned a structured findings list (`[severity] Title — 1-line explanation — file:line`) + a narrative appendix"
    - "Claude-main deduplicated overlapping findings (preserving same-line findings by 2 different agents with explicit cross-reference per Phase 02 Accumulated Decision `Cross-lens finding overlap handling convention`)"
    - "§2 populated, user presented findings as TITLES + 1-line explanations only (never diffs or code blocks) per CLAUDE.md §Code Review Phases"
    - "§3 triage table populated: every Blocker = `fix` (waiver forbidden), every Should = `fix` OR `waived (rationale)` (rationale non-empty), every Could = `fix` / `waived` / `defer-to-phase-X` / `won't-fix`, every Noted kept as observation"
    - "04-03-SUMMARY.md carries forward the adversarial poison confirmations (poisons are LOCKED by CONTEXT — Agent #1 verifies they still apply, doesn't re-design)"
  artifacts:
    - path: ".planning/phases/04-review-gate-persistence/04-REVIEW.md"
      provides: "Sections 2 (pre-class + 4-agent findings) and 3 (triage table) fully populated"
      contains: "### Agent #1 — Schema + migrations + backup"
      min_lines: 200
    - path: ".planning/phases/04-review-gate-persistence/04-03-SUMMARY.md"
      provides: "Wave 3 summary: agent type selection, finding counts per severity, triage summary, adversarial poison verifications for Plan 04-04"
      contains: "Adversarial poison verifications"
      min_lines: 40
  key_links:
    - from: "Plan 04-03 Task 1 commit (pre-class §2)"
      to: "Plan 04-03 Task 2 Agent spawn"
      via: "git log grep `pre-class VERIFICATION candidates` must precede 4 Agent tool calls"
      pattern: "docs\\(04-rev\\): pre-class VERIFICATION"
    - from: "Claude-main"
      to: "4 general-purpose sub-agents"
      via: "single tool-use message containing 4 parallel Agent tool calls"
      pattern: "Agent #[1-4]"
    - from: "Agent structured outputs"
      to: "`04-REVIEW.md` §2 sub-sections (one per agent)"
      via: "main-agent deduplication + assembly"
      pattern: "\\[(Blocker|Should|Could|Noted)\\]"
    - from: "`04-REVIEW.md` §2"
      to: "user triage chat → `04-REVIEW.md` §3 table"
      via: "titles+1-line explanation presentation (never diffs) → user decides per-finding → Claude records"
      pattern: "\\| fix \\||\\| waived \\||\\| defer-to-phase-"
---

<objective>
Pre-classify the 3 VERIFICATION candidates (plus any runtime walk deviations) into §2 FIRST, commit, THEN spawn the 4 parallel sub-agents in ONE tool-use message, consolidate their findings into §2, present them to the user as titles + 1-line explanations (not diffs), and capture the user's triage decisions into §3.

Purpose: This is the core audit activity of Phase 04. Pre-classification saves a full cycle of redundant agent discovery (each of 3 known items would otherwise be re-found by 2-4 agents). Spawning all 4 agents in a single tool-use message preserves the wall-clock parallelism that validated Phase 02 (54 findings in one slot). Presenting titles-only to the user is the CLAUDE.md contract that lets the user choose what to fix without being biased by diffs. The split between "pre-class / agents / triage" mirrors 02-02-PLAN.md adapted to Phase 04's persistence concerns with 2 adjustments: (1) pre-class sub-section added BEFORE agents; (2) agent scopes re-sliced by layer technique (schema+migrations / domain+pureté / stores+factory+providers / tests+tooling+CLAUDE.md sweep).

Output: `04-REVIEW.md` §2 populated (pre-class + 4-agent findings + narrative appendices) + §3 populated (user triage decisions) + 04-03-SUMMARY.md with adversarial poison verifications carried forward for Plan 04-04.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-review-gate-persistence/04-CONTEXT.md
@.planning/phases/04-review-gate-persistence/04-RESEARCH.md
@.planning/phases/04-review-gate-persistence/04-VALIDATION.md
@.planning/phases/04-review-gate-persistence/04-REVIEW.md
@.planning/phases/04-review-gate-persistence/04-01-SUMMARY.md
@.planning/phases/04-review-gate-persistence/04-02-SUMMARY.md
@.planning/phases/03-persistence-domain-models/03-VERIFICATION.md
@.planning/phases/03-persistence-domain-models/03-01-SUMMARY.md
@.planning/phases/03-persistence-domain-models/03-02-SUMMARY.md
@.planning/phases/03-persistence-domain-models/03-03-SUMMARY.md
@.planning/phases/03-persistence-domain-models/03-04-SUMMARY.md
@.planning/phases/03-persistence-domain-models/03-05-SUMMARY.md
@.planning/phases/03-persistence-domain-models/03-06-SUMMARY.md
@.planning/phases/02-review-gate-foundation/02-02-PLAN.md
@CLAUDE.md
@DEPENDENCIES.md

<interfaces>
<!-- 4 sub-agent scope contract, verbatim from CONTEXT §Sub-agent slicing. Agents MUST NOT silently expand scope. -->
<!-- All 4 agents are general-purpose per Phase 02 Accumulated Decision. -->

**Agent #1 — Schema + migrations + backup**
Scope (files to audit):
- `lib/infrastructure/db/app_database.dart` (Drift schema, 6 tables, FK CASCADE policy, partial unique index `idx_t_sessions_status_active`, BLOB MIRK-03, schemaVersion=2, MigrationStrategy)
- `lib/infrastructure/db/migrations/v1_to_v2_notes.dart` (ALTER raw `customStatement`)
- `lib/infrastructure/db/backup.dart` + `lib/infrastructure/db/schema_sanity.dart` + `lib/infrastructure/db/app_database_factory.dart` + `lib/infrastructure/db/pragma_setup.dart` + `lib/infrastructure/db/type_converters.dart`
- `drift_schemas/drift_schema_v1.json` + `drift_schema_v2.json` + `drift_schema_current.json` (frozen snapshots vs rolling)
- `test/infrastructure/db/**` (~13 test files)
- Pragma wiring (WAL via `NativeDatabase(setup:)`, FK + busy_timeout + synchronous via `beforeOpen`)

Focus:
- Schema integrity (PK / FK / UNIQUE / CHECK constraints correctly expressed)
- Migration framework soundness (V1→V2 notes migration exercises framework, no data-loss pattern, CI-gated schema dump fresh)
- Backup correctness (3-rolling, pre-migration invocation, SchemaSanityChecker assertion) — SPECIFICALLY re-verify that `DbBackupService.rotate` does NOT depend on `File.lastModifiedSync()` mtime (RESEARCH Pitfall 7 escalation risk). If `rotate` uses mtime, ESCALATE the pre-class Blocker #1 to Blocker-runtime + Blocker-test with a dedicated finding.
- PRAGMA wiring alignment between beforeOpen + NativeDatabase setup — no duplication, no split inconsistency

Output format:
- Structured findings list: `[Blocker|Should|Could|Noted] Title — 1-line explanation — file:line`
- Narrative appendix (prose audit notes)
- **Adversarial poison verification block** — Test #1 domain-purity double-violation poison recipe and Test #2 drift-schema-dump-stale poison recipe are LOCKED by CONTEXT.md (not Agent #1's to design). Agent #1 re-VERIFIES they still apply to Phase 03 code (filenames, column-add location, grep patterns) and flags drift if any.

**Agent #2 — Domain models + pureté**
Scope:
- `lib/domain/**` (entities Freezed: Session, Marker, MarkerCategory, MirkStyle + sealed MirkStyleConfig + UnknownConfig fallback, RevealedTile, PhotoRef, Envelope; @Assert invariants; errors taxonomy; ports/store-interfaces)
- Verify zero `import 'package:flutter/'` and zero `import 'package:drift/'` — even in generated `.g.dart` / `.freezed.dart` (should not have these imports for pure-Dart entities)
- Zero `is`-chain (should use polymorphism / sealed classes / pattern matching)
- Zero `dynamic` non-documenté (any use must have inline comment explaining why `Object?` wasn't appropriate)
- Zero singleton global caché
- Extension type IDs (6 types: SessionId, MarkerId, MarkerCategoryId, MirkStyleId, RevealedTileId, PhotoRefId) + ULID + IdGenerator seam — verify extension-type @JsonKey(fromJson/toJson) per-field (class-level JsonConverter doesn't work on extension types, per STATE.md Phase 03 decision)
- Envelope `{schemaVersion, type, payload}` validation + JsonMigrator framework + IdentityMigrationV1 sentinel + V1ToV2RenameRadius step correctness
- `test/domain/**` (~10 test files) for assertion quality (not placebo)

Focus:
- SC#2 compliance (no `is`-chains, no undocumented `dynamic`, no singletons in domain)
- Sealed-class dispatch correctness (MirkStyleConfig → UnknownConfig fallback)
- @Assert vs factory (Phase 03 decision: asserts require `factory` not `const factory` when calling methods — verify)
- Invariants robustness (trim checks, offset range, displayName non-empty)
- Envelope.fromJson must stay pure arrow redirect (Freezed 3.2.3 constraint — verify not accidentally refactored)
- **custom_lint silent-degrade verification:** run `dart run custom_lint` directly (bypassing `flutter analyze`) to confirm the plugin-load failure is still present (STATE.md known state). If `custom_lint` now loads successfully, promote the pre-class Noted to a Could `custom_lint re-enabled`. If the plugin fails differently (new error class), note it.

Output format: same as Agent #1.

**Agent #3 — Store layer + factory + providers**
Scope:
- `lib/infrastructure/stores/drift_session_store.dart` (SqliteException 2067 → ConcurrentActivationException wrap scope — ONLY 2067, others rethrown per STATE.md Phase 03 decision)
- `lib/infrastructure/stores/drift_revealed_tile_store.dart` (transactional OR-merge mergeMask, MIRK-03 idempotence + commutativity + monotone invariants runtime-enforced)
- `lib/infrastructure/stores/drift_marker_category_store.dart` (reassign-to-default + kCategoryDefaultId protection via CategoryInUseException + markerCount pre-count)
- `lib/infrastructure/stores/drift_marker_store.dart`
- `lib/infrastructure/stores/drift_mirk_style_store.dart`
- `lib/infrastructure/stores/sqlite_error_mapper.dart`
- `lib/application/providers/*_store_provider.dart` (7 @Riverpod keepAlive=true providers)
- `lib/infrastructure/ids/random_id_generator.dart` + `seeded_id_generator.dart` + `ulid.dart`
- `test/infrastructure/stores/**` (~6 test files) + `test/infrastructure/ids/**` (3 test files)

Focus:
- No Drift leakage into upper layers (domain / application provider return types are pure domain types, not Drift `Companion` / `DataClass`)
- Transactions correctly used for multi-write operations (mergeMask SELECT+UPDATE must be transactional; reassign-to-default must be transactional)
- FK CASCADE actually cascades at runtime (not just schema-declared) — test coverage check
- SqliteException wrap scope narrow (only 2067 on activate, not wide-catch all DB errors)
- Extension-type ID usage: stores accept/return extension-type IDs, not raw Strings (compile-time safety)
- Riverpod provider keepAlive rationale sound (process singleton for AppDatabase, store-provider keepAlive for invalidate-storm avoidance per STATE.md)
- IdGenerator constructor asymmetry (SessionStore + RevealedTileStore have IdGenerator, MarkerStore doesn't — pre-allocated MarkerId) — correctness rationale

Output format: same as Agent #1.

**Agent #4 — Tests + fixtures + tooling + CLAUDE.md sweep**
Scope:
- Test quality across all of Phase 03 (`test/domain/**` + `test/infrastructure/**`): assertions réelles vs placebo, mock correctness, `@Tags(['migration'])` discipline, `dart_test.yaml`
- `test/fixtures/` (drift_schemas/, json/session_v{1,2}.json, markers_only_v1.json, mirk_style_unknown_renderer.json, db_seed/v1_baseline.sql — verify 70 rows, cohérence with frozen V2 schema)
- `tool/check_domain_purity.dart` + `tool/test/check_domain_purity_test.dart` (verify covers exit codes 0/1/2 per STATE.md Phase 01 contract; verify grep patterns robust against codegen `.g.dart` / `.freezed.dart`)
- `pubspec.yaml` Phase 03 deltas (drift 2.32.1, drift_flutter, sqlite3_flutter_libs, freezed 3.2.5, json_serializable, build_runner, custom_lint 0.8.1, riverpod_lint 3.1.0, riverpod_generator 4.0.0+1) + `dependency_overrides analyzer ^10.0.0 + dart_style 3.1.7` — verify pinning discipline (no `^` or `~` on direct deps)
- `analysis_options.yaml` (custom_lint plugin declared)
- `lib/config/constants.dart` deltas (kDbFilename, kDbBackupDirName, kMaxDbBackups, kDbBusyTimeoutMs — verify NO magic numbers hors constants.dart in Phase 03 code)
- `DEPENDENCIES.md` spot-check of Phase 03 additions (drift / drift_flutter / sqlite3_flutter_libs / freezed / build_runner / custom_lint / riverpod_lint / riverpod_generator)
- CLAUDE.md anti-patterns sweep on all Phase 03 `.dart` code:
  - Naming conventions: `xxxFilename` (absolute path) vs `xxxFileName` (no ext) vs `xxxBasename` (with ext) vs `xxxDir`; `valueByKey` for Maps; `xxxSet` for Sets; `xxxs` for Lists
  - Magic numbers outside `constants.dart`
  - DTOs sans sémantique distincte (réplique 1:1 = doublon)
  - Wrappers sans valeur ajoutée (80%+ des méthodes = `_a.sameMethod(sameArgs)`)
  - Commentaires narrant le quoi (pas le pourquoi)
  - Early returns vs nested ifs
  - Timeouts sur appels externes
  - Async + BuildContext guard (`if (!context.mounted) return;`) — but note Phase 03 has no widgets
- **Additional investigation** (per RESEARCH Open Question #3): search for ADDITIONAL `UnimplementedError` throws, flaky Windows-specific tests, analyzer-plugin silent-degrade analogues beyond the 3 pre-classified — report as structured findings.

Output format: same as Agent #1.

**Claude-main role after agents return:**
1. Receive 4 structured findings lists + 4 narrative appendices + Agent #1 adversarial poison verification block
2. Deduplicate overlapping findings — if the same file:line appears in two agents, PRESERVE BOTH with explicit cross-reference per Phase 02 Accumulated Decision "Cross-lens finding overlap handling convention" (do NOT merge)
3. Group into §2 by sub-section (Pre-known + Agent #1..#4)
4. Inline the 4 narrative appendices inside `<details><summary>Audit Notes</summary>` block
5. Save Agent #1's adversarial poison verification block at the end of Agent #1 sub-section with heading `**Adversarial poison verifications (consumed by Plan 04-04):**` — Plan 04-04 Task 1 + Task 2 read this block to confirm poisons still apply.
6. Present findings to user as TITLES + 1-line explanation ONLY (no diffs, no code blocks, no file contents)
7. Let user choose per finding: `fix` / `waived (rationale)` / `defer-to-phase-X` / `won't-fix`
8. Write §3 triage table with user decisions
9. Commit §2 + §3: `docs(04-rev): record audit findings and triage decisions`

**Pre-class 3 entries format (Plan 04-03 Task 1 writes BEFORE spawn):**
```markdown
### Pre-known from VERIFICATION

Pre-classified from `03-VERIFICATION.md §Outstanding minor items` (3 entries) + any runtime walk deviations flagged by Plan 04-02 (N more entries).

| # | Finding | Severity | Source | Notes |
|---|---------|----------|--------|-------|
| P1 | Flaky `backup_test.dart::rotate keeps the 3 newest when 4 exist` — 1 failure / ~30 runs on Windows parallel; mtime-ordering fragility | Blocker | 03-VERIFICATION.md §Outstanding minor items #1 | Fix candidate: `Future.delayed(10ms)` between backup file creations OR sort by filename (contains `_hhmm.ss_` timestamp). Agent #1 ESCALATES to Blocker-runtime if `DbBackupService.rotate` ALSO uses mtime. |
| P2 | `custom_lint` silently degraded under analyzer-10 (API rename `Element2` breaks `custom_lint_core` 0.8.1) | Noted | 03-VERIFICATION.md §Outstanding minor items #2 | Accept + document in STATE.md Accumulated Decisions + add DEPENDENCIES.md marker. `flutter analyze --fatal-infos --fatal-warnings` stays green via analyzer-10 stack. Re-verify at each deps bump + Phase 15 latest. |
| P3 | `computeRevealMask` throws `UnimplementedError` (Phase 09 scope) | Should | 03-VERIFICATION.md §Outstanding minor items #3 | Add permanent test guard `test/domain/compute_reveal_mask_no_callers_test.dart` scanning `lib/` + `test/` for callers outside the definition site. Removed when Phase 09 implements. |
| P4 (if applicable) | <Runtime walk deviation from Plan 04-02> | Blocker | 04-02 §1b observed | <details of deviation, expected vs observed> |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-class 3 VERIFICATION candidates (+ any walk deviations) into §2 BEFORE spawning agents — MUST commit first</name>
  <files>.planning/phases/04-review-gate-persistence/04-REVIEW.md</files>
  <action>
    PREREQUISITE: verify Plans 04-01 and 04-02 are complete — `04-REVIEW.md` §1 populated AND §1b populated. If not, STOP and instruct user to complete prior plans.

    Edit `04-REVIEW.md` §2 `Pre-known from VERIFICATION` sub-section, replacing the `(pending — 3 entries: flaky... | custom_lint... | computeRevealMask...)` placeholder with the 3-entry (or 4-entry if walk deviations exist) table from `<interfaces>` above.

    Sources:
    - Entry P1 (flaky backup rotate) — verbatim from `03-VERIFICATION.md §Outstanding minor items #1` (line ~159 "The Windows-parallel flakiness of backup_test.dart::rotate keeps the 3 newest when 4 exist")
    - Entry P2 (custom_lint silently degraded) — verbatim from `03-VERIFICATION.md §Outstanding minor items #2` (line ~160 "custom_lint is confirmed silently degraded in 03-06-SUMMARY")
    - Entry P3 (computeRevealMask UnimplementedError) — verbatim from `03-VERIFICATION.md §Outstanding minor items #3` (line ~161 "computeRevealMask throws UnimplementedError by design (Phase 09 scope)")
    - Entry P4..Pn (optional) — any deviations flagged by Plan 04-02 runtime walk (e.g. journal_mode != wal, table missing, `DbBackupService.rotate` mtime dependency if Agent #1 pre-analysis already suspects it). Read 04-02-SUMMARY.md to check.

    Rationale for pre-class first: Phase 02 Plan 02-02 implicitly did this by seeding agent scopes with known items from Phase 01 context. Phase 04 makes it explicit: agents would otherwise all 4 re-discover the flaky test (appears in test runner output), the custom_lint degrade (pubspec dependency_overrides comment), and computeRevealMask (UnimplementedError grep hit). Pre-class saves ~2-4 redundant finding instances per agent and lets their attention focus on adjacencies (other flaky Windows tests, other UnimplementedError stubs, other silently-degraded analyzer plugins).

    The pre-class table MUST be written verbatim BEFORE spawning any Agent tool call. This is a hard ordering constraint from CONTEXT §specifics and RESEARCH Pitfall-family.

    Commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): pre-class VERIFICATION candidates into §2" --files .planning/phases/04-review-gate-persistence/04-REVIEW.md
    ```

    This commit is the hard gate. Task 2 MUST NOT start until this commit lands.

    Anti-pattern guards:
    - Do NOT include fix decisions in the pre-class table — just severity + source. §3 triage (Task 3) decides fix/waive.
    - Do NOT spawn any Agent tool call in this task — pure documentation edit.
    - Do NOT paraphrase the 03-VERIFICATION text — copy-paste the observation language, adapt minimally (e.g. fold line breaks for the table cell).
    - If runtime walk in Plan 04-02 returned all-expected values and no deviations, DO NOT invent P4 — pre-class stays at 3 entries.
  </action>
  <verify>
    <automated>bash -c 'grep -q "^### Pre-known from VERIFICATION" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; grep -qE "P1.*flaky.*backup.*rotate" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; grep -q "custom_lint" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; grep -q "computeRevealMask" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; git log --oneline -5 -- .planning/phases/04-review-gate-persistence/04-REVIEW.md | grep -q "pre-class VERIFICATION"'</automated>
  </verify>
  <done>
    §2 `Pre-known from VERIFICATION` sub-section contains at minimum the 3 pre-class entries (P1 flaky backup, P2 custom_lint silently-degraded, P3 computeRevealMask). If Plan 04-02 runtime walk flagged deviations, those are added as P4+. Placeholder `(pending — 3 entries: ...)` no longer appears. Commit `docs(04-rev): pre-class VERIFICATION candidates into §2` is on `main`. Task 2 is now unblocked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Spawn 4 sub-agents in ONE tool-use message (parallel) and consolidate findings into §2</name>
  <files>.planning/phases/04-review-gate-persistence/04-REVIEW.md</files>
  <action>
    PREREQUISITE: verify Task 1 committed (`docs(04-rev): pre-class VERIFICATION candidates into §2` in `git log`). If not, STOP.

    In a **single tool-use message** (NOT 4 separate messages — parallelism is lost otherwise per 02-RESEARCH Anti-Patterns + CONTEXT §specifics "4 sub-agents are launched in one tool-use message"), spawn 4 sub-agents using the `Agent` / `Task` tool. Each gets:

    1. The scope block from `<interfaces>` above — verbatim paste into the agent prompt
    2. A reminder of CLAUDE.md rules (no emoji, 160-char lines, GOSL header on .dart only, naming conventions, licences interdites: GPL/AGPL)
    3. The output contract: structured findings list (`[severity] Title — 1-line explanation — file:line`) + narrative appendix, returned directly as the agent's final message (NO report .md file creation — per Claude's default Notes: "Do NOT Write report/summary/findings/analysis .md files. Return findings directly as your final assistant message")
    4. An explicit mention of the 3 pre-classified items (P1/P2/P3) with instruction: **do NOT re-flag these; DO look for adjacencies** (additional flaky Windows tests, additional UnimplementedError stubs, additional silently-degraded analyzer plugins)
    5. Cross-lens instruction: if Agent finds a same-line issue that another concern-slice agent would logically catch too, flag it anyway — dedup happens centrally per "Cross-lens finding overlap handling convention"

    All 4 agents are `agent_type="general-purpose"` (no exceptions — STATE.md Phase 02 Accumulated Decision).

    Agent-specific additions:
    - Agent #1 MUST return an `Adversarial poison verifications (consumed by Plan 04-04):` block at the end of its findings, confirming the 2 CI-poison recipes from CONTEXT still apply to Phase 03 code as-is (file paths, column-add location, grep patterns) — any drift flagged explicitly.
    - Agent #2 MUST run `dart run custom_lint` directly to verify P2 silent-degrade state has not changed. Output of that run goes in Agent #2 narrative appendix.
    - Agent #4 MUST search for additional `UnimplementedError` throws and flaky test candidates beyond P3 — report findings structured.

    After all 4 return:
    1. Parse each agent's structured findings list
    2. Deduplicate: if same file:line appears in 2 agents, keep both with explicit cross-reference note (e.g. "Cross-ref: also flagged by Agent #N under concern Y")
    3. Assemble `04-REVIEW.md §2` under the four sub-sections (Agent #1..#4), preserving the Pre-known from VERIFICATION sub-section Task 1 just committed
    4. Inline the 4 narrative appendices inside `<details><summary>Audit Notes</summary>` block
    5. Save Agent #1's adversarial poison verification block at the end of Agent #1 sub-section with heading `**Adversarial poison verifications (consumed by Plan 04-04):**`
    6. Count findings by severity (Blockers / Shoulds / Coulds / Noted). Sanity check: zero Blockers + zero Shoulds across 4 agents is suspicious for a 40+ `.dart` + 25 test Phase 03 audit — flag to user before committing. Expected: at minimum 2-5 Blockers + 10-20 Shoulds based on Phase 02 precedent scaled to Phase 03's ~2× codebase.
    7. Commit:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): record audit findings from 4 parallel sub-agents" --files .planning/phases/04-review-gate-persistence/04-REVIEW.md
       ```

    IMPORTANT: do NOT present findings to user yet — Task 3 below handles the user-facing presentation + triage separately. Keep them as §2 entries only.

    Anti-pattern guards:
    - Send ALL 4 Agent tool calls in ONE assistant message (single tool-use block containing 4 Agent invocations). Serial messages = lost parallelism = single-thorough-Explore-in-disguise.
    - Do NOT let any agent silently expand scope. If Agent #1 wants to opine on domain purity, that's Agent #2's job — cross-ref note, don't re-own.
    - If Agent #1 finds `DbBackupService.rotate` uses `File.lastModifiedSync()` mtime, ESCALATE pre-class P1 to Blocker-runtime + Blocker-test (add a new finding entry reflecting runtime scope) per RESEARCH Pitfall 7 instruction.
    - Monitor wall-clock: if audit exceeds 40 min (Phase 02 was 25 min for smaller codebase per STATE.md), note for 04-03-SUMMARY.md. If agent times out, serialize fallback (subsequent agents on separate messages) — Phase 04 makes this a documented acceptable fallback.
  </action>
  <verify>
    <automated>bash -c 'grep -cE "^\[(Blocker|Should|Could|Noted)\]" .planning/phases/04-review-gate-persistence/04-REVIEW.md | awk "{ if (\$1 &gt; 5) exit 0; else exit 1 }"'</automated>
  </verify>
  <done>
    §2 of `04-REVIEW.md` contains structured findings from all 4 agents (grep count for severity markers > 5, realistic floor). Narrative appendix filled inside `<details>` block. Adversarial poison verifications captured at end of Agent #1 sub-section. Pre-class 3 entries (from Task 1) remain intact and unchanged. §1 + §1b remain unchanged. Commit `docs(04-rev): record audit findings from 4 parallel sub-agents` logged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Present findings as titles to user + capture triage decisions into §3</name>
  <files>.planning/phases/04-review-gate-persistence/04-REVIEW.md</files>
  <action>
    Claude-main converts §2 (pre-class + 4 agent sub-sections) into a user-facing presentation: one line per finding, format `[severity] Title — 1-line explanation`. NO file contents, NO diffs, NO code blocks. User reviews and decides per finding.

    Claude posts in chat, ordered by severity (Blockers first, then Shoulds, then Coulds, then Noted), pre-class entries flagged distinctly:

    > "Audit terminé (pre-class + 4 sub-agents en parallel). Voici les findings — **titres + explication courte uniquement**, tu choisis quoi fixer. Je n'affiche pas de diffs : c'est à toi de décider.
    >
    > **Pre-class from VERIFICATION (3 entries):**
    > P1. [Blocker] Flaky `backup_test.dart::rotate` — <explanation> — source: 03-VERIFICATION.md
    > P2. [Noted] `custom_lint` silently degraded — <explanation> — source: 03-VERIFICATION.md
    > P3. [Should] `computeRevealMask` UnimplementedError Phase 09 scope — <explanation> — source: 03-VERIFICATION.md
    > (P4+ if Plan 04-02 walk deviations)
    >
    > **Blockers (doivent être fixés avant Phase 05, waiver interdit):**
    > 1. [Blocker] <Title> — <1-line explanation> — <file:line>
    > ... (from Agent #N sub-section)
    >
    > **Shoulds (fortement recommandé, waivable avec rationale inline):**
    > N. [Should] ...
    >
    > **Coulds (bas-coût-haut-bénéfice, optionnel):** ...
    > **Noted (observation seule, pas d'action demandée):** ...
    >
    > Pour chaque finding, réponds : `fix` / `waived (rationale)` / `defer-to-phase-X` / `won't-fix`. Tous les Blockers sont implicitement `fix` sauf si tu exprimes un disagreement explicite (auquel cas on rediscute avant de bouger — pas de waiver direct sur Blocker).
    >
    > Si ça te va aussi comme Phase 02 : 'fix tous les Blockers + Shoulds + Coulds' blanket-approve en un mot (j'en ai noté ça comme pattern dans STATE.md Accumulated Decisions) — tu gardes le droit de pinpoint-waiver si un finding spécifique te gêne."

    After user responds with per-finding decisions (or blanket-approve), Claude:
    1. Builds §3 triage table — one row per finding in §2 (pre-class P1-Pn + Agent-scoped entries)
    2. Validates: every Blocker MUST be `fix` (Blocker-waiver forbidden per CONTEXT.md — if user attempts to waive a Blocker, Claude pushes back and asks to reclassify to Should or accept the fix)
    3. Validates: every `waived` Should has an inline rationale (not just "ok")
    4. Writes §3 to `04-REVIEW.md` (replacing the `(pending)` placeholder in the triage table)
    5. Commits:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): record triage decisions for N findings" --files .planning/phases/04-review-gate-persistence/04-REVIEW.md
       ```
    6. Summarizes back to user with counts per decision category (X fix / Y waived / Z deferred / W won't-fix / V noted)

    Anti-pattern guards:
    - Never present findings as diffs, code blocks, or file contents — only titles + 1-line explanation. This is the "user chooses what to fix" contract from CLAUDE.md §Code Review Phases.
    - Pre-class entries (P1, P2, P3, P4+) appear in the triage table with their P# labels preserved so traceability from 03-VERIFICATION.md / 04-02 walk is preserved.
    - If user blanket-approves ("fix tous") but a Noted has severity Noted (observation only), it stays `noted` in the triage table, not `fix` — Noted never flips to fix unless user explicitly re-classifies it.
  </action>
  <verify>
    <automated>bash -c 'grep -qE "^\| [P0-9]+ \|" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; ! grep -q "| (pending) |" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; git log --oneline -5 -- .planning/phases/04-review-gate-persistence/04-REVIEW.md | grep -q "record triage decisions"'</automated>
  </verify>
  <done>
    §3 triage table has at least one row with `| N |` or `| PN |` label (grep matches). Placeholder `| (pending) |` no longer appears. Every Blocker row has Decision=`fix`. Every row with Decision=`waived` has non-empty Rationale. Pre-class entries P1-Pn have their labels preserved. Commit `docs(04-rev): record triage decisions for N findings` on `main`. User has signaled "triage validé, go adversarial" (or equivalent) to unblock Plan 04-04.
  </done>

  <what-built>
    Claude-main converts §2 into user-facing presentation (titles + 1-line explanations, no diffs), user triages per finding, Claude records decisions in §3 table, commits.
  </what-built>
  <how-to-verify>
    - §3 table in `04-REVIEW.md` has one row per finding from §2 (pre-class P1-Pn + agent-scoped entries), every row has Decision + Rationale filled
    - `git log --oneline -1` shows `docs(04-rev): record triage decisions`
    - No `| (pending) |` placeholder remains in §3
    - User has confirmed the chosen disposition per finding (or blanket-approve signal)
  </how-to-verify>
  <resume-signal>
    User types "triage validé, go adversarial" or equivalent to unblock Plan 04-04.
    If user wants to revisit any finding, Claude re-opens §3 and re-commits before moving to Plan 04-04.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write 04-03-SUMMARY.md carrying adversarial poison verifications forward + triage summary for Plan 04-05</name>
  <files>.planning/phases/04-review-gate-persistence/04-03-SUMMARY.md</files>
  <action>
    Create `04-03-SUMMARY.md` capturing:

    - **Pre-class source traceability:** 3 entries from 03-VERIFICATION.md + any runtime walk deviations from 04-02
    - **Agent type selection per sub-agent:** all 4 `general-purpose` (per STATE.md Phase 02 Accumulated Decision locked for review-gate consistency)
    - **Wall-clock time per agent** (if capturable from tool timing) — useful signal for future review gates
    - **Total findings count per severity:** X Blockers / Y Shoulds / Z Coulds / W Noted across 4 agents + 3-4 pre-class
    - **Cross-lens overlap occurrences:** count of same-line findings flagged by multiple agents (preserved with cross-ref, not deduplicated)
    - **Adversarial poison verifications (consumed by Plan 04-04):** verbatim copy of Agent #1's verification block — confirms the 2 CI-poison recipes from CONTEXT still apply to Phase 03 code, any drift explicit:
      - Test #1 `adversarial/04-domain-import-flutter-and-drift`: `lib/domain/sessions/session.dart` + `lib/domain/markers/marker.dart` still exist, imports-section locations confirmed
      - Test #2 `adversarial/04-schema-drift-stale`: `lib/infrastructure/db/app_database.dart` still has the `t_sessions` table declaration, column-addition site confirmed
    - **Triage summary:** N to-fix, M waived (with rationale snippets), K deferred, L won't-fix, V noted
    - **Escalation flags (if any):**
      - P1 escalation: did Agent #1 confirm `DbBackupService.rotate` uses mtime? If yes, P1 becomes Blocker-runtime + Blocker-test (two entries in §3)
      - P2 re-check: did Agent #2 confirm `custom_lint` still fails to load? What error class?
      - Additional UnimplementedError stubs found by Agent #4?
    - **Any scope overflow / surprise observations** worth carrying forward (e.g. a concern that a future plan should track)

    Do NOT prepend the GOSL Dart header (markdown file).

    Commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): summarize Wave 3 audit + triage (04-03-SUMMARY.md)" --files .planning/phases/04-review-gate-persistence/04-03-SUMMARY.md
    ```
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/04-review-gate-persistence/04-03-SUMMARY.md &amp;&amp; grep -q "Adversarial poison verifications" .planning/phases/04-review-gate-persistence/04-03-SUMMARY.md &amp;&amp; grep -qE "(Blockers|Shoulds|Coulds)" .planning/phases/04-review-gate-persistence/04-03-SUMMARY.md'</automated>
  </verify>
  <done>
    `04-03-SUMMARY.md` exists on `main`. Contains adversarial poison verifications block (Plan 04-04 reads this for Test #1 + Test #2). Contains triage summary (Plan 04-05 reads this for fix-loop planning). Any P1 escalation decision, additional UnimplementedError findings, or custom_lint state update documented.
  </done>
</task>

</tasks>

<verification>
- §2 `Pre-known from VERIFICATION` filled BEFORE agent spawn (Task 1 commit precedes Task 2 commit in git log)
- §2 has findings from all 4 agents + narrative appendix + Agent #1 adversarial poison verification block
- §3 triage table is populated; every Blocker = `fix`, every waived Should has rationale, pre-class P# labels preserved
- Findings presented as titles + explanations (not diffs) — verified by user "triage validé" signal
- `04-03-SUMMARY.md` exists with adversarial poison verifications (Plan 04-04 input) + triage summary (Plan 04-05 input) + P1 escalation decision
- 4 commits on `main`: pre-class / agent findings / triage / summary
- User has unblocked Plan 04-04
</verification>

<success_criteria>
Plan 04-03 complete when:
1. §2 pre-class 3 (or 4+) entries committed to `main` BEFORE any Agent tool call
2. 4 sub-agents spawned in a single parallel tool-use message (verifiable by reviewing the execution transcript — not by file check), all `general-purpose`
3. `04-REVIEW.md` §2 populated with structured findings + narrative appendix + adversarial poison verification block
4. `04-REVIEW.md` §3 triage table populated with user decisions (pre-class P# labels preserved; every Blocker = `fix`)
5. Findings presented as titles + explanations (not diffs) — verified by user resume signal
6. `04-03-SUMMARY.md` carries adversarial poison verifications to Plan 04-04 + triage summary to Plan 04-05
7. User has explicitly unblocked Plan 04-04 via "triage validé, go adversarial" signal
</success_criteria>

<output>
After completion, `.planning/phases/04-review-gate-persistence/04-03-SUMMARY.md` exists with:
- Pre-class 3 source traceability
- Agent #1..#4 type selection + wall-clock
- Finding counts per severity
- Cross-lens overlap count
- Adversarial poison verifications (Plan 04-04 input)
- Triage summary (Plan 04-05 input)
- Escalations (P1 runtime mtime? custom_lint state? additional UnimplementedError?)
</output>
</content>
</invoke>