---
phase: 04-review-gate-persistence
plan: 05
type: execute
wave: 5
depends_on:
  - 04-03
  - 04-04
files_modified:
  - .planning/phases/04-review-gate-persistence/04-REVIEW.md
  - .planning/phases/04-review-gate-persistence/.fixes-expected
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - DEPENDENCIES.md
  - test/domain/compute_reveal_mask_no_callers_test.dart
  - test/infrastructure/db/backup_test.dart
autonomous: false
requirements:
  - SC#1
  - SC#2
  - SC#4
must_haves:
  truths:
    - "BEFORE any mutation of §3 triage rows, an integer snapshot of the `| fix |` row count is written to `.planning/phases/04-review-gate-persistence/.fixes-expected` (Phase 02 Plan 04 precedent — live grep drifts as rows flip to `| fix (done <hash>) |`)"
    - "Every finding triaged as `fix` in §3 has a corresponding atomic commit on `main` with prefix `fix(04-rev):` or `refactor(04-rev):` or `test(04-rev):` (NEVER `docs(04-rev):` for fixes — docs commits are excluded from fix tally)"
    - "Pre-class fixes applied with the correct commit type:"
    - "  - Blocker #1 flaky `backup_test.dart` — `test(04-rev):` if test-only deterministic fix OR `fix(04-rev):` + `test(04-rev):` (runtime + test) if P1 escalation revealed mtime dependency in production `DbBackupService.rotate`"
    - "  - Noted #2 `custom_lint` silently-degraded — 2 `docs(04-rev):` commits (DEPENDENCIES.md + STATE.md Accumulated Decisions) — EXCLUDED from fix tally"
    - "  - Should #3 `computeRevealMask` no-callers guard — `test(04-rev): add computeRevealMask no-callers guard` with new file `test/domain/compute_reveal_mask_no_callers_test.dart`"
    - "Every fix commit passes CI green BEFORE the next is pushed (bisectable, revertable per-finding)"
    - "After all fixes applied, the final commit on `main` has CI green on all 3 jobs (gates + android + ios)"
    - "§5 populated: final commit hash + CI run URL + `All 3 jobs green` status + date"
    - "`04-REVIEW.md` header: `**Status:** open` → `closed`; `**Closed:** (pending)` → `**Closed:** 2026-04-18`"
    - "Zero Blockers remain unfixed (CONTEXT.md Blocker-waiver forbidden)"
    - "Every waived Should has inline rationale in §3 (re-verified here; already enforced in Plan 04-03)"
    - "`.planning/STATE.md` Accumulated Decisions contains a new entry for `custom_lint silently-degraded` acceptance (Phase 04 Noted #2) + Phase 04 completion marker"
    - "`.planning/ROADMAP.md` Phase 04 row flipped to `Complete` with date 2026-04-18"
    - "`.fixes-expected` scratch file DELETED at phase closure (matches Phase 02 Plan 04 lifecycle)"
    - "Phase 05 is unblocked — gsd-verifier can read §5 + §3 + §4 + final CI green to sign Phase 04 complete"
  artifacts:
    - path: ".planning/phases/04-review-gate-persistence/04-REVIEW.md"
      provides: "Final state — status=closed, §5 filled, all 5 sections complete"
      contains: "**Status:** closed"
      min_lines: 280
    - path: "DEPENDENCIES.md"
      provides: "New marker for custom_lint silently-degraded (Phase 04 Noted #2 acceptance)"
      contains: "silently-degraded"
    - path: ".planning/STATE.md"
      provides: "Accumulated Decisions new entry for custom_lint silent-degrade + Phase 04 completion"
      contains: "Phase 04-review-gate-persistence"
    - path: "test/domain/compute_reveal_mask_no_callers_test.dart"
      provides: "Permanent guard test until Phase 09 implements computeRevealMask"
      contains: "computeRevealMask is unimplemented until Phase 09"
  key_links:
    - from: "`.fixes-expected` snapshot (integer count)"
      to: "Task 2 fix-commit tally verification"
      via: "snapshot taken BEFORE any §3 mutation; verify command compares snapshot vs `git log --grep='^(fix|refactor|test)\\(04-rev\\)'` count"
      pattern: "\\.fixes-expected"
    - from: "`04-REVIEW.md` §3 rows marked `fix`"
      to: "Atomic commits on main"
      via: "`git log --extended-regexp --grep='^(fix|refactor|test)\\(04-rev\\):'`"
      pattern: "^(fix|refactor|test)\\(04-rev\\):"
    - from: "Final commit on main (after fixes)"
      to: "CI green run URL in §5"
      via: "`gh run view <id>` confirms all 3 jobs green (gates / android / ios)"
      pattern: "All 3 jobs green"
    - from: "`04-REVIEW.md` status header"
      to: "gsd-verifier gate-closed check"
      via: "status transition `open` → `closed` + `.fixes-expected` deletion + Phase 05 unlock"
      pattern: "\\*\\*Status:\\*\\* closed"
---

<objective>
Close the review loop for Phase 04: apply every finding triaged as `fix` in §3 as atomic commits, gate each commit on CI-green before proceeding to the next, apply the 3 pre-class fixes (Blocker flaky backup test / Noted custom_lint documentation / Should computeRevealMask no-callers guard), confirm the final state on `main` is green on all 3 jobs, archive the closure into §5, mark `04-REVIEW.md` status=closed, update STATE.md + ROADMAP.md, and unblock Phase 05.

Purpose: The gate-closed condition is the whole point of the review. CONTEXT.md makes it explicit: `Blockers + Shoulds fixed + CI green + 04-REVIEW.md committed + runtime walk + adversarial evidence + STATE/ROADMAP updated`. This plan operationalizes that — it's the only way Phase 05 GPS can open. Phase 02 Plan 04 proved the mechanical pattern on 42 fixes in ~307 min wall-clock; Phase 04 re-applies with 3 pre-class fixes plus whatever §3 triage decided.

Output: `04-REVIEW.md` fully complete (5 sections, status=closed), `main` CI green, Phase 04 signed off, STATE.md + ROADMAP.md updated, `.fixes-expected` scratch deleted.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-review-gate-persistence/04-CONTEXT.md
@.planning/phases/04-review-gate-persistence/04-RESEARCH.md
@.planning/phases/04-review-gate-persistence/04-REVIEW.md
@.planning/phases/04-review-gate-persistence/04-03-SUMMARY.md
@.planning/phases/04-review-gate-persistence/04-04-SUMMARY.md
@.planning/phases/02-review-gate-foundation/02-04-PLAN.md
@.planning/phases/02-review-gate-foundation/.fixes-expected
@DEPENDENCIES.md
@CLAUDE.md

<interfaces>
<!-- Fix workflow contract from CONTEXT.md §Fix workflow + gate-closed criteria -->
<!-- Heavily patterned on 02-04-PLAN — main adaptations: 04-rev prefix + `test(04-rev):` added as first-class commit type (SchemaSanityChecker guard + compute_reveal_mask guard are test files) -->

**Atomic commit format:**
```
<type>(04-rev): <title from triage table>

Addresses [Blocker|Should|Could] finding #N from 04-REVIEW.md:
<1-line rationale, if not self-evident>
```

- `type ∈ {fix, refactor, test}` for code/config/test fixes; `docs` ONLY for documentation-only changes that are NOT themselves fixes (e.g. triage-row mutation markers, STATE.md/DEPENDENCIES.md updates, §5 closure — these do NOT count toward fix tally)
- **Counting discipline:** fix-vs-triaged verification uses `^(fix|refactor|test)\(04-rev\):` only. `docs(04-rev):` commits EXCLUDED.
- One finding = one `fix`/`refactor`/`test` commit (unless the finding's fix intrinsically spans a production file + a test file — those go in 2 commits: `fix(04-rev):` then `test(04-rev):` with the same finding # — tally counts BOTH; the `.fixes-expected` snapshot anticipates this by counting `| fix |` rows AND each row emits at least 1 fix commit)
- Each commit passes CI green BEFORE the next is pushed

**Per-commit verification loop (local before push):**
```bash
flutter analyze --fatal-infos --fatal-warnings
dart format --line-length 160 --set-exit-if-changed .
flutter test
dart test test/domain/ test/infrastructure/
dart run tool/check_headers.dart
dart run tool/check_licenses.dart
dart run tool/check_dependencies_md.dart
dart run tool/check_domain_purity.dart
# All 8 must be exit 0

git add <files>
git commit -m "..."
git push

# Watch CI
gh run watch --exit-status
# Only proceed to next finding once conclusion=success
```

**Blocker-waiver forbidden (CONTEXT.md):**
If user tried to waive a Blocker in Plan 04-03 §3 triage, Plan 04-03 already pushed back. If one slipped through (e.g. reclassified to Should with override rationale), treat as Should for commit workflow but flag in 04-05-SUMMARY.md so gsd-verifier can challenge.

**`.fixes-expected` snapshot (critical for verification integrity, Phase 02 precedent):**
Task 1 mutates §3 triage rows as work progresses (`| fix |` → `| fix (done <hash>) |`) to make progress visible. This mutation breaks any grep that counts `| fix |` rows during execution. To preserve ground-truth count, Task 1 writes snapshot BEFORE any mutation:
- Path: `.planning/phases/04-review-gate-persistence/.fixes-expected`
- Content: single integer (`| fix |` row count at start of Task 1, from triage rows ONLY — exclude `| noted |`, `| waived |`, `| defer-to-phase-X |`, `| won't-fix |`)
- Lifecycle: written once at start of Task 1, read by Task 1 verify, deleted at end of Task 2 (phase closure)

**Pre-class fixes (mandatory, derived from 04-REVIEW.md §2 Pre-known from VERIFICATION + §3 triage):**

**P1 — Flaky `backup_test.dart::rotate keeps the 3 newest when 4 exist` (Blocker):**
Standard path (test-only): add determinism. Options:
1. `Future.delayed(Duration(milliseconds: 10))` between consecutive `takeBackup()` calls in the test (normalizes Windows ~10ms mtime resolution) — 1 commit: `test(04-rev): stabilize backup rotate test against Windows mtime precision`
2. Sort by filename (which contains `yyyymmdd_hhmm.ss_` timestamp — already sortable) instead of mtime — 1 commit, may also require runtime rewrite if `DbBackupService.rotate` uses mtime

Escalation path (runtime + test): if Agent #1 (Plan 04-03) confirmed `DbBackupService.rotate` uses `File.lastModifiedSync()` mtime, this becomes TWO fixes:
- `fix(04-rev): rotate DbBackup by filename timestamp, not mtime` (runtime, `lib/infrastructure/db/backup.dart`)
- `test(04-rev): align backup rotate test with filename-timestamp ordering` (test, `test/infrastructure/db/backup_test.dart`)

Check 04-03-SUMMARY.md for the P1 escalation decision; apply accordingly.

**P2 — `custom_lint` silently-degraded (Noted) — DOCUMENTATION ONLY:**
Two `docs(04-rev):` commits (EXCLUDED from fix tally):

1. Add/update a marker in `DEPENDENCIES.md` for `custom_lint` (and `custom_lint_core`) indicating silently-degraded status. Choose format per CONTEXT §Claude's Discretion — suggested: add a `**Note:**` column cell or a table footnote:
   ```markdown
   **Note:** `custom_lint` 0.8.1 silently degrades under analyzer-10 (API rename `Element2` breaks `custom_lint_core`). `flutter analyze --fatal-infos --fatal-warnings` still green via analyzer-10 stack. Re-verify at each deps bump + Phase 15 polish latest.
   ```
   Commit: `docs(04-rev): mark custom_lint silently-degraded in DEPENDENCIES.md`

2. Add entry to `.planning/STATE.md ## Accumulated Decisions`:
   ```markdown
   - [Phase 04-review-gate-persistence]: `custom_lint` silently-degraded (analyzer-10 API break) formally accepted as Noted in REVIEW.md §3 — no operational impact (`flutter analyze --fatal-infos --fatal-warnings` green via analyzer-10 stack). Re-verify at each deps bump + Phase 15 polish latest. Promoting the existing Phase 03 STATE.md decision to a Phase 04 review-gate-signed-off status.
   ```
   Commit: `docs(state): accept custom_lint silently-degraded as Phase 04 Noted`

**P3 — `computeRevealMask` UnimplementedError no-callers guard (Should):**
Create `test/domain/compute_reveal_mask_no_callers_test.dart` per 04-RESEARCH.md Example 2 reference pattern (pure-Dart `File + regex` variant — no `rg` process dep):

```dart
// Copyright (c) 2026 THONGVAN Alexis
// Licensed under the Good Old Software License v1.0
// See LICENSE file for details
//
// WORKAROUND: guard temporaire jusqu'à Phase 09 où computeRevealMask sera
// implémenté + ce test supprimé. CLAUDE.md §Workarounds.

import 'dart:io';

import 'package:test/test.dart';

void main() {
  test('computeRevealMask has no callers outside its definition site (Phase 09 guard)', () {
    const definitionSite = 'lib/domain/revealed/reveal_calculator.dart';
    const selfSite = 'test/domain/compute_reveal_mask_no_callers_test.dart';
    final callers = <String>[];
    for (final root in ['lib', 'test']) {
      for (final entity in Directory(root).listSync(recursive: true)) {
        if (entity is! File || !entity.path.endsWith('.dart')) continue;
        final normalizedPath = entity.path.replaceAll(r'\', '/');
        if (normalizedPath.endsWith(definitionSite)) continue;
        if (normalizedPath.endsWith(selfSite)) continue;
        if (entity.readAsStringSync().contains('computeRevealMask')) {
          callers.add(normalizedPath);
        }
      }
    }
    expect(callers, isEmpty,
        reason: 'computeRevealMask is unimplemented until Phase 09. Callers found: $callers');
  });
}
```

Commit: `test(04-rev): add computeRevealMask no-callers guard for Phase 09 scope`

**§5 format (target for Task 2):**
```markdown
## 5. CI-green confirmation

- **Final commit on main:** `<hash>` (after all Blocker + non-waived Should fixes)
- **CI run URL:** https://github.com/ThongvanAlexis/GOSL-MirkFall/actions/runs/<id>
- **Status:** All 3 jobs green (gates / android / ios)
- **Date:** 2026-04-18

---
_Phase 04 closed: 2026-04-18_
_Phase 05 unblocked._
```

**Status transition:**
At the very end, update `04-REVIEW.md` header:
- `**Status:** open` → `**Status:** closed`
- `**Closed:** (pending)` → `**Closed:** 2026-04-18`

Commit the status flip AFTER §5 is filled and CI is confirmed green — it's the closing signal.

**ROADMAP.md Phase 04 row update:**
```
| 04. Review Gate — Persistence | N/N | Complete    | 2026-04-18 |
```
(N = actual plan count, filled at update time)

**Gate-closed fail-fast verification (Task 2 `<verify>`, sourced from CONTEXT §Fix workflow & gate-closed criteria):**
The Task 2 verify block enforces ALL 5 gate-closed criteria enumerated in CONTEXT §Fix workflow — not a subset. The check composes with `set -e` so the FIRST failing criterion aborts before the status flip commit is attempted. Enumerated criteria:

1. **5 top-level sections present** — `grep -c '^## [1-5]\.' == 5` (locked pattern from 02 gsd-verifier contract, carried Phase 04)
2. **§1b runtime walk has all 6 PRAGMA outputs archived** — `awk '/^### 1b\./,/^## 2\./' 04-REVIEW.md | grep -c PRAGMA >= 6` (6 because the canonical §1b format lists 5 mandatory PRAGMAs + 1 `page_size` for journal proof)
3. **§4 has exactly 3 evidence blocks** — Test #1 CI run URL + Test #2 CI run URL + Test #3 commit hash. `awk '/^## 4\./,/^## 5\./' 04-REVIEW.md | grep -cE '(https://github.com|commit:)' == 3`
4. **Every §3 row with `| Blocker |` severity has `| fix |` as Decision** (no waivers for Blockers per CONTEXT §Fix workflow + §Gate-closed). Extract §3 Blocker rows, assert each contains the `| fix |` token (or `| fix (done ` for already-processed rows — both variants count as "fix").
5. **Status + placeholders + ROADMAP + STATE + DEPENDENCIES + .fixes-expected** (existing checks, preserved)

If ANY of 1-5 fails, verify exits non-zero and the gate does NOT close. This is deliberately strict: the Task 2 `<automated>` block is the last machine-verifiable gate before Phase 05 opens; a lax check here destroys the review-gate contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot `.fixes-expected` → apply all fix-triaged findings as atomic commits, CI-gated</name>
  <files>(dynamic — determined by §3 triage table in 04-REVIEW.md + the 3 pre-class fix targets. Typically: `test/infrastructure/db/backup_test.dart`, `lib/infrastructure/db/backup.dart` (if P1 escalated), `test/domain/compute_reveal_mask_no_callers_test.dart` (new), `DEPENDENCIES.md`, `.planning/STATE.md`, plus whatever §3 triage chose)</files>
  <action>
    PREREQUISITE: verify Plans 04-03 and 04-04 complete — §3 triage table populated, §4 has 3 evidence blocks, no `adversarial/04-*` branches remain. If not, STOP.

    **Step 0 — Snapshot TOFIX count BEFORE any mutation.** Because Task 1 rewrites triage rows as it progresses (`| fix |` → `| fix (done <hash>) |`), a live grep would drift. Snapshot once at start:
    ```bash
    grep -cE "^\| [PN0-9]+ \|.*\| fix \|" .planning/phases/04-review-gate-persistence/04-REVIEW.md \
      > .planning/phases/04-review-gate-persistence/.fixes-expected
    echo "TOFIX snapshotted: $(cat .planning/phases/04-review-gate-persistence/.fixes-expected) findings to fix"
    ```
    (The `.fixes-expected` file is `.gitignore`'d by convention — scratch state for this plan's verify step only, deleted at end of Task 2. Phase 02 precedent — the Phase 02 `.fixes-expected` file is visible in `git status` as untracked at time of writing, proof the pattern ran.)

    Read `04-REVIEW.md` §3. Build an ordered fix list:
    1. All Blockers (non-waivable, MUST be fixed) — INCLUDING pre-class P1 (flaky backup rotate)
    2. All Shoulds marked `fix` (non-waived) — INCLUDING pre-class P3 (computeRevealMask no-callers guard)
    3. Optional: Coulds marked `fix` (rare — most Coulds are `defer-to-phase-15-polish`)

    Note: pre-class P2 (custom_lint silently-degraded Noted) stays `noted` in triage — its DEPENDENCIES.md + STATE.md entries are `docs(04-rev):` commits, handled in Task 2.

    For each finding in order:
    1. Read file:line from §2 (or from the pre-class table for P1/P3)
    2. Apply the minimal fix addressing the explanation (no scope creep — one finding = one change)
    3. For Phase 04 pre-class fixes, use the patterns specified in `<interfaces>`:
       - **P1 test-only path:** edit `test/infrastructure/db/backup_test.dart` per the chosen determinism strategy
       - **P1 escalated runtime+test path:** two commits, `fix(04-rev):` then `test(04-rev):` — BOTH count toward fix tally
       - **P3:** write `test/domain/compute_reveal_mask_no_callers_test.dart` per `<interfaces>` reference
    4. Run local verification (all 8 commands from `<interfaces>` must exit 0)
    5. Commit with `fix(04-rev):` / `refactor(04-rev):` / `test(04-rev):` prefix (NOT `docs(04-rev):` — docs don't count):
       ```bash
       git add <specific-files>
       git commit -m "fix(04-rev): <title from triage row>

       Addresses [<severity>] finding #<N> from 04-REVIEW.md:
       <1-line rationale if needed>"
       git push
       ```
    6. Watch CI:
       ```bash
       gh run watch --exit-status
       ```
       Wait for `conclusion=success`. If failure, do NOT proceed to next finding — fix the regression (a new atomic commit `fix(04-rev): address regression on finding #N CI failure` is acceptable). Never force the next fix on top of a red CI.
    7. Update §3 triage table: change the row's Decision from `fix` to `fix (done <hash>)` so progress is visible. Commit this mutation separately as `docs(04-rev): mark finding #N as fixed` — it is a documentation commit, NOT a fix, and must NOT inflate the fix tally.

    **Special cases (pattern adapted from Phase 02):**
    - If a fix requires changing `DEPENDENCIES.md` (e.g. subtle Phase 03 entry omission surfaced by Agent #4 spot-check), include corresponding `pubspec.yaml` / `pubspec.lock` change in the SAME commit — otherwise `check_dependencies_md.dart` itself fails CI.
    - If a fix requires `pubspec.yaml` change (new version pin), run `flutter pub get` before committing so `pubspec.lock` is in sync.
    - If Agent #3 flagged magic numbers in Phase 03 code (e.g. hardcoded buffer sizes outside `kBitmapBytes`), extract to `lib/config/constants.dart` with appropriate `k`-prefixed name.
    - If P1 escalates to runtime + test (Agent #1 found mtime dep in `DbBackupService.rotate`), apply runtime fix FIRST, then test fix — test should now pass without determinism workaround.
    - If §3 contains a finding that pre-dates Phase 04 (e.g. a Phase 01 backlog item like MPL-unreachable — DEFERRED per 04-CONTEXT §Hors scope, should NOT be in §3 anyway), it's a scope-overflow. Flag for 04-05-SUMMARY + ignore during fix loop.
    - If NO findings are marked `fix` and pre-class P1/P3 have been triaged `waived` or `defer`, `.fixes-expected` snapshot contains `0`, verify trivially passes. Skip directly to Task 2. Note in 04-05-SUMMARY: "Zero findings required fixing — Phase 04 closed on pure audit + adversarial evidence."

    Throughout: maintain discipline — ONE finding per `fix(04-rev):` / `refactor(04-rev):` / `test(04-rev):` commit, CI green between each, no batch pushes. Anti-pattern guards:
    - Do NOT batch 5 fixes into one commit because "they're all style"
    - Do NOT use `docs(04-rev):` for a code fix (skews the fix tally)
    - Do NOT skip the §3 mutation marker commit (future debugger needs to trace which fix addressed which finding)
  </action>
  <verify>
    <automated>bash -c 'EXPECTED=$(cat .planning/phases/04-review-gate-persistence/.fixes-expected 2>/dev/null || echo 0); APPLIED=$(git log --oneline --extended-regexp --grep="^(fix|refactor|test)\(04-rev\):" | wc -l); echo "expected_fixes=$EXPECTED applied_fix_commits=$APPLIED"; [ "$APPLIED" -ge "$EXPECTED" ]'</automated>
  </verify>
  <done>
    `.fixes-expected` snapshot written at start of task. Every finding marked `fix` in §3 (per the snapshot) has at least one corresponding `fix(04-rev):` / `refactor(04-rev):` / `test(04-rev):` commit on `main`. `docs(04-rev):` commits are NOT counted in the tally. Pre-class P1 applied (test-only OR runtime+test escalation). Pre-class P3 applied (new `compute_reveal_mask_no_callers_test.dart`). CI green on each fix commit before next. Final fix commit has CI green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply P2 documentation fixes + final CI-green confirmation + §5 fill + status=closed + STATE/ROADMAP update + unblock Phase 05</name>
  <files>
    .planning/phases/04-review-gate-persistence/04-REVIEW.md
    .planning/phases/04-review-gate-persistence/.fixes-expected
    .planning/STATE.md
    .planning/ROADMAP.md
    DEPENDENCIES.md
  </files>
  <action>
    After Task 1 applied all code/test fixes, Claude:

    **Step 1 — Apply P2 custom_lint documentation (2 `docs(04-rev):` commits, EXCLUDED from fix tally):**

    1a. Edit `DEPENDENCIES.md` to mark `custom_lint` silently-degraded. Choose format per CONTEXT §Claude's Discretion (suggested: add a `**Note:**` footnote or extra column under the `custom_lint` row):
    ```
    | custom_lint | 0.8.1 | Apache-2.0 | Dev-only static analysis ... | **Note:** silently-degraded under analyzer-10 — `custom_lint_core` API rename `Element2` breaks plugin load. `flutter analyze --fatal-infos --fatal-warnings` green via analyzer-10 stack. Re-verify at each deps bump + Phase 15 polish latest. |
    ```
    Commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): mark custom_lint silently-degraded in DEPENDENCIES.md" --files DEPENDENCIES.md
    ```

    1b. Add entry to `.planning/STATE.md ## Accumulated Decisions` (append at end of the Phase 03 decisions block):
    ```markdown
    - [Phase 04-review-gate-persistence]: `custom_lint` silently-degraded (analyzer-10 API break) formally accepted as Noted in REVIEW.md §3 — no operational impact (`flutter analyze --fatal-infos --fatal-warnings` green via analyzer-10 stack). Re-verify at each deps bump + Phase 15 polish latest. Promoting the existing Phase 03 STATE.md decision to a Phase 04 review-gate-signed-off status.
    ```
    Commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(state): accept custom_lint silently-degraded as Phase 04 Noted" --files .planning/STATE.md
    ```

    **Step 2 — Capture final commit hash on `main`:**
    ```bash
    FINAL_HASH=$(git rev-parse --short HEAD)
    ```

    **Step 3 — Verify most recent CI run is green (the last commit should already have a green run — no need to push trivial commit to re-trigger):**
    ```bash
    RUN_ID=$(gh run list --branch main --limit 1 --json databaseId --jq '.[0].databaseId')
    gh run view $RUN_ID --json conclusion,jobs --jq '{conclusion, jobs: [.jobs[] | {name, conclusion}]}'
    ```
    Expected: `conclusion: success`, all 3 jobs (gates / android / ios) with `conclusion: success`.

    If most recent run is NOT green, do NOT proceed — return to Task 1 and fix the regression.

    **Step 4 — Fill `04-REVIEW.md` §5:**
    ```markdown
    ## 5. CI-green confirmation

    - **Final commit on main:** `<FINAL_HASH>`
    - **CI run URL:** https://github.com/ThongvanAlexis/GOSL-MirkFall/actions/runs/<RUN_ID>
    - **Status:** All 3 jobs green (gates / android / ios)
    - **Date:** 2026-04-18
    ```

    **Step 5 — Flip the header:**
    - `**Status:** open` → `**Status:** closed`
    - `**Closed:** (pending)` → `**Closed:** 2026-04-18`
    - Update trailer: `_Phase 04 closed: 2026-04-18_`

    **Step 6 — Commit closure:**
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): close Phase 04 Review Gate — CI green on <FINAL_HASH>" --files .planning/phases/04-review-gate-persistence/04-REVIEW.md
    ```

    **Step 7 — Present closure summary to user + wait for explicit approval (see `<how-to-verify>` for exact chat prompt):**

    After user approval:

    **Step 8 — Update STATE.md (Phase 04 complete + progress counters):**
    - Update frontmatter `current_plan`, `stopped_at`, `progress.completed_phases` (+1), `progress.total_plans` (+5), `progress.completed_plans` (+5), `progress.percent`
    - Update `## Current Position` block: Phase 05 ready, last activity 2026-04-18

    **Step 9 — Update ROADMAP.md:**
    - Phase 04 row: `| 04. Review Gate — Persistence | 5/5 | Complete    | 2026-04-18 |`
    - Phase 04 checklist: `[x] **Phase 04: Review Gate — Persistence** - Audit phase 03 ... (completed 2026-04-18)`

    **Step 10 — Delete scratch snapshot (Phase 02 precedent lifecycle):**
    ```bash
    rm -f .planning/phases/04-review-gate-persistence/.fixes-expected
    ```

    **Step 11 — Final commit:**
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(state): Phase 04 Review Gate closed — Phase 05 unblocked" --files .planning/STATE.md .planning/ROADMAP.md .planning/phases/04-review-gate-persistence/.fixes-expected
    ```

    Anti-pattern guard: Do NOT mark Phase 04 complete in STATE.md / ROADMAP.md until user has explicitly said "OK close". Skipping this is the single biggest risk of losing the review-gate contract. `.fixes-expected` deletion is ALSO gated on user approval — premature deletion destroys the verification snapshot before Task 1 verify can confirm.
  </action>
  <verify>
    <automated>bash -c 'set -e; REVIEW=.planning/phases/04-review-gate-persistence/04-REVIEW.md; \
      # Gate-closed criterion 1 — 5 top-level sections present (locked Phase 02 gsd-verifier contract)
      [ "$(grep -c "^## [1-5]\." $REVIEW)" = "5" ]; \
      # Gate-closed criterion 2 — §1b has all 6 PRAGMA outputs archived (5 mandatory + page_size for journal proof)
      PRAGMA_COUNT=$(awk "/^### 1b\./,/^## 2\./" $REVIEW | grep -c PRAGMA); [ "$PRAGMA_COUNT" -ge 6 ]; \
      # Gate-closed criterion 3 — §4 has exactly 3 evidence blocks (Test #1 CI URL + Test #2 CI URL + Test #3 commit hash)
      EVIDENCE_COUNT=$(awk "/^## 4\./,/^## 5\./" $REVIEW | grep -cE "(https://github.com|commit:)"); [ "$EVIDENCE_COUNT" -ge 3 ]; \
      # Gate-closed criterion 4 — every §3 Blocker row has `| fix |` decision (no Blocker waivers per CONTEXT)
      BLOCKER_ROWS=$(awk "/^## 3\./,/^## 4\./" $REVIEW | grep "| Blocker |" || true); \
      if [ -n "$BLOCKER_ROWS" ]; then NON_FIX=$(echo "$BLOCKER_ROWS" | grep -v "| fix" || true); [ -z "$NON_FIX" ] || { echo "GATE FAIL: Blocker row without fix decision: $NON_FIX"; exit 1; }; fi; \
      # Gate-closed criterion 5 — status + placeholders + ROADMAP + STATE + DEPENDENCIES + .fixes-expected
      grep -q "\*\*Status:\*\* closed" $REVIEW; \
      ! grep -q "(pending)" $REVIEW; \
      grep -qE "04.*Review Gate.*Persistence.*Complete" .planning/ROADMAP.md; \
      [ ! -f .planning/phases/04-review-gate-persistence/.fixes-expected ]; \
      grep -q "silently-degraded" DEPENDENCIES.md; \
      grep -q "Phase 04-review-gate-persistence" .planning/STATE.md; \
      echo "gate-closed: all 5 criteria pass"'</automated>
  </verify>
  <done>
    All 5 CONTEXT §Fix workflow gate-closed criteria pass (5 sections / 6+ PRAGMAs §1b / 3 evidence blocks §4 / zero non-`fix` Blocker rows §3 / status=closed + ROADMAP Complete + STATE entry + DEPENDENCIES marker + .fixes-expected deleted). `04-REVIEW.md` has `**Status:** closed` and no `(pending)` placeholders. ROADMAP.md Phase 04 row shows `Complete` with date 2026-04-18. STATE.md reflects completed Phase 04, Phase 05 ready, new Accumulated Decisions entry for custom_lint. DEPENDENCIES.md has silently-degraded marker for custom_lint. `.fixes-expected` scratch file deleted. User has signed off with "OK close". Final commit on main.
  </done>

  <what-built>
    After Task 1 applied all code/test fixes, Claude:
    1. Applies P2 custom_lint documentation (2 `docs(04-rev):` commits — DEPENDENCIES.md + STATE.md)
    2. Captures final commit hash + verifies CI green on all 3 jobs
    3. Fills `04-REVIEW.md` §5 with hash + run URL + status + date
    4. Flips header `**Status:** open` → `closed` + fills `**Closed:** 2026-04-18`
    5. Commits closure `docs(04-rev): close Phase 04 Review Gate — CI green on <hash>`
    6. Asks user to confirm Phase 04 marked complete + Phase 05 unblocked
    7. Updates STATE.md + ROADMAP.md after user approval
    8. Deletes `.fixes-expected` scratch file
    9. Final commit `docs(state): Phase 04 Review Gate closed — Phase 05 unblocked`
  </what-built>
  <how-to-verify>
    Claude presents to user:

    > "Phase 04 terminée du côté fixes. Récap :
    >
    > - **§1 User-observed findings :** <captured content snippet or 'aucune observation'>
    > - **§1b Runtime walk :** confirmed ON / OFF (DB path: `<path>`, all 6 PRAGMA matched)
    > - **§2 audit findings :** pre-class 3 (P1 Blocker flaky / P2 Noted custom_lint / P3 Should computeRevealMask) + Agent #1-4 totaux (X Blockers + Y Shoulds + Z Coulds + W Noted)
    > - **§3 triage :** A fix, B waived (avec rationale), C deferred, D won't-fix, V noted
    > - **§4 adversarial :** 3/3 tests prouvés (check_domain_purity double violation, drift schema dump stale, SchemaSanityChecker row-loss regression guard) — 2 CI URLs archived + 1 permanent test commit hash
    > - **Fixes appliquées :** E commits atomiques `fix(04-rev):`/`refactor(04-rev):`/`test(04-rev):` sur main, chacun CI-gated (`.fixes-expected`=$E matches applied count)
    > - **P2 docs :** 2 commits `docs(04-rev):` (DEPENDENCIES.md + STATE.md) — exclus du fix tally
    > - **§5 CI green :** commit `<hash>`, run `<URL>`, 3 jobs verts (gates + android + ios)
    > - **Status :** `04-REVIEW.md` flip vers `closed`, Phase 05 prête à démarrer
    >
    > Tu valides la fermeture de la Review Gate Persistence ? Réponds :
    > - `OK close` → je marque Phase 04 complete dans STATE.md + ROADMAP.md, supprime `.fixes-expected`, Phase 05 est unblocked
    > - `attends` → un point à rediscuter avant closure (liste lequel)
    > - `reopen` → un finding manqué / un fix à refaire (liste lequel)"

    Expected user response: `OK close`.

    Verification after completion (the `<automated>` verify block enforces ALL 5 gate-closed criteria from CONTEXT §Fix workflow as a single fail-fast `set -e` bash script — see block above for inline comments mapping each criterion):
    - Criterion 1: `grep -c "^## [1-5]\." 04-REVIEW.md` returns 5
    - Criterion 2: `awk "/^### 1b\./,/^## 2\./" 04-REVIEW.md | grep -c PRAGMA` returns ≥ 6 (5 mandatory + page_size)
    - Criterion 3: `awk "/^## 4\./,/^## 5\./" 04-REVIEW.md | grep -cE "(https://github.com|commit:)"` returns ≥ 3 (Test #1 CI URL + Test #2 CI URL + Test #3 commit hash)
    - Criterion 4: every §3 row with `| Blocker |` severity has `| fix` in its decision column (no Blocker waivers)
    - Criterion 5: status closed + no `(pending)` + ROADMAP Complete + `.fixes-expected` deleted + DEPENDENCIES silently-degraded marker + STATE Phase 04 entry
  </how-to-verify>
  <resume-signal>
    User types "OK close" (or equivalent) → Phase 04 signed off, Phase 05 unblocked.
    If user says "attends" or "reopen", Claude reopens relevant section (§3 for re-triage, §2 for new findings, or re-enters Task 1 for additional fixes) and iterates — `04-REVIEW.md` status stays `open` until resolved.
  </resume-signal>
</task>

</tasks>

<verification>
- `.fixes-expected` snapshot taken at start of Task 1, contains integer count of `| fix |` rows BEFORE any mutation
- Every `fix`-triaged finding has an atomic `fix(04-rev):` / `refactor(04-rev):` / `test(04-rev):` commit on main (verified against snapshot, not live grep)
- `docs(04-rev):` commits EXCLUDED from fix tally (custom_lint DEPENDENCIES.md + STATE.md entries + §3 mutation markers + §5 closure)
- Pre-class P1 applied (Blocker flaky backup fix — test-only or escalated runtime+test)
- Pre-class P2 applied (Noted custom_lint — 2 docs commits, DEPENDENCIES.md + STATE.md)
- Pre-class P3 applied (Should computeRevealMask no-callers guard — new test file)
- Final CI run on main is green on all 3 jobs (gates + android + ios)
- §5 filled with hash, run URL, status, date
- `04-REVIEW.md` status flipped to `closed`
- `.fixes-expected` scratch file deleted at phase closure
- STATE.md Accumulated Decisions has Phase 04 entry + Phase 04 marked complete in progress counters
- ROADMAP.md Phase 04 row = Complete with date 2026-04-18
- User has signed off with "OK close"
- Task 2 `<automated>` verify block enforces ALL 5 CONTEXT §Fix workflow gate-closed criteria as a single `set -e` fail-fast script (5 sections / 6+ PRAGMAs §1b / 3 evidence blocks §4 / zero non-`fix` Blocker rows §3 / closure state + artefacts)
</verification>

<success_criteria>
Plan 04-05 (and Phase 04 as a whole) complete when:
1. All §3 `fix` findings have matching atomic `fix(04-rev):` / `refactor(04-rev):` / `test(04-rev):` commits on main
2. Pre-class P1 + P3 applied as fix commits; P2 applied as 2 docs commits (excluded from fix tally)
3. Final `main` commit has CI green on all 3 jobs
4. `04-REVIEW.md` has 5 sections fully populated (§1 user / §1b runtime walk / §2 pre-class + 4 agents / §3 triage / §4 adversarial / §5 CI-green) AND `**Status:** closed` AND `**Closed:** 2026-04-18`
5. No `adversarial/04-*` branches on remote (carried from Plan 04-04)
6. User has explicitly unblocked Phase 05 via checkpoint signal
7. STATE.md + ROADMAP.md reflect Phase 04 complete + custom_lint Accumulated Decisions entry
8. DEPENDENCIES.md has silently-degraded marker for custom_lint
9. `.fixes-expected` scratch file deleted
10. Task 2 gate-closed verify passes all 5 CONTEXT criteria (5 sections / 6+ PRAGMAs §1b / 3 evidence blocks §4 / zero non-`fix` Blocker §3 / closure artefacts)
</success_criteria>

<output>
After completion, create `.planning/phases/04-review-gate-persistence/04-05-SUMMARY.md` listing:
- Count of fixes applied per type (fix / refactor / test — reconciled against `.fixes-expected` snapshot before deletion)
- Pre-class P1 path taken (test-only OR runtime+test escalation) + rationale
- Pre-class P3 test file commit hash
- Pre-class P2 docs commits hashes (DEPENDENCIES.md + STATE.md)
- Hash range (first fix commit to final closure commit) for future bisect reference
- Any late-discovered findings during Task 1 that surfaced outside the original §2 list (added to §2 retroactively or deferred)
- Final CI run URL
- Phase 04 closure timestamp
- List of findings deferred forward (with target phase IDs — these carry into STATE.md Pending Todos)
- Phase 04 overall wall-clock (Plans 04-01 through 04-05) if captured
</output>
</content>
</invoke>