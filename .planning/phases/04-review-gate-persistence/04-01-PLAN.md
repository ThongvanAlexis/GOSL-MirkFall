---
phase: 04-review-gate-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-review-gate-persistence/04-REVIEW.md
autonomous: false
requirements:
  - SC#3
must_haves:
  truths:
    - "`04-REVIEW.md` exists with the 5-section skeleton (User-observed findings + sub-§1b Runtime walk / Claude audit findings / Triage decisions / Adversarial evidence / CI-green confirmation)"
    - "§1 contains either the user's verbatim IDE observations OR an explicit `aucune observation utilisateur` marker, committed BEFORE any later plan spawns sub-agents or runs the walk"
    - "§1b placeholder sub-section exists in the skeleton so Plan 04-02 can fill it without re-editing the upstream structure"
    - "§2 contains a `Pre-known from VERIFICATION` sub-section placeholder so Plan 04-03 Task 1 fills 3 entries (flaky backup rotate / custom_lint silently-degraded / computeRevealMask UnimplementedError) BEFORE spawning the 4 sub-agents"
    - "§4 contains 3 empty sub-blocks (Test 1 domain-purity double violation, Test 2 drift schema dump stale, Test 3 SchemaSanityChecker row-loss permanent unit test) — mirrors 02-REVIEW.md §4 contract with persistence-shaped ingredients"
    - "Two atomic commits on main: `docs(04-rev): scaffold 04-REVIEW.md 5-section skeleton` + `docs(04-rev): capture user-observed findings into 04-REVIEW.md §1`"
  artifacts:
    - path: ".planning/phases/04-review-gate-persistence/04-REVIEW.md"
      provides: "5-section review artifact with §1 User-observed findings filled and §1b + §2 pre-class + §4 adversarial placeholders scaffolded"
      contains: "# Phase 04: Review Gate — Persistence Review"
      min_lines: 60
  key_links:
    - from: "Claude-main"
      to: "User (IDE review session on Phase 03 artefacts)"
      via: "CLAUDE.md §Code Review Phases protocol step 1 — solicitation message"
      pattern: "qu'as-tu vu ?|findings?|IDE"
    - from: "`04-REVIEW.md` §1"
      to: "Plan 04-02 Wave 2 runtime walk + Plan 04-03 Wave 3 agent spawn trigger"
      via: "§1 must be non-empty (verbatim user capture or explicit 'aucune observation') before 04-02 runs — strict user-first protocol"
      pattern: "## 1\\. User-observed findings"
    - from: "`04-REVIEW.md` §2 sub-section `Pre-known from VERIFICATION`"
      to: "Plan 04-03 Task 1 pre-classification before agent spawn"
      via: "CONTEXT §Triage 3 candidats — pre-class saves redundant agent discovery cycles"
      pattern: "### Pre-known from VERIFICATION"
---

<objective>
Encode the CLAUDE.md §Code Review Phases protocol as a hard ordering gate for Phase 04: user's IDE observations on Phase 03 persistence artefacts are captured into `04-REVIEW.md` §1 BEFORE Claude spawns any audit sub-agent or runs the runtime walk. This plan also scaffolds the 5-section `04-REVIEW.md` artifact (including the new §1b Runtime walk sub-section and the §2 `Pre-known from VERIFICATION` slot) that subsequent plans will fill in.

Purpose: The user-first rule is non-negotiable (explicitly anti-parallelized in CONTEXT.md §Ordering strict). Encoding it as Plan 04-01 with a blocking checkpoint makes the protocol enforceable, not cosmetic. If the user is biased by Claude's findings before posting their own observations on Phase 03 (Drift schema, migrations, domain purity, store behavior), the review's value is destroyed. Phase 04 adds §1b Runtime walk and §2 Pre-known placeholders into the skeleton so Plans 04-02 / 04-03 can fill them without re-editing the upstream structure.

Output: `04-REVIEW.md` committed with a complete 5-section skeleton (with §1b + §2 pre-class + §4 three-test placeholders mirroring Phase 02 exemplar) and §1 populated with the user's verbatim IDE observations (or explicit "aucune observation" if the user has nothing to add).
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-gate-persistence/04-CONTEXT.md
@.planning/phases/04-review-gate-persistence/04-RESEARCH.md
@.planning/phases/04-review-gate-persistence/04-VALIDATION.md
@.planning/phases/02-review-gate-foundation/02-01-PLAN.md
@.planning/phases/02-review-gate-foundation/02-REVIEW.md
@.planning/phases/03-persistence-domain-models/03-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Key rules this plan MUST encode. No codebase exploration needed. -->

From CLAUDE.md §Code Review Phases (verbatim binding constraint):
```
**Protocole :**

- Tu me demandes d'abord ce que j'ai vu qui a besoin d'être revu
- Ensuite tu fais ton audit
- Tu affiches uniquement les **titres** avec une courte explication, et tu me laisses choisir ce qu'on corrige
```

From 04-CONTEXT.md §decisions (Ordering strict):
```
- User poste ses findings IDE en chat AVANT que Claude spawn quoi que ce soit
- Claude capture verbatim dans 04-REVIEW.md §1
- ENSUITE runtime walk Windows (plan dédié)
- ENSUITE spawn les 4 sub-agents en single tool-use message
- Parallèle (user tape pendant que agents tournent) explicitement rejeté
```

From 04-RESEARCH.md Example 1 — `04-REVIEW.md` 5-section skeleton contract (adapted from 02-REVIEW.md exemplar):
```
## 1. User-observed findings (IDE review)
### 1b. Runtime walk Windows
## 2. Claude audit findings
### Pre-known from VERIFICATION
### Agent #1 — Schema + migrations + backup
### Agent #2 — Domain models + pureté
### Agent #3 — Store layer + factory + providers
### Agent #4 — Tests + fixtures + tooling + CLAUDE.md sweep
## 3. Triage decisions
## 4. Adversarial evidence
### Test 1: Domain purity import violation (Flutter + Drift)
### Test 2: Drift schema dump stale
### Test 3: SchemaSanityChecker row-loss detection (permanent unit test)
## 5. CI-green confirmation
```

GOSL header policy: `tool/check_headers.dart` targets `.dart` files only. `.md` review artefacts are EXEMPT — do NOT prepend the GOSL header to `04-REVIEW.md`. Consistent with Phase 02 precedent (STATE.md: "GOSL Dart copyright header NOT prepended to .md review artifacts").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the 5-section 04-REVIEW.md skeleton (with §1b + §2 pre-class + §4 three-test placeholders)</name>
  <files>.planning/phases/04-review-gate-persistence/04-REVIEW.md</files>
  <action>
    Create `.planning/phases/04-review-gate-persistence/04-REVIEW.md` with the 5-section skeleton matching the format locked in 02-REVIEW.md but adapted for Phase 04's persistence concerns. Sections MUST be in this order and named exactly (gsd-verifier greps `^## [1-5]\.` to confirm all 5 present):

    ```
    # Phase 04: Review Gate — Persistence Review

    **Opened:** 2026-04-18
    **Status:** open
    **Closed:** (pending)

    ## 1. User-observed findings (IDE review)

    *Captured verbatim at phase start, BEFORE Claude's audit.*

    (awaiting user input — Task 2 of this plan fills this section)

    ### 1b. Runtime walk Windows

    *Filled by Plan 04-02 Task 3 after the user runs the walk script and pastes outputs.*

    <details>
    <summary>DB path + file sizes + PRAGMA + schema + index dumps</summary>
    (pending — filled by Plan 04-02)
    </details>

    ## 2. Claude audit findings

    *Filled by Plan 04-03: first the 3 pre-classified VERIFICATION candidates, then the 4 parallel sub-agents in ONE tool-use message.*

    Format: `[severity] Title — 1-line explanation — file:line`. Severities: Blocker / Should / Could / Noted.

    ### Pre-known from VERIFICATION

    *Filled by Plan 04-03 Task 1 BEFORE spawning sub-agents. Source: 03-VERIFICATION.md §Outstanding minor items. Committed as `docs(04-rev): pre-class 3 VERIFICATION candidates into §2` before any Agent tool call.*

    (pending — 3 entries: flaky `backup_test.dart::rotate` Blocker | `custom_lint` silently degraded Noted | `computeRevealMask` UnimplementedError Should)

    ### Agent #1 — Schema + migrations + backup
    (pending)

    ### Agent #2 — Domain models + pureté
    (pending)

    ### Agent #3 — Store layer + factory + providers
    (pending)

    ### Agent #4 — Tests + fixtures + tooling + CLAUDE.md sweep
    (pending)

    <details>
    <summary>Audit Notes (narrative appendix, per agent)</summary>
    (pending)
    </details>

    ## 3. Triage decisions

    *Filled by Plan 04-03 Task 3 after user selects what to fix. Every Blocker MUST be `fix` (waiver forbidden per CONTEXT.md). Every Should MUST be either `fix` or `waived` with inline rationale.*

    | # | Finding | Severity | Decision | Rationale |
    |---|---------|----------|----------|-----------|
    | (pending) | | | | |

    ## 4. Adversarial evidence

    *Filled by Plan 04-04. Two CI-branch evidence blocks (Tests #1, #2) + one permanent unit-test evidence block (Test #3).*

    ### Test 1: Domain purity import violation (Flutter + Drift)
    *Branch `adversarial/04-domain-import-flutter-and-drift`: one branch, TWO violations. CI step `Check domain purity (lib/domain/ imports)` must fail with exit 1 listing BOTH `lib/domain/sessions/session.dart` (Flutter import) AND `lib/domain/markers/marker.dart` (Drift import).*

    (pending)

    ### Test 2: Drift schema dump stale
    *Branch `adversarial/04-schema-drift-stale`: add a column to `t_sessions` in `app_database.dart`, run `build_runner build` (mandatory — otherwise `flutter analyze` fails first per RESEARCH Pitfall 1), do NOT run `drift_dev schema dump`. CI step `Check drift schema (current) is committed and fresh` must fail with `git diff --exit-code` showing stale `drift_schemas/drift_schema_current.json`.*

    (pending)

    ### Test 3: SchemaSanityChecker row-loss detection (permanent unit test)
    *NOT a throwaway branch. Permanent test `test/infrastructure/db/migration_v1_to_v2_data_loss_test.dart` on `main`. Injects V1 fixture (70 rows), runs adversarial migration with `ALTER TABLE` + `DELETE FROM t_sessions WHERE rowid % 2 = 0`, asserts `SchemaSanityChecker.assertNoLoss` throws `MigrationFailureException` with exact row-count diff. Evidence = commit hash + green `dart test` output.*

    (pending)

    ## 5. CI-green confirmation

    *Filled by Plan 04-05 Task 2 after all Blocker + non-waived Should fixes are applied and CI is green.*

    - **Final commit on main:** (pending)
    - **CI run URL:** (pending)
    - **Status:** (pending)
    - **Date:** (pending)

    ---
    _Phase 04 closed: (pending)_
    _Phase 05 unblocked._
    ```

    GOSL header policy: do NOT prepend the GOSL Dart copyright header to this `.md` file. `tool/check_headers.dart` targets `.dart` files only. Markdown review artefacts are EXEMPT (Phase 02 STATE.md decision: "GOSL Dart copyright header NOT prepended to .md review artifacts").

    After writing, verify the 5 top-level sections are present:
    ```bash
    grep -c "^## [1-5]\\." .planning/phases/04-review-gate-persistence/04-REVIEW.md
    # must return 5
    ```

    Then commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): scaffold 04-REVIEW.md 5-section skeleton" --files .planning/phases/04-review-gate-persistence/04-REVIEW.md
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; [ "$(grep -c '^## [1-5]\.' .planning/phases/04-review-gate-persistence/04-REVIEW.md)" = "5" ] &amp;&amp; grep -q "^### 1b\. Runtime walk Windows" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; grep -q "^### Pre-known from VERIFICATION" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; grep -q "^### Test 3: SchemaSanityChecker row-loss detection" .planning/phases/04-review-gate-persistence/04-REVIEW.md</automated>
  </verify>
  <done>`04-REVIEW.md` exists, all 5 top-level sections present (grep count = 5), §1b Runtime walk sub-section present, §2 `Pre-known from VERIFICATION` sub-section present, §4 has the 3 Phase-04-specific test placeholders (domain-purity / drift-schema-stale / row-loss permanent unit), `**Status:** open`, sections 1-5 in correct order, skeleton committed as `docs(04-rev): scaffold 04-REVIEW.md 5-section skeleton`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-first IDE review — solicit, capture, commit into §1</name>
  <files>.planning/phases/04-review-gate-persistence/04-REVIEW.md</files>
  <action>
    This is the CLAUDE.md §Code Review Phases protocol gate for Phase 04. Strict ordering: Claude asks FIRST, user answers, Claude captures verbatim, THEN Plan 04-02 Wave 2 runtime walk AND Plan 04-03 Wave 3 agent spawn unblock.

    Claude-main posts in chat as the first non-automated action of this wave:

    > "Avant que je lance le runtime walk Windows (Plan 04-02) puis les 4 sub-agents d'audit (Plan 04-03), qu'as-tu vu dans ton IDE qui mérite d'être revu sur la Phase 03 Persistence ?
    >
    > Scope audit suggéré de ton côté :
    > - `lib/domain/` (entities Freezed, sealed MirkStyleConfig + UnknownConfig fallback, @Assert invariants, errors, ports stores, extension type IDs)
    > - `lib/infrastructure/db/` (AppDatabase schema V1+V2, migrations, backup + schema_sanity + factory, pragma setup)
    > - `lib/infrastructure/stores/` (5 Drift stores + SqliteException 2067 mapper)
    > - `lib/infrastructure/ids/` (ULID + RandomIdGenerator + SeededIdGenerator)
    > - `lib/application/providers/` (7 @Riverpod providers keepAlive=true)
    > - `test/domain/`, `test/infrastructure/db/`, `test/infrastructure/stores/`, `test/infrastructure/ids/`
    > - `test/fixtures/` (drift_schemas/, json/v{1,2}, db_seed/v1_baseline.sql, mirk_style_unknown_renderer.json)
    > - `tool/check_domain_purity.dart` + son test
    > - `drift_schemas/drift_schema_v{1,2,_current}.json`
    > - `pubspec.yaml` deltas Phase 03 + `dependency_overrides analyzer ^10.0.0 + dart_style 3.1.7` + `analysis_options.yaml`
    > - `lib/config/constants.dart` deltas (kDbFilename, kDbBackupDirName, kMaxDbBackups, kDbBusyTimeoutMs)
    > - `DEPENDENCIES.md` Phase 03 entries
    > - `.github/workflows/ci.yml` nouveaux steps (check_domain_purity, drift schema dump guard, plain-Dart tests scoped)
    >
    > Poste ta liste telle quelle — je la capture verbatim dans `04-REVIEW.md` §1 avant de déclencher runtime walk + audit. Si tu n'as rien vu, dis-le explicitement — je note 'aucune observation utilisateur' et on enchaîne sur Plan 04-02."

    User responds with either:
    - A list of findings (any format — bullets, prose, file references) → Claude captures verbatim into §1
    - "rien vu" / "aucune observation" / equivalent → Claude writes `*Aucune observation utilisateur — l'user n'a pas identifié de point à revoir dans son IDE.*` into §1

    Either way, Claude then:
    1. Edits `04-REVIEW.md` §1 to replace `(awaiting user input — Task 2 of this plan fills this section)` with the captured content. Leave §1b placeholder untouched (Plan 04-02 fills it).
    2. Commits:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04-rev): capture user-observed findings into 04-REVIEW.md §1" --files .planning/phases/04-review-gate-persistence/04-REVIEW.md
       ```
    3. Confirms to user: "§1 capturé et committé. J'enchaîne sur Plan 04-02 runtime walk dès que tu valides (réponse : `OK walk` ou équivalent)."

    Anti-pattern guard: Do NOT spawn any Agent/Task tool call AND do NOT launch the runtime walk in this wave before §1 is committed. The whole point of the checkpoint is to enforce user-first — CONTEXT.md explicitly rejects the parallel approach (user typing while agents run).
  </action>
  <verify>
    <automated>bash -c '! grep -q "awaiting user input" .planning/phases/04-review-gate-persistence/04-REVIEW.md &amp;&amp; git log --oneline -5 -- .planning/phases/04-review-gate-persistence/04-REVIEW.md | grep -q "capture user-observed findings"'</automated>
  </verify>
  <done>
    `04-REVIEW.md` §1 contains the user's verbatim IDE observations on Phase 03 artefacts (or explicit "aucune observation utilisateur" marker). The placeholder `awaiting user input` no longer appears anywhere in the file. A commit `docs(04-rev): capture user-observed findings into 04-REVIEW.md §1` is on `main`. User has signaled "OK walk" (or equivalent) explicitly to unblock Plan 04-02 runtime walk.
  </done>

  <what-built>
    Nothing automated. This checkpoint IS the protocol gate itself: Claude asks the user what they saw in their IDE on Phase 03, captures the verbatim answer (or explicit "rien vu"), writes it into `04-REVIEW.md` §1, and commits. This MUST happen before Plan 04-02 runs the runtime walk AND before Plan 04-03 spawns any audit sub-agent.
  </what-built>
  <how-to-verify>
    After Claude has posted the solicitation and user has responded:
    - `grep -A2 "## 1\\. User-observed findings" .planning/phases/04-review-gate-persistence/04-REVIEW.md | grep -v "awaiting user input"` — should NOT show the placeholder
    - `git log --oneline -1 -- .planning/phases/04-review-gate-persistence/04-REVIEW.md` — should show the capture commit
    - §1b Runtime walk placeholder remains intact (Plan 04-02 fills it later)
  </how-to-verify>
  <resume-signal>
    User types "OK walk" or "§1 validé, go" to signal that Plan 04-02 may run the runtime walk.
    If user re-opens IDE findings after seeing this prompt, Claude appends (not replaces) to §1 and re-commits before proceeding to Plan 04-02.
  </resume-signal>
</task>

</tasks>

<verification>
- `04-REVIEW.md` exists with 5 sections in correct order (grep `^## [1-5]\.` returns 5)
- §1b Runtime walk sub-section present (grep `^### 1b\. Runtime walk Windows` matches)
- §2 `Pre-known from VERIFICATION` sub-section present (grep matches)
- §4 contains the 3 Phase-04-specific test placeholders (domain-purity / drift-schema-stale / row-loss permanent unit)
- §1 is populated (no placeholder `awaiting user input` remains)
- Two commits on `main`: scaffold + user capture
- User has explicitly signaled "OK walk" (or equivalent) for Plan 04-02 to proceed
</verification>

<success_criteria>
Plan 04-01 complete when:
1. `.planning/phases/04-review-gate-persistence/04-REVIEW.md` exists on `main` with the 5-section skeleton + §1b + §2 pre-class placeholder + §4 three-test placeholders
2. §1 contains either the user's verbatim IDE findings OR an explicit "aucune observation utilisateur" marker
3. Two atomic commits logged (scaffold + capture) with `docs(04-rev):` prefix
4. CI green (markdown-only changes, guard scripts not affected — scaffold + capture commits should both trigger green CI, confirming nothing else regressed)
5. User has explicitly unblocked Plan 04-02 via "OK walk" or equivalent signal
</success_criteria>

<output>
After completion, create `.planning/phases/04-review-gate-persistence/04-01-SUMMARY.md` summarizing:
- Skeleton commit hash
- §1 capture commit hash
- Verbatim excerpt of user observations (or "aucune observation")
- Timestamp of user unblock signal received
- Confirmation that §1b, §2 pre-class, §4 three-test placeholders are intact and ready for Plans 04-02 / 04-03 / 04-04 to fill
</output>
</content>
</invoke>