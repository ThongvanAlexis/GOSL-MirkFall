---
phase: 05-gps-session-lifecycle
plan: 04
plan_id: 05-04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - lib/presentation/screens/session_list_screen.dart
  - lib/presentation/screens/session_detail_screen.dart
  - lib/presentation/screens/settings_screen.dart
  - lib/presentation/screens/permission_rationale_screen.dart
  - lib/presentation/screens/permission_denied_screen.dart
  - lib/presentation/screens/oem_guidance_screen.dart
  - lib/presentation/widgets/active_session_banner.dart
  - lib/presentation/widgets/app_shell.dart
  - lib/presentation/router.dart
  - lib/presentation/screens/placeholder_home_screen.dart
  - lib/application/providers/session_list_provider.dart
  - lib/main.dart
  - test/presentation/screens/session_list_screen_test.dart
  - test/presentation/screens/session_detail_screen_test.dart
  - test/presentation/screens/settings_screen_test.dart
  - test/presentation/screens/permission_rationale_screen_test.dart
  - test/presentation/screens/permission_denied_screen_test.dart
  - test/presentation/screens/oem_guidance_screen_test.dart
  - test/presentation/widgets/active_session_banner_test.dart
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - SESS-05
  - SESS-06
  - SESS-08
  - SESS-09
  - GPS-01
  - GPS-07
  - GPS-08
  - GPS-05
must_haves:
  truths:
    - "Route `/` renders SessionListScreen (replaces PlaceholderHomeScreen as home) with sessions sorted by startedAtUtc DESC"
    - "FAB `+` opens create-session dialog (name input + Start immediate / Create only); empty state shows CTA 'Créer ma première session'"
    - "Tap session row navigates to `/sessions/:id`"
    - "SessionDetailScreen shows status dashboard (chrono, last fix, fix count, distanceFilter) when active; shows summary + delete button when stopped"
    - "Delete is blocked when session is active — inline error message"
    - "Rename dialog accessible via menu; persists via sessionStore.update"
    - "SettingsScreen exposes distanceFilter slider (range 2-100), persists via sessionSettingsProvider, displays current value"
    - "PermissionRationaleScreen copy matches CONTEXT.md §Permission flow rationale exactly (FR)"
    - "Start button on SessionListScreen / SessionDetailScreen triggers full permission flow: rationale -> OS prompt -> OEM guidance (if matched) -> session start"
    - "PermissionDeniedScreen CTA 'Ouvrir les paramètres' invokes permission_handler.openAppSettings()"
    - "OemGuidanceScreen renders vendor-specific steps based on OemDetector result + link to dontkillmyapp.com/[vendor]"
    - "ActiveSessionBanner appears on all routes EXCEPT `/sessions/:id` when session is active; shows name + inline Stop button"
    - "Router declares new routes /sessions, /sessions/:id, /settings, /permissions/rationale, /permissions/denied, /permissions/oem"
    - "PlaceholderHomeScreen removed or reduced to legacy re-export"
    - "SESS-06 end-to-end: starting a session while one is already active surfaces ConcurrentActivation dialog; confirming triggers stop(current)+start(new) chain (DB invariant from Phase 03 now wired through UI)"
  artifacts:
    - path: "lib/presentation/screens/session_list_screen.dart"
      provides: "SessionListScreen widget consuming sessionStore.watchAll()"
      contains: "FloatingActionButton"
      min_lines: 80
    - path: "lib/presentation/screens/session_detail_screen.dart"
      provides: "SessionDetailScreen with live status + stop + rename + delete"
      contains: "ActiveSessionController"
      min_lines: 100
    - path: "lib/presentation/screens/settings_screen.dart"
      provides: "Settings screen with distanceFilter slider (Phase 13 will extend)"
      contains: "Slider"
      min_lines: 40
    - path: "lib/presentation/screens/permission_rationale_screen.dart"
      provides: "GPS-01 pre-prompt full-screen rationale"
      contains: "Pour suivre ton exploration"
      min_lines: 40
    - path: "lib/presentation/screens/permission_denied_screen.dart"
      provides: "GPS-07 denied recovery with Open-Settings CTA"
      contains: "openAppSettings"
      min_lines: 30
    - path: "lib/presentation/screens/oem_guidance_screen.dart"
      provides: "GPS-08 OEM-specific guidance with dontkillmyapp.com link"
      contains: "dontkillmyapp.com"
      min_lines: 50
    - path: "lib/presentation/widgets/active_session_banner.dart"
      provides: "Cross-route active-session indicator"
      contains: "kSessionActiveBannerHeightDp"
      min_lines: 30
    - path: "lib/presentation/router.dart"
      provides: "Updated go_router with 6 new routes + shell wrapper"
      contains: "/sessions/:id"
    - path: "lib/main.dart"
      provides: "main.dart unchanged structurally (Option A lazy Riverpod) — comment added noting Phase 05 wiring"
      contains: "ProviderScope"
  key_links:
    - from: "lib/presentation/screens/session_list_screen.dart"
      to: "lib/domain/sessions/session_store.dart"
      via: "watchAll() stream"
      pattern: "watchAll"
    - from: "lib/presentation/screens/session_list_screen.dart"
      to: "lib/application/controllers/active_session_controller.dart"
      via: "start() via ref.read(activeSessionControllerProvider.notifier)"
      pattern: "activeSessionControllerProvider"
    - from: "lib/presentation/screens/session_detail_screen.dart"
      to: "lib/application/controllers/active_session_controller.dart"
      via: "stop() + state consumption"
      pattern: "activeSessionControllerProvider"
    - from: "lib/presentation/screens/settings_screen.dart"
      to: "lib/application/providers/session_settings_provider.dart"
      via: "setDistanceFilterMeters on slider change"
      pattern: "setDistanceFilterMeters"
    - from: "lib/presentation/widgets/active_session_banner.dart"
      to: "lib/application/controllers/active_session_controller.dart"
      via: "watches activeSessionControllerProvider for Tracking state"
      pattern: "Tracking"
    - from: "lib/presentation/router.dart"
      to: "lib/presentation/screens/session_list_screen.dart"
      via: "ShellRoute with app_shell.dart wraps + / = SessionListScreen"
      pattern: "SessionListScreen"
---

<objective>
Land the full Phase 05 UI: 6 screens + 1 cross-route widget + 1 app shell + router updates, replacing `PlaceholderHomeScreen` as the `/` route. Everything here consumes controllers and providers from Plans 05-01/05-02/05-03 — no new platform integration, no DB writes outside provider wiring.

Purpose: Concentrating the UI in one plan keeps Flutter-test churn contained. Reviewers can spot-check widget structure, ensure CLAUDE.md `if (!context.mounted) return;` is honored after every `await`, and verify copy matches CONTEXT.md verbatim. The router change lands here because it's UI-shell-level; main.dart stays structurally untouched (CONTEXT.md Option A).

**Scope note (Task 1 touches 11+ production files + 3 tests):** Router + shell + banner + session screens + provider + main update are deliberately grouped because they form a single atomic navigation rewrite — splitting would leave the codebase in a broken intermediate state where `/` renders SessionListScreen but the banner is not wired, or where the router has stubs pointing to nothing. The high cognitive load is traded for a coherent landing. Executor guidance: implement in the order listed in the action block (provider → shell → banner → list → detail → router → main → delete placeholder → tests); the file set is large but the operation is a single semantic change ("replace the home route").

Output:
- SessionListScreen at `/` (replaces PlaceholderHomeScreen)
- SessionDetailScreen at `/sessions/:id`
- SettingsScreen at `/settings` (minimal Phase 05; Phase 13 extends)
- PermissionRationaleScreen, PermissionDeniedScreen, OemGuidanceScreen at `/permissions/...`
- ActiveSessionBanner widget + AppShell using ShellRoute wrapping banner + route body
- Router updates (6 new routes, shell integration)
- 6 widget tests green + 1 banner widget test green (+ the settings_screen_test stub from Plan 05-01 turned green)
- main.dart comment update (no structural change)
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gps-session-lifecycle/05-CONTEXT.md
@.planning/phases/05-gps-session-lifecycle/05-RESEARCH.md
@.planning/phases/05-gps-session-lifecycle/05-VALIDATION.md
@.planning/phases/05-gps-session-lifecycle/05-01-PLAN.md
@.planning/phases/05-gps-session-lifecycle/05-02-PLAN.md
@.planning/phases/05-gps-session-lifecycle/05-03-PLAN.md
@CLAUDE.md

@lib/presentation/router.dart
@lib/presentation/screens/placeholder_home_screen.dart
@lib/presentation/screens/debug_menu_screen.dart
@lib/main.dart
@lib/application/controllers/active_session_controller.dart
@lib/application/state/active_session_state.dart
@lib/application/permissions/location_permission_flow.dart
@lib/application/providers/session_settings_provider.dart
@lib/domain/sessions/session_store.dart
@lib/domain/errors/location_permission_errors.dart
@lib/infrastructure/platform/oem_detector.dart
@lib/config/constants.dart

<interfaces>
<!-- Contracts from Plans 05-01/05-02/05-03 this plan consumes. -->

From lib/application/state/active_session_state.dart (Plan 05-03):
```dart
sealed class ActiveSessionState { const ActiveSessionState(); }
final class Idle extends ActiveSessionState { const Idle(); }
final class Starting extends ActiveSessionState { final SessionId sessionId; }
final class Tracking extends ActiveSessionState {
  final SessionId sessionId;
  final DateTime startedAtUtc;
  final int fixCount;
  final Fix? lastFix;
  final int distanceFilterMeters;
}
final class ErrorState extends ActiveSessionState { final GpsError error; }
```

From lib/application/controllers/active_session_controller.dart (Plan 05-03):
```dart
@Riverpod(keepAlive: true)
class ActiveSessionController extends _$ActiveSessionController {
  Future<void> start(SessionId id);   // throws ConcurrentActivationException
  Future<void> stop();
}
```

From lib/application/providers/session_settings_provider.dart (Plan 05-03):
```dart
class SessionSettingsSnapshot {
  final int distanceFilterMeters;
  final bool permissionFlowCompleted;
  final bool oemGuidanceSeen;
}
@Riverpod(keepAlive: true) class SessionSettings extends _$SessionSettings {
  Future<void> setDistanceFilterMeters(int value);
  Future<void> markPermissionFlowCompleted();
  Future<void> markOemGuidanceSeen();
}
```

From lib/application/permissions/location_permission_flow.dart (Plan 05-03):
```dart
Future<LocationPermissionOutcome> requestLocationAlways({...seam...});
Future<bool> openLocationSettings();
```

From lib/domain/sessions/session_store.dart (Plan 05-01):
```dart
Stream<List<Session>> watchAll();
```

From lib/infrastructure/platform/oem_detector.dart (Plan 05-02):
```dart
sealed class OemFamily { ... }
class OemDetector { Future<OemFamily> detect(); }
```

From lib/config/constants.dart (Plan 05-01):
- `kSessionActiveBannerHeightDp = 40.0`

Current router (lib/presentation/router.dart — Phase 01 shape):
```dart
// 3 routes: / (PlaceholderHomeScreen), /about, /debug.
// Phase 05 adds: /sessions, /sessions/:id, /settings, /permissions/rationale, /permissions/denied, /permissions/oem.
// Also introduces ShellRoute wrapping everything with AppShell (banner injection).
```

Existing debug menu (lib/presentation/screens/debug_menu_screen.dart — 7-tap) stays untouched; accessible from /about per Phase 01.
</interfaces>

<locked_decisions>
<!-- From 05-CONTEXT.md decisions + 05-RESEARCH.md, still binding. -->

1. `/` = SessionListScreen (replaces PlaceholderHomeScreen). Sessions sorted `startedAtUtc` DESC. FAB `+` opens create dialog. Empty state CTA "Créer ma première session".
2. `/sessions/:id` = SessionDetailScreen with status dashboard (active) or summary+delete (stopped). Rename via menu. Delete blocked when active.
3. Start a new session while one is active: UI catches `ConcurrentActivationException` from controller and prompts "Stop X first? [Stop and Start]" — tapping triggers `stop()` then `start()`.
4. `/settings` minimal — only distanceFilter slider. Phase 13 extends.
5. Permission flow entered ONLY on first Start when `permission_flow_completed == false`. Flow: PermissionRationaleScreen -> on Continuer -> `requestLocationAlways()` -> on granted: if OEM matched and `oem_guidance_seen == false` -> OemGuidanceScreen -> on OK -> start session; if permanentlyDenied/whileInUseOnly -> PermissionDeniedScreen.
6. ActiveSessionBanner: ~40dp slim bar with "Session active : [name] • Stop" + tap-navigates. Shown on all routes EXCEPT `/sessions/:id`.
7. Copy is FR (CONTEXT.md). CONTEXT.md verbatim for rationale screen body. OEM screen copy vendor-specific — Claude's discretion but concise + actionable.
8. `main.dart` UNCHANGED structurally. Option (a) lazy Riverpod stays; add ONE comment above `runApp` noting Phase 05 wiring of ActiveSessionController as first productive consumer.
9. Widget tests use Riverpod `ProviderContainer` + `UncontrolledProviderScope` pattern; overrides for every controller/provider so widgets render without real geolocator/DB.
10. NO `url_launcher` dep added (05-RESEARCH.md Open Question #4 — reuse `share_plus`). OEM guidance link opens via `share_plus.share('https://dontkillmyapp.com/[vendor]')` — OS share sheet acceptable friction.
11. CLAUDE.md rule: every `await` inside a widget MUST be followed by `if (!context.mounted) return;`. Apply systematically.
12. `const` constructors wherever possible (performance — CLAUDE.md).
13. No `setState` after async work without `mounted` check.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core session screens + banner + app shell + router</name>
  <files>
    lib/presentation/screens/session_list_screen.dart,
    lib/presentation/screens/session_detail_screen.dart,
    lib/presentation/widgets/active_session_banner.dart,
    lib/presentation/widgets/app_shell.dart,
    lib/presentation/router.dart,
    lib/presentation/screens/placeholder_home_screen.dart,
    lib/application/providers/session_list_provider.dart,
    lib/main.dart,
    test/presentation/screens/session_list_screen_test.dart,
    test/presentation/screens/session_detail_screen_test.dart,
    test/presentation/widgets/active_session_banner_test.dart
  </files>
  <behavior>
    - SessionListScreen:
      - AppBar title "Mes sessions".
      - Body: `StreamBuilder` on `sessionStoreProvider.future -> watchAll()` (or equivalent Riverpod stream pattern).
      - Empty state: icon + "Aucune session pour l'instant" + FilledButton "Créer ma première session".
      - List rendering: each session = ListTile with displayName + subtitle (startedAtUtc formatted + "• active" if status==active) + trailing chevron.
      - FAB `+` opens `_CreateSessionDialog` (TextField displayName + two buttons: "Créer" + "Créer et démarrer").
      - Tap list tile navigates `/sessions/:id`.
      - Scaffold wrapped by ShellRoute in router — banner injected automatically.
    - SessionDetailScreen:
      - AppBar title = session displayName + overflow menu (Rename / Delete).
      - Watches `activeSessionControllerProvider` state.
      - If `Tracking(sessionId == widget.sessionId)`: status dashboard (chrono since startedAtUtc, last fix lat/lon + accuracy + timestamp, fixCount, distanceFilterMeters); big red "Arrêter" FilledButton.
      - If `Idle` or tracking-other-session: summary card (displayName + startedAtUtc + durée + fix count from fixStore.countBySession) + "Démarrer" FilledButton + "Supprimer" OutlinedButton.
      - Start button: runs permission-flow-if-needed (Plan 05-03 helpers) then invokes controller.start(id). Catches `ConcurrentActivationException` -> shows AlertDialog "Une autre session est active. Stop X and Start Y?" with confirmation.
      - Stop button: invokes controller.stop().
      - Delete button: confirmation dialog; if session is active, shows inline error "Arrête la session d'abord" and does NOT call delete.
      - Rename: AlertDialog with TextField + OK -> sessionStore.update(session.copyWith(displayName: newName)).
    - ActiveSessionBanner:
      - Watches `activeSessionControllerProvider`.
      - If state is `Tracking`: render slim Container (height `kSessionActiveBannerHeightDp`, ListTile-style) with "Session active : [displayName] • Stop" + onTap=navigate `/sessions/:id`.
      - Inline Stop icon triggers `controller.stop()` directly.
      - Non-Tracking states: render `const SizedBox.shrink()`.
    - AppShell:
      - Widget that wraps any child with `Column([ActiveSessionBanner, Expanded(child: child)])`.
      - Except for `/sessions/:id` — ShellRoute branches dictate whether banner is shown. Implementation: pass a route-path predicate; banner renders SizedBox.shrink on that path.
    - Router:
      - ShellRoute with AppShell builder wraps all routes.
      - Routes: `/` (SessionListScreen), `/sessions/:id` (SessionDetailScreen parsing `state.pathParameters['id']` into SessionId), `/settings` (SettingsScreen — stubbed import from Task 2), `/permissions/rationale`, `/permissions/denied`, `/permissions/oem` (stubs from Task 2), `/about` (unchanged Phase 01), `/debug` (unchanged).
    - PlaceholderHomeScreen: Reduce to a 5-line deprecation re-export that points to SessionListScreen. Or delete outright + grep `/` references. PREFER: delete the file entirely and update any test that imports it (Phase 01's widget tests — update to import SessionListScreen).
    - main.dart: add a single comment line above `runApp` noting Phase 05 wires ActiveSessionController as first productive Riverpod consumer; structurally unchanged.
    - Tests:
      - `session_list_screen_test.dart` (was stub Plan 05-01) turns GREEN:
        - `emptyStateShowsCreateFirstCta` (no sessions -> CTA visible).
        - `rendersSessionsFromWatchAll` (seed 3 sessions, all rendered, DESC order).
        - `activeSessionRowShowsActiveBadge`.
        - `fabOpensCreateDialog`.
        - `tapSessionNavigatesToDetail`.
        - `sess09_hundredSessionsRenderWithoutTimeout` (seed 100 sessions via fake SessionStore, scroll and assert no frame-timeout).
      - `session_detail_screen_test.dart` turns GREEN:
        - `rendersStatusDashboardWhenActive`.
        - `rendersSummaryCardWhenIdle`.
        - `stopButtonInvokesControllerStop`.
        - `deleteBlockedWhenActiveShowsError`.
        - `deleteOnStoppedInvokesSessionStore`.
        - `renameDialogPersistsNewDisplayName`.
        - `startWithConcurrentActivationExceptionShowsConfirmDialog` (SESS-06 end-to-end — assert the dialog appears, confirming triggers controller.stop() followed by controller.start(new) in that order via fake controller call-order assertions).
      - `active_session_banner_test.dart` GREEN:
        - `rendersEmptyOnIdle`.
        - `rendersBannerOnTracking`.
        - `stopIconInvokesControllerStop`.
        - `tapNavigatesToSessionDetail`.
  </behavior>
  <action>
1. Create `lib/presentation/widgets/app_shell.dart`:
```dart
class AppShell extends ConsumerWidget {
  const AppShell({required this.child, required this.currentLocation, super.key});
  final Widget child;
  final String currentLocation;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final hideBanner = currentLocation.startsWith('/sessions/');
    return Column(
      children: [
        if (!hideBanner) const ActiveSessionBanner(),
        Expanded(child: child),
      ],
    );
  }
}
```

2. Create `lib/presentation/widgets/active_session_banner.dart`:
   - `ConsumerWidget`, watches `activeSessionControllerProvider`.
   - Use pattern matching on `AsyncValue.valueOrNull` — only render when `value is Tracking`.
   - Banner tile: `SizedBox(height: kSessionActiveBannerHeightDp, child: Material(color: Theme.of(context).colorScheme.tertiaryContainer, child: InkWell(onTap: () => context.go('/sessions/${tracking.sessionId.value}'), child: Row([..., icon Stop button]))))`.

3. Create `lib/presentation/screens/session_list_screen.dart`:
   - `ConsumerWidget` watching a `sessionStream` provider (derive from sessionStoreProvider):
     ```dart
     @riverpod
     Stream<List<Session>> sessionList(Ref ref) async* {
       final store = await ref.watch(sessionStoreProvider.future);
       yield* store.watchAll();
     }
     ```
     Add this in `lib/application/providers/session_list_provider.dart` (NEW file — declared in this task's `<files>` element + frontmatter `files_modified`).
   - Build: `AsyncValue.when`:
     - `loading` -> `CircularProgressIndicator.adaptive()`
     - `error` -> error widget
     - `data(sessions)` -> if empty: empty-state column with CTA; else `ListView.separated` of tiles.
   - FAB `+` -> `showDialog(...)` returning a `_CreateSessionDialog` StatelessWidget.
   - `_CreateSessionDialog` has TextField + 2 buttons. "Créer" calls `sessionStore.insert(Session(...))`. "Créer et démarrer" additionally calls `activeSessionControllerProvider.notifier.start(newId)` wrapped by permission flow (`_maybeRunPermissionFlow(context)` helper).

4. Create `lib/presentation/screens/session_detail_screen.dart`:
   - Parameter `SessionId sessionId`.
   - Watches `activeSessionControllerProvider`.
   - Build: `AsyncValue.when`:
     - data -> conditional content based on state variant (pattern match on sealed ActiveSessionState).
   - Chrono: use `StreamBuilder` on `Stream.periodic(1s)` combined with `tracking.startedAtUtc` -> formatted "HH:mm:ss" or "mm:ss" based on duration.
   - Last fix display: "lat, lon ± accuracyM (timestamp)".
   - Stop button: `onPressed: () async { await ref.read(activeSessionControllerProvider.notifier).stop(); if (!context.mounted) return; /* no navigation needed - banner disappears */ }`.
   - Start button branch (Idle state): calls `_handleStart(context, ref)` helper that:
     - Read sessionSettings — if permission flow not completed, push `/permissions/rationale` (Task 2 screen) and await outcome.
     - If completed, call `activeSessionControllerProvider.notifier.start(sessionId)` inside try/catch for `ConcurrentActivationException`.
   - Delete button: AlertDialog confirm -> if state is Tracking for this session, show inline error; else `sessionStore.delete(id)` + `context.pop()`.
   - Rename: AlertDialog with TextField -> `sessionStore.update(session.copyWith(displayName: newName))`.

5. Update `lib/presentation/router.dart`:
   - Wrap with ShellRoute using AppShell.
   - Remove `/` -> PlaceholderHomeScreen; add `/` -> SessionListScreen.
   - Add `/sessions/:id` -> SessionDetailScreen (parse `SessionId(state.pathParameters['id']!)`).
   - Add stubs for `/settings`, `/permissions/rationale`, `/permissions/denied`, `/permissions/oem` — Task 2 replaces the stubs with real screens.
   - Keep `/about` and `/debug` unchanged.

6. Delete or reduce `lib/presentation/screens/placeholder_home_screen.dart`:
   - Prefer delete. Remove file and fix any imports.
   - Phase 01 tests referring to PlaceholderHomeScreen: update to either (a) remove (test was a smoke of "/" renders something) or (b) re-point to SessionListScreen.

7. Update `lib/main.dart`:
   - Add a single comment above `runApp`:
```dart
// Phase 05 — ActiveSessionController becomes the first productive consumer
// of appDatabaseProvider (Option A lazy resolution per CONTEXT.md). No
// structural change to main.dart; Riverpod resolves the DB on first
// ref.watch(appDatabaseProvider.future) from the controller.
```
   - Do NOT change runZonedGuarded structure.

8. Run `dart run build_runner build --delete-conflicting-outputs` to emit session_list_provider.g.dart.

9. Fill in 3 widget test files:
   - setUp: `setUpAll(() => TestWidgetsFlutterBinding.ensureInitialized())` if needed for SharedPreferences mock.
   - Use `UncontrolledProviderScope` with overrides:
     - `sessionStoreProvider` -> fake (hand-roll; exposes a `StreamController<List<Session>>` for watchAll + mutable list for insert/update/delete).
     - `activeSessionControllerProvider` -> fake via `AsyncNotifierProvider` override returning controllable state.
     - `fixStoreProvider` -> fake (returns fake count).
     - Wrap with `MaterialApp.router` or `MaterialApp(home: ...)` depending on route testing needs.
   - Tests per Behavior list.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter test test/presentation/screens/session_list_screen_test.dart test/presentation/screens/session_detail_screen_test.dart test/presentation/widgets/active_session_banner_test.dart</automated>
  </verify>
  <done>
SessionListScreen renders watchAll output + FAB + empty state; SessionDetailScreen status dashboard live-updates on Tracking state changes; delete-when-active blocked; rename persists; ConcurrentActivation flow confirmed; banner shows/hides based on state + route; router wired to new tree; PlaceholderHomeScreen removed; main.dart structurally unchanged; 3 widget test files green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings + permission screens + OEM guidance screen + remaining tests</name>
  <files>
    lib/presentation/screens/settings_screen.dart,
    lib/presentation/screens/permission_rationale_screen.dart,
    lib/presentation/screens/permission_denied_screen.dart,
    lib/presentation/screens/oem_guidance_screen.dart,
    lib/presentation/router.dart,
    test/presentation/screens/settings_screen_test.dart,
    test/presentation/screens/permission_rationale_screen_test.dart,
    test/presentation/screens/permission_denied_screen_test.dart,
    test/presentation/screens/oem_guidance_screen_test.dart
  </files>
  <behavior>
    - SettingsScreen:
      - Scaffold with AppBar "Paramètres".
      - ListView with a single Card containing:
        - Label "Filtre de distance" + current value "X m" + helper text "Plus dense = trace plus précise, consommation batterie plus élevée".
        - Slider value `sessionSettings.distanceFilterMeters.toDouble()`, min 2.0, max 100.0, divisions 98 (1m steps), onChanged throttled + onChangeEnd persists via `sessionSettingsProvider.notifier.setDistanceFilterMeters(v.round())`.
      - Secondary ListTile "Aide : batterie & arrière-plan" that navigates `/permissions/oem` (makes OemGuidanceScreen accessible post-first-run per CONTEXT.md).
    - PermissionRationaleScreen:
      - Full-screen Scaffold.
      - Column with icon + title "Pour suivre ton exploration" + body text (CONTEXT.md verbatim):
        "MirkFall a besoin de ta localisation en arrière-plan pour continuer à révéler le brouillard pendant que ton téléphone est dans ta poche, écran éteint. Tes positions restent sur ton téléphone. Aucun serveur, aucune publicité, aucune analytique."
      - FilledButton "Continuer" -> invokes permission flow (requestLocationAlways) and routes based on outcome:
        - granted -> if OEM match and !oem_guidance_seen: `/permissions/oem`; else pop with `true`.
        - whileInUseOnly -> pop with `true` (caller handles partial grant).
        - denied or permanentlyDenied -> `/permissions/denied`.
      - TextButton "Pas maintenant" -> pop with `false`.
    - PermissionDeniedScreen:
      - Scaffold with icon + title + text explaining.
      - FilledButton "Ouvrir les paramètres" -> `openLocationSettings()` (from permission flow helper, which calls `openAppSettings()` under the hood).
      - TextButton "Retour" -> pop.
    - OemGuidanceScreen:
      - Consumes `oemDetectorProvider` to detect current device family.
      - Renders vendor-specific 2-step guidance card based on `sealed OemFamily`:
        - XiaomiFamily -> steps for Xiaomi/MIUI battery saver whitelist + "Autostart" permission.
        - SamsungFamily -> Samsung Device Care -> App power management.
        - HuaweiFamily -> HUAWEI battery management.
        - OnePlusFamily -> OxygenOS battery settings.
        - OppoFamily -> ColorOS battery whitelist.
        - OtherOem -> generic message "Ton device n'est pas un battery-killer connu; aucune étape spécifique."
        - IosDevice -> render a note "iOS gère automatiquement; aucune étape requise" + no actions.
      - "Plus d'info : dontkillmyapp.com/[vendor]" link -> `share_plus.share('https://dontkillmyapp.com/xiaomi')` etc.
      - "OK j'ai fait" FilledButton -> `sessionSettingsProvider.notifier.markOemGuidanceSeen()` + pop.
    - Router:
      - Replace Task 1 stubs with the real screens.
    - Tests:
      - `settings_screen_test.dart` GREEN: `rendersCurrentDistanceFilterValue`, `sliderOnChangeEndPersistsViaProvider`, `helpTileNavigatesToOem`.
      - `permission_rationale_screen_test.dart` GREEN: `displaysContextMdCopyVerbatim`, `continuerInvokesRequestLocationAlways`, `notMaintenantPopsWithFalse`, `deniedOutcomeRoutesToDeniedScreen`.
      - `permission_denied_screen_test.dart` GREEN: `openSettingsInvokesHandler`, `retourPops`.
      - `oem_guidance_screen_test.dart` GREEN: `rendersXiaomiStepsWhenXiaomiFamily`, `rendersSamsungSteps`, `rendersIosNoteWhenIos`, `okMarksOemGuidanceSeen`, `linkInvokesShare`.
  </behavior>
  <action>
1. Create `lib/presentation/screens/settings_screen.dart`:
   - `ConsumerStatefulWidget` to hold a local slider value (for smooth dragging without rebuild storm).
   - `initState`: read current value from provider.
   - On `Slider.onChangeEnd`: `await ref.read(sessionSettingsProvider.notifier).setDistanceFilterMeters(localValue.round()); if (!mounted) return;`.
   - Helper tile for `/permissions/oem` navigation.

2. Create `lib/presentation/screens/permission_rationale_screen.dart`:
   - `ConsumerWidget`.
   - Body text VERBATIM from CONTEXT.md §Permission flow rationale écran (cited in <locked_decisions> above).
   - Continuer button: async handler calls `requestLocationAlways()`, switches on outcome, navigates/pops accordingly. Use `if (!context.mounted) return;` after every `await`.

3. Create `lib/presentation/screens/permission_denied_screen.dart`:
   - Simple Scaffold with explanation + 2 buttons.
   - "Ouvrir les paramètres" onPressed: `await openLocationSettings(); if (!context.mounted) return;` (no navigation; user returns manually from system settings).

4. Create `lib/presentation/screens/oem_guidance_screen.dart`:
   - `ConsumerStatefulWidget` to cache the Future result of `oemDetectorProvider.future`.
   - `initState` kicks off the detection `Future<OemFamily>`.
   - Build uses pattern match on sealed `OemFamily` to select a `_OemCard` widget with appropriate vendor copy.
   - Link handler calls `share_plus`'s `Share.share('https://dontkillmyapp.com/$vendorSlug')`.
   - OK button calls `markOemGuidanceSeen()` then pops.

5. Update `lib/presentation/router.dart` — replace Task 1 route stubs with actual screen classes.

6. Fill in 4 widget test files per Behavior list:
   - Use ProviderContainer overrides for:
     - `sessionSettingsProvider` with a seeded `SessionSettingsSnapshot(distanceFilterMeters: 15, ...)`.
     - `oemDetectorProvider` returning `const XiaomiFamily()` / `const IosDevice()` etc. per test.
     - A test seam for `requestLocationAlways` (either expose it as a provider, OR make PermissionRationaleScreen take an optional `Future<LocationPermissionOutcome> Function()` override for testing).
   - For share test: override `share_plus` via its mock interface OR verify `Share.share` invocation via a test seam.

7. Run `flutter test test/presentation/screens/settings_screen_test.dart test/presentation/screens/permission_rationale_screen_test.dart test/presentation/screens/permission_denied_screen_test.dart test/presentation/screens/oem_guidance_screen_test.dart` — all green.
  </action>
  <verify>
    <automated>flutter test test/presentation/screens/ && node -e "const fs=require('fs');if(!fs.readFileSync('DEPENDENCIES.md','utf8').includes('share_plus')){process.exit(1)}"</automated>
  </verify>
  <done>
SettingsScreen slider persists via provider; PermissionRationaleScreen copy matches CONTEXT.md verbatim; Denied screen invokes openAppSettings; OemGuidanceScreen renders vendor-specific steps + share-link works; all 4 new test files green; router has all 6 routes + ShellRoute + banner wrapping. **share_plus reuse note:** this plan does NOT add share_plus — it was already pinned + audited in Phase 01 (DEPENDENCIES.md entry at line 48, share_plus 12.0.2 BSD-3-Clause, audit date 2026-04-17). No new DEPENDENCIES.md audit required; the automated verify above just confirms the existing entry is still in place.
  </done>
</task>

</tasks>

<verification>
- `dart run build_runner build` succeeds.
- `flutter test test/presentation/` all green (8 test files — Plan 05-01 stubs now green: session_list, session_detail, settings, permission_rationale, permission_denied, oem_guidance, active_session_banner + any test banner derivatives).
- `flutter analyze` zero warning across `lib/presentation/`.
- `dart run tool/check_headers.dart` green.
- `flutter test` full suite green (Phase 01-04 regressions clean).
- Visual sanity: run `flutter run -d windows` (if desktop target working) and navigate /, /sessions/:id, /settings, /permissions/*. NOT required for automated verify but recommended.
</verification>

<success_criteria>
- `/` renders SessionListScreen — PlaceholderHomeScreen removed from codebase.
- SessionDetailScreen live-updates tracking dashboard (chrono, fixCount, lastFix) on state changes.
- ActiveSessionBanner appears on all routes except `/sessions/:id` when Tracking.
- SettingsScreen slider persists distanceFilter.
- Permission flow UI chain (rationale -> OS -> OEM/denied -> session start) functions end-to-end in widget tests with fakes.
- OemGuidanceScreen renders vendor-specific guidance for all 6 OEM families.
- `share_plus` reused for dontkillmyapp.com link (no url_launcher added).
- CLAUDE.md `if (!context.mounted) return;` discipline preserved after every await in widgets.
- All 8 Wave-0 widget-test stubs turned GREEN.
- main.dart structurally unchanged (Phase 03 STATE.md promise kept).
</success_criteria>

<output>
After completion, create `.planning/phases/05-gps-session-lifecycle/05-04-SUMMARY.md` documenting:
- UI surface map (what screen lives at which route).
- State machine -> widget rendering decision tree.
- OEM guidance copy per family (verbatim so reviewers can spot-check).
- Widget test fixtures reused (fake SessionStore, fake ActiveSessionController — candidates for `test/helpers/` promotion in Phase 07+).
- Handoff for Plan 05-05 (auto-resume post-kill): the SessionNotificationService + route `/sessions/:id` + payload `resume:<sessionId>` contract.
</output>
