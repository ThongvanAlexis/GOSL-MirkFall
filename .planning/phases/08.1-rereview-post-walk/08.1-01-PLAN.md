---
phase: 08.1-rereview-post-walk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
  - .planning/ROADMAP.md
  - .planning/phases/08.1-rereview-post-walk/08.1-01-SUMMARY.md
autonomous: false
requirements: []
success_criteria:
  - user-first-protocol-gate
must_haves:
  truths:
    - "`08.1-REVIEW.md` exists on `main` with a 5-section skeleton (User-observed findings / Claude audit findings / Triage decisions / Adversarial evidence / CI-green confirmation) — `grep -c '^## [1-5]\\.' 08.1-REVIEW.md` returns exactly 5"
    - "§1 contains either the user's verbatim IDE observations on the Phase 08 49-fix delta (commits between `254b5d2` and current main tip) OR the explicit `Aucune observation utilisateur` marker (Phase 04 + 06 + 08 precedent), committed BEFORE any later plan reads walk artifacts or spawns sub-agents"
    - "§1b placeholder sub-section exists in the skeleton with per-device `<details>` blocks for Android + iOS walk evidence, so Plan 08.1-02 can fill them without re-editing the upstream structure"
    - "§2 contains a `Pre-known from 08.1-CONTEXT` sub-section placeholder with 7 empty rows + a `Smell heuristics hot-spots (re-check post-Phase 08 refactors)` table placeholder (6 components pre-listed per 08.1-RESEARCH §Pattern 4: PmtilesDownloadController / MapCameraController / ActiveSessionController+State / DownloadState / MapScreen lifecycle / StyleRewriter) so Plan 08.1-03 Task 1 fills them BEFORE spawning the 4 sub-agents"
    - "§4 contains N empty sub-blocks for walk-finding-driven regression tests (count determined by walk findings — Plan 08.1-04 fills ; default = 0 permanent tests + gate-verification-only per 08.1-RESEARCH §Pattern 3 default-skip adversarial)"
    - "ROADMAP.md is amended: Phase 08.1 row in Progress table added with `0/5 | In Progress | -` ; Phase 08.1 section `Plans: 0 plans (run /gsd:plan-phase 08.1)` flipped to `Plans: 5 plans (08.1-01..08.1-05)` with the 5 Plans entries populated"
    - "Two atomic commits on main: `docs(08.1-rev): scaffold 08.1-REVIEW.md 5-section skeleton`, `docs(08.1-rev): capture user-observed findings into 08.1-REVIEW.md §1` + `docs(roadmap): add Phase 08.1 row + 5 plan list` folded into either the scaffold commit OR the capture commit (Claude's discretion — both keep bisectability)"
    - "NO fresh walk artifacts referenced in this plan — walk capture is Plan 08.1-02 responsibility (blocking checkpoint there)"
    - "NO agent spawn, NO pre-class, NO audit reasoning in Plan 08.1-01 — strict user-first protocol locked Phases 02+04+06+08 (third cycle re-encoded)"
  artifacts:
    - path: ".planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md"
      provides: "5-section review artifact with §1 user-first capture slot + §1b per-device walk-evidence placeholders + §2 pre-class (7 items) + smell heuristics hot-spots re-check table (6 rows) + §4 walk-finding placeholder"
      contains: "# Phase 08.1: Re-Review — Post-Walk Audit"
      min_lines: 100
    - path: ".planning/ROADMAP.md"
      provides: "Phase 08.1 Progress row + 5-plan list populated"
      contains: "Phase 08.1"
  key_links:
    - from: "Claude-main"
      to: "User (IDE review session on Phase 08 49-fix delta — commits `254b5d2` → current main tip covering ~25 lib/ + ~25 test/ + ~6 tool/ + ~5 docs files)"
      via: "CLAUDE.md §Code Review Phases protocol step 1 — solicitation message"
      pattern: "qu'as-tu vu|findings?|IDE"
    - from: "`08.1-REVIEW.md` §1"
      to: "Plan 08.1-02 Wave 2 walk-evidence capture + Plan 08.1-03 Wave 3 agent spawn trigger"
      via: "§1 must be non-empty (verbatim user capture or explicit marker) before 08.1-02 / 08.1-03 — strict user-first protocol locked Phases 02+04+06+08"
      pattern: "## 1\\. User-observed findings"
    - from: "`08.1-REVIEW.md` §2 sub-section `Pre-known from 08.1-CONTEXT`"
      to: "Plan 08.1-03 Task 1 pre-classification before agent spawn"
      via: "08.1-RESEARCH §Pattern 4 — 5-7 pre-class items (narrower than Phase 08's 10)"
      pattern: "### Pre-known from 08\\.1-CONTEXT"
    - from: "ROADMAP.md Phase 08.1 row"
      to: "gsd-verifier Phase 08.1 open-then-close lifecycle"
      via: "Status `0/5 | In Progress | -` at Plan 08.1-01 close ; flipped `5/5 | Complete | YYYY-MM-DD` at Plan 08.1-05 closure"
      pattern: "\\| Phase 08\\.1"
---

<objective>
Encode the CLAUDE.md §Code Review Phases protocol as a hard ordering gate for Phase 08.1 (re-review post-walk) AND open Phase 08.1 formally in ROADMAP.md. User's IDE observations on the Phase 08 49-fix delta (everything between commit `254b5d2` and current main tip) are captured into `08.1-REVIEW.md` §1 BEFORE Claude reads any walk artifact or spawns any audit sub-agent. ROADMAP is amended the same wave to flip Phase 08.1 from `0/TBD` placeholder to `0/5 | In Progress` with the 5-plan list populated — this gives gsd-verifier a structural handle throughout the phase.

Purpose: The user-first rule is the non-negotiable meta-process locked Phases 02+04+06+08. Phase 08.1 is the fourth cycle. Re-encoding it here (as opposed to assuming "user already understands") validates the rule as structural, not ceremonial. If the user is biased by Claude's findings before posting their own observations on the 49-fix delta (especially the 9 smell-tagged refactors which are architecturally load-bearing), the re-review's value is destroyed — we would just ratify what we already landed. Phase 08.1 reuses the Phase 08 review-gate template with documented narrowing (smell-heuristics re-check lens per 08.1-RESEARCH §Pattern 4 ; 5-7 pre-class items instead of Phase 08's 10 since iOS fix resolved + ROADMAP+REQUIREMENTS already synced + CountryResolver edges test-covered row #24). Walk-evidence is Plan 08.1-02 responsibility (separate wave, blocking checkpoint there) — this plan is scaffold + user-first only.

Output: `08.1-REVIEW.md` committed with a complete 5-section skeleton + §1b per-device walk placeholders + §2 7 pre-class rows + smell hot-spots re-check table (6 rows) + §4 walk-finding-driven placeholders. §1 populated verbatim (or explicit "Aucune observation utilisateur"). ROADMAP.md amended (Phase 08.1 row + 5-plan list). Two or three atomic commits on main.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-rereview-post-walk/08.1-CONTEXT.md
@.planning/phases/08.1-rereview-post-walk/08.1-RESEARCH.md
@.planning/phases/08.1-rereview-post-walk/08.1-VALIDATION.md
@.planning/phases/08-review-gate-map/08-01-PLAN.md
@.planning/phases/08-review-gate-map/08-REVIEW.md
@.planning/phases/08-review-gate-map/08-05-SUMMARY.md
@.planning/phases/06-review-gate-gps/06-01-PLAN.md
@.planning/phases/06-review-gate-gps/06-REVIEW.md
@CLAUDE.md

<interfaces>
<!-- Key rules this plan MUST encode verbatim. -->

From CLAUDE.md §Code Review Phases (verbatim binding constraint — both the old rule + the 2026-04-23 smell-heuristics delta):
```
**Protocole :**

- Tu me demandes d'abord ce que j'ai vu qui a besoin d'être revu
- Ensuite tu fais ton audit
- Tu affiches uniquement les **titres** avec une courte explication, et tu me laisses choisir ce qu'on corrige

### En review faire attention specifiquement a :

**Code alambiqué par empilement de fix** (defensive null chains, flags booléens accumulés, early returns with "// fix for edge case", wrappers-of-wrappers, try/catch rattrapant des exceptions qu'un design correct aurait rendues impossibles)

**State machine tirée par les cheveux** (enum d'états dont plusieurs valeurs ne diffèrent que par 1-2 champs, transitions pas-vraiment-un-graphe, états intermédiaires sync-only, dispatcher géant qui duplique)
```

From 08.1-CONTEXT.md §Ordering strict user-first protocol (inherited from 08-CONTEXT):
```
- User poste ses findings IDE en chat AVANT que Claude spawn quoi que ce soit
- Claude capture verbatim dans 08.1-REVIEW.md §1 (ou 'Aucune observation utilisateur' marker)
- ENSUITE §1b walk-evidence capture via Plan 08.1-02 (requires docs/phase-08.1-walk.md + screenshots committed by user first)
- ENSUITE pre-class §2 des 5-7 items handoff (narrower than Phase 08's 10)
- ENSUITE spawn 4 sub-agents en single tool-use message
- Parallèle (user tape pendant que agents tournent) explicitement rejeté
```

**5-section skeleton contract** (adapted from 08-REVIEW.md exemplar, shaped for Phase 08.1 re-review lens):
```
## 1. User-observed findings (IDE review)
### 1b. Device walk evidence (Phase 08.1 re-review trigger)
## 2. Claude audit findings
### Pre-known from 08.1-CONTEXT
### Smell heuristics hot-spots (re-check post-Phase 08 refactors)
### Agent #1 — Download pipeline + atomicity (re-check Phase 08 smell-surgery)
### Agent #2 — Controllers + state + Riverpod wiring (re-check Phase 08 shape-consolidation)
### Agent #3 — Presentation + router + walk-findings triage
### Agent #4 — Tests + tooling + natives + CI + CLAUDE.md cross-cutting + smell transverses
## 3. Triage decisions
## 4. Adversarial evidence
(N walk-finding-driven permanent test blocks — count determined by Plan 08.1-04 ; default N=0)
## 5. CI-green confirmation
```

**7 pre-class items expected in §2 (08.1-RESEARCH §Pattern 4):**
1. All 26 Phase 08 Noted items — confirm still deferred (out of scope unless walk re-surfaces) — **Noted**
2. Phase 08 Row #38 ActiveSessionController reconcile-pattern rewrite scope-down — Agent #2 verifies `_bestEffort` helper extraction holds — **Noted**
3. Phase 08 Row #39 MapScreen deactivate microtask scope-down — Agent #3 verifies named-helper extract holds under walk — **Noted**
4. Phase 08 Row #33 drop_then_retry soak test timeout (60s ≈ 36s retry backoffs, 1 CI flake observed) — Agent #4 lens: bump OR split? — **Could**
5. Walk findings hot-spots — category-inline like Phase 08 smell hot-spots, NOT a single finding row — **category-inline**
6. Smell heuristics hot-spots re-check after refactors — did rows #11/#12/#20/#29/#35/#36/#37 introduce new smells? — **category-inline**
7. ROADMAP + REQUIREMENTS sync confirmed — Phase 08 closure amended both ; Plan 08.1-01 adds Phase 08.1 row ; Plan 08.1-05 flips to Complete — **Noted** positive confirmation if no drift

**Smell heuristics hot-spots re-check table (6 components, pre-listed — 08.1-RESEARCH §Code Examples / Smell-Heuristics Hot-Spots Table Template):**
| Component | File path | Primary Agent | Phase 08 action | Phase 08.1 lens |
|-----------|-----------|---------------|-----------------|-----------------|
| PmtilesDownloadController 7-step | `lib/infrastructure/downloads/pmtiles_download_controller.dart` | #1 | Row #1 break-on-pause ; Row #5 staging-nuke ; Row #20 dispatcher collapse | Did dispatcher collapse hide corner case walk hits? Pause/resume clean on real device? |
| MapCameraController follow/pan | `lib/application/controllers/map_camera_controller.dart` | #2 | Row #11 listeners consolidation ; Row #14 16-line comment trim ; Row #35 timestamp replaces flag+Timer ; Row #36 Centering→Following(hasFirstFix) collapse | State collapse break any follow-me edge under walk? .g.dart regenerate clean CI? |
| ActiveSessionController + State | `lib/application/controllers/active_session_controller.dart` + `.../state/active_session_state.dart` | #2 | Row #37 drop ErrorState → AsyncError ; Row #38 `_bestEffort` helper (scope-down) | ErrorState drop cause UI pattern-match misses walk? `_bestEffort` swallow walk error? |
| DownloadState | `lib/domain/downloads/download_state.dart` | #1 | Row #20 unified snapshot field + polymorphic getters ; Row #29 transition graph doc ; Row #42 Completed/Cancelled carry DownloadJob | Polymorphic `snapshot` getter resolve correctly at all UI sites post-refactor? |
| MapScreen lifecycle | `lib/presentation/screens/map_screen.dart` | #3 | Row #39 deactivate microtask named-helper extract (scope-down) ; Row #40 SchedulerBinding.addPostFrameCallback defer | Named-helper resist real-device lifecycle push? postFrameCallback visible slow CPU? |
| StyleRewriter + validators | `lib/infrastructure/map/style_rewriter.dart` + `style_layer_order.dart` | #1 | Row #3 external http[s] URL runtime rejection ; Row #22 shared `_iterateStyleLayers` generator | Regression risk low — simple extracts. Noted unless walk flags. |

**Phase 08 49-fix delta scope (per 08.1-RESEARCH §Code Examples):**
The commits between `254b5d2` (Phase 08 closure) and current main tip touched 83 files (~25 lib/ + ~25 test/ + ~6 tool/ + ~5 docs). User IDE review SHOULD cover this delta. Full enumeration:
- `git log --grep='08-rev' --name-only --pretty=format: --since="2026-04-23" | sort -u | grep -v '^$'`

GOSL header policy: `tool/check_headers.dart` targets `.dart` files only. `.md` review artefacts are EXEMPT — do NOT prepend the GOSL header to `08.1-REVIEW.md`. Consistent with Phase 02+04+06+08 precedent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the 5-section 08.1-REVIEW.md skeleton (§1b walk-evidence placeholders + §2 pre-class (7) + smell re-check table (6) + §4 walk-finding placeholder)</name>
  <files>.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md</files>
  <action>
    Create `.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md` with the 5-section skeleton matching the format locked in 02/04/06/08-REVIEW.md exemplars but adapted for Phase 08.1's re-review-post-walk lens. Sections MUST be in this order and named exactly (`gsd-verifier` greps `^## [1-5]\.` to confirm all 5 present).

    Use the Write tool to create the file with the following contents verbatim (adapt only the `*(pending …)*` marker copy if rewording improves clarity):

    ```markdown
    # Phase 08.1: Re-Review — Post-Walk Audit Review

    **Opened:** 2026-04-24
    **Status:** open
    **Closed:** (pending)

    ## 1. User-observed findings (IDE review)

    *Captured verbatim at phase start, BEFORE Claude reads walk artifacts and BEFORE Claude spawns any audit sub-agent.*

    (awaiting user input — Task 2 of this plan fills this section)

    ### 1b. Device walk evidence (Phase 08.1 re-review trigger)

    *Filled by Plan 08.1-02 after the user completes Android + iOS walks and commits `docs/phase-08.1-walk.md` + screenshots under `docs/phase-08.1-walk-screenshots/`. Plan 08.1-02 BLOCKS on a `checkpoint:human-verify` gate until those artifacts exist on main. Per-device collapsed `<details>` sections (Phase 06 + Phase 08 precedent).*

    <details>
    <summary>Android — Walk (pending Plan 08.1-02)</summary>
    (pending — filled by Plan 08.1-02 from `docs/phase-08.1-walk.md` + `docs/phase-08.1-walk-screenshots/android-*.png`)
    </details>

    <details>
    <summary>iOS — Walk (pending Plan 08.1-02)</summary>
    (pending — filled by Plan 08.1-02 from `docs/phase-08.1-walk.md` + `docs/phase-08.1-walk-screenshots/ios-*.png`)
    </details>

    **Walk-evidence snapshot:** *(pending — 1-paragraph summary PASS / PASS-with-findings / FAIL, filled by Plan 08.1-02)*

    ## 2. Claude audit findings

    *Filled by Plan 08.1-03: first the 7 pre-classified 08.1-CONTEXT handoff items + the Smell heuristics hot-spots re-check table, then the 4 parallel sub-agents in ONE tool-use message (re-balanced slicing per 08.1-RESEARCH §Pattern 2 around Phase 08 delta + walk findings).*

    Format: `[severity] Title — 1-line explanation — file:line`. Severities: Blocker / Should / Could / Noted. Smell-tagged findings get an inline `[smell:fix-on-fix]` or `[smell:over-state-machine]` tag after severity.

    ### Pre-known from 08.1-CONTEXT

    *Filled by Plan 08.1-03 Task 1 BEFORE spawning sub-agents. Source: 08.1-CONTEXT.md §Implementation Decisions inherited from 08-CONTEXT + 08.1-RESEARCH §Pattern 4 (narrower than Phase 08's 10). Committed as `docs(08.1-rev): pre-class 7 handoff items + 6 smell re-check rows into §2` before any Agent tool call.*

    (pending — 7 entries: Phase 08 Noted deferral confirm Noted | Row #38 `_bestEffort` holds Noted | Row #39 named-helper holds Noted | Row #33 soak timeout Could | Walk findings hot-spots category-inline | Smell re-check category-inline | ROADMAP+REQ sync Noted)

    ### Smell heuristics hot-spots (re-check post-Phase 08 refactors)

    *Filled by Plan 08.1-03 Task 1 alongside pre-class items. Source: 08.1-RESEARCH §Code Examples — Smell-Heuristics Hot-Spots Table Template. Re-check lens (not fresh smell-discovery): did Phase 08 refactors INTRODUCE a new smell? Is there a regression? Is there a walk-findings-aggravated shape?*

    | Component | File path | Primary Agent | Phase 08 action | Phase 08.1 lens |
    |-----------|-----------|---------------|-----------------|-----------------|
    | (pending — 6 rows) | | | | |

    ### Agent #1 — Download pipeline + atomicity (re-check Phase 08 smell-surgery)
    *Scope: `lib/infrastructure/downloads/**` + 8 soak scenarios (6 original + 2 Phase 08 edges) + `lib/domain/downloads/download_state.dart` (row #20/#29 dispatcher-collapse refactor landed). Hot spots: `pmtiles_download_controller.dart` (rows #1/#5/#7/#10/#19/#29) + `download_state.dart` (rows #20/#29) + `binary_concatenator.dart` (row #30) + `atomic_renamer.dart` (row #9). Lens: did dispatcher collapse introduce regressions? Does break-on-pause fix hold?*
    (pending)

    ### Agent #2 — Controllers + state + Riverpod wiring (re-check Phase 08 shape-consolidation)
    *Scope: `lib/application/controllers/**` + `lib/application/state/active_session_state.dart` + all `.g.dart` regenerated. Hot spots: `map_camera_controller.dart` (rows #11/#14/#35/#36 — 4 commits, 2 smell-refactors) + `active_session_controller.dart` + `active_session_state.dart` (rows #37/#38) + `country_resolver_controller.dart` (rows #12/#23) + `installed_maps_controller.dart` (row #13). Lens: post-refactor regression risk on walk — did ErrorState drop break UI pattern-match? Did listener consolidation introduce races?*
    (pending)

    ### Agent #3 — Presentation + router + walk-findings triage
    *Scope: `lib/presentation/screens/**` + `lib/presentation/widgets/**` + `lib/application/routing/router.dart` + **walk-findings go here first**. Hot spots: `map_screen.dart` (rows #14/#39/#40) + `session_burger_menu.dart` (row #41 `_DistanceRow` honest-placeholder) + `maps_download_screen.dart` (row #43 duration constants) + `map_download_progress_chip.dart` + `map_follow_me_fab.dart`. Lens: scope-down on MapScreen.deactivate (row #39) held under real device lifecycle? `_DistanceRow` placeholder matched user UX expectations?*
    (pending)

    ### Agent #4 — Tests + tooling + natives + CI + CLAUDE.md cross-cutting + smell transverses
    *Scope: all `test/**` touched by 49-fix + `integration_test/` (4 files absorbed) + `tool/test/*` (4 new paired tests) + `.github/workflows/ci.yml` (new "Check style no external URL" step) + platform channels (Kotlin + Swift, row #49 BootCompletedReceiver fix) + `DEPENDENCIES.md` (row #48 integration_test entry). Lens: inertness guards holding? CI gate still catching? Test-file drift from 49-fix loop? Paired tests for tooling exhaustive? Smell heuristics cross-cutting — any fix from the loop introduced a new over-state-machine or fix-on-fix?*
    (pending)

    <details>
    <summary>Audit Notes (narrative appendix, per agent)</summary>
    (pending)
    </details>

    ## 3. Triage decisions

    *Filled by Plan 08.1-03 Task 5 after user selects what to fix. Every Blocker MUST be `fix` (waiver forbidden). Every Should MUST be either `fix` or `waived` with inline rationale. Smell-tagged findings get an explicit decision (fix / refactor / defer) visible in the table. Row # is LOCAL to Phase 08.1 §3; when cross-referencing Phase 08 rows use `08§3-#N` format (08.1-RESEARCH §Pitfall 6).*

    | # | Finding | Severity | Smell tag | Decision | Rationale | Commit hash |
    |---|---------|----------|-----------|----------|-----------|-------------|
    | (pending) | | | | | | |

    ## 4. Adversarial evidence

    *Filled by Plan 08.1-04. **Default = no new CI gate, no new adversarial branch** (08.1-RESEARCH §Pattern 3 default-skip — 1-branch-per-gate-script convention means no new script → no new branch). Plan 08.1-04 verifies existing gates still green + may add walk-finding-driven permanent regression tests (inertness-guarded, pattern Phase 04 Test #3 / Phase 08 7-test wave).*

    **Existing CI gates verified live (Plan 08.1-04 Task 1):**

    - `tool/check_style_no_external_url.dart` (Phase 08) — exit 0 on current main
    - `tool/check_avoid_maplibre_leak.dart` (Phase 07) — exit 0 on current main
    - `tool/check_avoid_remote_pmtiles.dart` (Phase 07) — exit 0 on current main
    - `tool/check_platform_manifests.dart` (Phase 06) — exit 0 on current main
    - `tool/check_headers.dart` (Phase 01) — exit 0 on current main
    - `tool/check_licenses.dart` (Phase 01) — exit 0 on current main
    - `tool/check_dependencies_md.dart` (Phase 01) — exit 0 on current main
    - `tool/check_domain_purity.dart` (Phase 03) — exit 0 on current main

    (pending — Plan 08.1-04 confirms all 8 gates green + adversarial branch `adversarial/08-style-external-url` is still deleted remote)

    **Walk-finding-driven regression tests (count TBD — Plan 08.1-04 fills if walk surfaced a reproducible bug):**

    *(default N=0 — no permanent test added unless walk findings demand. If added, each gets its own sub-block with File / Type / Covers / Commit / Verify command / Inertness guard quote / Mutation experiment — Phase 08 §4 format precedent.)*

    (pending — Plan 08.1-04 fills with N test blocks per walk-finding regression ; may be zero)

    ## 5. CI-green confirmation

    *Filled by Plan 08.1-05 Task 3 after all Blocker + non-waived Should fixes are applied and CI is green.*

    - **Final commit on main:** (pending)
    - **CI run URL:** (pending)
    - **Status:** (pending — all 3 jobs gates+android+ios green, optionally integration-tests if job still split)
    - **Date:** (pending)

    ---
    _Phase 08.1 closed: (pending)_
    _Phase 09 remains unblocked (no new gating from 08.1; re-review is pure additive audit)._
    ```

    GOSL header policy: do NOT prepend the GOSL Dart copyright header to this `.md` file. `tool/check_headers.dart` targets `.dart` files only.

    After writing, verify the 5 top-level sections are present and the sub-sections are in place:
    ```bash
    grep -c "^## [1-5]\\." .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    # must return 5

    grep -q "^### 1b\\. Device walk evidence" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "^### Pre-known from 08\\.1-CONTEXT" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "^### Smell heuristics hot-spots (re-check post-Phase 08 refactors)" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "^### Agent #1" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "^### Agent #4" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```

    Then commit:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(08.1-rev): scaffold 08.1-REVIEW.md 5-section skeleton" --files .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && [ "$(grep -c "^## [1-5]\\." .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md)" = "5" ] && grep -q "^### 1b\\. Device walk evidence" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "^### Pre-known from 08\\.1-CONTEXT" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "^### Smell heuristics hot-spots" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "^### Agent #1" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "^### Agent #4" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md'</automated>
  </verify>
  <done>`08.1-REVIEW.md` exists, all 5 top-level sections present (grep count = 5), §1b per-device walk placeholders present, §2 `Pre-known from 08.1-CONTEXT` sub-section present, §2 `Smell heuristics hot-spots (re-check post-Phase 08 refactors)` sub-section present, §4 default-skip-adversarial commentary present with 8 existing gates listed + walk-finding-driven N=0 default, `**Status:** open`, sections 1-5 in correct order, skeleton committed as `docs(08.1-rev): scaffold 08.1-REVIEW.md 5-section skeleton`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-first IDE review — solicit, capture, commit into §1 (protocol gate — blocks all downstream plans)</name>
  <files>.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md</files>
  <action>
    This is the CLAUDE.md §Code Review Phases protocol gate for Phase 08.1. Strict ordering: Claude asks FIRST, user answers, Claude captures verbatim, THEN Plan 08.1-02 walk-evidence capture AND Plan 08.1-03 agent spawn unblock.

    Claude-main posts in chat as the first non-automated action of this wave:

    > "Phase 08.1 est un **re-review post-walk**. Avant que je lance la capture walk-evidence (Plan 08.1-02) puis les 4 sub-agents d'audit (Plan 08.1-03), qu'as-tu vu dans ton IDE qui mérite d'être revu sur le delta Phase 08 ?
    >
    > **Scope du delta à auditer** (commits entre `254b5d2` Phase 08 closure et current main tip) :
    >
    > Les 49 fix+refactor commits du fix-loop Phase 08 Plan 08-05 ont touché ~83 fichiers :
    > - **~25 fichiers lib/ de production** (dont : `pmtiles_download_controller.dart`, `map_camera_controller.dart`, `active_session_controller.dart` + `active_session_state.dart`, `download_state.dart`, `map_screen.dart`, `style_rewriter.dart`, `country_resolver.dart`, `binary_concatenator.dart`, `atomic_renamer.dart`, `download_queue_store.dart`, `http_chunk_downloader.dart`, `first_launch_bootstrap.dart`, `country_delete_service.dart`, `disk_space_checker.dart`, `style_layer_order.dart`, `session_burger_menu.dart`, `map_download_progress_chip.dart`, `map_follow_me_fab.dart`, `maps_download_screen.dart`, `maps_manage_screen.dart`, `constants.dart`, `maplibre_map_view.dart`, `country_catalog.dart`, `map_errors.dart`, `map_providers.g.dart`)
    > - **~25 fichiers test/** (controllers + domain + infrastructure + fakes + presentation — dont 3 NEW : `world_bundle_sha256_test.dart`, `manifest_atomicity_contract_test.dart`, `no_httpclient_in_unit_tests_test.dart`)
    > - **~6 fichiers tool/** (dont 4 NEW paired tests : `check_style_no_external_url_test.dart`, `generate_tiny_pmtiles_test.dart`, `generate_world_sha256_test.dart`, `prepare_style_test.dart`, `simplify_polygons_test.dart`)
    > - **~5 fichiers docs/config** (AndroidManifest.xml row #49, ci.yml new step, dart_test.yaml, DEPENDENCIES.md row #48, assets/maps/style.json row #2/#15)
    >
    > **Focus smell-heuristics re-check** (08.1-RESEARCH §Pattern 4) — 6 composants refactorés Phase 08 à re-vérifier :
    > - `PmtilesDownloadController` (row #1 break-on-pause, row #5 staging-nuke, row #20 dispatcher collapse) — dispatcher collapse a-t-il caché un corner case ?
    > - `MapCameraController` (row #11 listeners consolidation, row #35 timestamp replaces flag+Timer, row #36 Centering→Following(hasFirstFix) collapse) — state collapse casse un follow-me edge ?
    > - `ActiveSessionController + State` (row #37 drop ErrorState → AsyncError, row #38 `_bestEffort` helper scope-down) — drop ErrorState a cassé un UI pattern-match ? `_bestEffort` swallow une erreur du walk ?
    > - `DownloadState` (row #20 unified snapshot + polymorphic getters, row #29 transition graph doc, row #42 Completed/Cancelled carry DownloadJob) — polymorphic `snapshot` getter résout correctement à tous les sites UI ?
    > - `MapScreen.deactivate` (row #39 named-helper extract scope-down, row #40 postFrameCallback defer) — named-helper tient sous lifecycle real device ?
    > - `StyleRewriter + validators` (row #3 external http[s] URL runtime rejection, row #22 shared `_iterateStyleLayers` generator) — regression risk low, mais lens ouvert si walk flag.
    >
    > **Poste ta liste telle quelle** — je la capture verbatim dans `08.1-REVIEW.md` §1 avant de demander le walk + audit. Si tu n'as rien vu, dis-le explicitement — je note 'Aucune observation utilisateur' et on enchaîne sur Plan 08.1-02 (walk capture).
    >
    > **Note** : le walk Android + iOS lui-même (findings observés en runtime sur device) est capturé séparément en Plan 08.1-02 — cette solicitation est pour ta review IDE / lecture de code, pas pour le walk."

    User responds with either:
    - A list of findings (any format — bullets, prose, file references) → Claude captures verbatim into §1
    - "rien vu" / "aucune observation" / equivalent → Claude writes `*Aucune observation utilisateur — l'user n'a pas identifié de point à revoir dans son IDE sur le delta Phase 08.*` into §1 (Phase 04 + 06 + 08 precedent)

    Either way, Claude then:
    1. Edits `08.1-REVIEW.md` §1 to replace `(awaiting user input — Task 2 of this plan fills this section)` with the captured content. Leave §1b placeholder untouched (Plan 08.1-02 fills it).
    2. Commits:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(08.1-rev): capture user-observed findings into 08.1-REVIEW.md §1" --files .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
       ```
    3. Confirms to user: "§1 capturé et committé. J'enchaîne sur Plan 08.1-01 Task 3 (ROADMAP amendment : ajoute Phase 08.1 row + 5-plan list) dès que tu valides (réponse : `OK roadmap` ou équivalent)."

    Anti-pattern guard: Do NOT spawn any Agent/Task tool call AND do NOT read walk artifacts (which don't exist yet anyway) in this wave before §1 is committed. The whole point of the checkpoint is to enforce user-first — CONTEXT explicitly rejects the parallel approach (user typing while agents / walk-review run).
  </action>
  <verify>
    <automated>bash -c '! grep -q "awaiting user input" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && git log --oneline -5 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -q "capture user-observed findings"'</automated>
  </verify>
  <done>
    `08.1-REVIEW.md` §1 contains the user's verbatim IDE observations on the Phase 08 49-fix delta (or explicit "Aucune observation utilisateur" marker). The placeholder `awaiting user input` no longer appears anywhere in the file. A commit `docs(08.1-rev): capture user-observed findings into 08.1-REVIEW.md §1` is on `main`. User has signaled "OK roadmap" (or equivalent) explicitly to unblock Task 3.
  </done>

  <what-built>
    Nothing automated. This checkpoint IS the protocol gate itself: Claude asks the user what they saw in their IDE on the Phase 08 49-fix delta (83 files across 49 commits), captures the verbatim answer (or explicit "rien vu"), writes it into `08.1-REVIEW.md` §1, and commits. This MUST happen before Task 3 runs, before Plan 08.1-02 captures walk evidence, and before Plan 08.1-03 spawns any audit sub-agent.
  </what-built>
  <how-to-verify>
    After Claude has posted the solicitation and user has responded:
    - `grep -A2 "## 1\\. User-observed findings" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -v "awaiting user input"` — should NOT show the placeholder
    - `git log --oneline -1 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md` — should show the capture commit
    - §1b per-device placeholders remain intact (Plan 08.1-02 fills them)
  </how-to-verify>
  <resume-signal>
    User types "OK roadmap" or "§1 validé, go" to signal that Task 3 may run the ROADMAP amendment.
    If user re-opens IDE findings after seeing this prompt, Claude appends (not replaces) to §1 and re-commits before proceeding to Task 3.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP.md — add Phase 08.1 row to Progress table + populate 5-plan list</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    This task opens Phase 08.1 formally in ROADMAP.md. Fire only AFTER Task 2 checkpoint passed (user signaled "OK roadmap" or equivalent).

    **Step A — Add Phase 08.1 row to the Progress table:**

    Use the Edit tool. Find the Progress table in `.planning/ROADMAP.md` (under `## Progress` > `| Phase | Plans Complete | Status | Completed |`). The current rows show Phase 08 at `5/5 | Complete | 2026-04-24` and Phase 09 at `0/TBD | Not started | -`. Insert a new row BETWEEN them :

    ```
    | 08.1 Re-Review — Post-Walk Audit (INSERTED) | 0/5 | In Progress | - |
    ```

    **Step B — Populate the Phase 08.1 Plans list:**

    Find the Phase 08.1 section in ROADMAP.md (currently around line 182: `### Phase 08.1: Re-Review — Post-Walk Audit (INSERTED)`). The current content is:

    ```
    **Goal:** Re-review Phase 07 + Phase 08 after successful Android + iOS walks. Catch what Phase 08's first review pass missed. Reuse 08-CONTEXT.md smell heuristics + 4-agent pattern, narrowed scope to delta (walk findings + areas the 49-fix loop touched).
    **Requirements**: —
    **Depends on:** Phase 8
    **Plans:** 0 plans (run /gsd:plan-phase 08.1)

    Plans:
    - [ ] TBD (run /gsd:plan-phase 08.1 to break down)
    ```

    Replace `**Plans:** 0 plans (run /gsd:plan-phase 08.1)` with `**Plans** (5 plans, 5 waves):` and replace the `Plans:` + `- [ ] TBD` lines with the 5-plan list :

    ```
    **Plans** (5 plans, 5 waves):
    - [ ] 08.1-rereview-post-walk/08.1-01-PLAN.md — Wave 1: Scaffold 08.1-REVIEW.md 5-section skeleton + §1b per-device walk placeholders + §2 7 pre-class + smell re-check table (6 rows) + §4 default-skip-adversarial + §1 user-first IDE capture + ROADMAP Phase 08.1 row + 5-plan list
    - [ ] 08.1-rereview-post-walk/08.1-02-PLAN.md — Wave 2: Walk-evidence capture — user runs Android + iOS walks, commits docs/phase-08.1-walk.md + docs/phase-08.1-walk-screenshots/, Plan 08.1-02 extracts into §1b per-device <details> blocks (blocking checkpoint on walk artifacts present)
    - [ ] 08.1-rereview-post-walk/08.1-03-PLAN.md — Wave 3: Pre-class 7 handoff items + 6 smell re-check rows into §2 + 4 parallel sub-agent audits (re-balanced slicing around Phase 08 delta: #1 downloads + #2 controllers + #3 presentation+walk + #4 tests+tooling+natives+CI+CLAUDE.md) + findings synthesis + user triage into §3 with smell-tag column
    - [ ] 08.1-rereview-post-walk/08.1-04-PLAN.md — Wave 4: Adversarial wave DEFAULT-SKIP (no new CI gate → no new branch per 1-branch-per-gate-script convention) — verify 8 existing gates still green + existing adversarial branches still deleted remote + add N walk-finding-driven inertness-guarded permanent regression tests (default N=0 unless walk demands)
    - [ ] 08.1-rereview-post-walk/08.1-05-PLAN.md — Wave 5: Fix-loop (Strategy A or B per user directive at kickoff) + §5 CI-green closure + status=closed + STATE.md + ROADMAP.md update (Phase 08.1 row flipped 0/5 → 5/5 Complete with date) + Phase 09 remains unblocked
    ```

    **Step C — Commit:**

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(roadmap): open Phase 08.1 — add Progress row + 5-plan list" --files .planning/ROADMAP.md
    ```

    Verify post-commit :
    ```bash
    grep -q "08.1 Re-Review — Post-Walk Audit" .planning/ROADMAP.md
    grep -q "0/5 | In Progress" .planning/ROADMAP.md
    grep -q "08.1-rereview-post-walk/08.1-01-PLAN.md" .planning/ROADMAP.md
    grep -q "08.1-rereview-post-walk/08.1-05-PLAN.md" .planning/ROADMAP.md
    ! grep -q "run /gsd:plan-phase 08.1" .planning/ROADMAP.md  # placeholder replaced
    ```
  </action>
  <verify>
    <automated>bash -c 'grep -q "08.1 Re-Review — Post-Walk Audit" .planning/ROADMAP.md && grep -q "0/5 | In Progress" .planning/ROADMAP.md && grep -q "08.1-rereview-post-walk/08.1-01-PLAN.md" .planning/ROADMAP.md && grep -q "08.1-rereview-post-walk/08.1-05-PLAN.md" .planning/ROADMAP.md && ! grep -q "run /gsd:plan-phase 08.1" .planning/ROADMAP.md && git log --oneline -1 -- .planning/ROADMAP.md | grep -q "open Phase 08.1"'</automated>
  </verify>
  <done>
    ROADMAP.md Progress table contains a new Phase 08.1 row with `0/5 | In Progress | -`. Phase 08.1 section's `Plans:` field replaced from `0 plans (run /gsd:plan-phase 08.1)` to `5 plans, 5 waves` with the 5-plan list populated. Placeholder `TBD (run /gsd:plan-phase 08.1 to break down)` removed. Single atomic commit `docs(roadmap): open Phase 08.1 …` on main.
  </done>
</task>

</tasks>

<verification>
- `08.1-REVIEW.md` exists with 5 sections in correct order (grep `^## [1-5]\.` returns 5)
- §1b device walk placeholders per-device `<details>` blocks present (Android + iOS)
- §2 `Pre-known from 08.1-CONTEXT` sub-section present (pending 7 entries)
- §2 `Smell heuristics hot-spots (re-check post-Phase 08 refactors)` sub-section present (pending 6 rows)
- §4 default-skip-adversarial commentary present with 8 existing gates listed + walk-finding-driven N=0 default
- §1 is populated (no placeholder `awaiting user input` remains)
- Two atomic commits on `main`: scaffold + user capture (Task 1 + Task 2)
- Third atomic commit (Task 3) : ROADMAP amendment with Phase 08.1 row + 5-plan list
- User has explicitly signaled "OK roadmap" (or equivalent) for Task 3 to proceed AND "OK for 08.1-02" (or equivalent) implied in the same turn or next message before Plan 08.1-02 is kicked off
</verification>

<success_criteria>
Plan 08.1-01 complete when:
1. `.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md` exists on `main` with the 5-section skeleton + §1b per-device walk placeholders + §2 pre-class (7 rows pending) + smell re-check table (6 rows pending) + §4 default-skip-adversarial commentary
2. §1 contains either the user's verbatim IDE findings OR an explicit "Aucune observation utilisateur" marker
3. Three atomic commits logged (scaffold + user capture + ROADMAP amend) with `docs(08.1-rev):` / `docs(roadmap):` prefixes
4. CI green (markdown-only changes — scaffold + capture + ROADMAP commits trigger green CI, confirming nothing else regressed)
5. ROADMAP.md Progress table has Phase 08.1 row `0/5 | In Progress` ; Phase 08.1 section has 5-plan list
6. User has explicitly unblocked Plan 08.1-02 via "OK walk" / "OK 08.1-02" / equivalent signal
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-rereview-post-walk/08.1-01-SUMMARY.md` summarizing:
- Skeleton commit hash
- §1 capture commit hash + verbatim excerpt of user observations (or "Aucune observation utilisateur")
- ROADMAP amend commit hash
- Confirmation that §1b (per-device walk), §2 pre-class (7) + smell re-check (6) + §4 (default-skip + walk-finding-driven N=0) placeholders are intact and ready for Plans 08.1-02 / 08.1-03 / 08.1-04 to fill
- Timestamp of user unblock signal received to proceed to Plan 08.1-02
</output>
</content>
</invoke>