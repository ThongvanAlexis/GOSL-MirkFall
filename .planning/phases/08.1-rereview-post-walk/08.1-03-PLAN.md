---
phase: 08.1-rereview-post-walk
plan: 03
type: execute
wave: 3
depends_on:
  - 08.1-02
files_modified:
  - .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
  - .planning/phases/08.1-rereview-post-walk/.fixes-expected
  - .planning/phases/08.1-rereview-post-walk/08.1-03-SUMMARY.md
autonomous: false
requirements: []
success_criteria:
  - audit-wave-complete
must_haves:
  truths:
    - "§2 `Pre-known from 08.1-CONTEXT` table is populated with 7 rows BEFORE any Agent tool call is made — committed as `docs(08.1-rev): pre-class 7 handoff items + 6 smell re-check rows into §2`"
    - "§2 `Smell heuristics hot-spots (re-check post-Phase 08 refactors)` table is populated with 6 rows (PmtilesDownloadController / MapCameraController / ActiveSessionController+State / DownloadState / MapScreen lifecycle / StyleRewriter) BEFORE any Agent tool call — same commit as pre-class"
    - "Four `general-purpose` sub-agents spawned in ONE tool-use message (single assistant message containing four Task tool invocations — locked Phase 02+04+06+08 precedent ; sequential spawn explicitly rejected). Each agent receives (a) its re-balanced layer+delta scope per 08.1-RESEARCH §Pattern 2, (b) the cross-cutting smell-heuristics RE-CHECK brief verbatim (per 08.1-RESEARCH §Pitfall 4: 'did Phase 08 refactor introduce a new smell? regression? walk-findings-aggravated shape?' — NOT 'find smells from scratch'), (c) its prioritized smell re-check hot-spot, (d) pre-class heads-up (26 Noted items still deferred unless walk re-surfaces), (e) walk findings summary with per-agent-relevant observations"
    - "Each agent receives an EXPLICIT `files NOT in scope` negation section per 08.1-RESEARCH §Pitfall 2 (Over-Scoping) — agents should NOT re-audit files the 49-fix loop did not touch AND the walk did not flag"
    - "Each agent returns structured findings in `[severity] Title — 1-line explanation — file:line` format + optional smell tag — findings synthesised into §2 per-agent sub-sections"
    - "Per-agent narrative audit notes archived in collapsed `<details>` appendix at end of §2 (Phase 02+04+06+08 precedent) — not shown to user in triage surface"
    - "Cross-lens overlap preservation : a finding surfaced by 2 agents with different severities is kept under BOTH lenses with `(also flagged by Agent #N — severity X)` cross-reference, NOT deduplicated"
    - "§3 Triage decisions table presented to user with columns `# | Finding | Severity | Smell tag | Decision | Rationale | Commit hash` — user chooses fix/waive/defer per row ; smell-tag column visible"
    - "Row # is LOCAL to Phase 08.1 §3 ; when cross-referencing Phase 08 rows use `08§3-#N` format (08.1-RESEARCH §Pitfall 6 — row numbering collision)"
    - "User triage decisions captured inline §3 — commit `docs(08.1-rev): record triage decisions into §3`"
    - "`.fixes-expected` integer snapshot written (Phase 02+04+06+08 precedent) for Plan 08.1-05 tally verification"
  artifacts:
    - path: ".planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md"
      provides: "§2 fully populated (7 pre-class + 6 smell re-check + 4 agent findings + narrative appendix) + §3 triage table filled with user decisions"
      contains: "### Agent #1"
      min_lines: 250
    - path: ".planning/phases/08.1-rereview-post-walk/.fixes-expected"
      provides: "Integer count of `fix` + `refactor` triage decisions — scratch for Plan 08.1-05 fix-loop"
  key_links:
    - from: "08.1-CONTEXT §Implementation Decisions (inherited from 08-CONTEXT) + 08.1-RESEARCH §Pattern 4 pre-class"
      to: "§2 Pre-known from 08.1-CONTEXT table (7 rows)"
      via: "Task 1 verbatim extraction — Plan 08.1-03 fills table BEFORE agent spawn so agents arrive briefed on what not to rediscover"
      pattern: "### Pre-known from 08\\.1-CONTEXT"
    - from: "08.1-RESEARCH §Code Examples — Smell-Heuristics Hot-Spots Table Template"
      to: "§2 Smell heuristics hot-spots re-check table (6 rows)"
      via: "Task 1 — pre-commit before agents receive brief"
      pattern: "### Smell heuristics hot-spots"
    - from: "CLAUDE.md §En review faire attention à (2026-04-23 delta) RE-CHECK lens"
      to: "Every agent prompt — cross-cutting smell-heuristics RE-CHECK brief"
      via: "Task 2 — each of 4 agents receives the RE-CHECK version of the brief (not 'find smells', but 'did refactor introduce new smells / regression / walk-aggravation')"
      pattern: "Code alambiqué|State machine tirée"
    - from: "Four Task tool calls"
      to: "One single assistant message (parallel spawn)"
      via: "Task 2 — locked Phase 02+04+06+08 precedent"
      pattern: "4 Task tool invocations in single message"
    - from: "Walk findings in §1b"
      to: "Agent #3 brief prioritized + other agents briefed with per-agent-relevant observations"
      via: "Task 2 prompt builder reads §1b walk-evidence snapshot + forwards relevant items per agent"
      pattern: "walk findings"
    - from: "Findings in §2"
      to: "Triage table in §3"
      via: "Task 4 — synthesis flattened into single numbered list for user"
      pattern: "## 3\\. Triage decisions"
---

<objective>
Execute the core audit wave of Phase 08.1 : pre-classify 7 handoff items + 6 smell re-check hot-spots into §2 BEFORE spawning agents ; then spawn 4 `general-purpose` sub-agents in a SINGLE tool-use message (parallel execution per locked Phases 02+04+06+08 precedent) with re-balanced slicing around Phase 08 delta + walk findings + CLAUDE.md smell-heuristics RE-CHECK brief (per 08.1-RESEARCH §Pitfall 4) ; synthesise findings into §2 ; present triage table to user ; capture user triage decisions inline §3.

Purpose: The 4-agent parallel audit is Phase 08.1's main signal generator. Critical differences vs Phase 08 :
1. **RE-CHECK lens** — agents are briefed that Phase 08 already did a smell-pass on these 9 components (6 in the re-check table). Their lens is "did the refactor INTRODUCE a new smell? Is there a regression? Is there a walk-findings-aggravated shape?" NOT "find smells from scratch" (which would duplicate Phase 08 findings).
2. **Walk findings as primary input** — §1b walk evidence (captured Plan 08.1-02) is the fresh signal. Agent #3 (presentation + walk-findings triage) is the primary landing zone ; other agents receive per-agent-relevant walk observations.
3. **Scope narrowed to Phase 08 delta** — agents have EXPLICIT "files NOT in scope" negations. Re-auditing files the 49-fix loop did not touch + the walk did not flag = waste per 08.1-RESEARCH §Pitfall 2.
4. **Pre-class items narrower** — 7 items (not Phase 08's 10) — many Phase 08 items are now closed (iOS fix landed, ROADMAP synced, CountryResolver edges have 4 new tests, tools have paired tests).

The SINGLE tool-use message parallel spawn is locked. The smell-heuristics RE-CHECK brief is Phase 08.1's SECOND application of the CLAUDE.md 2026-04-23 delta — validates the template on narrower scope.

Output: §2 fully populated (7 pre-class + 6 smell re-check + 4 agent findings + narrative appendix) ; §3 triage table filled with user decisions (fix / refactor / waive / defer, smell-tag visible per row). Two atomic commits on main (pre-class + post-triage). User chooses triage in one pass. `.fixes-expected` integer snapshot ready for Plan 08.1-05.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-rereview-post-walk/08.1-CONTEXT.md
@.planning/phases/08.1-rereview-post-walk/08.1-RESEARCH.md
@.planning/phases/08.1-rereview-post-walk/08.1-VALIDATION.md
@.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
@.planning/phases/08-review-gate-map/08-03-PLAN.md
@.planning/phases/08-review-gate-map/08-REVIEW.md
@.planning/phases/08-review-gate-map/08-05-SUMMARY.md
@.planning/phases/06-review-gate-gps/06-03-PLAN.md
@CLAUDE.md
@DEPENDENCIES.md

<interfaces>
<!-- Pre-class + smell re-check + agent brief contract. -->

**7 pre-class items (08.1-RESEARCH §Pattern 4 — narrower than Phase 08's 10) — table rows to fill in Task 1.**

Rows columns : `# | Item | Severity | Rationale`.

1. **All 26 Phase 08 Noted items** (10 defer-to-v2 + 16 accepted-as-is) — confirm still deferred unless walk re-surfaces one. — **Noted** — If walk findings re-surface e.g. row #52 bootstrap heal `.substring(0, 10)` magic number, that item promotes to Should/Could in §3 triage. Otherwise all 26 stay deferred.
2. **Phase 08 Row #38 ActiveSessionController reconcile-pattern rewrite** — scope-downed to `_bestEffort(ctx, op)` helper extraction ; full rewrite deferred. — **Noted** — Agent #2 verifies the helper extraction holds under walk lifecycle scenarios (kill-and-resume, rapid start/stop).
3. **Phase 08 Row #39 MapScreen deactivate microtask** — scope-downed to named-helper extract + restructured docstring ; provider-lifecycle redesign deferred to Phase 10 / Riverpod 4.x. — **Noted** — Agent #3 verifies the extract holds under walk (rotation, rapid route changes).
4. **Phase 08 Row #33 drop_then_retry soak test timeout** — 60s timeout with ~36s retry backoffs ; 1 CI flake observed during Plan 08-05 Relay 5. — **Could** — Agent #4 lens: should timeout be bumped to 90s OR should soak be split? Candidate fix-in-loop if flake recurs during Phase 08.1 closure CI runs.
5. **Walk findings hot-spots** — category-inline (NOT a single finding row). Source: §1b walk-evidence snapshot + walk-finding-driven regression test candidates from Plan 08.1-02 §1b. Agents arrive briefed on the walk observations mapped to their agent scope.
6. **Smell heuristics hot-spots re-check after refactors** — category-inline. See `### Smell heuristics hot-spots (re-check post-Phase 08 refactors)` table below. Did rows #11/#12/#20/#29/#35/#36/#37 introduce new smells? Is there walk-aggravated regression?
7. **ROADMAP + REQUIREMENTS sync confirmed** — Phase 08 closure amended both (MAP-05/06/07/08/10 Complete ; Plan 07-07 scope-reduced). Plan 08.1-01 added Phase 08.1 row to Progress table. Plan 08.1-05 flips Phase 08.1 to Complete. — **Noted** positive confirmation if no drift. Agent #4 lens flag if ROADMAP has regressed.

**6 smell heuristics hot-spots RE-CHECK (08.1-RESEARCH §Code Examples — Smell-Heuristics Hot-Spots Table Template) — table rows to fill in Task 1.**

Columns : `Component | File path | Primary Agent | Phase 08 action | Phase 08.1 lens`.

1. **PmtilesDownloadController 7-step** | `lib/infrastructure/downloads/pmtiles_download_controller.dart` | **#1** | Row #1 break-on-pause ; Row #5 staging-nuke ; Row #20 dispatcher collapse (via DownloadState) | Did the dispatcher collapse (row #20) hide any corner case the walk hits? Does pause/resume still produce clean state transitions on real device?
2. **MapCameraController follow/pan** | `lib/application/controllers/map_camera_controller.dart` | **#2** | Row #11 listeners consolidation ; Row #14 16-line comment trim ; Row #35 timestamp replaces flag+Timer ; Row #36 Centering→Following(hasFirstFix) collapse | Did state collapse break any follow-me edge case under walk ? Did post-refactor .g.dart regenerate cleanly under CI ?
3. **ActiveSessionController + State** | `lib/application/controllers/active_session_controller.dart` + `.../state/active_session_state.dart` | **#2** | Row #37 drop ErrorState, route through AsyncError ; Row #38 `_bestEffort` helper extract (scope-down) | Does dropped ErrorState cause UI pattern-match misses on walk? Does `_bestEffort` helper swallow an error the walk surfaces?
4. **DownloadState** | `lib/domain/downloads/download_state.dart` | **#1** | Row #20 unified snapshot field + polymorphic getters ; Row #29 documentation + transition graph + justification ; Row #42 Completed/Cancelled carry DownloadJob | Does polymorphic `snapshot` getter resolve correctly at all UI sites post-refactor ?
5. **MapScreen lifecycle** | `lib/presentation/screens/map_screen.dart` | **#3** | Row #39 deactivate microtask named-helper extract (scope-down) ; Row #40 SchedulerBinding.addPostFrameCallback for maplibre defer | Does the named-helper resist a real-device lifecycle push? Does postFrameCallback make the defer visible on slow CPU?
6. **StyleRewriter + validators** | `lib/infrastructure/map/style_rewriter.dart` + `style_layer_order.dart` | **#1** | Row #3 external http[s] URL runtime rejection ; Row #22 shared _iterateStyleLayers generator extract | Regression risk low — these were simple extracts. Noted unless walk flags.

**Cross-cutting smell-heuristics RE-CHECK brief (each agent receives this VERBATIM in their prompt) :**

```
## Smell-heuristics RE-CHECK brief (Phase 08.1 lens)

Phase 08 (ceci est la deuxième application de CLAUDE.md §En review faire attention à 2026-04-23 delta) a déjà fait un smell-pass sur les 6 composants de la table ci-dessus. Ta lens N'EST PAS "trouve des smells from scratch" — ça serait re-créer les findings Phase 08.

Ta lens EST :
1. **Did Phase 08 refactor INTRODUCE a new smell?** — le fix lui-même a-t-il créé de la defensive null chaining, des flags booléens accumulés, early returns avec `// fix for edge case`, wrappers-de-wrappers, try/catch rattrapant des exceptions qu'un design correct aurait rendues impossibles ?
2. **Is there a REGRESSION?** — le refactor collapse (row #11/#12/#20/#29/#35/#36/#37) a-t-il cassé un corner case, une sequence de transitions, un UI pattern-match ?
3. **Is there a walk-findings-AGGRAVATED shape?** — le walk a-t-il surfaced un bug qui révèle que le refactor Phase 08 n'est pas allé assez loin, ou a rendu une shape apparente seulement sur device ?

**N'EST PAS ton lens** :
- Further-simplifier un refactor Phase 08 "parce que ça pourrait être plus simple" sans évidence concrète (bug, test failure, walk-observation). C'est 08.1-RESEARCH §Pitfall 4.
- Proposer un 3e refactor sur un composant déjà refactoré deux fois Phase 08. Document le smell, mais décision triage est "defer" par défaut sauf si walk-evidence concret.

**Pattern definitions (verbatim binding from CLAUDE.md §En review faire attention à) :**

**Code alambiqué par empilement de fix** :
- conditions imbriquées défensives qui protègent contre des états qui ne devraient pas exister (`if x != null && x.ready && !x.cancelled && x.state != FOO`)
- flags booléens ajoutés au fil de l'eau (`isInitialized`, `hasBeenProcessed`, `shouldSkip`)
- early returns avec commentaires `// fix for edge case when...`
- wrappers-de-wrappers, fonctions `doXReally()` ou `handleXFinal()`
- try/catch rattrapant des exceptions qu'un design correct aurait rendues impossibles

**State machine tirée par les cheveux** :
- enum d'états dont plusieurs valeurs ne diffèrent que par 1-2 champs
- transitions pas-vraiment-un-graphe (tout le monde peut aller partout)
- états "intermédiaires" sync-only
- dispatcher géant (switch/if-else) dupliquant la logique entre branches

Smell tags à utiliser dans tes findings : `[smell:fix-on-fix]` (code alambiqué) ou `[smell:over-state-machine]` (state machine tirée par les cheveux).
```

**Agent slicing (re-balanced for Phase 08.1 delta per 08.1-RESEARCH §Pattern 2) :**

Per 08.1-RESEARCH §Pitfall 2 : every agent's brief MUST explicitly list the files they cover (derived from `git log --grep='08-rev' --name-only` intersected with walk findings + smell re-check components). Agent prompts have an explicit "files NOT in scope" negation section.

| Agent | Primary scope (files IN scope) | Prioritized smell re-check hot-spot | Files NOT in scope (examples) |
|-------|--------------------------------|-------------------------------------|-------------------------------|
| **#1 — Download pipeline + atomicity (re-check Phase 08 smell-surgery)** | `lib/infrastructure/downloads/**` (pmtiles_download_controller.dart rows #1/#5/#7/#10/#19/#29 ; binary_concatenator.dart row #30 ; atomic_renamer.dart row #9 + EXDEV test ; download_queue_store.dart row #34 ; http_chunk_downloader.dart row #7 + f95c55c format ; country_delete_service.dart) + `lib/domain/downloads/download_state.dart` (row #20/#29/#42) + `lib/infrastructure/installed_maps/first_launch_bootstrap.dart` (row #8 heal) + `test/infrastructure/downloads/**` (incl. 8 soak scenarios + manifest_atomicity_contract NEW + no_httpclient_in_unit_tests NEW) | PmtilesDownloadController 7-step + DownloadState dispatcher collapse + StyleRewriter (secondary — shares `lib/infrastructure/map/` with #4's style.json audit) | `lib/application/controllers/**` (→ Agent #2) ; `lib/presentation/**` (→ Agent #3) ; `tool/**` (→ Agent #4) ; `lib/domain/map/**` (Phase 08 already cleared, 49-fix did not touch)
| **#2 — Controllers + state + Riverpod wiring (re-check Phase 08 shape-consolidation)** | `lib/application/controllers/active_session_controller.dart` + `active_session_controller.g.dart` + `lib/application/state/active_session_state.dart` (row #37/#38) + `lib/application/controllers/country_resolver_controller.dart` + `.g.dart` (row #12/#23) + `lib/application/controllers/download_queue_controller.dart` + `.g.dart` + `lib/application/controllers/installed_maps_controller.dart` + `.g.dart` (row #13) + `lib/application/controllers/map_camera_controller.dart` + `.g.dart` (row #11/#14/#35/#36) + `lib/application/providers/map_providers.g.dart` + `test/application/controllers/**` | MapCameraController fix-on-fix + ActiveSessionController over-state-machine (+State row #37 drop ErrorState) | `lib/infrastructure/downloads/**` (→ Agent #1) ; `lib/presentation/**` (→ Agent #3) ; `.g.dart` format drift (→ Agent #4 sweep)
| **#3 — Presentation + router + walk-findings triage** | `lib/presentation/screens/map_screen.dart` (row #14/#39/#40) + `lib/presentation/screens/maps_download_screen.dart` (row #43) + `lib/presentation/screens/maps_manage_screen.dart` + `lib/presentation/widgets/session_burger_menu.dart` (row #41 `_DistanceRow`) + `lib/presentation/widgets/map_download_progress_chip.dart` + `lib/presentation/widgets/map_follow_me_fab.dart` + `lib/application/routing/router.dart` + `test/presentation/screens/map_screen_test.dart` + `test/presentation/screens/maps_download_screen_test.dart` + `test/presentation/screens/maps_manage_screen_test.dart` + `test/presentation/widgets/map_download_progress_chip_test.dart` + `test/presentation/widgets/map_follow_me_fab_test.dart` | MapScreen lifecycle (row #39 named-helper + row #40 postFrameCallback) + walk findings PRIMARY landing zone | `lib/infrastructure/**` (→ Agents #1, #2, #4) ; `lib/domain/**` (Phase 08 cleared)
| **#4 — Tests + tooling + natives + CI + CLAUDE.md cross-cutting + smell transverses** | All `test/**` touched by 49-fix (NOT already covered by #1/#2/#3 scopes) + `integration_test/**` (4 files absorbed) + `tool/check_platform_manifests.dart` + `tool/test/check_platform_manifests_test.dart` + `tool/check_style_no_external_url.dart` + `tool/test/check_style_no_external_url_test.dart` (Phase 08 NEW) + `tool/prepare_style.dart` + `tool/generate_tiny_pmtiles.dart` + `tool/generate_world_sha256.dart` + `tool/simplify_polygons.dart` + `tool/test/prepare_style_test.dart` + `tool/test/generate_tiny_pmtiles_test.dart` + `tool/test/generate_world_sha256_test.dart` + `tool/test/simplify_polygons_test.dart` + `tool/README.md` (row #17) + `android/app/src/main/AndroidManifest.xml` (row #49 BootCompletedReceiver android:exported guard) + Kotlin platform channel files + Swift platform channel files + `.github/workflows/ci.yml` (Check style no external URL step) + `dart_test.yaml` + `DEPENDENCIES.md` (row #48 integration_test entry) + `assets/maps/style.json` (row #2/#15 8→7 layer drift) + `lib/config/constants.dart` (row #44 _k... hoist sweep) + CLAUDE.md cross-cutting sweep across 49-fix delta | Transversal pattern sweep — smell that escaped #1/#2/#3 layer vision (fix-on-fix spread across multiple files) + .g.dart format drift risk + inertness guard decay on 7 new permanent tests | `lib/domain/**` (Phase 08 cleared except download_state.dart → Agent #1) ; Phase 01-06 tooling files unchanged by 49-fix

**Output contract per agent (locked Phase 02+04+06+08) :**

- Structured findings : `[severity] Title — 1-line explanation — file:line` format. Severities Blocker/Should/Could/Noted. Smell-tagged findings get trailing `[smell:fix-on-fix]` or `[smell:over-state-machine]` after severity.
- Narrative appendix : prose audit report (3-10 paragraphs), archived in §2 collapsed Audit Notes `<details>` at end.
- Cross-lens overlap preservation : finding surfaced by 2 agents with different severities preserved under BOTH lenses with `(also flagged by Agent #N — severity X)` cross-reference, NOT deduplicated.

**Triage table format (Phase 02+04+06+08 convention + Phase 08.1 specifics) :**

Columns `# | Finding (title + 1-line + file:line) | Severity | Smell tag | Decision | Rationale | Commit hash`.

Smell-tag column values : `—` (no tag), `[smell:fix-on-fix]`, `[smell:over-state-machine]`.
Decision column values : `fix` / `refactor` (smell-triggered architecture change) / `waived` / `defer-to-phase-XX` / `accepted-as-is`.
Row numbering : LOCAL to Phase 08.1 §3. Cross-reference to Phase 08 row = `08§3-#N` (08.1-RESEARCH §Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-class 7 handoff items + 6 smell re-check rows into §2 BEFORE agent spawn</name>
  <files>.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md</files>
  <action>
    Use the Edit tool to replace the two pending markers in `08.1-REVIEW.md` §2 :

    **A — `### Pre-known from 08.1-CONTEXT` table** — replace `(pending — 7 entries: ...)` with a 7-row markdown table. Columns : `# | Item | Severity | Rationale`.

    Row contents (verbatim from `<interfaces>` §"7 pre-class items") :

    1. All 26 Phase 08 Noted items deferral confirm | **Noted** | If walk findings re-surface e.g. row #52 bootstrap heal magic-number, item promotes to Should/Could in §3. Otherwise all 26 stay deferred.
    2. Phase 08 Row #38 ActiveSessionController reconcile-pattern scope-down to `_bestEffort(ctx, op)` | **Noted** | Agent #2 verifies helper extraction holds under walk lifecycle (kill-and-resume, rapid start/stop).
    3. Phase 08 Row #39 MapScreen deactivate microtask scope-down to named-helper | **Noted** | Agent #3 verifies extract holds under walk (rotation, rapid route changes). Provider-lifecycle redesign deferred to Phase 10 / Riverpod 4.x.
    4. Phase 08 Row #33 drop_then_retry soak test timeout (60s ≈ 36s retry backoffs, 1 CI flake observed) | **Could** | Agent #4 lens: bump to 90s OR split soak? Fix-in-loop candidate if flake recurs during 08.1 closure CI runs.
    5. Walk findings hot-spots | **category-inline** | Source: §1b walk-evidence snapshot + walk-finding regression test candidates from Plan 08.1-02. Not a single finding row — agents arrive briefed on walk observations mapped to agent scope.
    6. Smell heuristics hot-spots re-check after refactors | **category-inline** | See `### Smell heuristics hot-spots (re-check post-Phase 08 refactors)` table below. Did rows #11/#12/#20/#29/#35/#36/#37 introduce new smells or walk-aggravated regression?
    7. ROADMAP + REQUIREMENTS sync confirmed | **Noted** | Phase 08 closure amended both (MAP-05/06/07/08/10 Complete ; Plan 07-07 scope-reduced). Plan 08.1-01 added Phase 08.1 row. Plan 08.1-05 flips to Complete. Agent #4 flag if drift re-introduced.

    Format as a 4-column markdown table.

    **B — `### Smell heuristics hot-spots (re-check post-Phase 08 refactors)` table** — replace `(pending — 6 rows)` with the 6-row table. Columns : `Component | File path | Primary Agent | Phase 08 action | Phase 08.1 lens`.

    Rows (verbatim from `<interfaces>` §"6 smell heuristics hot-spots RE-CHECK") :

    1. PmtilesDownloadController 7-step | `lib/infrastructure/downloads/pmtiles_download_controller.dart` | **#1** | Row #1 break-on-pause ; Row #5 staging-nuke ; Row #20 dispatcher collapse (via DownloadState) | Dispatcher collapse (row #20) hide corner case walk hits? Pause/resume clean state transitions on real device?
    2. MapCameraController follow/pan | `lib/application/controllers/map_camera_controller.dart` | **#2** | Row #11 listeners consolidation ; Row #14 16-line comment trim ; Row #35 timestamp replaces flag+Timer ; Row #36 Centering→Following(hasFirstFix) collapse | State collapse break follow-me edge case under walk? .g.dart regenerate clean CI?
    3. ActiveSessionController + State | `lib/application/controllers/active_session_controller.dart` + `.../state/active_session_state.dart` | **#2** | Row #37 drop ErrorState → AsyncError ; Row #38 `_bestEffort` helper extract (scope-down) | Dropped ErrorState cause UI pattern-match miss on walk? `_bestEffort` swallow walk-surfaced error?
    4. DownloadState | `lib/domain/downloads/download_state.dart` | **#1** | Row #20 unified snapshot field + polymorphic getters ; Row #29 transition graph doc ; Row #42 Completed/Cancelled carry DownloadJob | Polymorphic `snapshot` getter resolve correctly at all UI sites post-refactor?
    5. MapScreen lifecycle | `lib/presentation/screens/map_screen.dart` | **#3** | Row #39 deactivate microtask named-helper extract (scope-down) ; Row #40 SchedulerBinding.addPostFrameCallback defer | Named-helper resist real-device lifecycle push? postFrameCallback visible slow CPU?
    6. StyleRewriter + validators | `lib/infrastructure/map/style_rewriter.dart` + `style_layer_order.dart` | **#1** | Row #3 external http[s] URL runtime rejection ; Row #22 shared `_iterateStyleLayers` generator | Regression risk low — simple extracts. Noted unless walk flags.

    After both tables written, commit in ONE atomic commit :

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(08.1-rev): pre-class 7 handoff items + 6 smell re-check rows into §2" --files .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```

    Verify post-commit :
    ```bash
    grep -q "PmtilesDownloadController 7-step" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "MapCameraController follow/pan" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "ActiveSessionController + State" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "DownloadState" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "MapScreen lifecycle" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "StyleRewriter + validators" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c 'grep -q "PmtilesDownloadController 7-step" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "MapCameraController follow/pan" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "ActiveSessionController + State" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "DownloadState" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "MapScreen lifecycle" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "StyleRewriter + validators" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && git log --oneline -1 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -q "pre-class 7 handoff items"'</automated>
  </verify>
  <done>§2 Pre-known from 08.1-CONTEXT filled with 7 rows. §2 Smell heuristics hot-spots re-check filled with 6 rows. Single atomic commit on main. NO agent spawned yet — spawn unblocked for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn 4 general-purpose sub-agents in ONE single tool-use message (parallel) — each briefed with re-balanced layer+delta scope + smell RE-CHECK brief + walk findings + files NOT in scope</name>
  <files>(read-only during agent run — findings captured by Claude-main in Task 3)</files>
  <action>
    **CRITICAL structural requirement (locked Phase 02+04+06+08 precedent) :** The 4 agents MUST be spawned in ONE single tool-use message. ONE assistant message containing FOUR Task tool invocations in the same function_calls block. NOT four sequential messages.

    Each agent is `general-purpose` subagent_type. Each receives a prompt built from this template, filled per the agent's scope.

    **Prompt template (Claude-main assembles per agent) :**

    Part 1 — Header :
    > You are conducting a code review audit as part of Phase 08.1 Re-Review — Post-Walk Audit for the MirkFall project. This is a RE-review : Phase 08 already audited the code ; you are checking the delta (49-fix loop commits between Phase 08 closure `254b5d2` and current main tip) + walk findings from the user's Android + iOS walks.

    Part 2 — Role + files IN scope (explicit, from the Agent slicing table) :
    > Your primary scope is the following files (derived from `git log --grep='08-rev' --name-only` intersected with walk findings + smell re-check hot-spots) :
    > <list files IN scope verbatim from the Agent slicing table for this agent>

    Part 3 — Files NOT in scope (explicit negation per 08.1-RESEARCH §Pitfall 2) :
    > You SHOULD NOT audit the following files. They are either Phase 08-cleared (not touched by 49-fix loop + not walk-flagged), other agents' territory, or out of Phase 08.1 delta entirely :
    > <list files NOT in scope verbatim>
    >
    > If you find an issue in a file NOT in scope, include it in findings with `(out of scope — flagged for completeness)` suffix but do NOT dive deep into that file.

    Part 4 — Verification targets : concrete checks the agent must perform. See per-agent list below.

    Part 5 — Prioritized smell re-check hot-spot : the row from §2 Smell heuristics hot-spots re-check table assigned to this agent. Ask the agent to apply the posed Q as primary lens on that component.

    Part 6 — Walk findings per-agent-relevant excerpt : extract from §1b walk-evidence snapshot the observations most relevant to this agent's scope. (Agent #3 gets ALL walk findings ; Agents #1/#2/#4 get filtered subsets based on scope match.)

    Part 7 — Cross-cutting smell-heuristics RE-CHECK brief (VERBATIM from `<interfaces>` §"Cross-cutting smell-heuristics RE-CHECK brief") : full block copied verbatim. **source-of-truth: `<interfaces>` §Cross-cutting smell-heuristics RE-CHECK brief** — if the brief ever needs to evolve, edit the `<interfaces>` block only and re-run ; do NOT fork the copy into Part 7 out-of-band (per checker revision-mode info note 2026-04-24, redundancy between <interfaces> lines 123-155 and this Part 7 is intentional source-copy for agent-prompt assembly, NOT two divergent definitions). CRITICAL difference from Phase 08 : the brief explicitly says "did refactor INTRODUCE a new smell? regression? walk-aggravation?" NOT "find smells from scratch".

    Part 8 — Pre-class heads-up : list the 7 pre-class items + rationale. Tell agent NOT to re-raise them unless walk re-surfaces ; reference row number if relevant.

    Part 9 — Expected output : two blocks.
    - `### Structured findings` — numbered list, `N. [severity] Title — 1-line explanation — file:line [optional smell tag]`
    - `### Narrative audit notes` — prose audit report (3-10 paragraphs) organized by sub-area

    Part 10 — Closing rule : do NOT silently deduplicate with sibling agents. Cross-lens overlap preservation per Phase 02+04+06+08 convention.

    **Per-agent verification targets (Part 4 per agent) :**

    **Agent #1** — Download pipeline + atomicity (re-check Phase 08 smell-surgery) :
    - Did Row #20 DownloadState dispatcher collapse hide a corner case? Check all UI sites using the polymorphic `snapshot` getter — do they all resolve correctly?
    - Does Row #1 break-on-pause hold? Re-verify pause/resume produces clean state transitions. If walk findings mention pause/resume anomaly, prioritize this.
    - Does Row #5 staging-nuke on reassembled-sha mismatch clean correctly without leaking `.pmtiles` target?
    - 8 soak scenarios (6 original + 2 Phase 08 edges corrupt-chunk + rename-target-exists) all pass on current main? (Agent can run `dart test --tags soak` if needed, or trust CI green evidence from `254b5d2` + subsequent commits.)
    - Row #30 binary_concatenator catch-scope narrow — does the narrowed catch still cover all failure modes observed in Phase 07 soak + walk?
    - Row #9 AtomicRenamer EXDEV test + cleanup — does the EXDEV fallback work on iOS Documents vs tmp volume separation?
    - `lib/infrastructure/installed_maps/first_launch_bootstrap.dart` row #8 heal path : does `_healOrphanCountryFiles` handle the walk-surfaced manifest+file drift scenarios?
    - 3 new permanent tests (world_bundle_sha256 / manifest_atomicity_contract / no_httpclient_in_unit_tests) — inertness guards holding? Any test pattern that would silently pass on a refactor breaking the guard?
    - StyleRewriter row #3 external URL rejection : does the runtime check hit BEFORE any MapLibre tile fetch attempt? Does `check_style_no_external_url` CI gate catch all known external URL patterns?
    - **Prioritized hot-spot** : PmtilesDownloadController 7-step state-machine re-check (row #20 dispatcher collapse) + DownloadState polymorphic snapshot getter

    **Agent #2** — Controllers + state + Riverpod wiring (re-check Phase 08 shape-consolidation) :
    - Row #37 ActiveSessionState.ErrorState drop → AsyncError : grep confirms no UI consumer pattern-matches on ErrorState? If walk findings mention a UI state rendering anomaly during GpsError, prioritize this.
    - Row #38 ActiveSessionController `_bestEffort` helper : does it swallow an error the walk surfaces? Verify the log-on-swallow pattern actually logs visible info to FileLogger.
    - Row #11 MapCameraController listeners consolidation into build : does it introduce a race at MapView attach/detach? Walk kill-and-resume scenario would stress this.
    - Row #35 `DateTime? _lastProgrammaticMoveAt` timestamp replaces flag+Timer echo-suppression : does the timestamp comparison window correctly filter programmatic vs user pans? Walk pan-then-follow scenario stresses this.
    - Row #36 MapCameraCentering → Following(hasFirstFix) collapse : does the collapsed state correctly distinguish "following but no first fix yet" vs "following with first fix"? UI progress indication depends on this.
    - Row #12 CountryResolverController listener consolidation to single broadcast path : does the consolidation cause any missed updates on installed-manifest changes? Walk download-then-delete scenario stresses this.
    - Row #13 InstalledMapsController uses installedManifestProvider : verify no stale-data race on initial load.
    - .g.dart regeneration : was every controller file touched by 49-fix regenerated correctly? Any format drift persisting (local 3.41.7 vs CI 3.41.5)?
    - CountryResolverController viewport debounced 500ms — walk pan-fast scenario stresses debounce ; any missed viewport update?
    - FirstLaunchBootstrap pre-init `main.dart` order : respects Zone discipline (Phase 04 Zone-mismatch precedent)? Walk cold-start stresses this.
    - **Prioritized hot-spot** : MapCameraController fix-on-fix + ActiveSessionController over-state-machine re-check

    **Agent #3** — Presentation + router + walk-findings triage :
    - **Walk findings PRIMARY landing zone** — read §1b walk-evidence verbatim + triage each observation as one potential finding (severity TBD per observation gravity).
    - Row #39 MapScreen deactivate named-helper `_nullifyMapViewProviderAfterDeactivate` : does it resist real-device lifecycle push (rotation, rapid route changes)? Walk rotation scenario stresses this.
    - Row #40 SchedulerBinding.addPostFrameCallback for maplibre defer : does the defer make a frame visible on slow CPU? Walk slow-device / emulator scenario stresses this.
    - Row #41 SessionBurgerMenu `_DistanceRow` honest placeholder : does the placeholder UX match walk expectations ("Distance: —" rather than "Distance: 0.0 km" lying)?
    - Row #43 MapsDownloadScreen duration constants hoisted : verify snackbar durations visible on walk (not too short, not too long).
    - MapDownloadProgressChip + MapsDownloadScreen copy UX : does NOT promise "background continues" (background deferred to V2). Walk screen-lock-during-download scenario confirms.
    - MapFollowMeFab walk interaction : follow-me toggle UX clear? Manual-pan-detection transitions Following → FreePan within the 1000ms debounce window (row #35 timestamp change)?
    - SessionBurgerMenu responsive drawer 75%/40% : walk portrait + landscape orientation confirmation.
    - Router 5 new routes + back-navigation : any deep-link fails on current main? Walk URL bar / share intent scenarios.
    - `context.mounted` post-await : every async gap in presentation code still guarded?
    - Riverpod 3.x pattern compliance : `AsyncValue.value` not `valueOrNull` ; `ProviderScope(overrides: [...])` inline.
    - MapScreen `mapViewBuilderForTest` typedef seam — no MapLibre in widget tests, confirmed by run.
    - **Prioritized hot-spot** : MapScreen lifecycle (row #39 named-helper + row #40 postFrameCallback) + WALK FINDINGS

    **Agent #4** — Tests + tooling + natives + CI + CLAUDE.md cross-cutting + smell transverses :
    - `check_style_no_external_url.dart` CI gate : still green on current main? Paired test 7 scenarios still exhaustive? Any new URL shape that would slip through?
    - 4 integration tests under `integration_test/` : inertness guards holding? `FakeMapView.showMapInvocations.isNotEmpty` pre-asserts still protecting?
    - 3 new permanent unit tests (world_bundle_sha256 / manifest_atomicity_contract / no_httpclient_in_unit_tests) : inertness guards holding?
    - 4 new tool paired tests (generate_tiny_pmtiles / generate_world_sha256 / prepare_style / simplify_polygons) : any edge case missed? Deterministic output still verified?
    - CI `.github/workflows/ci.yml` : 8 gate steps + android + ios + optionally integration-tests jobs — all green on current main? `gh run list --branch main --limit 1` confirms.
    - AndroidManifest.xml row #49 BootCompletedReceiver `android:exported` guard : still present + correctly scoped? `check_platform_manifests` still catching drift?
    - Platform channels Kotlin + Swift : post-refactor, any drift between Kotlin string literal + Swift string literal + Dart constant?
    - DEPENDENCIES.md row #48 integration_test entry : present + licence + audit date current?
    - `assets/maps/style.json` rows #2/#15 (8→7 layer drift) : layer count matches `kStyleLayerOrder` constant? Regression test still passes?
    - `lib/config/constants.dart` row #44 `_k...` hoist sweep : no orphan magic number re-introduced since closure?
    - tool/README.md row #17 : documents all 8 Phase 07 tool scripts? Any tool added post-closure not documented?
    - **Transversal smell-heuristics** : smell that escaped Agents #1-3 layer vision. Examples : fix-on-fix spread across multiple files in different layers ; .g.dart format drift that `dart format --set-exit-if-changed` could re-surface ; inertness guard decay on any new test (every new test since Phase 04 has inertness guard — verify 100%).
    - CLAUDE.md anti-patterns sweep across 49-fix delta : magic numbers hors constants.dart / naming conventions / DTOs sans sémantique / delegation wrappers / commentaires narrant quoi / path concat manuel / const constructors / is-chains.
    - Row #33 soak timeout flake recovery : `gh run rerun --failed` pattern still valid? Timeout bump candidate if flake recurs during Phase 08.1 closure CI.
    - **Prioritized hot-spot** : Transversal pattern sweep + inertness guard decay check (not a single component)

    **Sub-agent spawn protocol (Task 2 execution) :**

    In the NEXT assistant message after Task 1 commit is on main, Claude-main assembles ONE function_calls block with FOUR Task tool invocations. Each invocation has `subagent_type: general-purpose`, `description` like `"Phase 08.1 Agent #N — <scope>"`, `prompt` with the full template above filled per agent.

    Wait for all 4 agents to return. Capture raw output for Task 3 synthesis.

    Anti-pattern guards :
    - Do NOT spawn agents sequentially.
    - Do NOT modify agent prompts per-run based on what Agent #1 returned before #2 is spawned.
    - Do NOT filter / deduplicate findings before Task 3.
    - Do NOT forget the "files NOT in scope" negation section per agent (08.1-RESEARCH §Pitfall 2).
    - Do NOT send agents the Phase 08 smell-brief instead of the Phase 08.1 RE-CHECK brief (different lens).
  </action>
  <verify>
    <automated>bash -c 'PRECLASS_SHA=$(git log --oneline --grep="pre-class 7 handoff items" -1 --format=%H .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md); SYNTH_SHA=$(git log --oneline --grep="synthesise 4-agent audit findings" -1 --format=%H .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md); test -n "$PRECLASS_SHA" && { test -z "$SYNTH_SHA" || [ "$(git rev-list --count ${PRECLASS_SHA}..${SYNTH_SHA} -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md)" -le "1" ]; }'</automated>
    <!-- Rationale (per checker revision-mode warning 2026-04-24, task_completeness): Task 2 is a structural spawn event (4 Task tool calls in ONE assistant message) with no file mutation, so direct automation is impossible. The structural proxy above asserts via git log that between the pre-class commit (Task 1) and the synthesis commit (Task 3), at most one intervening commit exists on 08.1-REVIEW.md — consistent with a single-message parallel spawn immediately followed by synthesis. Documented exception: single-message parallel spawn is a runtime-only observable (chat log / tool-use structure), not a filesystem artifact. The spawn itself is verified by (a) the pre-class precondition commit, (b) the synthesis postcondition commit shape in Task 3, and (c) each Agent prompt containing the RE-CHECK brief verbatim (checked by Claude-main at prompt-assembly time, per Part 7 of the prompt template). Downgrading to checkpoint subtype was rejected because no user interaction is required — the spawn is autonomous. -->
  </verify>
  <done>4 Task tool invocations completed in ONE single assistant message. Each of 4 agents has returned structured findings + narrative audit notes. Raw findings captured in Claude-main memory for Task 3 synthesis. NO §2 agent sub-sections written yet — that's Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Synthesise agent findings into §2 per-agent sub-sections + archive narrative appendix</name>
  <files>.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md</files>
  <action>
    Use the Edit tool to fill §2's four agent sub-sections (`### Agent #1 — …` through `### Agent #4 — …`) with the structured findings from Task 2.

    **For each agent sub-section :**

    1. Replace the `(pending)` line under the header with the agent's structured findings, numbered globally across the agent's return :

       ```
       1. [severity] Title — 1-line explanation — file:line [optional smell tag]
       2. [severity] Title — 1-line explanation — file:line
       ...
       ```

    2. If an agent surfaced a finding that overlaps another agent's territory, keep BOTH under their respective agent sub-sections with `(also flagged by Agent #N — severity X)` cross-reference. Phase 08.1 locks cross-lens overlap preservation per Phase 02+04+06+08 convention.

    3. If an agent found NO findings in a category, write `*No findings in this scope.*` explicitly — do not leave empty.

    **For the narrative appendix :**

    Replace the `(pending)` line inside the `<details><summary>Audit Notes (narrative appendix, per agent)</summary>` block with the concatenated narrative from all 4 agents, structured as :

    ```markdown
    #### Agent #1 — Download pipeline + atomicity (re-check Phase 08 smell-surgery)

    <Agent #1 narrative verbatim>

    #### Agent #2 — Controllers + state + Riverpod wiring (re-check Phase 08 shape-consolidation)

    <Agent #2 narrative verbatim>

    #### Agent #3 — Presentation + router + walk-findings triage

    <Agent #3 narrative verbatim>

    #### Agent #4 — Tests + tooling + natives + CI + CLAUDE.md cross-cutting + smell transverses

    <Agent #4 narrative verbatim>
    ```

    Do NOT edit or summarize agent narratives — verbatim archive.

    Commit :
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(08.1-rev): synthesise 4-agent audit findings + narrative appendix into §2" --files .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c 'grep -q "#### Agent #1" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "#### Agent #4" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && git log --oneline -1 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -q "synthesise 4-agent audit findings"'</automated>
  </verify>
  <done>§2 four agent sub-sections filled with structured findings (numbered per-agent, severity + title + file:line + optional smell tag). Narrative appendix archived verbatim in collapsed `<details>` block. Cross-lens overlap preserved. Single atomic commit on main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Present triage table to user — user chooses fix/refactor/waive/defer per row + smell tag visible + `.fixes-expected` snapshot</name>
  <files>(presentation-only — no file mutation until triage captured + `.fixes-expected` written)</files>
  <action>
    Present the full findings list to user as a flattened numbered triage surface.

    **Format rules per CLAUDE.md §Code Review Phases (verbatim binding) :**

    > Tu affiches uniquement les **titres** avec une courte explication, et tu me laisses choisir ce qu'on corrige

    Surface shown in chat :

    ```
    Phase 08.1 Re-Review — Findings triage

    [Blocker / Should / Could / Noted] Title — 1-line explanation — file:line [smell tag]
    [...]
    ```

    Organize by severity first (Blockers → Shoulds → Coulds → Noteds), then by agent within severity. Number globally 1..N. Include the smell tag column inline.

    After presenting, ask :

    > "Pour chaque row, choisis une décision : `fix` (simple fix) / `refactor` (smell-tagged, architecture change) / `waived` (Should seulement, avec raison) / `defer-to-phase-XX` (Could/Noted, avec raison) / `accepted-as-is` (Noted, no action). Blanket decisions autorisées (ex : 'fix tous les Blockers et Shoulds'), précédent Phase 02+04+06+08.
    >
    > **Note Phase 08.1** : Phase 08 a déjà fait 49 fix+refactor — Phase 08.1 est delta-only. Si le count total est petit (typiquement 5-20 findings attendus per 08.1-RESEARCH §Open Question 3), Strategy A per-finding est recommandé pour Plan 08.1-05. Si c'est gros (>20), Strategy B batched est envisageable.
    >
    > Tu peux aussi répondre row-by-row ou par lots si tu préfères."

    User responds with decisions. Capture them into §3 triage table (columns `# | Finding | Severity | Smell tag | Decision | Rationale | Commit hash`) via Edit tool replacing the `(pending)` row.

    Strict rules :
    - Blockers MUST be `fix` (no waiver per CONTEXT §Fix workflow + gate-closed criteria inherited from 08-CONTEXT)
    - Shoulds MUST be `fix` or `waived` (inline rationale mandatory for waiver)
    - Could/Noted may be `fix` / `defer-to-phase-XX` / `accepted-as-is`
    - Smell-tagged findings : decision can be `refactor` (smell-triggered architecture change) separate from standard `fix` — this distinction is explicit per 08.1-CONTEXT inherited convention
    - Per 08.1-RESEARCH §Pitfall 4 : smell-tagged findings without concrete evidence (bug, test failure, walk-observation) default to `Could` / `accepted-as-is` (downgraded at triage by user signal)

    Once §3 table is populated with user decisions, commit :
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(08.1-rev): record triage decisions into §3 (N fix + M refactor + K waived + L deferred)" --files .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```

    Where N/M/K/L are actual counts for the commit subject line.

    Snapshot the count of `fix` + `refactor` decisions into scratch `.planning/phases/08.1-rereview-post-walk/.fixes-expected` (integer, one line — Phase 02+04+06+08 precedent) for Plan 08.1-05 tally verification :

    ```bash
    echo "<N+M>" > .planning/phases/08.1-rereview-post-walk/.fixes-expected
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "chore(08.1-rev): snapshot .fixes-expected for Plan 08.1-05 tally" --files .planning/phases/08.1-rereview-post-walk/.fixes-expected
    ```

    Or fold `.fixes-expected` into the triage commit (Claude's discretion — both acceptable).

    Report back to user with triage summary counts and ask : "§3 triage committé. J'enchaîne sur Plan 08.1-04 (adversarial verification — DEFAULT SKIP per 08.1-RESEARCH §Pattern 3 unless walk demanded new permanent tests) dès que tu valides (réponse : `OK adversarial` ou équivalent)."
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/08.1-rereview-post-walk/.fixes-expected && [ "$(cat .planning/phases/08.1-rereview-post-walk/.fixes-expected)" -ge "0" ] && git log --oneline -5 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -q "record triage decisions into §3"'</automated>
  </verify>
  <done>§3 Triage decisions table populated with user decisions per row (smell-tag column visible, row numbering LOCAL to Phase 08.1 with `08§3-#N` cross-ref format for Phase 08 references). `.fixes-expected` scratch file contains integer count of `fix` + `refactor` decisions. Commit `docs(08.1-rev): record triage decisions into §3 …` on main. User has signaled "OK adversarial" to unblock Plan 08.1-04.</done>

  <what-built>
    Nothing automated. This checkpoint IS the triage gate itself : Claude-main presents findings as title + explanation surface per CLAUDE.md §Code Review Phases ; user chooses fix/refactor/waive/defer per row (or blanket) ; Claude captures decisions into §3 + writes `.fixes-expected` snapshot.
  </what-built>
  <how-to-verify>
    After user responds :
    - `grep -v "| (pending) |" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -q "| fix |"` or similar — decisions present
    - `.fixes-expected` exists with integer count
    - `git log --oneline -1 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md` shows triage commit
  </how-to-verify>
  <resume-signal>
    User types "OK adversarial" or "§3 validé, go 08.1-04" to signal that Plan 08.1-04 may run the adversarial verification (default-skip) wave.
  </resume-signal>
</task>

</tasks>

<verification>
- Task 1 : §2 pre-class (7 rows) + smell re-check (6 rows) committed BEFORE any agent spawn
- Task 2 : 4 agents spawned in ONE single tool-use message (structural constraint — chat log shows 4 Task calls in one message)
- Task 3 : §2 Agent #1-#4 sub-sections filled + narrative appendix archived
- Task 4 : §3 triage table filled with user decisions + `.fixes-expected` snapshot + user unblock signal
- 3 atomic commits on main (pre-class + synthesis + triage) + optional 4th for `.fixes-expected` snapshot if not folded into triage commit
</verification>

<success_criteria>
Plan 08.1-03 complete when :
1. §2 Pre-known from 08.1-CONTEXT = 7 rows ; §2 Smell heuristics hot-spots re-check = 6 rows — both filled before agent spawn
2. 4 `general-purpose` agents spawned in ONE single tool-use message with re-balanced layer+delta scope + smell RE-CHECK brief + walk findings + explicit "files NOT in scope" negation per 08.1-RESEARCH §Pitfall 2
3. §2 Agent #1..#4 sub-sections filled with structured findings + cross-lens overlap preserved
4. §2 collapsed narrative appendix archived with 4 agent reports verbatim
5. §3 triage table filled with user decisions (smell-tag column visible + row numbering LOCAL to 08.1)
6. `.fixes-expected` integer snapshot written for Plan 08.1-05
7. 3-4 atomic commits on main (pre-class / synthesis / triage / optional fixes-expected)
8. User signals "OK adversarial" to unblock Plan 08.1-04
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-rereview-post-walk/08.1-03-SUMMARY.md` summarizing :
- Pre-class commit hash + 7 items listed + 6 smell re-check hot-spots
- Synthesis commit hash + findings count per-agent
- Triage commit hash + decision counts (N fix / M refactor / K waived / L deferred / P accepted-as-is)
- `.fixes-expected` integer
- Walk findings triage summary : how many walk-surfaced observations became §3 rows ; how many candidate permanent regression tests forwarded to Plan 08.1-04
- Surprise findings (findings not anticipated in CONTEXT pre-class — for STATE.md Accumulated Decisions at closure)
- Cross-lens overlaps surfaced (list of findings flagged by 2+ agents with severities)
- Smell-tagged findings count + decision breakdown (fix-as-patch vs refactor-as-architecture vs defer — compare to Phase 08's 9 smell-refactors for retrospective)
- Timestamp of user unblock signal for Plan 08.1-04
</output>
</content>
</invoke>