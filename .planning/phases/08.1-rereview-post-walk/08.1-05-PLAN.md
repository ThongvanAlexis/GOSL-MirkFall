---
phase: 08.1-rereview-post-walk
plan: 05
type: execute
wave: 5
depends_on:
  - 08.1-04
files_modified:
  - .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
  - .planning/phases/08.1-rereview-post-walk/.fixes-expected
  - .planning/phases/08.1-rereview-post-walk/08.1-05-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: []
success_criteria:
  - phase-08.1-closed
must_haves:
  truths:
    - "User explicitly chooses fix-loop strategy at start of plan: Strategy A per-finding atomic commits OR Strategy B batched. Decision recorded verbatim in 08.1-05-SUMMARY.md. Per 08.1-RESEARCH §Open Question 3 + Pitfall 5, if N ≤ 15 Strategy A recommended (single session feasible) ; if N > 15 Strategy B recommended + relay handoff pattern (Phase 08 precedent)."
    - "Every finding triaged as `fix` OR `refactor` in §3 has a corresponding commit on `main` with prefix `fix(08.1-rev):` / `refactor(08.1-rev):` / `test(08.1-rev):` / `docs(08.1-rev):` / `chore(08.1-rev):` / `ci(08.1-rev):` (per nature). Refactor commits specifically address smell-tagged findings requiring architecture change (Phase 08 9-refactor precedent)."
    - "Optional pre-fix `chore(08.1-rev): dart format align with CI gate` IF any Agent reported drift (Phase 04 + 06 + 08 surprise-Blocker pattern — apply first if drift exists, before tackling other Should/Blocker fixes)."
    - "Every fix commit (or batch) passes CI green BEFORE the next is pushed — bisectable + revertable per finding (or per batch)."
    - "After all fixes applied, the final commit on `main` has CI green on all 3 jobs (gates / android / ios) + `integration-tests` if job split. `gh run view <hash>` confirms all jobs success."
    - "§5 populated: final commit hash + CI run URL + `All N jobs green (gates / android / ios [+ integration-tests])` status + date `2026-04-XX`"
    - "`08.1-REVIEW.md` header: `**Status:** open` → `closed`; `**Closed:** (pending)` → `**Closed:** 2026-04-XX`; trailing footer `_Phase 08.1 closed: (pending)_` → `_Phase 08.1 closed: 2026-04-XX_`; `_Phase 09 remains unblocked_` stays unchanged."
    - "Zero Blockers remain unfixed (waiver forbidden per inherited 08-CONTEXT Fix workflow + gate-closed criteria). Zero Should rows in §3 with `Decision: fix` remain without a `Commit hash` populated."
    - "Every Should triaged as `waived` has inline non-empty Rationale in §3 (re-verified at closure)."
    - "Smell-tagged findings triage decision documented (fix / refactor / defer) — visible in §3 smell-tag column."
    - "Gate-closed checklist Task 4: all items ticked explicitly in §5 closure footer OR in 08.1-05-SUMMARY."
    - "`.planning/STATE.md` Accumulated Decisions contains a new entry for Phase 08.1 closure recording : (1) fix strategy chosen (A or B), (2) walk outcome (PASS/PASS-with-findings/FAIL + findings count), (3) any surprise findings from re-review, (4) smell-refactor re-check outcome (did Phase 08's 9 smell-refactors introduce new smells? count of new smell-tagged findings from agents), (5) Phase 08.1 completion marker `[Phase 08.1-rereview-post-walk]: re-review closed YYYY-MM-DD — N Blockers fixed + M Shoulds fixed/waived; Phase 09 remains unblocked.`"
    - "`.planning/ROADMAP.md` Phase 08.1 row in the Progress table flipped to `5/5 | Complete | 2026-04-XX`. Phase 09 row remains `Not started` (unchanged — Phase 08.1 is re-review, not a blocker)."
    - "`.planning/STATE.md` `current_plan` updated to `Phase 08.1 re-review closed, Phase 09 Fog Rendering remains unblocked`. `stopped_at` updated to `Phase 08.1 closure committed YYYY-MM-DD`. `progress.completed_plans` incremented by 5 (08.1-01..05); `progress.completed_phases` UNCHANGED at 8 — see Step B rationale (insert-phases via `/gsd:insert-phase` do not bump the integer phase counter)."
    - "`.fixes-expected` scratch file DELETED at phase closure (Phase 02+04+06+08 Plan 05 lifecycle — snapshot was a tally check, not permanent artifact)."
    - "Phase 09 remains unblocked — gsd-verifier can read §5 + §3 + §4 + final main CI green to sign Phase 08.1 complete. Phase 09 was unblocked by Phase 08 closure ; Phase 08.1 is a re-audit that does NOT re-block Phase 09."
  artifacts:
    - path: ".planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md"
      provides: "Final state — status=closed, §5 filled, all 5 sections complete, closure footer dated"
      contains: "**Status:** closed"
      min_lines: 300
    - path: ".planning/STATE.md"
      provides: "Accumulated Decisions : Phase 08.1 closure entry + walk outcome + smell re-check retrospective ; current_plan + stopped_at + progress updated"
      contains: "Phase 08.1-rereview-post-walk"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 08.1 row flipped to 5/5 Complete with date"
      contains: "08.1 Re-Review — Post-Walk Audit"
  key_links:
    - from: "`.fixes-expected` snapshot (integer count from Plan 08.1-03 Task 4)"
      to: "Plan 08.1-05 fix-commit tally verification"
      via: "snapshot taken at Plan 08.1-03 Task 4 ; verify command compares snapshot vs count of `fix|refactor|test|chore|docs|ci (08.1-rev):` commits"
      pattern: "\\.fixes-expected"
    - from: "`08.1-REVIEW.md` §3 rows marked `fix` / `refactor`"
      to: "Atomic commits on main"
      via: "`git log --extended-regexp --grep='^(fix|refactor|test|chore|ci|docs)\\(08\\.1-rev\\):'`"
      pattern: "^(fix|refactor|test|chore|ci|docs)\\(08\\.1-rev\\):"
    - from: "Final commit on main (after fixes)"
      to: "CI run URL in §5"
      via: "`gh run view <id>` confirms all jobs green"
      pattern: "All .* jobs green"
    - from: "`08.1-REVIEW.md` status header transition open → closed"
      to: "gsd-verifier gate-closed check"
      via: "status transition + `.fixes-expected` deletion + STATE.md / ROADMAP.md update"
      pattern: "\\*\\*Status:\\*\\* closed"
    - from: "Walk outcome + smell-refactor re-check retrospective"
      to: "STATE.md Accumulated Decisions Phase 08.1 closure entry"
      via: "Phase 08.1 is the SECOND application of CLAUDE.md 2026-04-23 smell-heuristics delta — record retrospective as precedent for Phases 10/12/14/16"
      pattern: "\\[smell:(fix-on-fix|over-state-machine)\\]"
---

<objective>
Close the re-review loop for Phase 08.1 : apply every finding triaged as `fix` OR `refactor` in §3 as atomic commits (or user-approved batches), gate each commit on CI-green before proceeding to the next, confirm the final state on `main` is green on all jobs, archive the closure into §5, mark `08.1-REVIEW.md` status=closed, update STATE.md + ROADMAP.md to flip Phase 08.1 to Complete, delete the `.fixes-expected` scratch tally, and confirm Phase 09 remains unblocked.

Purpose: The gate-closed condition is the whole point of the re-review. 08.1-CONTEXT (inherited from 08-CONTEXT §Fix workflow + gate-closed criteria) makes it explicit : every Blocker fixed (waiver forbidden), every Should fixed OR explicitly waived inline §3, CI green on the final main commit, `08.1-REVIEW.md` complete with 5 sections + §1b walk evidence + §2 7 pre-class + smell re-check + 4 agent findings + §3 triage with smell-tag column + §4 adversarial evidence (gates + branch hygiene + N walk-finding tests) + §5 CI-green confirmation. This plan operationalizes that — it's the only way Phase 08.1 can close cleanly. Phase 02+04+06+08 Plan 05 proved the mechanical pattern ; Phase 08.1 reuses + extends with walk-outcome retrospective for STATE.md precedent (Phase 08.1 is the second application of CLAUDE.md 2026-04-23 smell-heuristics delta).

Key difference vs Phase 08 Plan 08-05 :
1. **Expected smaller fix count** — Phase 08 landed 49 atomic fix+refactor commits ; Phase 08.1 typically 5-20 (per 08.1-RESEARCH §Open Question 3). Strategy A per-finding feasible without relay handoff.
2. **Walk-outcome retrospective** — STATE.md entry records the walk result (PASS / PASS-with-findings / FAIL) + smell-refactor re-check outcome (did Phase 08's 9 smell-refactors introduce new smells?) for Phases 10/12/14/16 precedent.
3. **Phase 09 remains unblocked** — Phase 08.1 is re-review, not a blocker. STATE.md + ROADMAP.md reflects Phase 08.1 as a SIBLING row (not replacement of Phase 08), Phase 09 eligibility unchanged.

Output: `08.1-REVIEW.md` fully complete (5 sections, status=closed), `main` CI green on all jobs, Phase 08.1 signed off in STATE.md + ROADMAP.md, `.fixes-expected` scratch deleted, Phase 09 remains unblocked.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.1-rereview-post-walk/08.1-CONTEXT.md
@.planning/phases/08.1-rereview-post-walk/08.1-RESEARCH.md
@.planning/phases/08.1-rereview-post-walk/08.1-VALIDATION.md
@.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
@.planning/phases/08.1-rereview-post-walk/.fixes-expected
@.planning/phases/08.1-rereview-post-walk/08.1-03-SUMMARY.md
@.planning/phases/08.1-rereview-post-walk/08.1-04-SUMMARY.md
@.planning/phases/08-review-gate-map/08-05-PLAN.md
@.planning/phases/08-review-gate-map/08-05-SUMMARY.md
@.planning/phases/06-review-gate-gps/06-05-PLAN.md
@.planning/phases/04-review-gate-persistence/04-05-PLAN.md
@CLAUDE.md
@DEPENDENCIES.md

<interfaces>
<!-- Fix-loop strategies + gate-closed checklist contract inherited from 08-CONTEXT §Fix workflow + gate-closed criteria + Phase 04/06/08 precedents. -->

**Fix-loop strategies (user chooses at Task 1) :**

**Strategy A — per-finding atomic commit (Phase 02 + Phase 08 precedent) :**
- One commit per §3 `fix` or `refactor` row
- Each commit gated on CI green before next push
- Maximum bisectability (`git bisect` lands on individual finding)
- Wall-clock cost : N findings × ~6 min CI cycle
- Best when N ≤ 15 findings (typical for Phase 08.1 re-review scope)

**Strategy B — batched fix loop (Phase 04 + Phase 06 precedent) :**
- Group related fixes into batches of ~3-5 commits
- Each batch gated on CI green before next batch pushed
- Coarser bisectability
- Wall-clock cost : M batches × ~10 min CI cycle
- Best when N > 15 findings

User decision recorded verbatim in `08.1-05-SUMMARY.md` with rationale. STATE.md Accumulated Decisions records strategy choice.

**Commit prefix conventions (inherited from 08-CONTEXT) :**
- `fix(08.1-rev): <title>` — runtime behavior bug fix
- `refactor(08.1-rev): <title>` — smell-tagged architecture change (distinct from fix to preserve smell-vs-patch distinction)
- `test(08.1-rev): <title>` — added/modified test (not already in Plan 08.1-04)
- `docs(08.1-rev): <title>` — pure documentation / markdown / comment fix
- `chore(08.1-rev): <title>` — tooling / config / dep bump
- `ci(08.1-rev): <title>` — CI workflow change

**Gate-closed checklist (inherited from 08-CONTEXT §Fix workflow + gate-closed criteria verbatim + adapted for Phase 08.1) :**

1. ✅ Tous findings `Blocker` fixés (pas de waiver possible — CONTEXT lock)
2. ✅ Tous findings `Should` soit fixés soit explicitly waived avec rationale inline §3
3. ✅ Findings `smell`-tagged triage explicite (fix / refactor / defer) documenté
4. ✅ CI verte sur le commit final `main` (gates + android + ios [+ integration-tests if split])
5. ✅ `08.1-REVIEW.md` 5 sections remplies :
   - §1 user observations verbatim (or 'Aucune observation utilisateur' marker)
   - §1b per-device walk-evidence with screenshots + cadence table + verdict + walk-finding candidates list
   - §2 7 pre-class items + Smell heuristics hot-spots re-check table (6 rows) + 4-agent findings + narrative appendix
   - §3 triage table filled with all findings + smell-tag column + decisions
   - §4 adversarial evidence populated (8 gates + branch hygiene + N walk-finding test blocks)
   - §5 CI-green confirmation
6. ✅ 8 CI gates still live + green on final commit (inherited baseline confirmed Plan 08.1-04 Task 1)
7. ✅ ROADMAP amended : Phase 08.1 → 5/5 Complete with date ; Phase 09 row unchanged
8. ✅ No new adversarial branch pushed (Phase 08.1 default-skip convention held Plan 08.1-04 Task 2)
9. ✅ `gsd-verifier` grep `^## [1-5]\\.` `08.1-REVIEW.md` returns 5 headings
10. ✅ Walk-finding regression tests (if any, Plan 08.1-04 Task 3) live on main with inertness guards + documented mutation experiments

If any checkbox unchecked at Task 4, Phase 08.1 does NOT close — return to fix-loop.

**Walk-outcome retrospective for STATE.md Accumulated Decisions entry (Phase 08.1-specific — not in Phase 08) :**

Template for closure entry (filled at Task 4 Step B) :

```
- [Phase 08.1-rereview-post-walk]: re-review closed YYYY-MM-DD — N Blockers fixed + M Shoulds fixed/waived + K refactor(smell) commits + L documentation commits. Walk outcome: <PASS / PASS-with-findings / FAIL> (Android <status> + iOS <status>, walk date YYYY-MM-DD, walk doc docs/phase-08.1-walk.md). Smell-refactor re-check result: <0 new smells introduced by Phase 08's 9 refactors / <X> new smell-tagged findings from agents — all decided <fix|refactor|defer>>. Walk-finding-driven regression tests added: <N>. Adversarial default-skip convention held: 0 new CI gates, 0 new adversarial branches. Fix strategy: <A per-finding OR B batched with N batches grouped by <X>>. Phase 08.1 is the SECOND review-gate encoding of CLAUDE.md 2026-04-23 smell-heuristics delta — validates template on narrower scope (5-7 findings vs Phase 08's 49). Precedent for Phases 10/12/14/16 REVIEW gates. Phase 09 Fog Rendering remains unblocked (Phase 08.1 is re-audit, not blocker).
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: User chooses fix-loop strategy (A per-finding OR B batched) + confirms .fixes-expected count</name>
  <files>(decision capture — no file mutation)</files>
  <action>
    Claude-main reads `.planning/phases/08.1-rereview-post-walk/.fixes-expected` integer written in Plan 08.1-03 Task 4 :

    ```bash
    FIXES=$(cat .planning/phases/08.1-rereview-post-walk/.fixes-expected)
    echo "§3 triage has $FIXES rows marked fix or refactor."
    ```

    Post to user :

    > "Plan 08.1-05 starting. §3 triage marked **$FIXES** rows as `fix` or `refactor`. Choose fix-loop strategy :
    >
    > **Strategy A** — Per-finding atomic commits. Wall-clock ≈ $((FIXES * 6)) min. Best bisectability. Phase 08.1 typical count (5-20) → single-session feasible.
    > **Strategy B** — Batched. Wall-clock ≈ $((FIXES / 4 * 10)) min for ~$((FIXES / 4)) batches of ~4 fixes each. Coarser bisect.
    >
    > **Precedent** : Phase 04 Strategy B (31 findings / 10 batches). Phase 06 Strategy B (22 fixes / 6 batches). Phase 08 Strategy A (49 commits / 5 relays). For Phase 08.1 with FIXES=$FIXES, Strategy A is recommended if $FIXES ≤ 15 (single session no relay) ; Strategy B if $FIXES > 15.
    >
    > Smell-tagged findings triaged as `refactor` (architecture change) are separate commits from `fix` ones regardless of strategy — smell-vs-patch distinction preserved (Phase 08 9-refactor precedent).
    >
    > Choose : `A per-finding` / `B batched` / `B batched with N batches` (specify N).
    >
    > Also preview any `chore(08.1-rev): dart format align` pre-fix : if Agent #4 reported format drift in §2, that's the first commit regardless of strategy (Phase 04+06+08 surprise-Blocker pattern)."

    User responds with A / B / B-with-N. Claude-main captures in :
    - A scratch variable for Task 2 loop shape
    - `08.1-05-SUMMARY.md` (created at plan end) with rationale quote

    If user chose B, ask : "Group by : (i) severity (Blockers first batch, etc.) / (ii) file proximity / (iii) concern (state-machine smell refactor one batch, etc.) / (iv) my judgment." Capture choice.

    Resume signal : user types `A` / `B` / `B with N batches, group by <X>`.
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/08.1-rereview-post-walk/.fixes-expected && [ "$(cat .planning/phases/08.1-rereview-post-walk/.fixes-expected)" -ge "0" ]'</automated>
  </verify>
  <done>User has chosen fix-loop strategy (A or B), Claude-main has captured the decision + grouping (if B), ready to execute fix-loop in Task 2.</done>

  <what-built>Nothing automated. This checkpoint captures user strategy decision for the fix-loop.</what-built>
  <how-to-verify>User has posted explicit strategy choice in chat.</how-to-verify>
  <resume-signal>User types `A` / `B` / `B with N batches, group by severity|file|concern|judgment`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute fix-loop — commits/batches per strategy, each gated on CI green before next push</name>
  <files>(many files per fix — dynamic, determined by §3 triage)</files>
  <action>
    **Pre-fix check** : if Agent #4 reported dart format drift in §2, run `dart format --line-length 160 lib/ test/ tool/` and commit as `chore(08.1-rev): dart format align with CI gate` BEFORE any other fix. Verify CI green on this commit.

    **Strategy A execution** (per-finding) :
    - For each row in §3 with `Decision: fix` or `refactor` :
      1. Apply the change in the targeted files (guided by the finding's `file:line` reference)
      2. Stage + commit with appropriate prefix :
         - `fix(08.1-rev): <short title from §3>` if Decision = fix
         - `refactor(08.1-rev): <short title>` if Decision = refactor (smell-tagged)
         - `test(08.1-rev):` / `docs(08.1-rev):` / `chore(08.1-rev):` / `ci(08.1-rev):` as appropriate
      3. Push to main
      4. Watch CI : `gh run watch`
      5. If CI green → update §3 row's `Commit hash` column with the SHA → next row
      6. If CI red → diagnose, fix, re-commit (NEW commit, not amend), re-watch. Do NOT proceed to next row until current green.

    **Strategy B execution** (batched) :
    - Group §3 `fix` + `refactor` rows by user's grouping criterion into N batches
    - For each batch :
      1. Apply ALL changes in the batch's rows
      2. Stage + commit as ONE commit with subject `fix(08.1-rev): batch N/M — <descriptive title>` OR split by type if batch has mixed fix/refactor/docs (e.g. `refactor(08.1-rev): batch 2/6 — smell cleanup (2 findings)`)
      3. Push to main
      4. Watch CI
      5. If green → update ALL §3 rows in batch with batch commit SHA → next batch
      6. If red → diagnose, fix, re-commit, re-watch
    - Commit bodies MUST list the §3 row numbers addressed (e.g. `Addresses §3 rows #1, #3, #7`) for bisect traceability.

    **Common rules for both strategies :**
    - After each commit (or batch) hits green CI, edit `08.1-REVIEW.md` §3 to replace `| (pending) |` with `| <SHA> |` in Commit hash column. Commit as `docs(08.1-rev): mark §3 rows <n>[..<m>] as fixed` OR fold into next fix commit if natural.
    - Blocker findings go first (waiver forbidden). Then Should. Then Could (if user triaged any as fix).
    - Smell-tagged findings triaged `refactor` use `refactor(08.1-rev):` prefix — visible in git log + serves STATE.md retrospective.
    - Never amend a pushed commit — always new commit (locked Phase 02+04+06+08).
    - Per 08.1-RESEARCH §Pitfall 5 : if context budget approaches limit (single session), signal orchestrator for relay handoff. `.fixes-expected` + §3 (pending) markers are the handoff state.

    **Verify at task end :**
    ```bash
    # Expected fix/refactor commit count matches snapshot
    EXPECTED=$(cat .planning/phases/08.1-rereview-post-walk/.fixes-expected)
    ACTUAL=$(git log --extended-regexp --grep='^(fix|refactor|test|chore|ci|docs)\(08\.1-rev\):' --since=<plan_start_date> | grep -c "^commit")
    # Strategy A: ACTUAL >= EXPECTED
    # Strategy B: ACTUAL < EXPECTED but batch count × avg ≈ EXPECTED

    # Final CI green
    gh run list --limit 1 --branch main --json conclusion --jq ".[0].conclusion"
    # Must be "success"

    # No pending rows in §3
    ! grep -q "| (pending) |" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c '! grep -q "| (pending) |" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && gh run list --limit 1 --branch main --json conclusion --jq ".[0].conclusion" | grep -qi "success"'</automated>
  </verify>
  <done>All §3 `fix` + `refactor` rows have Commit hash populated (no `(pending)` remaining). Final commit on main has CI green on all jobs. Strategy A : N commits on main (≈ EXPECTED). Strategy B : M batch commits (≈ EXPECTED / 4).</done>
</task>

<task type="auto">
  <name>Task 3: Populate §5 CI-green confirmation + flip 08.1-REVIEW.md status → closed</name>
  <files>.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md</files>
  <action>
    Use the Edit tool to :

    **A — Populate §5 CI-green confirmation** — replace `(pending)` markers :

    ```markdown
    - **Final commit on main:** `<SHA>` — `<commit subject from git log -1 main>`
    - **CI run URL:** `<URL from gh run view --json url>`
    - **Status:** All <N> jobs green (gates / android / ios [/ integration-tests])
    - **Date:** 2026-04-XX
    ```

    Values :
    - `<SHA>` = `git rev-parse HEAD`
    - `<commit subject>` = `git log -1 --format=%s`
    - `<URL>` = `gh run list --limit 1 --branch main --json url --jq ".[0].url"`
    - `<N>` = 3 or 4 depending on integration-tests job split status (inherited from Phase 08 Plan 08-04)
    - `<DATE>` = today's date

    **B — Flip header status** — replace `**Status:** open` with `**Status:** closed` and `**Closed:** (pending)` with `**Closed:** 2026-04-XX`.

    **C — Flip trailing footer** — replace `_Phase 08.1 closed: (pending)_` with `_Phase 08.1 closed: 2026-04-XX_`. Leave `_Phase 09 remains unblocked (no new gating from 08.1; re-review is pure additive audit)._` as-is.

    Commit :
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(08.1-rev): populate §5 CI-green + flip status to closed" --files .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```

    Verify :
    ```bash
    grep -q '^\*\*Status:\*\* closed' .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ! grep -q "(pending)" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "^## 5\\. CI-green confirmation" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "All .* jobs green" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c 'grep -q "^\*\*Status:\*\* closed" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && ! grep -q "(pending)" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -q "All .* jobs green" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && git log --oneline -1 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -q "populate §5 CI-green"'</automated>
  </verify>
  <done>§5 populated with final commit hash + CI run URL + jobs-green status + date. Header status flipped to closed. Trailing footer dated. No `(pending)` markers remain anywhere. One atomic commit on main.</done>
</task>

<task type="auto">
  <name>Task 4: Gate-closed checklist verification + STATE.md + ROADMAP.md update + delete .fixes-expected + handoff confirmation</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/08.1-rereview-post-walk/.fixes-expected (deleted)</files>
  <action>
    **Step A — Run gate-closed checklist verification (10 items from 08.1-CONTEXT inherited §Fix workflow + gate-closed criteria) :**

    ```bash
    set -e
    echo "Phase 08.1 gate-closed checklist:"

    # 1. Zero Blockers unfixed
    BLOCKER_UNFIXED=$(grep "^| .* | Blocker |" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -v "Commit hash\| [a-f0-9]\{7,\} |" | grep -v "| waived |" | wc -l)
    [ "$BLOCKER_UNFIXED" = "0" ] && echo "  ✅ 1. Zero Blockers unfixed" || { echo "  ❌ 1. $BLOCKER_UNFIXED Blocker(s) unfixed" >&2; exit 1; }

    # 2. Zero Should with `fix` decision + empty hash
    SHOULD_PENDING=$(grep "^| .* | fix |" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep "| (pending) |\\|| *|$" | wc -l)
    [ "$SHOULD_PENDING" = "0" ] && echo "  ✅ 2. Zero Should fix-pending" || { echo "  ❌ 2. $SHOULD_PENDING Should fix(es) lack commit hash" >&2; exit 1; }

    # 3. Smell-tagged findings triage explicit
    SMELL_UNDECIDED=$(grep "\[smell:" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -v "| fix \|| refactor \|| waived \|| defer-to-\|| accepted-as-is \|" | wc -l)
    [ "$SMELL_UNDECIDED" = "0" ] && echo "  ✅ 3. All smell-tagged findings triaged" || { echo "  ❌ 3. $SMELL_UNDECIDED smell finding(s) lack triage decision" >&2; exit 1; }

    # 4. CI green on final main commit
    gh run list --limit 1 --branch main --json conclusion --jq ".[0].conclusion" | grep -qi "success" && echo "  ✅ 4. CI green on final main commit" || { echo "  ❌ 4. CI not green" >&2; exit 1; }

    # 5. 5-section structure intact
    SECTIONS=$(grep -c "^## [1-5]\\." .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md)
    [ "$SECTIONS" = "5" ] && echo "  ✅ 5. 5 sections present" || { echo "  ❌ 5. Section count: $SECTIONS (expected 5)" >&2; exit 1; }

    # 6. 8 CI gates still live + green (inherited baseline from Plan 08.1-04 Task 1)
    dart run tool/check_headers.dart > /dev/null 2>&1 && \
      dart run tool/check_licenses.dart > /dev/null 2>&1 && \
      dart run tool/check_dependencies_md.dart > /dev/null 2>&1 && \
      dart run tool/check_domain_purity.dart > /dev/null 2>&1 && \
      dart run tool/check_platform_manifests.dart > /dev/null 2>&1 && \
      dart run tool/check_avoid_maplibre_leak.dart > /dev/null 2>&1 && \
      dart run tool/check_avoid_remote_pmtiles.dart > /dev/null 2>&1 && \
      dart run tool/check_style_no_external_url.dart > /dev/null 2>&1 && \
      echo "  ✅ 6. All 8 CI gates green on final commit" || { echo "  ❌ 6. A CI gate failed" >&2; exit 1; }

    # 7. ROADMAP amendment scheduled (Phase 08.1 row ready to flip — done in Step C below)
    grep -q "08.1 Re-Review — Post-Walk Audit" .planning/ROADMAP.md && echo "  ✅ 7. ROADMAP Phase 08.1 row present (will flip to 5/5 Complete in Step C)" || { echo "  ❌ 7. ROADMAP Phase 08.1 row missing" >&2; exit 1; }

    # 8. No new adversarial branch pushed (Phase 08.1 default-skip)
    ! git ls-remote --heads origin 'adversarial/08.1-*' | grep -q '.' && echo "  ✅ 8. No new adversarial/08.1-* branches (default-skip held)" || { echo "  ❌ 8. Unexpected adversarial/08.1-* branch exists" >&2; exit 1; }

    # 9. gsd-verifier grep 5 headings
    [ "$(grep -c '^## [1-5]\\.' .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md)" = "5" ] && echo "  ✅ 9. gsd-verifier ^## [1-5]. grep returns 5" || { echo "  ❌ 9. Section grep fails" >&2; exit 1; }

    # 10. Walk-finding regression tests present if landed (N>0 scenario) — if N=0 scenario, skip
    WALK_TESTS=$(git log --extended-regexp --grep='^test\(08\.1-rev\):.*walk-regression|regression guard' --oneline | wc -l)
    if [ "$WALK_TESTS" -gt "0" ]; then
      # Verify each walk-regression test exists + has @Tags
      echo "  ✅ 10. $WALK_TESTS walk-finding regression test commit(s) found on main"
    else
      echo "  ✅ 10. N=0 walk-finding regression tests (default-skip per Plan 08.1-04)"
    fi

    echo "ALL 10 GATE-CLOSED CHECKS PASS."
    ```

    If any fails, return to Task 2 to fix.

    **Step B — Update STATE.md :**

    Reconcile against current STATE.md values (verified 2026-04-24) :
    - Pre-08.1 baseline : `completed_phases: 8`, `completed_plans: 42`, `percent: 98`.
    - Post-08.1 closure : `completed_phases: 8` (UNCHANGED), `completed_plans: 47` (42 + 5), `percent: recomputed`.

    **Rationale for keeping `completed_phases` at 8** (cite verbatim in the STATE.md commit body — addresses checker revision_mode state_consistency blocker) :

    > Phase 08.1 was inserted via the `/gsd:insert-phase` decimal pathway documented in ROADMAP.md §Phase Numbering ("Décimales réservées à d'éventuelles insertions urgentes via `/gsd:insert-phase`"). The `progress.completed_phases` counter tracks the integer phase count (01..16 per ROADMAP.md §Phases) — decimal insert-phases do NOT bump this counter, because the integer phase sequence is unchanged (Phase 09 Fog Rendering is still the next integer-numbered phase). The 5 new plans (08.1-01..08.1-05) DO increment `completed_plans` from 42 to 47, because `completed_plans` is a pure tally independent of phase numbering. CLAUDE.md §Code Review Phases directive "pas de .5, utiliser des numéros séquentiels" is scoped to STANDARD sequential code-review phases (01 Code / 02 Review Gate / 03 Code / ...) ; the `/gsd:insert-phase` decimal escape hatch (ROADMAP.md §Phase Numbering) is the explicit exception for urgent post-walk re-reviews, and Phase 08.1 exercises it for the first time. This rationale is a precedent for any future `/gsd:insert-phase` invocations (e.g. hypothetical 10.1, 12.1).

    Use Edit tool to update `.planning/STATE.md` :

    1. Update frontmatter (pre-08.1 values on left, post-08.1 on right — apply exactly) :
       ```yaml
       current_plan: "Phase 08.1 re-review closed, Phase 09 Fog Rendering remains unblocked"
       stopped_at: "Phase 08.1 closure committed 2026-04-XX — 5 plans done via /gsd:insert-phase decimal pathway (completed_phases counter unchanged per ROADMAP §Phase Numbering escape-hatch; completed_plans 42 → 47)"
       last_updated: "2026-04-XXThh:mm:ss.XXXZ"
       progress:
         total_phases: 16        # unchanged (integer phase count still 16)
         completed_phases: 8     # UNCHANGED — /gsd:insert-phase decimal insertion does not bump the integer phase counter
         total_plans: 47         # was 42 — +5 plans from Phase 08.1 (08.1-01..05)
         completed_plans: 47     # was 42 — +5 plans completed
         percent: 100            # 47/47 until Phase 09 expands total_plans
       ```

       Note : `total_plans` also bumps +5 (42 → 47) because 5 new plans exist. Keep `percent` = completed_plans / total_plans recomputed. Do NOT touch `total_phases` (still 16 integer phases).

    2. Update the **Current Focus** paragraph + **Current Position** block to reference Phase 08.1 closure + Phase 09 still unblocked. Replace the paragraph describing Phase 08 closure with an analogous one for Phase 08.1 (walk outcome + smell re-check retrospective + fix count).

    3. Add entry to Accumulated Decisions → Decisions section using the template from `<interfaces>` §Walk-outcome retrospective :

       ```
       - [Phase 08.1-rereview-post-walk]: re-review closed YYYY-MM-DD — N Blockers fixed + M Shoulds fixed/waived + K refactor(smell) commits + L documentation commits. Walk outcome: <PASS / PASS-with-findings / FAIL> (Android <status> + iOS <status>, walk date YYYY-MM-DD, walk doc docs/phase-08.1-walk.md). Smell-refactor re-check result: <0 new smells introduced by Phase 08's 9 refactors / <X> new smell-tagged findings from agents — all decided <fix|refactor|defer>>. Walk-finding-driven regression tests added: <N>. Adversarial default-skip convention held: 0 new CI gates, 0 new adversarial branches. Fix strategy: <A per-finding OR B batched with N batches grouped by <X>>. Numbering: inserted via /gsd:insert-phase (ROADMAP §Phase Numbering decimal escape-hatch) — completed_phases counter unchanged, completed_plans incremented by 5. CLAUDE.md §Code Review Phases "pas de .5" directive is scoped to standard sequential phases, NOT /gsd:insert-phase insertions. Phase 08.1 is the SECOND review-gate encoding of CLAUDE.md 2026-04-23 smell-heuristics delta — validates template on narrower scope (5-7 findings vs Phase 08's 49). Precedent for Phases 10/12/14/16 REVIEW gates AND for future /gsd:insert-phase invocations (10.1, 12.1, etc.). Phase 09 Fog Rendering remains unblocked (Phase 08.1 is re-audit, not blocker).
       ```

       Replace N/M/K/L + Walk outcome + smell-refactor result + strategy placeholders with actual values from Task 2 outcomes + §3 triage + 08.1-02-SUMMARY + 08.1-04-SUMMARY.

    Commit (include the numbering rationale in the commit body, not just the subject) :
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(state): record phase 08.1 closure + walk retrospective + smell re-check outcome" --files .planning/STATE.md
    ```

    **Step C — Update ROADMAP.md :**

    Use Edit tool :

    1. Phase 08.1 progress row — find
       ```
       | 08.1 Re-Review — Post-Walk Audit (INSERTED) | 0/5 | In Progress | - |
       ```
       (added by Plan 08.1-01 Task 3) and replace with
       ```
       | 08.1 Re-Review — Post-Walk Audit (INSERTED) | 5/5 | Complete    | 2026-04-XX |
       ```

    2. Phase 08.1 plan list — replace each `- [ ]` with `- [x]` + append ` (completed 2026-04-XX)` to the end of each line :
       ```
       - [x] 08.1-rereview-post-walk/08.1-01-PLAN.md — Wave 1: ... (completed 2026-04-XX)
       - [x] 08.1-rereview-post-walk/08.1-02-PLAN.md — Wave 2: ... (completed 2026-04-XX)
       - [x] 08.1-rereview-post-walk/08.1-03-PLAN.md — Wave 3: ... (completed 2026-04-XX)
       - [x] 08.1-rereview-post-walk/08.1-04-PLAN.md — Wave 4: ... (completed 2026-04-XX)
       - [x] 08.1-rereview-post-walk/08.1-05-PLAN.md — Wave 5: ... (completed 2026-04-XX)
       ```

    3. Phase 08.1 section head — find `### Phase 08.1: Re-Review — Post-Walk Audit (INSERTED)` (header itself unchanged) but ensure the Goal paragraph references closure :
       - Optionally append a `**Closed:** 2026-04-XX` line after the `**Depends on:**` line.
       - Leave `**Goal:**` text unchanged.

    Commit :
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(roadmap): flip Phase 08.1 to complete + record 5 plans done" --files .planning/ROADMAP.md
    ```

    **Step D — Delete `.fixes-expected` scratch :**

    ```bash
    git rm .planning/phases/08.1-rereview-post-walk/.fixes-expected
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "chore(08.1-rev): delete .fixes-expected scratch at phase closure" --files .planning/phases/08.1-rereview-post-walk/.fixes-expected
    ```

    **Step E — Final handoff message to user :**

    > "✅ Phase 08.1 Re-Review — Post-Walk Audit CLOSED 2026-04-XX
    >
    > - 5 plans complete (08.1-01..08.1-05)
    > - N Blockers + M Shoulds fixed (strategy : <A/B>)
    > - K smell-tagged refactors (second application of CLAUDE.md 2026-04-23 smell-heuristics delta)
    > - Walk outcome : <PASS / PASS-with-findings / FAIL> (Android + iOS walks 2026-04-XX)
    > - N walk-finding-driven regression tests landed (inertness-guarded)
    > - Adversarial default-skip : 0 new CI gates, 0 new adversarial branches
    > - 8 existing CI gates verified green on final commit
    > - CI green on final main commit `<SHA>` — all jobs green
    > - STATE.md counters : completed_phases UNCHANGED (8) per /gsd:insert-phase decimal pathway ; completed_plans 42 → 47
    >
    > **Phase 09 Fog Rendering remains unblocked.** (Phase 08.1 was re-audit, not blocker.) Next : `/gsd:plan-phase 09`.
    >
    > **Retrospective for Phases 10/12/14/16** : re-review-post-walk pattern validated on narrower scope (5-7 findings vs Phase 08's 49). Smell-heuristics RE-CHECK brief (vs find-from-scratch) worked. Walk-evidence as §1b trigger worked. Default-skip adversarial wave saved wall-clock without losing signal. Full retrospective in 08.1-05-SUMMARY.md."
  </action>
  <verify>
    <automated>bash -c 'grep -q "5/5 | Complete" .planning/ROADMAP.md && grep -q "\[Phase 08.1-rereview-post-walk\]: re-review closed" .planning/STATE.md && ! test -f .planning/phases/08.1-rereview-post-walk/.fixes-expected && grep -q "^\*\*Status:\*\* closed" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && grep -qE "completed_phases: 8" .planning/STATE.md && grep -qE "completed_plans: 47" .planning/STATE.md'</automated>
  </verify>
  <done>All 10 gate-closed checklist items tick. STATE.md updated (frontmatter + Accumulated Decisions with Phase 08.1 closure entry including walk retrospective + smell re-check outcome + /gsd:insert-phase numbering rationale ; `completed_phases` unchanged at 8, `completed_plans` incremented 42 → 47, `total_plans` 42 → 47). ROADMAP.md updated (Phase 08.1 row = 5/5 Complete with date + 5-plan list checked). `.fixes-expected` deleted. Phase 09 unblocked-confirmation message posted. 3 final atomic commits on main (STATE + ROADMAP + .fixes-expected deletion).</done>
</task>

</tasks>

<verification>
- Task 1 : user strategy captured
- Task 2 : fix-loop executed, §3 all rows have commit hash, CI green on main
- Task 3 : §5 populated, status=closed, no (pending) markers anywhere
- Task 4 : 10 gate-closed checks all green, STATE + ROADMAP updated, .fixes-expected deleted, Phase 08.1 closed + Phase 09 remains unblocked
- `08.1-REVIEW.md` final : 5 sections + sub-sections + smell re-check table + §4 adversarial evidence + §5 CI-green + status=closed
- Final commit on main has CI green on all jobs
</verification>

<success_criteria>
Plan 08.1-05 complete when :
1. User chose fix-loop strategy (A or B) ; decision captured in 08.1-05-SUMMARY.md + STATE.md
2. All §3 `fix` + `refactor` rows have populated Commit hash column (no `(pending)` markers)
3. `08.1-REVIEW.md` §5 populated + header `**Status:** closed` + footer dated
4. Final commit on main has CI green on all jobs — run URL archived in §5
5. STATE.md Accumulated Decisions contains Phase 08.1 closure entry (including walk outcome + smell re-check retrospective + /gsd:insert-phase numbering rationale)
6. STATE.md frontmatter : `completed_phases` UNCHANGED at 8 (per `/gsd:insert-phase` decimal escape-hatch rationale) ; `completed_plans` 42 → 47 ; `total_plans` 42 → 47
7. ROADMAP.md Phase 08.1 row = 5/5 Complete with date + 5-plan list all [x]
8. `.fixes-expected` scratch deleted
9. 10 gate-closed checklist items all tick
10. Phase 09 unblocked-confirmation message posted to user
11. 8 CI gates still live + green (inherited baseline confirmed)
12. No new adversarial branch (default-skip convention held)
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-rereview-post-walk/08.1-05-SUMMARY.md` summarizing :
- Fix strategy chosen (A per-finding OR B batched with N batches grouped by X) + rationale verbatim from user
- Expected vs actual commit count
- Blocker count / Should count / refactor(smell) count / waived count / deferred count
- Walk outcome summary : PASS/PASS-with-findings/FAIL + walk date + Android status + iOS status
- Smell re-check retrospective : did Phase 08's 9 smell-refactors introduce new smells? How many new smell-tagged findings surfaced by Phase 08.1 agents? Decision breakdown (fix vs refactor vs defer vs accepted-as-is)
- Walk-finding-driven regression test count (N≥0) + file list (if N>0)
- Adversarial wave outcome : default-skip held ; 8 gates green ; 0 new branches
- Surprise findings surfaced during fix-loop
- Final commit hash + CI run URL + date
- Gate-closed checklist status (all 10 ticks)
- STATE.md counter transitions : `completed_phases` 8 (unchanged) ; `completed_plans` 42 → 47 ; `total_plans` 42 → 47 ; percent recomputed. Cite `/gsd:insert-phase` ROADMAP.md §Phase Numbering decimal escape-hatch rationale + CLAUDE.md "pas de .5" scope clarification.
- Phase 09 unblock confirmation : Phase 09 was unblocked by Phase 08 closure, Phase 08.1 is re-audit not blocker — Phase 09 eligibility unchanged
- Retrospective for Phases 10/12/14/16 : what worked in the re-review-post-walk pattern (smell RE-CHECK brief, walk-evidence as §1b trigger, default-skip adversarial, narrower pre-class, files-NOT-in-scope negation), what to adjust (any Pitfall that surfaced)
- Second-application validation of CLAUDE.md 2026-04-23 smell-heuristics delta : template stable on narrower scope
- Precedent note for future `/gsd:insert-phase` invocations (hypothetical 10.1, 12.1, etc.) : numbering rationale documented here is the template
</output>
