---
phase: 08.1-rereview-post-walk
plan: 02
type: execute
wave: 2
depends_on:
  - 08.1-01
files_modified:
  - .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
  - .planning/phases/08.1-rereview-post-walk/08.1-02-SUMMARY.md
autonomous: false
requirements: []
success_criteria:
  - walk-evidence-captured
must_haves:
  truths:
    - "A `checkpoint:human-verify` gate at Task 1 BLOCKS agent spawn until the user has (a) completed Android + iOS walks on real devices, (b) committed `docs/phase-08.1-walk.md` to main, (c) committed screenshots under `docs/phase-08.1-walk-screenshots/` to main. No bypass — per 08.1-RESEARCH §Pitfall 1 (Walk-Findings Not Yet Captured at Planning Time)."
    - "§1b of `08.1-REVIEW.md` is populated with per-device `<details>` blocks (Android + iOS) extracted verbatim from `docs/phase-08.1-walk.md` — NO fresh runtime walk performed BY CLAUDE (walk is user's responsibility via checkpoint). Format matches Phase 06 + Phase 08 per-device collapsed `<details>` precedent."
    - "The Android + iOS walk screenshots under `docs/phase-08.1-walk-screenshots/` are verified-present on disk BEFORE extraction starts; if any is referenced in walk doc but missing on disk, task fails loudly and escalates to user (missing asset requires user adjudication, not a fresh walk)."
    - "Android `<details>` block inlines ≥1 Android screenshot with relative markdown path (`../../../docs/phase-08.1-walk-screenshots/android-NN-*.png`) + a cadence/observations table + walk findings block verbatim from walk doc"
    - "iOS `<details>` block inlines ≥1 iOS screenshot with relative markdown path (`../../../docs/phase-08.1-walk-screenshots/ios-NN-*.png`) + cadence/observations table + walk findings block verbatim"
    - "§1b walk-evidence snapshot paragraph summarizes overall walk result: PASS / PASS-with-findings / FAIL + 1-3 sentence summary of the most salient observations (connects to what will become §3 walk-finding triage rows)"
    - "One atomic commit on main: `docs(08.1-rev): fill §1b walk evidence (Android + iOS)`"
    - "If walk evidence triage reveals any reproducible bug that demands a dedicated permanent regression test, that is NOT landed here — Plan 08.1-04 is the adversarial wave where walk-finding regression tests land (inertness-guarded)"
  artifacts:
    - path: "docs/phase-08.1-walk.md"
      provides: "User-captured walk findings (Android + iOS) — pre-requisite artifact landed by USER BEFORE this plan's checkpoint unblocks"
      contains: "Android"
    - path: "docs/phase-08.1-walk-screenshots/"
      provides: "Per-device screenshots directory (`android-NN-<step>.png` + `ios-NN-<step>.png` — Phase 07 convention)"
    - path: ".planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md"
      provides: "§1b walk evidence filled — Android + iOS `<details>` blocks + walk-evidence snapshot paragraph"
      contains: "phase-08.1-walk-screenshots"
  key_links:
    - from: "User device walks (Android Pixel 4a + iOS iPhone 17 Pro)"
      to: "`docs/phase-08.1-walk.md` + `docs/phase-08.1-walk-screenshots/`"
      via: "User runs apps on devices, observes, documents findings in markdown + saves screenshots. Plan 08.1-02 Task 1 `checkpoint:human-verify` blocks until artifacts committed."
      pattern: "docs/phase-08\\.1-walk\\.md"
    - from: "`docs/phase-08.1-walk.md` + walk screenshots"
      to: "`08.1-REVIEW.md` §1b per-device `<details>` blocks"
      via: "Verbatim extraction (no re-interpretation) — Phase 06/08 §1b pattern reused with Phase 08.1 artefacts"
      pattern: "<details>"
    - from: "Walk screenshots PNGs at `docs/phase-08.1-walk-screenshots/`"
      to: "Inline markdown image refs in §1b"
      via: "Relative paths `../../../docs/phase-08.1-walk-screenshots/*.png` (markdown renders inline from the review artefact location)"
      pattern: "phase-08\\.1-walk-screenshots"
---

<objective>
Fill `08.1-REVIEW.md` §1b by extracting walk evidence from the user-committed `docs/phase-08.1-walk.md` + `docs/phase-08.1-walk-screenshots/`. The walk IS the fresh runtime evidence that TRIGGERED Phase 08.1 (per 08.1-CONTEXT goal statement "Re-review Phase 07 + Phase 08 after successful Android + iOS walks"). Unlike Phase 08 Plan 08-02 (which extracted pre-existing Phase 07 smoke artifacts as evidence of a completed prior phase), Phase 08.1 Plan 08.1-02 extracts evidence from a walk that the user ran SPECIFICALLY to feed Phase 08.1.

Critical difference vs Phase 08 Plan 08-02 : the walk artifact may not yet exist at plan execution time. Task 1 is a `checkpoint:human-verify` gate blocking until `docs/phase-08.1-walk.md` + `docs/phase-08.1-walk-screenshots/` are committed to main. This is the STRUCTURAL protection against spawning agents on pre-walk state (which would produce findings that don't account for real device behavior — exactly the failure mode Phase 08.1 exists to prevent).

Purpose: §1b is the runtime observation component of the re-review. The walk is the new signal — everything Phase 08 missed was either architecturally invisible (refactor regression hidden behind happy-path tests) or device-lifecycle-specific (orientation, kill-and-resume, OEM battery-saver, airplane-on-off mid-download, etc). The walk surfaces these. Plan 08.1-02 captures them verbatim into §1b before Plan 08.1-03 spawns agents, so the agents can be briefed on real-device findings rather than re-audit the code in a vacuum.

Output: `08.1-REVIEW.md` §1b populated with (a) Android `<details>` block with screenshots inline + cadence/observations table + walk findings verbatim quote, (b) iOS `<details>` block with screenshots inline + cadence/observations table + walk findings verbatim quote, (c) Walk-evidence snapshot paragraph summarizing PASS/PASS-with-findings/FAIL. One atomic commit on main.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.1-rereview-post-walk/08.1-CONTEXT.md
@.planning/phases/08.1-rereview-post-walk/08.1-RESEARCH.md
@.planning/phases/08.1-rereview-post-walk/08.1-VALIDATION.md
@.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
@.planning/phases/08-review-gate-map/08-02-PLAN.md
@.planning/phases/08-review-gate-map/08-REVIEW.md
@.planning/phases/06-review-gate-gps/06-02-PLAN.md
@docs/phase-07-smoke.md
@docs/phase-07-ios-animate-camera-crash.md

<interfaces>
<!-- Walk-evidence capture contract — verbatim extraction, no re-interpretation. Walk artifacts are user-owned pre-requisite. -->

**Expected artifacts at Task 1 checkpoint (user-committed BEFORE this plan starts) :**

- `docs/phase-08.1-walk.md` — markdown document covering Android + iOS walk findings. Structure suggested (user's discretion per 08.1-RESEARCH §Open Question 2, default template = Phase 07 smoke steps 1-9) :
  - Android section : device + OS version + build hash + APK source URL (GitHub Actions artifact) + walk date UTC + duration + status PASS/PASS-with-findings/FAIL + step-by-step results table + findings block (bulleted list of observed issues, each with severity pre-assessment + suspected component/file)
  - iOS section : same structure for iPhone + IPA sideload source + walk date + duration + status + steps + findings
  - Walk protocol followed : 9 steps template (install + first launch / Session list / MapScreen / Burger menu / Airplane-mode / Country download / Manage country / Delete country / World fallback) + any probe-fix-loop-specific steps (pause/resume download to exercise row #1 break-on-pause ; rotation ; kill-and-resume ; offline-then-online during download)

- `docs/phase-08.1-walk-screenshots/` — directory with PNGs :
  - `android-NN-<step>.png` (one or more)
  - `ios-NN-<step>.png` (one or more)

**Relative path from `08.1-REVIEW.md` to screenshots:** `../../../docs/phase-08.1-walk-screenshots/<filename>.png`. Markdown renderers that follow GitHub conventions render inline.

**Format per-device (Phase 06 + Phase 08 §1b precedent):**
- `<details><summary>DeviceModel OS — Status DateISO (build <sha>)</summary>` opener
- Embedded screenshots as `![alt text](../../../docs/phase-08.1-walk-screenshots/<filename>.png)` lines
- Cadence/observations table (columns: Step / Observation / Evidence or Status)
- Verbatim walk findings block (if user documented findings inline) or "Verdict: PASS/PASS-with-findings/FAIL" summary
- `</details>` closer

**Walk-evidence snapshot structure:**
- Single paragraph under the two `<details>` blocks.
- Summarizes: (1) overall walk result PASS / PASS-with-findings / FAIL, (2) 1-3 sentences on the most salient observations that will become §3 walk-finding triage rows, (3) reference to the walk doc for full detail.

**Gate-check before extraction (Task 1) :** use `ls` on the walk artifacts. If ANY is missing, the task must FAIL loudly with the missing path to stderr so the orchestrator surfaces to user. Walk is user's responsibility — Claude does NOT invent walk observations. Per 08.1-RESEARCH §Pitfall 1 + Common Pitfalls §1 recovery : pause at checkpoint ; surface gap ; suggest walk protocol template.

**Walk NOT yet happened at plan execution time :** Default behavior when checkpoint fails. Claude posts walk protocol template to user :

> "Plan 08.1-02 requires `docs/phase-08.1-walk.md` + `docs/phase-08.1-walk-screenshots/` on main BEFORE I can extract §1b evidence. Walk artifacts not found.
>
> **Suggested walk protocol** (re-use Phase 07 `docs/phase-07-smoke.md` steps as template, adapt for Phase 08.1 re-review lens) :
>
> 1. Install APK (Android) / sideload IPA (iOS) from latest main tip
> 2. Cold start → SessionListScreen
> 3. Create + start session
> 4. /map : verify world bundle renders + AppBar affordances
> 5. Burger menu : 3 unwired tiles + 3 live-data rows sanity check
> 6. Airplane mode cold-start : map still renders (Phase 08 SC#1 re-verify on real device)
> 7. Country download (ARU or similar small country) : verify progress + completes + displays on map
> 8. Manage country : size + version match catalog
> 9. Delete country : disappears + world fallback
> 10. Phase 08 fix-loop probes (optional) — pause/resume download (row #1), rotation during map render (row #39), kill-and-resume (row #38)
>
> Observe + screenshot each step. Document findings (any severity) inline in the walk doc. Commit `docs/phase-08.1-walk.md` + screenshots to main, then tell me 'walk done' and I'll resume Plan 08.1-02.
>
> Alternative: if the walk is not on the critical path right now, say 'defer walk' — Plan 08.1-02 stays paused until walk artifacts land."

After user commits walk artifacts OR indicates defer, Plan 08.1-02 resumes (or explicitly pauses).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify user walk artifacts present on main — BLOCKING gate until `docs/phase-08.1-walk.md` + screenshots committed</name>
  <files>(verification only — no file mutation ; gate blocks Task 2 until walk artifacts exist)</files>
  <action>
    This is the STRUCTURAL gate that protects Phase 08.1 from spawning agents on a pre-walk state. 08.1-RESEARCH §Pitfall 1 explicitly flags this as the #1 failure mode of re-review phases : agents briefed on pre-walk evidence produce findings that don't account for real device behavior, which is exactly what Phase 08.1 exists to catch.

    **Step A — Run the gate check:**

    ```bash
    set -e

    # Primary artifact
    if ! test -f docs/phase-08.1-walk.md; then
      echo "MISSING: docs/phase-08.1-walk.md — walk has not been captured OR not yet committed" >&2
      exit 1
    fi

    # Screenshots directory
    if ! test -d docs/phase-08.1-walk-screenshots; then
      echo "MISSING: docs/phase-08.1-walk-screenshots/ directory — walk screenshots not committed" >&2
      exit 1
    fi

    # At least 1 Android + 1 iOS screenshot
    ANDROID_COUNT=$(ls docs/phase-08.1-walk-screenshots/android-*.png 2>/dev/null | wc -l)
    IOS_COUNT=$(ls docs/phase-08.1-walk-screenshots/ios-*.png 2>/dev/null | wc -l)

    if [ "$ANDROID_COUNT" = "0" ]; then
      echo "MISSING: no docs/phase-08.1-walk-screenshots/android-*.png files — Android walk evidence absent" >&2
      exit 1
    fi

    if [ "$IOS_COUNT" = "0" ]; then
      echo "MISSING: no docs/phase-08.1-walk-screenshots/ios-*.png files — iOS walk evidence absent" >&2
      exit 1
    fi

    echo "Pre-check OK: walk artifacts present — $ANDROID_COUNT Android screenshots + $IOS_COUNT iOS screenshots + docs/phase-08.1-walk.md"
    ```

    **Step B — If check FAILS (artifacts missing), post walk-protocol message to user:**

    Claude-main posts to chat (template in `<interfaces>` above — walk protocol + "alternative defer walk" option). Wait for user to commit artifacts + signal "walk done" OR signal "defer walk" (which pauses plan indefinitely).

    **Step C — If check PASSES, read the walk doc to understand the content for Task 2:**

    ```bash
    cat docs/phase-08.1-walk.md
    ls -la docs/phase-08.1-walk-screenshots/
    ```

    Verify the walk doc structure covers Android + iOS + has findings/observations.

    **Step D — If walk findings include reproducible bugs, flag them for Plan 08.1-04 :**

    Claude-main reads the walk findings section. Any finding marked as a reproducible bug (not just "polish observation") is candidate for a walk-finding-driven permanent regression test in Plan 08.1-04 (which otherwise defaults to N=0 tests). This plan does NOT land the tests — it just reads and forwards the intent. The actual test authorship is Plan 08.1-04's responsibility.

    **Anti-pattern guard:**
    - Do NOT silently fill §1b with placeholder "walk deferred" text — the gate must be LOUD.
    - Do NOT invent walk observations. The walk is user-owned.
    - Do NOT extract partial evidence if some screenshots are missing (would create inconsistent §1b).
  </action>
  <verify>
    <automated>bash -c 'test -f docs/phase-08.1-walk.md && test -d docs/phase-08.1-walk-screenshots && [ "$(ls docs/phase-08.1-walk-screenshots/android-*.png 2>/dev/null | wc -l)" -ge "1" ] && [ "$(ls docs/phase-08.1-walk-screenshots/ios-*.png 2>/dev/null | wc -l)" -ge "1" ]'</automated>
  </verify>
  <done>
    Walk artifacts present on main: `docs/phase-08.1-walk.md` + `docs/phase-08.1-walk-screenshots/` with ≥1 Android + ≥1 iOS screenshot. Claude-main has read the walk doc and captured findings intent for Task 2 extraction + Plan 08.1-04 forwarding. If artifacts missing, plan remains paused with walk-protocol message posted to user.
  </done>

  <what-built>
    Nothing automated. This checkpoint IS the protocol gate: verify user has committed walk artifacts. If missing, post walk-protocol template + pause plan. If present, proceed to Task 2 extraction.
  </what-built>
  <how-to-verify>
    - `test -f docs/phase-08.1-walk.md` → true
    - `test -d docs/phase-08.1-walk-screenshots` → true
    - `ls docs/phase-08.1-walk-screenshots/android-*.png | wc -l` → ≥1
    - `ls docs/phase-08.1-walk-screenshots/ios-*.png | wc -l` → ≥1
  </how-to-verify>
  <resume-signal>
    User commits walk artifacts to main + says "walk done" OR similar ("j'ai fait le walk", "artifacts on main").
    If user says "defer walk", plan pauses indefinitely ; resumes when user returns with artifacts.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extract walk evidence into 08.1-REVIEW.md §1b (Android + iOS `<details>` blocks + walk-evidence snapshot)</name>
  <files>.planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md</files>
  <action>
    Use the Edit tool to replace the `(pending — filled by Plan 08.1-02 …)` markers under §1b in `08.1-REVIEW.md` with the extracted evidence. Three replacements :

    **A — Android `<details>` block** — replace the line `(pending — filled by Plan 08.1-02 from docs/phase-08.1-walk.md + docs/phase-08.1-walk-screenshots/android-*.png)` inside the Android `<details>` block with extracted Android evidence. Structure :

    ```markdown
    **Device:** <device model from walk doc>
    **OS version:** <Android version>
    **MirkFall build:** `<build sha from walk doc>`
    **APK source:** <URL to GitHub Actions artifact from walk doc>
    **Walk date (UTC):** <date/time from walk doc>
    **Walk duration:** <duration>
    **Status:** <PASS / PASS-with-findings / FAIL from walk doc>
    **Source:** `docs/phase-08.1-walk.md` — Android section

    **Screenshots (inline):**

    ![Android — <alt from walk doc step 1>](../../../docs/phase-08.1-walk-screenshots/android-01-<step>.png)
    ![Android — <alt step 2>](../../../docs/phase-08.1-walk-screenshots/android-02-<step>.png)
    ... <one line per Android screenshot committed by user>

    **Cadence / observations table (extracted verbatim from `docs/phase-08.1-walk.md` Android section):**

    | # | Step | Result | Notes |
    |---|------|--------|-------|
    | <rows extracted verbatim from walk doc — one per step walked> |

    **Walk findings (verbatim from walk doc Android findings section if present, else "Verdict: <status>"):**

    > <verbatim blockquote extraction from walk doc OR "**Verdict: PASS/PASS-with-findings/FAIL**">

    **Verdict (verbatim from walk doc):** **<PASS / PASS-with-findings / FAIL>**
    ```

    Fill `<...>` placeholders from the actual walk doc content. Verbatim extraction only ; do NOT summarize or rephrase. If walk doc structure differs from template above, adapt the shape (e.g. timeline list instead of table) to match what the walk doc actually contains.

    **B — iOS `<details>` block** — replace the corresponding iOS pending marker with the same structure, adapted for iOS :

    ```markdown
    **Device:** iPhone <model from walk doc>
    **OS version:** iOS <version>
    **MirkFall build:** `<build sha from walk doc>`
    **IPA source:** <URL to GitHub Actions unsigned IPA artifact + sideload tool used>
    **Walk date (UTC):** <date/time from walk doc>
    **Walk duration:** <duration>
    **Status:** <PASS / PASS-with-findings / FAIL>
    **Source:** `docs/phase-08.1-walk.md` — iOS section

    **Screenshots (inline):**

    ![iOS — <alt step 1>](../../../docs/phase-08.1-walk-screenshots/ios-01-<step>.png)
    ![iOS — <alt step 2>](../../../docs/phase-08.1-walk-screenshots/ios-02-<step>.png)
    ... <one line per iOS screenshot>

    **Cadence / observations table (extracted verbatim):**

    | # | Step | Result | Notes |
    |---|------|--------|-------|
    | <rows extracted verbatim from walk doc iOS section> |

    **Walk findings (verbatim from walk doc iOS findings section if present):**

    > <verbatim blockquote extraction>

    **Verdict (verbatim from walk doc):** **<PASS / PASS-with-findings / FAIL>**
    ```

    Adapt shape to actual walk doc structure ; verbatim extraction, no re-interpretation.

    **C — Walk-evidence snapshot** — replace `*(pending — 1-paragraph summary PASS / PASS-with-findings / FAIL, filled by Plan 08.1-02)*` with :

    ```markdown
    <1-paragraph summary, plain prose. Content :

    - Overall walk result: PASS / PASS-with-findings / FAIL across both devices
    - 1-3 sentences summarizing the most salient observations that will become §3 walk-finding triage rows (highlight any reproducible bug that will drive a Plan 08.1-04 permanent regression test)
    - Reference: see `docs/phase-08.1-walk.md` + `<details>` blocks above for full detail>

    **Walk-finding-driven regression test candidates (for Plan 08.1-04 consideration):** <0 or more bullet points listing specific walk findings that should get inertness-guarded permanent tests. Plan 08.1-04 may accept all, accept some, or defer to §3 triage. If zero, write "None — walk findings are all polish/UX observations triaged in §3 without dedicated regression tests.">
    ```

    The `Walk-finding-driven regression test candidates` bullet forward-references Plan 08.1-04's default-skip-adversarial wave (per 08.1-RESEARCH §Pattern 3). If walk surfaced NO reproducible bugs, N=0 permanent tests — Plan 08.1-04 is a gate-verification-only wave. If walk surfaced reproducible bugs, those become candidate permanent test blocks in Plan 08.1-04 §4.

    After all three replacements, verify §1b placeholders are gone and the content is populated :
    ```bash
    # Placeholders in §1b should all be replaced
    if grep -q "pending — filled by Plan 08.1-02" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md; then
      echo "§1b still has pending markers" >&2
      exit 1
    fi

    # Screenshots referenced
    if [ "$(grep -c "phase-08.1-walk-screenshots" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md)" -lt "2" ]; then
      echo "§1b does not reference walk screenshots" >&2
      exit 1
    fi

    # Both device sections have Status / Verdict
    grep -q "Android" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    grep -q "iOS" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```

    Then commit :
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(08.1-rev): fill §1b walk evidence (Android + iOS)" --files .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md
    ```
  </action>
  <verify>
    <automated>bash -c '! grep -q "pending — filled by Plan 08.1-02" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && [ "$(grep -c "phase-08.1-walk-screenshots" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md)" -ge "2" ] && grep -qE "PASS|PASS-with-findings|FAIL" .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md && git log --oneline -1 -- .planning/phases/08.1-rereview-post-walk/08.1-REVIEW.md | grep -q "fill §1b walk evidence"'</automated>
  </verify>
  <done>§1b has two fully-populated per-device `<details>` blocks (Android + iOS) with screenshots inline + cadence/observations tables + verbatim walk findings + walk-evidence snapshot paragraph with walk-finding-driven regression test candidates list. All walk screenshot paths referenced. `(pending — filled by Plan 08.1-02 …)` markers all replaced. One atomic commit on main.</done>
</task>

</tasks>

<verification>
- Task 1 : walk artifacts present on main (docs/phase-08.1-walk.md + docs/phase-08.1-walk-screenshots/ with ≥1 Android + ≥1 iOS screenshot)
- Task 2 : §1b Android `<details>` block populated with screenshots + cadence table + verbatim findings + verdict
- Task 2 : §1b iOS `<details>` block populated with screenshots + cadence table + verbatim findings + verdict
- Task 2 : §1b walk-evidence snapshot paragraph present with walk-finding-driven regression test candidates for Plan 08.1-04
- No `pending — filled by Plan 08.1-02` markers remain in §1b
- One atomic commit on main: `docs(08.1-rev): fill §1b walk evidence (Android + iOS)`
</verification>

<success_criteria>
Plan 08.1-02 complete when:
1. Walk artifacts confirmed present on main (checkpoint blocking gate passed)
2. §1b in `08.1-REVIEW.md` fully populated with extracted walk evidence (verbatim, no re-interpretation)
3. ≥2 walk screenshots referenced inline with relative markdown paths
4. Walk-evidence snapshot paragraph forward-references Plan 08.1-04 walk-finding-driven regression test candidates (N≥0)
5. One atomic commit on main with `docs(08.1-rev):` prefix
6. CI green (markdown-only change)
7. User signals "OK audit" / "OK 08.1-03" / equivalent to unblock Plan 08.1-03
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-rereview-post-walk/08.1-02-SUMMARY.md` summarizing:
- Checkpoint gate result (walk artifacts present, timestamp)
- Walk doc summary (Android status, iOS status, overall walk verdict)
- Extraction commit hash
- Walk-finding-driven regression test candidates list (count + brief descriptions) — forwarded to Plan 08.1-04
- Decision applied: verbatim extraction, no re-interpretation
- Any deviation from verbatim-extraction rule (if walk doc structure forced adaptation, note it)
- Handoff: §2 pre-class + smell re-check + 4-agent audit unblocked for Plan 08.1-03
</output>
</content>
</invoke>