---
phase: 03
slug: persistence-domain-models
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-18
planner_signed_off: 2026-04-18
---

# Phase 03 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `package:test` 1.30.0 (already pinned Phase 02 dev_dependency) + Drift test utilities (`NativeDatabase.memory()`, `SchemaVerifier`) |
| **Config file** | `dart_test.yaml` with `migration` tag (shipped by 03-01 Task 2) |
| **Quick run command** | `dart test test/domain/ -r compact` (domain-only, seconds) |
| **Full suite command** | `dart test test/` |
| **Estimated runtime** | ~30-60s full suite (in-memory DB) |

**Ubuntu CI prerequisite:** `sudo apt-get install -y libsqlite3-0 libsqlite3-dev` runs in the `gates` job BEFORE `dart test` (shipped by 03-01 Task 3).

---

## Sampling Rate

- **After every task commit:** Run `dart test test/<lens>` (domain-only, infra-only, or migration-only depending on task focus — sub-30s)
- **After every plan wave:** Run `dart test test/` (full suite, ~30-60s)
- **Before `/gsd:verify-work`:** Full suite must be green in CI `gates` job
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Each row maps a requirement / success criterion to its authoritative test file, the plan (+ task) that ships it, and the wave at which it's validated. `W0` = test file is shipped by Wave 0 (Plan 03-01 fixtures + tooling). `W1` = shipped by Wave 1 (the listed plan's tasks write the actual tests).

| Requirement | Behavior | Test Type | Automated Command | Plan / Task | Wave | Status |
|-------------|----------|-----------|-------------------|-------------|------|--------|
| SESS-06 | Double-activation fails with DB constraint, not caller assertion | integration | `dart test test/infrastructure/stores/session_store_exclusivity_test.dart` | 03-06 / T1 | 5 | ⬜ pending |
| SESS-06 | Partial unique index `idx_t_sessions_status_active` exists in schema | schema | `dart test test/infrastructure/db/app_database_schema_test.dart` | 03-04 / T3 | 4 | ⬜ pending |
| SESS-06 | `SqliteException 2067` → `ConcurrentActivationException` mapping | unit | `dart test test/infrastructure/stores/session_store_error_mapping_test.dart` | 03-06 / T1 | 5 | ⬜ pending |
| SESS-06 | `@Assert` invariants (empty displayName → `AssertionError`) | unit | `dart test test/domain/session_invariants_test.dart` | 03-03 / T1 | 3 | ⬜ pending |
| MIRK-03 | Setting a bit cannot unset it (OR-monotone at store level) | integration | `dart test test/infrastructure/stores/revealed_tile_store_idempotence_test.dart` | 03-06 / T1 | 5 | ⬜ pending |
| MIRK-03 | `Uint8List | Uint8List` merge is idempotent and commutative (pure) | unit | `dart test test/domain/reveal_calculator_test.dart` | 03-02 / T2 | 2 | ⬜ pending |
| MIRK-03 | Concurrent writes → final bitmap = OR of both masks | integration | `dart test test/infrastructure/stores/revealed_tile_store_concurrent_test.dart` | 03-06 / T1 | 5 | ⬜ pending |
| MIRK-03 | Schema contract (bitmap BLOB NOT NULL + unique key) | schema | `dart test test/infrastructure/db/app_database_schema_test.dart` | 03-04 / T3 | 4 | ⬜ pending |
| SC#1 | `PRAGMA journal_mode=wal`, `synchronous=1`, `busy_timeout=5000`, `foreign_keys=1` | integration | `dart test test/infrastructure/db/app_database_pragma_test.dart` | 03-04 / T3 | 4 | ⬜ pending |
| SC#1 | V1 seed loads, schema dump matches canonical (V1→V1 identity) | migration | `dart test test/infrastructure/db/v1_identity_fixture_test.dart` | 03-04 / T3 | 4 | ⬜ pending |
| SC#4 | `lib/domain/` zero Flutter / Drift imports | static | `dart run tool/check_domain_purity.dart` (unit-tested via `dart test tool/test/check_domain_purity_test.dart`) | 03-01 / T3 (tool); enforced continuously 03-02+ | 1+ | ⬜ pending |
| SC#4 | All domain models generated by Freezed, immutable | compilation | `dart run build_runner build --delete-conflicting-outputs` + entity tests in `test/domain/` | 03-03 / T1 | 3 | ⬜ pending |
| SC#5 | `tile_math` + `reveal_calculator` run under `dart test` (no Flutter in graph) | unit | `dart test test/domain/tile_math_test.dart test/domain/reveal_calculator_test.dart` | 03-02 / T2 | 2 | ⬜ pending |
| SC#5 | `JsonMigrator` identity v1 + slot v2 (rename `mirk_radius_m` → `reveal_radius_m`) framework API | unit | `dart test test/domain/json_migrator_test.dart` | 03-02 / T3 | 2 | ⬜ pending |
| SC#5 | `JsonMigrator` fixture-driven v1→v2 integration (session_v1.json → migrate → session_v2.json payload) — relocated from 03-02 to 03-03 alongside Freezed `Envelope` (Blocker 1 fix) | integration | `dart test test/domain/json_migrator_v1_to_v2_test.dart` | 03-03 / T3 | 3 | ⬜ pending |
| SC#4 | `Envelope` is Freezed (ROADMAP verbatim list includes Envelope) + `Envelope.fromJson` rejects malformed envelopes with `ImportValidationException` | unit | `dart test test/domain/envelope_fromjson_test.dart` | 03-03 / T3 | 3 | ⬜ pending |
| SC#6 | Pre-migration backup file created before `onUpgrade` | integration | `dart test test/infrastructure/db/backup_test.dart` | 03-05 / T1 | 5 | ⬜ pending |
| SC#6 | Row-count regression → `MigrationFailureException` | integration | `dart test test/infrastructure/db/schema_sanity_test.dart` | 03-05 / T1 | 5 | ⬜ pending |
| SC#6 | V1→V2 fictive migration preserves rows, `notes` column writeable | migration | `dart test test/infrastructure/db/migration_v1_to_v2_test.dart` (via `SchemaVerifier`) | 03-05 / T2 | 5 | ⬜ pending |
| SC#6 | Opening upgradeable DB triggers `DbBackupService.takeBackup` BEFORE `onUpgrade` runs (Blocker 3) | integration | `dart test test/infrastructure/db/backup_on_upgrade_test.dart` | 03-05 / T3 | 5 | ⬜ pending |
| — (ULID) | 26 chars, Crockford base32, timestamp monotonic, k-sortable | unit | `dart test test/infrastructure/ids/ulid_test.dart` | 03-02 / T1 | 2 | ⬜ pending |
| — (IdGen) | Seeded IdGenerator → reproducible IDs | unit | `dart test test/infrastructure/ids/seeded_id_generator_test.dart` | 03-02 / T1 | 2 | ⬜ pending |
| — (cascade) | Delete session → markers + revealed_tiles gone | integration | `dart test test/infrastructure/stores/drift_session_store_cascade_test.dart` | 03-06 / T1 | 5 | ⬜ pending |
| — (cascade) | Delete category → markers reassigned to `cat_default` | integration | `dart test test/infrastructure/stores/marker_category_store_cascade_test.dart` | 03-06 / T1 | 5 | ⬜ pending |
| — (style) | Unknown `rendererType` JSON → `UnknownConfig(raw)` fallback | unit | `dart test test/domain/mirk_style_config_fromjson_test.dart` | 03-03 / T2 | 3 | ⬜ pending |
| — (tz) | DateTime UTC + offset round-trip, ISO 8601 export with `+HH:MM` | unit | `dart test test/domain/session_timezone_test.dart` | 03-03 / T1 | 3 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

All test files above are **new** — the project has no Phase 03 tests yet. Wave 0 = Plan 03-01 includes:

- [ ] `test/fixtures/drift_schemas/drift_schema_v1.json` — produced by `dart run drift_dev schema dump` (Plan 03-04 Task 1 dumps it; Plan 03-01 creates the parent directory but the file itself is generated when the AppDatabase exists)
- [ ] `test/fixtures/drift_schemas/drift_schema_v2.json` — produced by Plan 03-04 Task 2
- [ ] `test/generated_migrations/` — produced by `dart run drift_dev schema generate ... --data-classes --companions` (Plan 03-04 Task 2)
- [ ] `test/fixtures/db_seed/v1_baseline.sql` — hand-written INSERT statements (10 sessions, 50 markers, 5 tiles, 3 categories, 2 styles) for V1 identity test — **shipped by Plan 03-01 Task 2**
- [ ] `test/fixtures/json/session_v1.json` — sample session envelope for JsonMigrator — **shipped by Plan 03-01 Task 2**
- [ ] `test/fixtures/json/mirk_style_unknown_renderer.json` — malformed-rendererType sample for UnknownConfig fallback — **shipped by Plan 03-01 Task 2**
- [ ] `tool/check_domain_purity.dart` + `tool/test/check_domain_purity_test.dart` — grep-based static purity check — **shipped by Plan 03-01 Task 3**
- [ ] `dart_test.yaml` (optional) — `migration` tag for slower tests — **shipped by Plan 03-01 Task 2**
- [ ] CI `gates` step addition: `sudo apt-get install -y libsqlite3-0 libsqlite3-dev` BEFORE `dart test` — **shipped by Plan 03-01 Task 3**
- [ ] CI `gates` step addition: `dart run drift_dev schema dump lib/.../app_database.dart drift_schemas/ && git diff --exit-code drift_schemas/` — guard against forgotten regen — **shipped by Plan 03-01 Task 3**
- [ ] Dev-dep resolution: `custom_lint` + `riverpod_lint` at compatible versions (Research Open Question #1 — resolve via scratch-branch `pub get` as first task) — **shipped by Plan 03-01 Task 1**

`wave_0_complete` flips to `true` when Plan 03-01 SUMMARY is committed with all checkboxes ticked.

---

## Manual-Only Verifications

*None — all phase behaviors have automated verification.*

Rationale: Phase 03 is pure persistence + pure Dart domain. No UI surface, no device-specific hardware, no user interaction required for verification. Everything runs under `dart test` with an in-memory Drift DB.

---

## SESS-06 & MIRK-03 Coverage Matrix

| Requirement | Layer tested | Test type | Test command | Covers angle | Plan / Task |
|-------------|--------------|-----------|--------------|--------------|-------------|
| **SESS-06** | Domain (invariants) | unit | `dart test test/domain/session_invariants_test.dart` | Dart-level `@Assert` for obvious caller misuse | 03-03 / T1 |
| **SESS-06** | DB schema shape | schema | `dart test test/infrastructure/db/app_database_schema_test.dart` | Partial unique index exists on `status` with `WHERE status='active'` | 03-04 / T3 |
| **SESS-06** | DB enforcement at runtime | integration | `dart test test/infrastructure/stores/session_store_exclusivity_test.dart` | Two concurrent activation paths → one wins, other gets `ConcurrentActivationException` (wrapped from `SqliteException 2067`) | 03-06 / T1 |
| **SESS-06** | Store error mapping | unit | `dart test test/infrastructure/stores/session_store_error_mapping_test.dart` | `SqliteException 2067` → `ConcurrentActivationException`; other codes rethrow | 03-06 / T1 |
| **MIRK-03** | Pure bitmap algebra | unit | `dart test test/domain/reveal_calculator_test.dart` | `a \| b ≥ a` bitwise; `(a \| b) \| a == a \| b`; idempotence | 03-02 / T2 |
| **MIRK-03** | Store idempotence | integration | `dart test test/infrastructure/stores/revealed_tile_store_idempotence_test.dart` | Apply same mask twice → unchanged; additive mask → OR-merge; no bit ever turns off | 03-06 / T1 |
| **MIRK-03** | Concurrent writes | integration | `dart test test/infrastructure/stores/revealed_tile_store_concurrent_test.dart` | Two writes via `Future.wait` → final = OR of both masks; `setBitCount` matches popcount | 03-06 / T1 |
| **MIRK-03** | Schema contract | schema | `dart test test/infrastructure/db/app_database_schema_test.dart` | `t_revealed_tiles` has `bitmap BLOB NOT NULL` + unique `(session_id, parent_x, parent_y, parent_zoom)` | 03-04 / T3 |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (verified by planner in PLAN.md review)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task's `<verify>` is a live `dart test` / `flutter analyze` / `dart run tool/check_*` command)
- [x] Wave 0 covers all MISSING references (test files + fixtures + CI system deps) — Plan 03-01 ships fixtures + tool; Plan 03-04 ships the Drift-generated schema artifacts that depend on the AppDatabase existing
- [x] No watch-mode flags (`--watch`, etc.)
- [x] Feedback latency < 60s (full suite) / < 30s (per-task)
- [x] `nyquist_compliant: true` set in frontmatter once planner fills Task IDs + planner confirms the above

**Approval:** `nyquist_compliant: true`. Planner has mapped every requirement / success criterion / ancillary invariant to a specific test file, plan, task, and wave. Executor has no ambiguity on what to build or how it is verified.

**Revision 1 (revision-mode by planner):** Cascade wave renumbering (1 / 2 / 3 / 4 / 5 / 5 — Blocker 2 fix). New SC#6 backup-on-upgrade row added (Blocker 3). `JsonMigrator` v1→v2 fixture-driven test moved 03-02→03-03, `Envelope`-Freezed row added (Blocker 1). Every new test file has a task home; `nyquist_compliant: true` preserved.
